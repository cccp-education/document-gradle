= BACKLOG — Document Gradle (New Orleans 🟢)
:toc:

_Date revision_ : 2026-08-02 (session 032 — Dogfooding batch 61 articles cheroliv.com + cadrage DOC-TABLE-VALIDATE + DOC-PLANTUML-VALIDATE)
_Version_ : 0.0.1 (publié Maven Central)

'''
== Vision Produit

Un plugin Gradle N2 qui **cree du contenu AsciiDoc assiste par IA**
et le **publie dans tous les formats supportes par AsciidoctorJ**
(HTML5, PDF, EPUB3, DocBook 5, ManPage), sans sortir du pipeline Gradle.

Le plugin couvre deux missions complementaires :

1. **Generation IA** → un LLM (gpt-oss:120b-cloud) assiste la redaction
   de documentation, specs, manuels, rapports en AsciiDoc structure
2. **Publication multi-format** → chaque backend AsciidoctorJ est expose
   comme une tache Gradle isolee avec configuration fine

=== Domaine « Book » (structuré, mise en page, logique)

Au-dela de l'assemblage plat (DOC-11), document-gradle porte un *domaine
« Book »* de premier rang : un agregat DDD `BookTree` qui organise le contenu
AsciiDoc en **arbre propre** — Parties → Chapitres → Sections → Sous-sections
— derive de la table des matieres (ref `1.0.2.6.4` = profondeur 4). Ce domaine
rend le livre (1) **architecturé** (hiérarchie restituee en niveaux `==`/`===`/
`====`), (2) **mise en page** (front/body/back matter, sauts de page, page de
titre, `:toc:`), (3) **logique** (numérotation hiérarchique, navigation
précédent/suivant, ancres de reference croisée). Voir EPIC `DOC-BOOK-DOMAIN`
(cadrage S-211, `CADRAGE_EPIC_DOC_BOOK_DOMAIN.adoc`). Consommateur pilote :
`office/metiers/FPA` (livre FPA II scanné → livre PDF/EPUB navigable).

'''
== Boundary Codex (Brooklyn)

_Document = WRITE + PUBLISH. Codex = READ + RAG._

Voir `.agents/ARCHITECTURE_BOUNDARY.adoc` pour le detail complet.

[cols="2,5,5"]
|===
| | Document (ici) | Codex (Brooklyn)
| Mission | Creer + publier | Ingerer + chercher
| Entree | Prompt LLM ou AsciiDoc source | PDF, EPUB, AsciiDoc existant
| Sortie | HTML, PDF, EPUB, DocBook, ManPage | Vecteurs pgvector, chunks, knowledge base
| AsciidoctorJ | Implementation directe | Parsing AST + export KB
| LLM | Oui (langchain4j) | Non
| Base de donnees | Aucune | PostgreSQL + pgvector
|===

'''
== User Stories

=== US-DOC-01 — En tant que redacteur technique, je veux generer un squelette AsciiDoc via IA

*Priorite* : P1
*EPIC* : DOC-2

.Valeur
Gagner du temps sur la redaction de documentation technique : un prompt
structure produit un document AsciiDoc coherent, que l'humain valide et enrichit.

.Criteres d'acceptation
- La tache `generateDocument` accepte un prompt (fichier ou inline)
- Le LLM (gpt-oss:120b-cloud via langchain4j) produit un document AsciiDoc structure
- Le document respecte les conventions AsciiDoc du workspace (sectionnement, attributs, include)
- Un fichier `.adoc` valide est ecrit dans le repertoire de sortie
- La source n'est jamais modifiee — seul le fichier genere est produit

=== US-DOC-02 — En tant que formateur, je veux convertir un SPG AsciiDoc en PDF stagiaire

*Priorite* : P1
*EPIC* : DOC-4

.Valeur
Un SPG/SPD redige en AsciiDoc devient un PDF imprimable pour le stagiaire,
avec mise en page professionnelle (Asciidoctor PDF).

.Criteres d'acceptation
- `convertDocumentToPdf` produit un PDF depuis un fichier `.adoc` source
- Le theme PDF est configurable (attributs Asciidoctor, fichier theme YML)
- Les tableaux, listes, blocs de code et admonitions sont rendus correctement
- Le PDF est consultable sur mobile et desktop

=== US-DOC-03 — En tant que developpeur, je veux convertir une spec AsciiDoc en HTML5

*Priorite* : P1
*EPIC* : DOC-3

.Valeur
Une documentation technique en AsciiDoc devient une page HTML navigable,
ideale pour publication sur site statique ou GitHub Pages.

.Criteres d'acceptation
- `convertDocumentToHtml` produit un fichier HTML5 valide
- Le rendu inclut la table des matieres, les ancres, la syntaxe colorisee
- Les includes AsciiDoc sont resolus correctement
- Le fichier HTML s'affiche correctement dans Chrome, Firefox, Safari

=== US-DOC-04 — En tant que lecteur mobile, je veux exporter une formation en EPUB

*Priorite* : P2
*EPIC* : DOC-5

.Valeur
Les sessions de formation sont lisibles hors-ligne sur liseuse Kindle/Kobo
ou application de lecture EPUB.

.Criteres d'acceptation
- `convertDocumentToEpub` produit un fichier `.epub` valide
- La structure du document (chapitres, sections) est preservee dans la navigation
- Les images sont integrees au format EPUB
- Le fichier passe la validation epubcheck

=== US-DOC-05 — En tant qu'integrateur, je veux que les documents soient consommables par runner-gradle N3

*Priorite* : P2
*EPIC* : DOC-6
*Statut* : ✅ TERMINÉ (session 010)

.Valor
Les documents generes et convertis sont indexes dans le composite-context
du runner-gradle pour deploiement automatique.

.Criteres d'acceptation
- ✅ `collectDocumentRetrieve` produit un `metadata.json` conforme au contrat N3
- ✅ Le JSON contient : chemins des fichiers, formats, metadonnees de la source AsciiDoc
- ✅ `runner-gradle assembleCompositeContext` ingere le JSON sans erreur (contrat de facto respecté — `{source, entries, count}` dans composite-context.json)

=== US-DOC-8.3 — En tant qu'intégrateur, je veux que les release notes soient indexées par runner-gradle N3

*Priorite* : P3
*EPIC* : DOC-8
*Statut* : ✅ TERMINÉ (session 016)

.Valeur
Les release notes générées par `releaseNotesGenerate` sont collectées par
`collectDocumentRetrieve` et exposées dans `composite-context.json` +
`metadata.json` pour que runner-gradle `assembleCompositeContext` les
déploie via gh-pages.

.Critères d'acceptation
- ✅ `composite-context.json` inclut un tableau `releaseNotes` + `releaseNotesCount`
- ✅ Chaque entry contient `{source: "document", type: "release-notes", path, rendererType, exists}`
- ✅ `metadata.json` porte `releaseNotesPath` + `releaseNotesRenderer` du release notes primaire
- ✅ Quand le dir release-notes n'existe pas, les champs sont omis (JsonInclude.NON_NULL)
- ✅ Tests fonctionnels valident le flow complet (releaseNotesGenerate → collectDocumentRetrieve)
- ✅ Scénarios Cucumber BDD couvrent le cas avec release notes + le cas sans

=== US-DOC-06 — En tant que redacteur technique, je veux generer de la documentation technique au format DocBook

*Priorite* : P3
*EPIC* : DOC-5
*Statut* : ✅ TERMINÉ (session 005 + tests avancés session 019)

.Valeur
La documentation technique des plugins Gradle peut etre publiee au format DocBook 5,
standard pour la documentation d'API et les manuels de reference.

.Criteres d'acceptation
- ✅ `convertDocumentToDocBook` produit un fichier DocBook 5 valide
- ✅ Les elements DocBook (sections, para, programlisting) sont correctement generes
- ✅ Les tableaux AsciiDoc sont rendus en `<table>` ou `<informaltable>` (test unit + functional + Cucumber session 019)
- ✅ Les blocs de code source sont rendus en `<programlisting>` (test unit + functional + Cucumber session 019)
- ✅ Les listes AsciiDoc sont rendues en `<itemizedlist>` (non ordonnée) ou `<orderedlist>` (ordonnée) (test unit session 019)

=== US-DOC-07 — En tant que developpeur, je veux une page de manuel UNIX depuis mon AsciiDoc

*Priorite* : P3
*EPIC* : DOC-5
*Statut* : ✅ TERMINÉ (session 005 + tests avancés session 019)

.Valeur
Une tache Gradle produit une page de manuel (format troff) directement
depuis le document AsciiDoc source, pour integration dans le systeme.

.Criteres d'acceptation
- ✅ `convertDocumentToManPage` produit un fichier de page de manuel valide
- ✅ La section (1-8) est configurable via l'extension
- ✅ `man <fichier>` affiche correctement le contenu
- ✅ Les options formatées sont rendues en gras troff (`.B` ou `\fB`) (test unit + functional + Cucumber session 019)
- ✅ Les sections imbriquées (`===`) sont préservées (texte ou directive `.SS`) (test unit session 019)

=== US-DOC-08 — En tant qu'editeur, je veux incruster des diagrammes PlantUML dans mon document

*Priorite* : P1
*EPIC* : DOC-9
*Statut* : ✅ TERMINÉ (session 007)

.Valeur
Les diagrammes d'architecture, de sequence, de classe generes par
plantuml-gradle (HTOWN) sont automatiquement incrustes dans le document
final (HTML/PDF/EPUB) sans manipulation manuelle.

.Decision architecture (session 005) — Asciidoctor Diagram inline
- **Option 1 retenue** : Asciidoctor Diagram inline (déjà bundlé — `asciidoctorj-diagram 3.2.0` + `asciidoctorj-diagram-plantuml 1.2025.3`)
- L'auteur écrit `[plantuml]` dans son `.adoc`, le rendu se fait à la conversion
- Zéro dépendance cross-borough, zéro appel à plantumlGenerate
- La génération LLM des `.puml` reste en amont chez HTOWN (orchestré par runner-gradle N3)
- DT-04 (compileOnly plantuml-gradle) devient optionnel — non requis pour DOC-9

.Criteres d'acceptation
- ✅ `enrichDocument` detecte les blocs `[plantuml]` inline dans l'AsciiDoc source
- ✅ Le rendu PNG/SVG est produit à la conversion par Asciidoctor Diagram (déjà bundlé)
- ✅ Les blocs `[plantuml]` sont préservés dans l'AsciiDoc enrichi (pas d'échappement)
- Les diagrammes sont visibles dans tous les formats de sortie (HTML, PDF, EPUB) — vérifié par conversion
- Alternative : si un fichier `.puml` externe est référencé, il est résolu depuis le classpath

=== US-DOC-09 — En tant qu'editeur, je veux personnaliser le theme visuel de mon document

*Priorite* : P1
*EPIC* : DOC-10
*Statut* : ✅ TERMINÉ (session 008)

.Valeur
Le document final (PDF, HTML, EPUB) respecte la charte graphique du projet
(couleurs, polices, logos, en-tete/pied de page) via un systeme de theming
configurable dans le DSL Gradle.

.Decision architecture (session 008) — Theme = value object, pas task séparée
- **Option retenue** : `DocumentTheme` value object injecté à la conversion par attributs AsciidoctorJ
- Le thème n'est pas une tâche `applyTheme` distincte — il est câblé dans `ConvertDocumentTask`
- `toAttributes(format)` mappe vers les attributs AsciidoctorJ par backend :
  * HTML → `stylesheet` (CSS inline dans `<style>`)
  * PDF → `pdf-theme` + `pdf-front-cover-image` (logo)
  * EPUB → `epub-stylesheet`
  * DocBook/ManPage → vide (non supporté)
- DSL nested `document { theme { } }` via `fun interface Action` (SAM)
- Fallback : `DocumentTheme()` vide = défauts AsciidoctorJ

.Criteres d'acceptation
- ✅ Le DSL `document { }` expose une section `theme { }` avec :
  * `pdfTheme.set(file("talaria-theme.yml"))` — fichier YML Asciidoctor PDF
  * `htmlStylesheet.set(file("talaria.css"))` — feuille de style CSS
  * `epubStylesheet.set(file("epub.css"))` — CSS embarque dans l'EPUB
  * `logo.set(file("logo.png"))` — image titre pour PDF
- ✅ Le theme YML PDF supporte : couleurs, polices, marges, en-tete/pied de page (via Asciidoctor PDF)
- ✅ Le theme CSS HTML supporte : polices, couleurs, code highlighting, responsive (via AsciidoctorJ)
- ✅ Fallback : theme par defaut integre si aucun fichier fourni (DocumentTheme vide)
- ✅ Le theme est reutilisable cross-projet (fichier YML/CSS versionne)

=== US-DOC-10 — En tant qu'editeur, je veux injecter du HTML brut dans mon document AsciiDoc

*Priorite* : P2
*EPIC* : DOC-9
*Statut* : ✅ TERMINÉ (session 007)

.Valeur
Des fragments HTML (iframes, widgets, scripts, styles inline) peuvent etre
incrustes directement dans le document AsciiDoc via la syntaxe passthrough
`++++`, et sont rendus correctement dans la sortie HTML5.

.Criteres d'acceptation
- ✅ `enrichDocument` preserve les blocs passthrough `++++` sans les echapper
- ✅ Le HTML injecté est rendu tel quel dans `convertDocumentToHtml`
- Pour PDF/EPUB : le HTML est converti en equivalent natif ou ignore
  avec un avertissement
- ✅ Les includes AsciiDoc contenant du passthrough sont resolus recursivement

=== US-DOC-11 — En tant qu'auteur, je veux produire un livre complet depuis des photos de pages

*Priorite* : P1
*EPIC* : DOC-11 (Pipeline Livre Codex→Document)
*Statut* : ✅ TERMINÉ (session 012)

.Valeur
Pipeline complet de bout en bout : photos de pages d'un livre existant
→ OCR via Codex → AsciiDoc structure → enrichissement (PlantUML, images,
theme) → publication multi-format (HTML/PDF/EPUB). Le tout orchestre
par runner-gradle N3.

.Criteres d'acceptation
- ✅ Codex `collectOcr` produit un `OcrResult.structuredText` (AsciiDoc) par page (côté Codex, déjà livré)
- ✅ Les AsciiDoc OCRises sont assembles en un document unique (ordre des pages) — BookAssembler
- ✅ Document `assembleBook` incruste les photos originales en illustration (image:: directives)
- ✅ Document `enrichDocument` incruste les diagrammes PlantUML references (DOC-9 livré)
- ✅ Document applique le theme configure (DOC-10 livré)
- ✅ `convertDocumentToHtml` produit un livre HTML navigable (TOC, ancres) (DOC-3 livré)
- ✅ `convertDocumentToPdf` produit un PDF imprimeur (theme pro, pagination) (DOC-4 livré)
- ✅ `convertDocumentToEpub` produit un EPUB3 valide (epubcheck) (DOC-5 livré)
- ✅ Runner-gradle N3 `assembleCompositeContext` indexe tous les artefacts (DOC-6 livré)
- ✅ Le pipeline est reproductible : `./gradlew bookPipeline` en une commande — task composite lifecycle

.Flux DAG
----
[CODEX] collectOcr ── photo → LLM → AsciiDoc
    ├─ transformChunk → collectIngest → pgvector (RAG)
    └─ AsciiDoc source (fichier disque, pont N2↔N2)
            │
[DOCUMENT] ─────────┘
    ├─ enrichDocument (PlantUML, images, includes, passthrough)
    ├─ applyTheme (theme.yml, talaria.css)
    ├─ convertDocumentToHtml → HTML5
    ├─ convertDocumentToPdf  → PDF
    └─ convertDocumentToEpub → EPUB3
            │
[RUNNER N3] assembleCompositeContext → deploy gh-pages
----

=== US-DOC-12 — En tant qu'integrateur, je veux un DSL Gradle unifie pour le pipeline documentaire

*Priorite* : P2
*EPIC* : DOC-12
*Statut* : ✅ TERMINÉ (session 013)

.Valeur
Un bloc de configuration unique `document { }` dans `build.gradle.kts`
controle tout le pipeline : source, enrichissement, theme, formats de
sortie, metadata N3.

.Criteres d'acceptation
- ✅ DSL `document { }` expose :
  * `source.set(file("livre.adoc"))` — document source
  * `enrich { plantuml.set(true); images.set(true); passthrough.set(true) }`
  * `theme { pdfTheme.set(file("theme.yml")); htmlStylesheet.set(file("style.css")); epubStylesheet.set(file("epub.css")) }`
  * `outputs { html.set(true); pdf.set(true); epub.set(true) }`
  * `metadata { title.set("..."); author.set("..."); language.set("fr") }`
- ✅ La configuration est serialisable en `document-config.json` (task `serializeDocumentConfig`)
- ✅ Les valeurs par defaut sont documentees (enrich false, outputs html only, language fr)
- ✅ Le DSL est extensible par les boroughs consommateurs (runner, bakery)
- ✅ Nested blocks via `fun enrich(action: Action<DocumentEnrichDsl>)` (org.gradle.api.Action)
- ✅ Classes DSL concrètes avec `val` initialisés via `project.objects.property()`

.Decision technique (session 013) — Nested DSL via `org.gradle.api.Action<T>`
- **Option retenue** : `fun enrich(action: Action<DocumentEnrichDsl>)` (standard Gradle)
- Les `fun interface` SAM ne génèrent pas de lambdas à receiver en Kotlin DSL
- Classes DSL concrètes (non-abstract) avec `val` initialisés dans le plugin via ObjectFactory
- Rétro-compatibilité : flat properties `enrichPlantUml`, `formats` conservées, miroir des nested

=== US-DOC-12b — En tant qu'auteur, je veux configurer le pipeline livre dans un block DSL unifié `book { }`

*Priorite* : P2
*EPIC* : DOC-12 (extension)
*Statut* : ✅ TERMINÉ (session 018)

.Valeur
Les 4 propriétés du pipeline livre (DOC-11) sont groupées dans un block DSL
cohérent avec enrich/outputs/metadata/releaseNotes, au lieu de flat properties
éparpillées sur `DocumentExtension`.

.Criteres d'acceptation
- ✅ DSL `document { book { } }` expose : `pagesDir`, `photosDir`, `title`, `author`
- ✅ Rétro-compatibilité : les flat properties `bookPagesDir/bookPhotosDir/bookTitle/bookAuthor` restent fonctionnelles (mirror)
- ✅ Conventions par défaut : title="Untitled Book", author="Unknown Author"
- ✅ Scénarios Cucumber BDD couvrent book block, photos, et fallback defaults
- ✅ Sérialisation dans `document-config.json` via `SerializeDocumentConfigTask` (session 019 — BookConfig value object + DocumentConfigSerializer + task inputs + plugin wiring)
- ✅ Le block `book` est omis du JSON quand le pipeline livre n'est pas configuré (aucun pagesDir/photosDir)

=== US-DOC-9b — En tant qu'éditeur, je veux que les directives `image::` soient rendues dans le document final

*Priorite* : P3
*EPIC* : DOC-9 (extension)
*Statut* : ✅ TERMINÉ (session 018 HTML/PDF + session 019 EPUB)

.Valeur
Les directives `image::photo.png[]` du document AsciiDoc source sont préservées
par l'enrichissement et rendues correctement dans la sortie HTML (`<img>` tag),
PDF (image intégrée) et EPUB (image embarquée comme entrée du zip).

.Criteres d'acceptation
- ✅ `DocumentEnricher` préserve les `image::` sans échappement (2 tests unitaires)
- ✅ `convertDocumentToHtml` rend un `<img src="photo.png">` (test functional)
- ✅ `convertDocumentToPdf` intègre l'image (test functional)
- ✅ `convertDocumentToEpub` embarque l'image comme entrée du zip (test unit + functional)
- ✅ Scénario Cucumber `@doc9 @images` valide le critère HTML (session 018)
- ✅ Scénario Cucumber `@doc9 @images @epub` valide le critère EPUB (session 019)

=== US-DOC-8.5 — En tant qu'intégrateur, je veux que document-gradle soit publié sur Maven Central

*Priorite* : P1
*EPIC* : DOC-8.5
*Statut* : ✅ TERMINÉ (session 018)

.Valeur
Le plugin est consommable par tout projet Gradle externe via
`id("education.ccp.document")` sans compilation locale.

.Criteres d'acceptation
- ✅ `education.ccp:document-plugin:0.0.1` publié sur Maven Central (bundle 4d15e522)
- ✅ POM complet : name, description, licence Apache 2.0, developer, SCM
- ✅ Aucune dépendance -SNAPSHOT
- ✅ Dépendances `education.ccp:*` (i18n-contracts, opencode-session-contracts, pipeline-contracts) sur Central
- ✅ conventions-gradle 0.0.2 publié (bug fix SCM double .git) + 15 boroughs harmonisés

'''
== Dette Technique

[cols="1,5,2", options="header"]
|===
| ID | Description | Priorite
| DT-01 | Aucune — projet neuf, zero dette | -
| DT-02 | [ARCHI] Suppression dependance compileOnly codex-gradle dans build.gradle.kts | P0 — session 001
| DT-03 | [ARCHI] AsciidoctorJ en implementation directe (version geree par workspace-bom) | P0 — session 001
| DT-04 | [ARCHI] Dépendance compileOnly plantuml-gradle — *OPTIONNEL* (Asciidoctor Diagram inline suffit pour DOC-9) | P3 — reporté
| DT-05 | [ARCHI] Ajout dependance implementation Asciidoctor Diagram (PlantUML inline) — *DÉJÀ BUNDLÉ* (asciidoctorj-diagram 3.2.0) | ✅ TERMINÉ
|===

'''
== Risques

[cols="1,3,2,2", options="header"]
|===
| ID | Risque | Impact | Probabilite
| R-02 | Dependance langchain4j pour l'IA — cout token LLM | Moyen | Moyen
| R-03 | Asciidoctor PDF necessite Ruby (rouge) ou bibliotheques natives | Bloquant | Moyen
| R-04 | Les formats EPUB/DocBook/ManPage sont moins utilises — bugs latents | Faible | Faible
| R-05 | plantuml-gradle non execute avant enrichDocument → diagrammes manquants | Moyen | Moyen
| R-06 | Cout tokens OCR : 300 pages × gpt-oss:120b-cloud → budget significatif | Moyen | Eleve
| R-07 | Asciidoctor Diagram necessite Graphviz installe sur la machine | Moyen | Moyen
| BND-01 | Codex duplique une tache de conversion (ExportKnowledgeBase fait deja AsciiDoc→Markdown) | Moyen | Moyen
| BND-02 | Deux versions d'AsciidoctorJ cohabitent (codex vs document) | Faible | Faible
|===

_Note : R-01 (AsciidoctorJ version mismatch avec codex-gradle) supprime —_
_document a sa propre dependance AsciidoctorJ directe, pas de compileOnly sur codex._

'''
== Notes

* AsciidoctorJ est une dependance directe (implementation) — pas via codex-gradle.
  Les deux boroughs partagent la version via workspace-bom.
* La generation IA peut etre desactivee (fallback template) si le LLM n'est pas disponible.
* Boundary claire : Codex = READ + RAG (ingestion, vectorisation, recherche semantique).
  Document = WRITE + PUBLISH (creation IA, enrichissement, conversion multi-format).
  Voir `.agents/ARCHITECTURE_BOUNDARY.adoc`.
* Le LLM bridge est partage avec planner-gradle en compileOnly pour eviter duplication.
* plantuml-gradle est en compileOnly (composition) — un diagramme est un constituant
  atomique du document, pas un pair N2. Relation contenant→contenu.
* Priorite MVP0 : PDF stagiaire et HTML site statique > EPUB > DocBook > ManPage.
* Pipeline livre (photos → OCR → AsciiDoc → livre multi-format) : voir US-DOC-11.
  Codex gere l'OCR, Document gere la publication. Orchestration par runner-gradle N3.
* Le theme YML PDF et le CSS HTML/EPUB sont des fichiers versionnes dans le projet,
  pas generes par le plugin. Document les applique, ne les cree pas.

'''

'''
== EPIC DOC-TRANSLATE — Pipeline de traduction AsciiDoc (refacto transverse bakery→document)

_Cadré Session 021 (2026-07-19). Document est le bounded context naturel pour toute
manipulation d'AsciiDoc (parse, traduire, convertir, enrichir). Bakery n'a pas
vocation à maintenir un AST AsciiDoc — il devrait le consommer._

[CAUTION]
====
**Motivation** : bakery-gradle contient un pipeline de traduction AsciiDoc complet
(`PivotModel` AST, `AsciiDocParser`, `ContentTranslationService`, `PlantUmlTranslationAdapter`,
`SubtreeI18nPlanner`, `I18nDeltaApplier`) qui n'a rien à voir avec le métier de bakery
(site statique JBake). Ce code devrait appartenir à document-gradle (WRITE + TRANSLATE +
CONVERT + PUBLISH), le bounded context naturel pour la manipulation d'AsciiDoc.

**Bénéfices** :
. Document = single source of truth pour la manipulation d'AsciiDoc
. Réutilisation cross-borough : magic-stick, cccp.education, talaria peuvent traduire sans bakery
. Bakery allégé, recentré sur son vrai métier : site statique JBake
. Boundary claire : Document = WRITE + TRANSLATE + CONVERT + PUBLISH

**DAG** : bakery → document (nouvelle dépendance `implementation`)
====

=== Artefacts à migrer

[cols="3,3,2,2"]
|===
| Artefact bakery | Destination document | Lignes | Tests
| `PivotModel.kt` | `document/translation/PivotModel.kt` | 95 | 20 UT
| `AsciiDocParser.kt` | `document/translation/AsciiDocParser.kt` | 412 | 30 UT
| `PivotYamlRenderer.kt` | `document/translation/PivotYamlRenderer.kt` | 138 | 10 UT
| `AsciiDocRenderer.kt` | `document/translation/renderer/` | 111 | 5 UT
| `JbakeNativeRenderer.kt` | `document/translation/renderer/` | 127 | 5 UT
| `TextTranslatableClassifier.kt` | `document/translation/` | 58 | 10 UT
| `ContentTranslationService.kt` | `document/translation/` | 260 | 15 UT
| `PlantUmlTranslationAdapter.kt` | `document/translation/plantuml/` | 200 | 10 UT
| `I18nDeltaApplier.kt` | `document/translation/delta/` | 60 | 8 UT
| `ContentChecksum.kt` | `document/translation/delta/` | 20 | —
| `SubtreeI18nPlanner.kt` | `document/translation/plan/` | 66 | 10 UT
| `SiteTranslationPlan.kt` | `document/translation/plan/` | 80 | 20 UT
|===

=== User Stories — baby steps DDD/TDD/BDD

[cols="1,4,2,2,2"]
|===
| US | Description | Tests | Pts | Dépend de

| DOC-TRANSLATE-1
| *Déplacer `PivotModel` + `AsciiDocParser`* — copier les 2 fichiers dans
  `document/translation/`, changer le package `bakery.pivot` → `document.translation`.
  Copier les tests associés. Vérifier que bakery compile encore (les fichiers
  restent en place dans bakery, pas de suppression).
| UT : 20 PivotModel + 30 AsciiDocParser = 50 UT GREEN dans document
| 5
| —
| ✅ TERMINÉ (session 021)

| DOC-TRANSLATE-2
| *Déplacer `PivotYamlRenderer` + `AsciiDocRenderer` + `JbakeNativeRenderer` +
  `TextTranslatableClassifier`* — 4 fichiers dans `document/translation/renderer/`.
  Copier les tests.
| UT : 10 + 5 + 5 + 10 = 30 UT GREEN
| 5
| TRANSLATE-1
| ✅ TERMINÉ (session 021)

| DOC-TRANSLATE-3
| *Déplacer `ContentTranslationService` + `PlantUmlTranslationAdapter` +
  `PlantUmlClassifier`* — 3 fichiers. Dépend de `i18n-contracts` (déjà en
  implementation dans document). Copier les tests.
| UT : 15 + 10 + 14 = 39 UT GREEN
| 8
| TRANSLATE-2
| ✅ TERMINÉ (session 022)

| DOC-TRANSLATE-4
| *Déplacer `I18nDeltaApplier` + `ContentChecksum` + `SubtreeI18nPlanner` +
  `SiteTranslationPlan`* — 4 fichiers dans `document/translation/delta/` et
  `document/translation/plan/`. Copier les tests.
| UT : 8 + 10 + 20 = 38 UT GREEN
| 8
| TRANSLATE-3
| ✅ TERMINÉ (session 023)

| DOC-TRANSLATE-5
| *Câbler bakery comme consommateur* — remplacer les imports `bakery.pivot.*`
  et `bakery.i18n.*` dans bakery par `document.translation.*`. Ajouter
  `implementation("education.cccp:document-plugin:0.0.1")` dans bakery.
  Supprimer les fichiers source migrés de bakery. Vérifier 0 régression.
| FT + BDD : 0 régression bakery, tous les tests bakery passent
| 8
| TRANSLATE-4
|===

_Total_ : ~34 pts, 5 US.

=== Dépendances (DAG)

----
TRANSLATE-1 (PivotModel + Parser) ─→ TRANSLATE-2 (Renderers) ─→ TRANSLATE-3 (Translation) ─→ TRANSLATE-4 (Delta/Plan) ─→ TRANSLATE-5 (Câblage bakery)
----

=== Projection sessions

* *S021* — TRANSLATE-1 + TRANSLATE-2 (10 pts, PivotModel + AsciiDocParser + PivotYamlRenderer + AsciiDocRenderer + JbakeNativeRenderer + TextTranslatableClassifier) ✅ TERMINÉ
* *S022* — TRANSLATE-3 (8 pts, TranslationService + PlantUmlTranslationAdapter + PlantUmlClassifier) ✅ TERMINÉ
* *S023* — TRANSLATE-4 (8 pts, Delta + Plan) ✅ TERMINÉ
* *S024* — TRANSLATE-5 (8 pts, Câblage bakery consommateur, clôture EPIC)

Projection conservatrice : 5 sessions, ~34 pts, méthodo baby step DDD/TDD/BDD.

'''
== EPIC PUB — Publication Maven Central + Plugin Portal

_Source : configuration/EPIC_PUB.adoc (Session 096)_

_Note_ : document-gradle n'est pas dans le plan de publication Maven Central pour l'instant.
Seul le relocation comment est a mettre a jour pour coherence transverse.

=== PUB-2.2 — Mettre a jour le commentaire relocation

Dans le `build.gradle.kts`, remplacer :
```kotlin
// RELOCATION : prepare la migration du groupId education.cccp →
// <futur-domaine>. Activer avec -Prem relocationGroup="io.github.cccp-education"
// Effet : injecte <distributionManagement><relocation><groupId>...</groupId></relocation>
// dans le POM publie. Les consommateurs existants seront rediriges automatiquement
// vers le nouveau groupId lors de la prochaine maj de dependance.
```
Par :
```kotlin
// RELOCATION : garde comme echappatoire future.
// Activable avec -Prem relocationGroup="<futur-namespace>"
```

=== PUB-5 — Verification que le relocation est bien cable

Verifier que le bloc `maven-publish` + `signing` dans `build.gradle.kts` contient
bien la section `<relocation>` conditionnelle via `-Prem relocationGroup=...`.

'''
== EPIC DOC-TABLE-VALIDATE — Validation syntaxique des tableaux AsciiDoc post-traduction

_Cadré Session 033 (2026-08-02)._

[CAUTION]
====
**Motivation** : le `DocumentTranslator` traduit les cellules de tableau une par une
via LLM (`translateInlines`), mais rien ne vérifie que le tableau rendu par
`AsciiDocRenderer` reste syntaxiquement valide. Un LLM peut produire un contenu de
cellule qui casse les délimiteurs (`|`, `|===`), désaligne le nombre de colonnes,
ou corrompt les `cols` specs. Le bug #0124 de la session 032 (DescriptionList+BlockMacro
fusionnés) a été découvert pendant le dogfooding batch — une table corrompue par
traduction passerait inaperçue jusqu'au rendu JBake → page cassée en prod.

**Bénéfices** :
. Filet de sécurité pour tous les consommateurs de `document-gradle` (bakery, runner, slider, etc.)
. Détection précoce des corruptions LLM avant publication
. Rapport consolidé en mode batch pour diagnostic rapide
. Boundary claire : Document = garant de la syntaxe AsciiDoc produite
====

=== User Stories — baby steps DDD/TDD/BDD

[cols="1,4,2,2,2"]
|===
| US | Description | Tests | Pts | Dépend de

| DOC-TABLE-VALIDATE-1
| *`TableSyntaxValidator`* — value object DDD pur. Vérifie :
  - Cohérence `cols` vs nombre de colonnes header/rows (si `cols="2,3"` → 2 colonnes attendues)
  - Délimiteurs intacts : pas de `\|` parasite dans les cellules, pas de `|===` dans le contenu
  - Nombre de colonnes homogène : header et chaque row ont le même nombre de cellules
  - Retourne `TableValidationResult` sealed (Valid / Invalid avec `articleTitle`, `tableIndex`, `reason`)
| UT : 15-20 (cols mismatch, delimiter corruption, column count mismatch, valid table)
| 5
| —
| ✅ TERMINÉ (session 035)

| DOC-TABLE-VALIDATE-2
| *Intégration dans `DocumentTranslator.translateBlock(Table)`* — après traduction
  des cellules, appeler `TableSyntaxValidator.validate()` sur le bloc traduit.
  Si `Invalid` : wrapper dans `TranslationWarning` (ne pas bloquer la traduction
  des autres blocs). Logger le warning avec contexte (article, table index, reason).
| UT : 8-10 (mock validator, warning propagation, valid table no-op)
| 5
| VALIDATE-1
| ✅ TERMINÉ (session 035)

| DOC-TABLE-VALIDATE-3
| *Mode `--strict` vs `--lenient`* — DSL `translation { tableValidation { mode = STRICT } }`.
  - `STRICT` : `Invalid` → `TranslationException`, fail build
  - `LENIENT` (défaut) : `Invalid` → warning log + continuer
  - `OFF` : pas de validation (rétrocompatibilité)
| UT : 5-8 (strict throws, lenient warns, off skips) + FT : 2 (CLI -Pdocument.translateTableValidation=strict/lenient/off)
| 5
| VALIDATE-2
| ✅ TERMINÉ (session 036)

| DOC-TABLE-VALIDATE-4
| *Intégration dans `translateDocumentBatch`* — rapport consolidé
  `table-validation-report.json` produit dans l'output dir. Contient :
  `[{article, tableIndex, status, reason}]`. Agrégé par `BatchDocumentTranslator`.
| UT : 5-8 (report generation, empty report, multi-article aggregation) + FT : 1 (batch avec tables corrompues)
| 5
| VALIDATE-3
| ✅ TERMINÉ (session 036)

| DOC-TABLE-VALIDATE-5
| *Cucumber BDD* — scénarios end-to-end :
  - `@table-validation @valid` — table simple FR→EN, validation passe
  - `@table-validation @corrupted-delimiter` — LLM injecte `\|` dans une cellule → LENIENT warn, STRICT fail
  - `@table-validation @column-mismatch` — LLM produit une colonne en trop → Invalid
  - `@table-validation @cols-mismatch` — `cols="2,3"` mais 3 colonnes dans les rows
  - `@table-validation @batch-report` — batch 3 articles, 1 table corrompue → rapport JSON
| BDD : 5 scénarios
| 5
| VALIDATE-4
|===

_Total_ : ~25 pts, 5 US.

=== Dépendances (DAG)

----
VALIDATE-1 (TableSyntaxValidator) ─→ VALIDATE-2 (DocumentTranslator) ─→ VALIDATE-3 (strict/lenient) ─→ VALIDATE-4 (batch report) ─→ VALIDATE-5 (Cucumber)
----

=== Projection sessions

* *S033* — Cadrage EPICs ✅ TERMINÉ
* *S034* — Fondation Table POJO DDD ✅ TERMINÉ
* *S035* — VALIDATE-1 + VALIDATE-2 (10 pts, TableSyntaxValidator + intégration DocumentTranslator) ✅ TERMINÉ
* *S036* — VALIDATE-3 + VALIDATE-4 (10 pts, strict/lenient + batch report) ✅ TERMINÉ
* *S037* — VALIDATE-5 (5 pts, Cucumber BDD, clôture EPIC)

Projection conservatrice : 5 sessions, ~25 pts, méthodo baby step DDD/TDD/BDD.

'''
== EPIC DOC-PLANTUML-VALIDATE — Validation syntaxique des diagrammes PlantUML post-traduction (DRY via plantuml-gradle + BOM)

_Cadré Session 033 (2026-08-02)._

[CAUTION]
====
**Motivation** : le `PlantUmlTranslationAdapter` traduit les labels dans les blocs
`[plantuml]` mais rien ne vérifie que le diagramme reste syntaxiquement valide après
substitution. Un LLM peut produire un label qui casse la syntaxe PlantUML (guillemets
non échappés, identifiants réservés, `@startuml`/`@enduml` corrompus).

**DRY** : plantuml-gradle (HTOWN) expose déjà `PlantumlService.validateSyntax()` qui
utilise `net.sourceforge.plantuml.SourceStringReader` (le vrai parser PlantUML).
Ce validateur est self-contained, sans dépendance Gradle, et disponible via
`education.cccp:plantuml-plugin:0.0.2` sur Maven Central. Le BOM (`workspace-bom:0.0.13`)
versionne déjà `plantuml-plugin` comme dépendance.

**Stratégie DRY** :
. `document-gradle` ajoute `implementation(libs.plantuml.plugin)` (déjà dans le BOM)
. `PlantUmlTranslationAdapter` appelle `PlantumlService().validateSyntax()` après traduction
. Zéro duplication de logique de parsing PlantUML — single source of truth chez HTOWN
. Si un jour `plantuml-contracts` N0 est créé dans le BOM, le contrat de validation
  y sera extrait (actuellement : pas de contrat N0 dédié, le service HTOWN fait office)

**Bénéfices** :
. Filet de sécurité pour la traduction des diagrammes PlantUML dans les documents
. Réutilisation du validateur natif PlantUML (SourceStringReader) — pas de regex maison
. Cohérence cross-borough : même validateur utilisé par `validatePlantumlSyntax` (HTOWN)
  et par `translateDocument` (document)
. Prêt pour extraction future en `plantuml-validation-contracts` N0 si le besoin émerge
====

=== User Stories — baby steps DDD/TDD/BDD

[cols="1,4,2,2,2"]
|===
| US | Description | Tests | Pts | Dépend de

| DOC-PLANTUML-VALIDATE-1
| *Ajout dépendance `plantuml-plugin`* — `implementation(libs.plantuml.plugin)` dans
  `build.gradle.kts` de document-plugin. Le BOM versionne déjà `plantuml-plugin:0.0.2`.
  Vérifier que la compilation passe (pas de conflit de dépendances transitives).
  `PlantumlService` et `SyntaxValidationResult` sont accessibles.
| Compile OK + 1 FT (vérifie que PlantumlService est instanciable)
| 3
| —

| DOC-PLANTUML-VALIDATE-2
| *`PlantUmlSyntaxValidator` wrapper DDD* — value object dans
  `document.translation.plantuml`. Wrappe `PlantumlService.validateSyntax()`.
  Adapte `SyntaxValidationResult` (HTOWN) → `PlantUmlValidationResult` (document)
  avec contexte enrichi : `articleTitle`, `blockIndex`, `strategy` (TranslateLabels/
  PreserveTechnical/BorrowVocabulary). Le wrapper est nécessaire pour :
  - Découpler le domaine document du domaine plantuml (dependency inversion)
  - Enrichir l'erreur avec le contexte documentaire (quel article, quel bloc)
  - Préparer l'extraction future en contrat N0
| UT : 8-10 (valid diagram, missing @startuml, syntax error, strategy context)
| 5
| VALIDATE-1

| DOC-PLANTUML-VALIDATE-3
| *Intégration dans `PlantUmlTranslationAdapter.translate()`* — après traduction
  des labels, appeler `PlantUmlSyntaxValidator.validate()` sur le bloc traduit.
  Si `Invalid` : logger le warning avec contexte (article, block index, strategy).
  Ne pas bloquer la traduction des autres blocs (mode lenient par défaut).
| UT : 5-8 (valid after translation, corrupted by LLM, preserveTechnical skip)
| 5
| VALIDATE-2

| DOC-PLANTUML-VALIDATE-4
| *Mode `--strict` vs `--lenient`* — DSL `translation { plantumlValidation { mode = STRICT } }`.
  Cohérent avec DOC-TABLE-VALIDATE-3 (même enum `ValidationMode { STRICT, LENIENT, OFF }`).
  - `STRICT` : `Invalid` → `TranslationException`, fail build
  - `LENIENT` (défaut) : `Invalid` → warning log + continuer
  - `OFF` : pas de validation (rétrocompatibilité)
| UT : 3-5 (strict throws, lenient warns, off skips) + FT : 1 (CLI -Pdocument.translatePlantumlValidation=strict)
| 3
| VALIDATE-3

| DOC-PLANTUML-VALIDATE-5
| *Cucumber BDD* — scénarios end-to-end :
  - `@plantuml-validation @valid` — diagramme simple FR→EN, validation passe
  - `@plantuml-validation @corrupted-tags` — LLM corrompt `@startuml` → Invalid
  - `@plantuml-validation @broken-label` — guillemets non échappés dans un label → Invalid
  - `@plantuml-validation @preserve-technical` — stratégie PreserveTechnical, pas de validation (no-op)
  - `@plantuml-validation @strict-mode` — mode STRICT, diagramme corrompu → exception
| BDD : 5 scénarios
| 5
| VALIDATE-4
|===

_Total_ : ~21 pts, 5 US.

=== Dépendances (DAG)

----
VALIDATE-1 (dépendance plantuml-plugin) ─→ VALIDATE-2 (PlantUmlSyntaxValidator wrapper) ─→ VALIDATE-3 (intégration adapter) ─→ VALIDATE-4 (strict/lenient) ─→ VALIDATE-5 (Cucumber)
----

=== Note DRY — Extraction future en contrat N0

Aujourd'hui le validateur est consommé directement depuis `plantuml-plugin:0.0.2`
(implementation). Si d'autres boroughs (codex, slider, bakery) ont besoin de valider
du PlantUML, un contrat N0 `plantuml-validation-contracts` pourra être extrait dans
le BOM avec :
- `PlantUmlSyntaxValidator` interface
- `PlantUmlValidationResult` sealed class
- `ValidationMode` enum

Le wrapper `PlantUmlSyntaxValidator` dans document-gradle est conçu pour cette
extraction future : il ne dépend que de `PlantumlService.validateSyntax()` et
adapte le résultat. Le jour où le contrat N0 existe, il suffira de changer
l'implémentation du wrapper.

=== Projection sessions

* *S036* — VALIDATE-1 + VALIDATE-2 (8 pts, dépendance + wrapper DDD)
* *S037* — VALIDATE-3 + VALIDATE-4 (8 pts, intégration adapter + strict/lenient)
* *S038* — VALIDATE-5 (5 pts, Cucumber BDD, clôture EPIC)

Projection conservatrice : 3 sessions, ~21 pts, méthodo baby step DDD/TDD/BDD.

'''
== ✅ EPIC DOC-BOOK-VALIDATE — Validation du pipeline livre (TERMINÉ, Session 216)

_Cadré Session 070 — `office/metiers/FPA/Devenir_Formateur_Professionnel_d_Adultes_FPA_II/CADRAGE_EPIC_FPA_BOOK.adoc`._

[CAUTION]
====
**Motivation** : le pipeline livre (DOC-11) assemble les pages OCRisées par
codex en un document unique, mais rien ne **valide** la sortie. `BookAssembler`
assemble, mais aucune couche ne vérifie que la TOC est cohérente, que l'ordre
des pages est monotone, que les sections ne sont pas vides, que les références
PDF sont résolues. C'est la couche de texte qui valide la création de document
assisté par codex/IA.

**Frontière licence** : le contenu (livre scanné + transcription OCR) est
propriétaire/dérivé et reste dans `office/metiers/FPA/` (repo privé `cheroliv/office`).
Le code de validation (BookValidator + tests) est Apache 2.0 et vit ici, dans
document-gradle. On prend des morceaux du livre pour valider l'exécutable, mais
la rédaction du livre reste dans `office/`.

**Bénéfices** :
. Filet de sécurité pour tout consommateur du pipeline livre (codex, runner)
. Détection précoce des corruptions OCR/assemblage avant publication
. Boundary claire : Document = garant de la validité du livre produit
====

=== User Stories — baby steps DDD/TDD/BDD

[cols="1,4,2,2,2"]
|===
| US | Description | Tests | Pts | Dépend de

| DOC-BOOK-VALIDATE-0
| *Cadrage* — BookValidator DDD pur, décisions architecturales
| — (gouvernance pure)
| 0
| —

| DOC-BOOK-VALIDATE-1
| *`BookValidator`* — value object DDD pur. Vérifie :
  - TOC cohérente (chaque section référencée a une page)
  - Ordre des pages monotone (pas de page manquante/dupliquée)
  - Sections non vides (pas de page OCRisée vide)
  - Références PDF résolues (chaque `NNN.pdf` existe)
  - Retourne `BookValidationResult` sealed (Valid / Invalid avec raison)
| UT : 15-20 (TOC mismatch, page manquante, section vide, ref non résolue, valide)
| 5
| —

| DOC-BOOK-VALIDATE-2
| *Intégration dans `AssembleBookTask`* — après assemblage, appeler
  `BookValidator.validate()`. Si `Invalid` : warning log + continuer (lenient). ✅ TERMINÉ (S-078)
| UT : 6 (BookTocParser TDD) + FT : 2 (lenient/STRICT assembleBook)
| 5
| VALIDATE-1

| DOC-BOOK-VALIDATE-3
| *Mode `--strict` vs `--lenient`* — DSL `book { validationMode.set(ValidationMode.STRICT) }`
  ou CLI `-Pdocument.bookValidationMode=STRICT`. STRICT → `GradleException` fail build ;
  LENIENT (défaut) → warning. ✅ TERMINÉ (S-078)
| UT : 6 + FT : 2
| 3
| VALIDATE-2

| DOC-BOOK-VALIDATE-4
| *Cucumber BDD* — scénarios end-to-end : ✅ TERMINÉ (S-216)
  - `@book-validation @valid` — livre complet, validation passe
  - `@book-validation @missing-page` — page manquante → Invalid
  - `@book-validation @empty-section` — section vide → Invalid
  - `@book-validation @toc-mismatch` — TOC incohérente → Invalid
  - `@book-validation @strict` — mode STRICT → exception (`BookValidator.enforce` extrait du task)
| BDD : 5 scénarios 5/5 PASSED
| 5
| VALIDATE-3
|===

_Total_ : ~18 pts, 5 US.

=== Dépendances (DAG)

----
VALIDATE-0 (cadrage) ─→ VALIDATE-1 (BookValidator) ─→ VALIDATE-2 (intégration) ─→ VALIDATE-3 (strict/lenient) ─→ VALIDATE-4 (Cucumber)
----

=== Projection sessions

* *S-044* — VALIDATE-0 cadrage + VALIDATE-1 (5 pts, BookValidator DDD pur)
* *S-045* — VALIDATE-2 + VALIDATE-3 (8 pts, intégration + strict/lenient)
* *S-046* — VALIDATE-4 (5 pts, Cucumber BDD, clôture EPIC)
* *S-216* — VALIDATE-4 Cucumber BDD 5/5 PASSED (`@valid`/`@missing-page`/`@empty-section`/`@toc-mismatch`/`@strict`), `BookValidator.enforce` extrait du task, EPIC CLÔTURÉ

Projection conservatrice : 3 sessions, ~18 pts, méthodo baby step DDD/TDD/BDD. *Réalisé en 1 session (S-216) — EPIC ✅ TERMINÉ.*

'''
== ✅ EPIC DOC-CR3 — Revue CR-GLOBALE-003 : Création de Contenu Solide (TERMINÉ, Session 220)

_Source_ : `GLOBAL_CODE_REVIEW.adoc` (workspace root) — session CR-GLOBALE-003 (2026-08-04).
_Focus_ : solidité de la chaîne documentaire multi-format.
_Statut_ : DOC-CR3-1 ✅ TERMINÉ (S-219, TDD strict), DOC-CR3-3 ✅ TERMINÉ (S-219), DOC-CR3-2 ✅ TERMINÉ (S-220).

[cols="1,3,2,1", options="header"]
|===
| EPIC | Description | Priorité | Statut

| DOC-CR3-1
| **Réutiliser l'instance AsciidoctorJ** — `DocumentConverter.kt:106,128` crée un
`Asciidoctor.Factory.create()` par conversion (startup JRuby à chaque appel). Pour 5 formats = 5 startups.
Injecter/partager une instance ( lifecycle plugin) ou pool. Gain perf significatif sur le bookPipeline.
→ Résolu S-219 : `AsciidoctorHolder` (instance unique lazy+cache+synchronized), `DocumentConverter` délègue.
| 🟠 P1 | ✅ TERMINÉ (S-219)

| DOC-CR3-2
| **Auditer `SafeMode.UNSAFE` systématique** — `DocumentConverter.buildOptions` force `UNSAFE`
sur tous les backends. Justifiable en local, mais à documenter/limiter (include resolution, macros).
Ajouter une garde configurable `safeMode` DSL si du contenu externe est ingéré.
→ Résolu S-220 : `DocumentConverter.buildOptions` accepte un `SafeMode` configurable
  (UNSAFE/SERVER/SECURE, défaut UNSAFE pour backward-compat), propagé via le bloc DSL
  `converter { safeMode = ... }` (mirror flat `safeMode`) + flag CLI `-Pdocument.safeMode`.
  `ConvertDocumentTask` consomme la valeur. Baby-step DDD/TDD/BDD : 3 UT (`buildOptions` SafeMode)
  + 1 UT conversion + 4 FT (GradleTestKit DSL/CLI) + 3 scénarios Cucumber `@converter-safe-mode` (tous GREEN).
| 🟡 P2 | ✅ TERMINÉ (S-220)

| DOC-CR3-3
| **Supprimer le code mort** — `DocumentI18nBridge` et `DocumentGenerationGraph.graph` sont morts
(confirmé CR-GLOBALE-002). Décider : câbler ou expulser. Expulser recommandé sauf besoin explicite.
→ `DocumentI18nBridge` déjà retiré (S-030). `DocumentGenerationGraph.graph` retiré S-219 (fichier koog-free).
| 🟡 P2 | ✅ TERMINÉ (S-219)
|===

'''
== ✅ EPIC CROSS-DOC-BOOK — BDD cross-borough Codex→Document Book Pipeline (TERMINÉ, Session 210)

_Source_ : cadrage S-210 (`CADRAGE_EPIC_CROSS_DOC_BOOK.adoc`).
_Pattern_ : gouvernance pure S-057/S-063 + baby-step BDD strict.
_Portée_ : cross-borough Brooklyn (codex KDoc) + New Orleans (document Cucumber).

Le pont N2↔N2 entre codex et document est le filesystem
(`%03d-%s.adoc`). Le contrat fonctionne mais n'est jamais validé en
BDD end-to-end. Cet EPIC ajoute des tests Cucumber qui garantissent
la contractualité du flow `collectOcr → assembleBook`.

[cols="1,5,2,1", options="header"]
|===
| US | Description | Statut | Pts

| CROSS-DOC-BOOK-0
| Cadrage EPIC (gouvernance pure, zéro code). 6 constats, 8 décisions.
| ✅ TERMINÉ (S-210)
| 1

| CROSS-DOC-BOOK-1
| Cucumber BDD `cross_borough_book.feature` — scénarios `@cross-borough @book-pipeline`
  (pages assemblées, tri PageOrder, dégradé silencieux, formats non-adoc ignorés).
  Runner dédié `CrossBoroughCucumberRunner` + steps `CrossBoroughSteps`.
  Baby-step BDD strict RED → GREEN → REFACTOR.
| ✅ TERMINÉ (S-210)
| 3

| CROSS-DOC-BOOK-2
| Codex — KDoc clarification `CollectOcrTask.outputDir` (format `%03d-%s.adoc`
  documenté comme contrat formel N2↔N2, `@see document.BookAssembler`).
| ✅ TERMINÉ (S-210)
| 1

|===

*Total* : ~5 pts (3 US)

'''
== ✅ EPIC DOC-BOOK-DOMAIN — Domaine « Book » structuré (arbre, mise en page, logique)

_Cadré Session 211 — `.agents/CADRAGE_EPIC_DOC_BOOK_DOMAIN.adoc`._
_Consommateur_ : `office/metiers/FPA` (FPA-BOOK-4/5, livre FPA II scanné).

[CAUTION]
====
**Motivation** : `assembleBook` (DOC-11) produit aujourd'hui un *blob AsciiDoc
plat* — header + concaténation des pages, **aucun niveau de section**. Or la TOC
source (FPA, ~180 sections) encode une hiérarchie profonde par son `Référence`
(`1.0.2.6.4`). Le `doctype: book` AsciiDoc attend un arbre (parties `==`,
chapitres `===`, sections `====`) — l'assemblage plat casse la navigabilité
PDF/EPUB et empêche la validation humaine du rendu (FPA-BOOK-4).

**Besoin** : un agrégat DDD `BookTree` (Parties→Chapitres→Sections→Sous-sections)
dérivé du `ref` de la TOC, un `Matter` (FRONT/BODY/BACK), un `BookLayout` (mise
en page : niveaux, sauts de page, page de titre, `:toc:`), et un `BookNumbering`
(logique : numérotation, ancres, navigation). Le contenu FPA reste dans
`office/`, le domaine + tests sont Apache 2.0 ici.

**Bénéfices** :
. Livre PDF/EPUB *navigable* (sommaire cliquable, numérotation, ancres)
. Séparation front/body/back matter (préface, glossaire, index, annexes)
. `BookValidator` peut valider la *structure* (continuité ref, front/back présents)
. Réutilisable par tout consommateur du pipeline livre (codex, runner, FPA)
====

=== User Stories — baby steps DDD/TDD/BDD

[cols="1,4,2,2,2"]
|===
| US | Description | Tests | Pts | Dépend de | Statut

| DOC-BOOK-DOMAIN-0
| *Cadrage* — BookTree DDD, décisions architecturales, modèle du domaine
| — (gouvernance pure)
| 0 ✅ (S-211)
| —
| ✅ TERMINÉ (S-211)

| DOC-BOOK-DOMAIN-1
| *`BookNode` + `BookTree` + `BookTreeBuilder`* — value objects DDD purs.
  `fromSections(List<BookSection>)` dérive la hiérarchie depuis le `ref`
  (profondeur = nb segments, parent = préfixe). Unicité ref, lien parent, ordre.
| UT : 15 (depth, parent link, uniqueness, orphan, ordering, multi-level)
| 5
| —
| ✅ TERMINÉ (S-212)

| DOC-BOOK-DOMAIN-2
| *`Matter` (FRONT/BODY/BACK)* classifier + *`BookLayout`* value object
  (niveau de section par profondeur, politique saut de page, page de titre + `:toc:`).
| UT : 12 (matter, level→`==`, page-break, front/back emission)
| 4
| DOMAIN-1
| ✅ TERMINÉ (S-212)

| DOC-BOOK-DOMAIN-3
| *Refactor `BookAssembler.assemble(tree, layout)`* — émet AsciiDoc structuré
  (front + corps imbriqué niveaux + back). Ancienne `assemble` conservée (compat).
  Non-régression `BookAssemblerTest`.
| FT : assembleBook livre structuré (dogfooding FPA) ; UT : émission niveaux
| 5
| DOMAIN-2
| ✅ TERMINÉ (S-213)

| DOC-BOOK-DOMAIN-4
| *`BookNumbering`* — numérotation hiérarchique (`1`, `1.2`, `1.2.1`), ancres
  `[[ref]]`, navigation précédent/suivant. DDD pur.
| UT : 10 (numbering, anchors, prev/next)
| 4
| DOMAIN-1
| ✅ TERMINÉ (S-213)

| DOC-BOOK-DOMAIN-5
| *Cucumber BDD* `@book-tree` — 5 scénarios (arbre structuré, front/back matter,
  numérotation, navigation/ancres, compat blob plat). Runner `BookTreeCucumberRunner`
  + steps `BookTreeSteps` (préfixe « book-tree »).
| BDD : 6 scénarios (prev/next split)
| 5
| DOMAIN-3, DOMAIN-4
| ✅ TERMINÉ (S-215)

| DOC-BOOK-DOMAIN-6
| *Extension `BookValidator`* sur l'arbre (structurelle) : continuité `ref`,
  unicité, présence FRONT+BACK, monotonicité ordre de page. Lenient/strict réutilisé.
| UT : 8 + FT : 5 (dont 1 dogfooding FPA)
| 4
| DOMAIN-1, DOMAIN-2
| ✅ TERMINÉ (S-215)
|===

_Total_ : ~27 pts (6 US + cadrage). Séquentiel : US-1 → US-2 → US-3 → US-4 → US-5 → US-6.

=== Dépendances (DAG)

----
DOMAIN-0 (cadrage S-211) ─→ DOMAIN-1 (BookTree) ─→ DOMAIN-2 (Matter/Layout) ─→ DOMAIN-3 (Assembler)
                                          │                                            │
                                          └──→ DOMAIN-4 (Numbering) ─────────────────┤
                                          └──→ DOMAIN-6 (Validator arbre) ───────────┤
                                                                                        └─→ DOMAIN-5 (Cucumber)
----

=== Projection sessions

* *S-211* — DOMAIN-0 cadrage ✅ (ce document)
* *S-212* — DOMAIN-1 (5 pts, BookTree DDD pur) + DOMAIN-2 (4 pts, Matter/Layout) ✅
* *S-213* — DOMAIN-3 (5 pts, refactor Assembler) + DOMAIN-4 (4 pts, Numbering) ✅
* *S-215* — DOMAIN-5 (5 pts, Cucumber + dogfooding FPA) + DOMAIN-6 (4 pts, Validator arbre) ✅

**EPIC CLÔTURÉ le 2026-08-28 (S-215)** — 6/6 US TERMINÉES. Nord :
domaine `Book` structuré complet (arbre, matter, layout, numbering, navigation,
assembleur structuré + validation structurelle), Cucumber BDD `@book-tree`
6/6 PASSED, dogfooding FPA réel (`office/metiers/FPA`) PASSED. Consommateur
`office/metiers/FPA` débloqué (FPA-BOOK-4 bookPipeline navigable).

=== Extension consommateur (S-081, hors EPIC — suivi dans `office/metiers/FPA/BACKLOG.adoc` FPA-BOOK-6)

*Extension non-régression du domaine `Book` pour le consommateur FPA* (S-081) :
`- `BookAssembler.fpaAwareResolver(pagesDir)` combine un nouveau
  `FpaPageResolver` (résout `BookSection.page` → `NNN.adoc` + splits `NNN_N.adoc`,
  convention réelle des scans FPA) avec le resolver codex legacy `%03d-*.adoc`.
- `BookOcrFailureDetector` + `BookOcrIssue` + `OcrFailureReason` localisent les
  pages `[ILLISIBLE]`/vides avec la section TOC (ref + titre).
- `BookOcrIssueReport` sérialise la liste en `book-ocr-issues.json`.
- `AssembleBookTask` émet désormais le rapport OCR après assemblage.
- Tests : `FpaBookAssemblyTest` (UT) + `AssembleBookTaskFpaRichTocTest` (FT
  dogfooding corpus FPA complet 203 pages + TOC riche 116 sections) — tous PASSENT.

L'EPIC DOC-BOOK-DOMAIN reste ✅ TERMINÉ (6/6 US) ; ces ajouts sont des
*extensions de câblage consommateur* (FPA-BOOK-6) tracées côté FPA.

=== Note transversale (S-081) — convention anti-Python / LLM via codebase

*Règle* (issue de FPA-PY-MIGRATE, backlog `office/metiers/FPA`) : `document-gradle`
reste 100% Kotlin (aucun `.py` résiduel — vérifié S-081). Tout nouvel appel LLM
dans le pipeline livre doit consommer le hub `codebase` (`LlmBuildService` / koog),
jamais un script ad hoc. Les scripts Python prototypes (`fpa_*.py` côté FPA) sont
à purger au profit des tâches Gradle existantes (`codebase OcrTask`,
`document assembleBook`/`bookPipeline`). Voir aussi `codex-gradle` (même règle,
boundary CDX-OCR / CDEX-RAG-SOCLE).

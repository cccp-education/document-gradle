= PROMPT_REPRISE — document-gradle (New Orleans 🟢)
:session: 035
:date: 2026-08-03
:status: Session 034 terminée — Table POJO DDD + parseTableStructured + TDD/BDD (46 tests). Fondation DOC-TABLE-VALIDATE posée.
:max-lines: 70

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/document-gradle/document-plugin
./gradlew compileKotlin  # BUILD SUCCESSFUL
./gradlew test --tests "document.translation.*"  # GREEN
./gradlew tableTranslationCucumberTest  # 8/8 PASS
```

== Contexte de fin

Session 034 — Fondation DDD pour l'EPIC DOC-TABLE-VALIDATE :

. *Table.kt* — Modèle POJO : `Table(cols, header, body)` / `Row(cells)` /
  `Cell(inline, colSpan, rowSpan, halign, valign)` / `ColSpec(width, halign, valign)`.
  Fonctions pures : `extractTranslatable()`, `reinject()`, `toAsciiDoc()`, `toHtml()`.

. *AsciiDocParser.parseTableStructured()* — Parseur robuste gérant `\|`, `a|`/`h|`,
  `2+|`/`.3+|` (spans), `<|`/`^|`/`>|` (alignement), `.<|`/`.^|`/`.>|` (vertical),
  `[cols="<1,^2,>3"]` → `List<ColSpec>`.

. *Tests* — 19 UT TableTest + 19 UT AsciiDocParserTableTest + 8 Cucumber
  table_translation.feature. 46/46 PASS, zéro régression.

== Reprendre à

. `git status` + `git pull` (vérifier que le commit S034 est bien sur origin/main)
. S035 — DOC-TABLE-VALIDATE-1 + VALIDATE-2 (TableSyntaxValidator + intégration DocumentTranslator)
. Baby-step TDD/BDD : 15-20 UT TableSyntaxValidator → 8-10 UT DocumentTranslator integration

== Fichiers touchés S034

*document-gradle* :
. `Table.kt` — nouveau, 200 lignes (modèle DDD)
. `AsciiDocParser.kt` — +parseTableStructured, +parseColSpecs, +parseStructuredRows, +splitStructuredCells, +parseCellPrefix, +isCellPrefixLine, parseInline → internal
. `build.gradle.kts` — +tableTranslationCucumberTest task
. `TableTest.kt` — nouveau, 19 tests unitaires
. `AsciiDocParserTableTest.kt` — nouveau, 19 tests unitaires
. `table_translation.feature` — nouveau, 8 scénarios Cucumber
. `TableTranslationSteps.kt` — nouveau, steps Cucumber
. `TableTranslationCucumberRunner.kt` — nouveau, runner Cucumber
. `.agents/sessions/034-table-pojo-ddd.adoc` — rapport de session
. `PROMPT_REPRISE.adoc` — ce fichier
. `.agents/INDEX.adoc` — session 034, roadmap
. `.agents/SESSIONS_HISTORY.adoc` — entrée S034
. `.agents/archives/COMPLETED_TASKS_ARCHIVE_aout-2026.adoc` — archive S034

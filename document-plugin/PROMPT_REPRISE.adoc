= PROMPT_REPRISE — document-gradle (New Orleans 🟢)

== Mission session 243

Borough DORMANT (maintenance + dogfooding livre FPA SERVER). S-242 a clôturé
le backlog : audit `grep` marqueurs périmés → zéro match, tous les EPICs sont
✅ (dernier fermé : DOC-EPUBCHECK S-240). Options résiduelles toutes
conditionnelles à un besoin réel :

- *Composite EPUB (fusion 5e garde dans `validateDocument`)* : CADUQUE
  jusqu'à besoin N3 réel (décision 4 cadrage S-240).
- *Dogfooding PDF FPA* : pas de garde équivalente epubcheck pour le PDF —
  extension seulement si besoin réel remonté.
- *Maintenance* : 4 failures test bakery préexistantes (0.0.9 vs 0.0.8
  catalogue) — à corriger côté bakery si le pilote le souhaite.

== État du code (S-242)

- *0.0.15 PUBLIÉE* Maven Central (repo1 `<latest>0.0.15</latest>`).
- *Consommateurs vérifiés S-242* :
  - bakery-gradle (seul consommateur dur, `bakery-plugin/build.gradle.kts:43`) :
    compileKotlin VERT, `test` 1668 tests / 4 failures = exactement les 4
    préexistantes S-241 (prouvées par stash 0.0.12, non causées par 0.0.15).
  - runner-gradle N3 : zéro couplage compile (contrat de facto
    metadata.json/composite-context.json, prouvé BDD `n3PipelineCucumberTest`).
  - office/metiers/FPA : init-script `fpa-book-init.gradle.kts` + CLI
    `-Pdocument.*` (mavenLocal 0.0.15 présente) ; artefacts réels validés par
    les FT dogfooding du plugin (book.html lint Valid, book.epub epubcheck Valid).
- *Structure* : pattern S-006 « Plugin Indépendant + Racine Consommateur »
  restauré — `settings.gradle.kts` racine SANS `include(":document-plugin")`
  (le WIP S-227 l'avait réintroduit ; corrigé et commité S-242). Le plugin
  build autonome dans `document-plugin/`, la racine consomme le plugin publié.
- Baselines document : UT 3039/0, FT 126/126, 17 runners Cucumber dédiés
  VERTS, `epubCheckCucumberTest` 4/4 (re-vérifié S-242), `validatePlugins` VERT.

== Pièges Gradle à retenir (S-232..S-241)

1. *Snapshot collect* : `collectDocumentRetrieve` re-scan à chaque exécution
   (`outputs.upToDateWhen { false }`) — pas de `@InputFile` du rapport.
2. *mustRunAfter inverse la CLI* : conflit d'inputs si 2 tâches partagent
   un build.
3. *Chaîne N3 staged* : produce → validate → collect (3 builds).
4. *Property DSL sans convention reste unset même derrière `.orElse(...)`*.
5. *Dérivation knob sans dépendance de tâche* : jamais provider vers
   `outputFile` du producteur, jamais `.get()` configuration-time.
6. *`@InputFile` valide l'existence AVANT l'action* — porter les chemins
   audités en `@Input @Optional Property<String>` (pattern `htmlFilePath`
   / `epubFile`).
7. *`--tests` Gradle ne réduit PAS la découverte Cucumber* — filtrer côté
   annotation du runner (`FEATURES_PROPERTY_NAME`).
8. *Imports DSL consommateur* (S-239, documenté S-241) : les enums des
   gardes ne sont pas résolus implicitement — importer explicitement côté
   `build.gradle.kts` tiers. Liste dans README.md.
9. *epubcheck callback Java → null* : `info(feature, ...)` reçoit null —
   signature nullable obligatoire côté Kotlin (NPE sinon).
10. *Preuve négative epubcheck* (S-241) : le flip d'octet naïf est TOLÉRÉ
   (zip toujours lisible) — corrompre la STRUCTURE (supprimer l'entrée
   `mimetype` non compressée, première entrée OCF obligatoire) pour
   déclencher `Invalid`.
11. *Polling Central* (S-241) : `maven-metadata.xml` `<latest>` peut rester
   périmé ~27 min après l'acceptation AUTOMATIC — tester aussi le HTTP 200
   du POM versionné avant de déclarer la propagation.

== Anti-glue Cucumber (règle de propriété — S-238)

Chaque feature a UN runner propriétaire. Le runner global (glue
`document.scenarios`) ne découvre que `document.feature` +
`release-notes.feature` + `frontmatter_retranslate.feature`. Toute
nouvelle feature doit soit rejoindre un runner dédié (pattern : runner + glue + tag + CucumberTaskSpec dans build.gradle.kts), soit
explicitement rejoindre le global avec ses steps dans `document.scenarios`.
Le 17e runner dédié est `epubCheckCucumberTest` (S-240).

== Méthode

Baby-step DDD/TDD/BDD strict (RED → GREEN → REFACTOR), économie d'encre.
CONTRAINTE GLOBALE : *Zéro Python* — outputs (XML JUnit, rapports, logs)
analysés avec Read/Grep/xmllint/rapports Gradle natifs uniquement.

== Règles absolues (AGENT.adoc)

- English only dans le code/commentaires/tests (gouvernance .adoc en français).
- Baby-step DDD/TDD/BDD strict — UT (domaine pur) → FT (TestKit) → BDD (runner dédié).
- Zéro secret, zéro commit/push sans accord (S-241 : accord de commit/push
  ET publish accordé — exécuté).
- `bookPipeline` lit l'AsciiDoc/HTML sur disque en lecture seule (boundary
  Codex READ / Document WRITE+PUBLISH).
- Ne jamais lancer `./gradlew test` complet en fin de session sans permission.

== À vérifier en ouverture

1. `git status` (document-gradle + bakery-gradle + office/metiers/FPA).
2. `JAVA_HOME=/home/cheroliv/.sdkman/candidates/java/25.0.4-tem` (JDK 25).
3. `./gradlew compileKotlin` VERT (dans `document-plugin/`).
4. Central : `document-plugin` `<latest>` = 0.0.15.
5. Baseline : UT 3039/0, `epubCheckCucumberTest` 4/4, FT 126/126.

== Vision « livre FPA SERVER »

Verrous levés S-233..S-241 : statut N3 (`validationStatus=PASS`), knob
`outputFileName` de bout en bout, gems PDF/EPUB packagées, consommation
externe prouvée depuis Central, 5 gardes documentaires, EPUB FPA prouvé
valide (epubcheck W3C sur l'artefact réel), README consommateur, et désormais
*0.0.15 publiée* (5e garde + dogfooding + README) avec propagation
consommateur bakery. Le produit est servable, auditable, documenté et
distribué de bout en bout.
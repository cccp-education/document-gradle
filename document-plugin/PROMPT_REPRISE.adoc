= PROMPT_REPRISE — document-gradle (New Orleans 🟢)
:session: 005
:date: 2026-06-30
:status: DOC-3 + DOC-4 TERMINÉS — HTML5 + PDF via AsciidoctorJ, 50 tests PASS, prêt DOC-5
:max-lines: 70

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/document-gradle
./gradlew :document-plugin:tasks --group document   # 8 taches
./gradlew :document-plugin:check                     # 50 tests (28 unit + 12 functional + 10 cucumber)
```

== Contexte

Session 002 (2026-06-29) — DOC-1 Plugin stub TERMINÉ (18 tests, dogfood CNV-6).
Session 003 (2026-06-29) — DOC-2 generateDocument IA TERMINÉ (31 tests, koog+langchain4j).
Session 004 (2026-06-30) — DOC-3 + DOC-4 convertDocumentToHtml + convertDocumentToPdf TERMINÉS (50 tests).

== Objectif session 005 — DOC-5 (EPUB + DocBook + ManPage)

- Implémenter `convertDocumentToEpub` (backend epub3, binaire, sidecar `.sourcehash` comme PDF)
- Implémenter `convertDocumentToDocBook` (backend docbook5, texte, header comme HTML)
- Implémenter `convertDocumentToManPage` (backend manpage, texte, header comme HTML)
- ConvertDocumentTask : câbler les 3 backends restants (EPUB via convertBinary, DocBook/ManPage via convert texte)
- TDD/BDD baby-step : Red → Green, Cucumber features en amont
- Vérifier validation epubcheck pour EPUB3 (si disponible)

== Stack DOC-3/DOC-4 (rappel — AsciidoctorJ direct)

- **AsciidoctorJ 3.0.0** : html5 (texte, standalone=true, toFile=false), pdf (binaire, toFile=output)
- **asciidoctorj-pdf 2.3.23** : backend pdf (module séparé, dans bundle asciidoctor)
- **DocumentConverter** : convertisseur domaine (convert, convertToFile, shouldSkip, shouldSkipBinary, metadata)
- Distinction texte (hash dans fichier) vs binaire (fichier sidecar `.sourcehash`)
- `ConvertDocumentTask.when(fmt)` : HTML → convertToHtml, PDF → convertBinary, autres → stub no-op

== Pattern de surcharge (rappel)

```
CLI (-Pdocument.xxx) > DSL (block document { }) > convention (.convention())
```

== Roadmap Prioritaire (post DOC-5)

[cols="1,3,2]
|===
| EPIC | Description | Priorite
| DOC-5 | EPUB/DocBook/ManPage (3 backends restants) | P0 — session 005
| DOC-9 | enrichDocument (PlantUML, HTML, images, includes) | P1
| DOC-10 | Theme DSL (YML PDF + CSS HTML/EPUB) | P1
| DOC-11 | Pipeline livre Codex→Document (integration) | P1
| DOC-6 | Integration N3 runner-gradle | P2
| DOC-12 | DSL unifie `document { }` | P2
|===

== Regles absolues (rappel)

. COMMITS/GIT — INTERDICTION FORMELLE sans permission
. PUBLISH — INTERDICTION FORMELLE sans permission
. DAG N2 — importable par N3 runner-gradle, jamais importe N3
. ZERO secret/token/api key en dur
. DOCUMENT CONSOMME ASCIIDOC — lecture seule
. BOUNDARY CODEX — pas de dependance directe document→codex
. BOUNDARY PLANTUML — composition (contenant→contenu), compileOnly legitime
. Hook ouverture : lire `.agents/SESSION_CHECKLIST.adoc` en premier
. Procedure fermeture : 6 etapes obligatoires `.agents/SESSION_PROCEDURE.adoc`

== Méthode baby-step TDD/BDD

. DDD : ubiquitous language document (DocumentConfig, DocumentFormat, DocumentSource, DocumentConverter)
. TDD : Red → Green → Refactor, 1 test à la fois
. BDD : Cucumber .feature en amont (scénarios métier) → step definitions
. Pyramide : unit (rapide) → functional (GradleTestKit) → cucumber (acceptance)

== Fichiers clés

- `document-plugin/src/main/kotlin/document/DocumentConverter.kt` — convertisseur AsciidoctorJ (html5, pdf, skip, metadata)
- `document-plugin/src/main/kotlin/document/ConvertDocumentTask.kt` — task (HTML réel, PDF réel, stubs DOC-5)
- `document-plugin/src/test/kotlin/document/DocumentConverterTest.kt` — 15 tests unit (9 HTML + 6 PDF)
- `document-plugin/src/functionalTest/kotlin/document/DocumentPluginFunctionalTest.kt` — 12 tests functional
- `document-plugin/src/test/resources/features/document.feature` — 10 scénarios BDD
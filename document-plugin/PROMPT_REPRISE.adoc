= PROMPT_REPRISE — document-gradle (New Orleans 🟢)
:session: 220
:date: 2026-08-28
:status: Session 219 terminée — DOC-CR3-1 (réutilisation instance AsciidoctorJ, TDD) + DOC-CR3-3 (retrait code mort koog) ✅ ; DOC-CR3-2 reporté
:max-lines: 70

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/document-gradle/document-plugin
./gradlew compileKotlin  # BUILD SUCCESSFUL
./gradlew test --tests document.AsciidoctorHolderTest  # 1/1 PASSED
./gradlew test --tests document.DocumentConverterTest  # 43/43 PASSED
```

== Contexte de fin

Session 219 (DOC-CR3, perf + nettoyage) :
- DOC-CR3-1 ✅ TERMINÉ (TDD strict) — `AsciidoctorHolder` partage une instance
  AsciidoctorJ unique (lazy + cache + `synchronized`). `DocumentConverter.convert`
  / `convertToFile` délèguent à `AsciidoctorHolder.convertFile`. Gain : 1 démarrage
  JRuby pour N conversions (bookPipeline 5 formats = 1 au lieu de 5).
  - `AsciidoctorHolderTest` (1 UT) prouve `createCount == 1` sur 2 conversions.
  - `DocumentConverterTest` (43 UT) non-régressé.
- DOC-CR3-3 ✅ TERMINÉ — retrait du code mort `DocumentGenerationGraph.graph`
  (stratégie koog jamais lue, `execute()` réimplémente le flux) + 4 imports koog
  morts. Fichier koog-free. `DocumentGenerationGraphTest` non-régressé.
- DOC-CR3-2 🟡 REPORTÉ — garde `safeMode` DSL configurable (backward-compat
  UNSAFE par défaut). Laissé pour scope tight et vérifié.

== EPICs — état

- CROSS-DOC-BOOK ✅ TERMINÉ (S-210)
- DOC-BOOK-DOMAIN ✅ TERMINÉ (6/6 US — S-211..S-215)
- DOC-BOOK-VALIDATE ✅ TERMINÉ (5/5 US — VALIDATE-1/2/3 S-070/078, VALIDATE-4 S-216)
- DOC-CR3 🟡 EN COURS (2/3 US — CR3-1 S-219 ✅, CR3-3 S-219 ✅, CR3-2 reporté)

== Reprendre à (session 220)

. `git status` dans document-gradle
. Option A — DOC-CR3-2 : ajouter une garde `safeMode` DSL configurable
  (valeurs UNSAFE/SERVER/SECURE, défaut UNSAFE pour backward-compat) sur
  `DocumentConverter.buildOptions`. Baby-step TDD : `DocumentConverterTest`
  assert le `SafeMode` choisi dans les `Options`.
. Option B — EPIC suivant du BACKLOG (ex. DOC-TABLE/PLANTUML hardening, ou
  nouvelle US produit).
. Respecter baby-step : RED → GREEN → REFACTOR (`check` VERT), DDD/TDD/BDD.

== Note cross-borough

Le domaine « Book » (`BookTree`, `BookValidator`, `BookLayout`, `BookNumbering`)
est consommé par `office/metiers/FPA`. Validation structurelle + fichier forment
le filet de sécurité du pipeline livre. Aucun contenu FPA n'entre dans
document-gradle (frontière licence).

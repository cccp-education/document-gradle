= PROMPT_REPRISE — document-gradle (New Orleans 🟢)
:session: 222
:date: 2026-08-30
:status: Session 221 ✅ DOC-CR4 (Include Path Guard) ; Session 222 ✅ DOC-CR5 (Conversion Security Policy, cohérence SafeMode × IncludeGuard) — les deux EPICs CLÔTURÉS
:max-lines: 70

== Quick Start

----
cd /home/cheroliv/workspace/foundry/public/document-gradle/document-plugin
export JAVA_HOME=~/.sdkman/candidates/java/25.0.4-tem   # conventions-plugin:0.0.2 exige JVM 25
./gradlew compileKotlin
./gradlew test --tests "document.security.*"                  # 24 UT (12 CR4 + 12 CR5)
./gradlew functionalTest --tests "document.ConvertDocumentTaskSecurityPolicyFunctionalTest" --tests "document.ConvertDocumentTaskIncludeGuardFunctionalTest"
./gradlew securityPolicyCucumberTest                         # runner dédié 3/3 PASSED (global bruite par infra, non bloquant)
----

== Contexte de fin

Session 222 (DOC-CR5, défense en profondeur) :
- DOC-CR5 ✅ TERMINÉ (baby-step DDD/TDD/BDD strict) — `DocumentSecurityPolicy.advise`
  classe la combinaison `safeMode` (CR3-2) × `includeGuard` (CR4) :
  UNSAFE+OFF=Valid, UNSAFE+LENIENT=Warn, UNSAFE+STRICT=Reject, SERVER/SECURE=Valid.
  `ConvertDocumentTask.convert()` applique la sévérité via le mode `includeGuard`
  (STRICT→GradleException fail-fast, LENIENT→warn, OFF→no-op). Zéro nouveau knob.
- Tests : 12 UT `DocumentSecurityPolicyTest` + 3 FT `ConvertDocumentTaskSecurityPolicyFunctionalTest`
  (STRICT reject / LENIENT warn / SERVER valid) + 3 scénarios Cucumber `@converter-security-policy`
  (runner dédié `SecurityPolicyCucumberRunner`) TOUS GREEN.
- Note infra : le moteur Cucumber *global* borough reste bruite (DuplicateStepDefinitionException
  conventions plugin 0.0.2) — les runners *dédiés* sont GREEN. Préexistant, non bloquant.

== EPICs — état

- DOC-CR4 ✅ TERMINÉ (S-221, 4/4 US) — Include Path Guard.
- DOC-CR5 ✅ TERMINÉ (S-222, 3/3 US) — Conversion Security Policy.
- Tous les EPICs DOC-x livrés (DOC-0..DOC-15, DOC-TRANSLATE, DOC-TABLE/PLANTUML-VALIDATE,
  DOC-BOOK-* , CROSS-DOC-BOOK, DOC-CR3/CR4/CR5). Restent : maintenance infra Cucumber
  (conventions-gradle) ou nouvel EPIC produit.

== Reprendre à (session 223)

. `git status` dans document-gradle
. Choisir la prochaine action :
   . Option A — correction infra Cucumber borough (DuplicateStepDefinitionException conventions
     plugin 0.0.2, classpath doublon) — débloque `cucumberTest` global pour TOUTES les features.
   . Option B — EPIC produit : DOC-TABLE / PLANTUML hardening, ou enrichissement avancé.
   . Option C — dogfooding : publier un livre FPA via `bookPipeline` en mode SERVER.
. Respecter baby-step : RED → GREEN → REFACTOR (`check` VERT sur les tâches dédiées),
  DDD/TDD/BDD (unit + functional + cucumber).
. Build : utiliser JDK 25 (Temurin 25.0.4-tem via sdkman) — `conventions-plugin:0.0.2`
  exige JVM 25.

== Note cross-borough

Le domaine « Book » (`BookTree`, `BookValidator`, `BookLayout`, `BookNumbering`) est consommé
par `office/metiers/FPA`. Aucun contenu FPA n'entre dans document-gradle (frontière licence).

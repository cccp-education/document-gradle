= PROMPT_REPRISE — document-gradle (New Orleans 🟢)
:session: 043
:date: 2026-08-04
:status: Session 042 terminée — Support EPIC BKY-I18N-MASS : BlockChecksum/BlockDelta (MASS-1) + translateArticleWithDelta/translateSingleFileWithBlockDelta (MASS-2)
:max-lines: 70

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/document-gradle/document-plugin
./gradlew compileKotlin  # BUILD SUCCESSFUL
./gradlew test --tests "document.translation.*"  # BUILD SUCCESSFUL
```

== Contexte de fin

Session 042 (cross-borough bakery BKY-I18N-MASS) — delta bloc livré :
- `document/translation/delta/BlockChecksum.kt` : `BlockTranslationStatus` (TRANSLATED/PENDING),
  `BlockChecksumEntry` parse/serialize (legacy `"hash"`→PENDING), `sha256`/`computeForBlocks`
- `document/translation/delta/BlockDelta.kt` : `compute(previous, current)` → modified/preserved
  (statut ≠ TRANSLATED → re-traduit). 26 UT BlockDeltaTest PASS
- `DocumentTranslator.translateArticleWithDelta` (internal→public, preserve blocs indexés,
  shift table/plantuml, isEmpty→retourne previous). 4 UT DocumentTranslatorBlockDeltaTest PASS
- `ContentTranslationService.translateSingleFileWithBlockDelta` (public, fresh/delta path,
  écrit target). 1 UT BlockDeltaWireDebugTest PASS
- compileKotlin + publishToMavenLocal OK

== Reprendre à

. `git status` dans document-gradle
. Tous les EPICs document-gradle sont clos ; les changements S042 supportent bakery
. Prochaine étape : à définir (bakery suit l'EPIC BKY-I18N-MASS, MASS-2 issue en cours S192)

== Note cross-borough

Le `println` de debug `[block-delta]` dans `ContentTranslationService.translateSingleFileWithBlockDelta`
est à retirer une fois l'issue bakery fixée (S192 bakery) — aucun impact fonctionnel.
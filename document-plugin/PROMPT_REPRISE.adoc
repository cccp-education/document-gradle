= PROMPT_REPRISE — document-gradle (New Orleans 🟢)
:session: 017
:date: 2026-07-06
:status: DOC-8.4 terminé (300 tests, +11) — EPIC DOC-8 4/4 TERMINÉ. Prêt DOC-8.5 ou extension DOC-12.
:max-lines: 70

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/document-gradle/document-plugin
./gradlew test functionalTest            # 258 tests (218 unit + 40 functional)
./gradlew cucumberTest --no-daemon      # 42 scénarios BDD (6-7 min)
./gradlew publishToMavenLocal            # publie le plugin pour la racine consommateur
cd .. && ./gradlew tasks --group=document  # 12 tâches
```

== Architecture (session 006, stable)

Pattern « Plugin Indépendant + Racine Consommateur » (article #0124) :
- `document-plugin/` — build Gradle autonome (settings, wrapper 9.6.1, catalogue 68 lignes)
- Racine `document-gradle/` — consommateur dogfood minimal (6 lignes, catalogue 2 entrées)
- `@DisableCachingByDefault` + `@PathSensitive(PathSensitivity.NONE)` sur les tasks

== Contexte récent

Session 011 (2026-07-04) — DOC-7 CI + Publication Maven Central / Plugin Portal TERMINÉ (137 tests).
Session 012 (2026-07-04) — DOC-11 Book Pipeline Codex -> Document TERMINÉ (155 tests).
Session 013 (2026-07-04) — DOC-12 DSL unifié `document { }` TERMINÉ (180 tests).
Session 014 (2026-07-05) — DOC-8.1 Release Notes Pipeline TERMINÉ (186 tests).
Session 015 (2026-07-05) — DOC-8.2 Template configurable TERMINÉ (257 tests).
Session 016 (2026-07-05) — DOC-8.3 N3 metadata.json + composite-context.json TERMINÉ (289 tests).
Session 017 (2026-07-06) — DOC-8.4 Extension IA Ollama (résumé humain) TERMINÉ (300 tests, +11).

== Objectif session 018 — DOC-8.5 ou extension DOC-12

=== P1 — DOC-8.5 Publication Maven Central document-gradle

Publier `education.cccp:document-gradle` 0.0.1 sur Maven Central (nmcp).
Pré-requis : BOM 0.0.7 (déjà publiée), dépendances releases, POM complet, signing.
Vérifier que tous les `implementation` sont des releases (pas de -SNAPSHOT).

=== P2 — Extension DOC-12 (priorité P3)

Intégrer le block `book { }` (DOC-11) dans le DSL unifié `document { }`.
Actuellement `bookPagesDir`, `bookPhotosDir`, `bookTitle`, `bookAuthor` sont
des flat properties sur `DocumentExtension`. Les migrer dans un nested
block `book { }` (pattern enrich/outputs/metadata/releaseNotes).

=== P3 — DOC-9b enrichissement images

US-DOC-08 (DOC-9) mentionne "Les blocs `[plantuml]` sont préservés" mais
l'enrichissement images (`image::photo.png[]`) n'est pas testé explicitement
dans le rendu final HTML/PDF. Ajouter scénario BDD.

== EPIC DOC-8 — 4/4 TERMINÉ

- DOC-8.1 ✅ git log → AsciiDoc (ConventionalCommitParser, CliGitLogParser, AsciidocReleaseNotesRenderer)
- DOC-8.2 ✅ Template configurable (Markdown/JSON, configDriven, custom categories)
- DOC-8.3 ✅ N3 metadata.json + composite-context.json (ReleaseNotesArtifactEntry, collectReleaseNotes)
- DOC-8.4 ✅ Extension IA Ollama (OllamaAsciidocReleaseNotesRenderer, llmMode, fallback gracieux)

4 renderers release notes : asciidoc, markdown, json, ollama-asciidoc (décorateur IA).

== OllamaAsciidocReleaseNotesRenderer (rappel DOC-8.4)

Décorateur sur AsciidocReleaseNotesRenderer. Injecte `== Résumé` avant les
catégories via `DocumentLlmProvider`. Fallback gracieux si LLM jette (rendu
pur sans résumé). DSL : `rendererType.set("ollama-asciidoc")` + `llmMode.set("fake"|"ollama")`.
CLI : `-Pdocument.releaseNotesRendererType=ollama-asciidoc -Pdocument.releaseNotesLlmMode=fake`.

== Contrat N3 (session 016, stable)

`composite-context.json` : `{source, entries, count, releaseNotes, releaseNotesCount, timestamp}`
`metadata.json` : `{source, type, sessions, generatedAt, model, version, dependencies, releaseNotesPath?, releaseNotesRenderer?}`

== Stack (rappel)

- AsciidoctorJ 3.0.0 + asciidoctorj-pdf 2.3.23 + asciidoctorj-epub3 2.2.0 + asciidoctorj-diagram 3.2.0
- 4 renderers release notes (asciidoc, markdown, json, ollama-asciidoc) + N3 metadata releaseNotesPath/Renderer
- pipeline-contracts 0.0.2 (MEM-2 workspace-bom) + nmcp 1.5.0
- langchain4j ollama + koog (orchestrateur) pour le LLM

== Règles absolues (rappel)

. COMMITS/GIT — INTERDICTION FORMELLE sans permission
. PUBLISH — INTERDICTION FORMELLE sans permission
. DAG N2 — importable par N3 runner-gradle, jamais importe N3
. ZERO secret/token/api key en dur
. DOCUMENT CONSOMME ASCIIDOC — lecture seule
. BOUNDARY CODEX — pas de dependance directe document->codex
. BOUNDARY PLANTUML — Asciidoctor Diagram inline (zéro compileOnly pour DOC-9)
. Hook ouverture : lire `.agents/SESSION_CHECKLIST.adoc` en premier
. Procedure fermeture : 6 etapes obligatoires `.agents/SESSION_PROCEDURE.adoc`
. CODE EN ANGLAIS — aucun commentaire/texte français dans le code source
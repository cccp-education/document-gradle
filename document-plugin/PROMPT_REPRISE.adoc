= PROMPT_REPRISE — document-gradle (New Orleans 🟢)
:session: 044
:date: 2026-08-19
:status: Session 043 terminée — Sync gouvernance post-S207 (EPIC DOC-FRONTMATTER-RETRANSLATE déjà commit 9af5ed5, rattrapage INDEX/PROMPT_REPRISE/SESSIONS_HISTORY)
:max-lines: 70

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/document-gradle/document-plugin
./gradlew compileKotlin  # BUILD SUCCESSFUL
./gradlew test --tests "document.translation.*"  # BUILD SUCCESSFUL
```

== Contexte de fin

Session 043 (gouvernance pure, rattrapage) :
- S207 déjà commitée (`9af5ed5`) — EPIC DOC-FRONTMATTER-RETRANSLATE
  (`FrontmatterStaleDetector` + `retranslateFrontmatter()` +
  `RetranslateFrontmatterTask` + BDD 6 scénarios + wiring DocumentPlugin).
  Bump document-plugin 0.0.2 → 0.0.3 Central (bundle `1c27a917`).
- PROMPT_REPRISE/INDEX/SESSIONS_HISTORY étaient en retard de 5 sessions
  (pointaient S042). Rattrapage effectué.
- Aucun code modifié S043 — pure gouvernance.

== EPICs — tous clos

- DOC-TABLE-VALIDATE ✅ (S037)
- DOC-PLANTUML-VALIDATE ✅ (S040)
- DOC-TRANSLATE ✅ (S024)
- DOC-FRONTMATTER-RETRANSLATE ✅ (S207)
- DOC-CR3 🟠 (revue code, 3 items P1/P2 non traités) :
  * DOC-CR3-1 : Réutiliser instance AsciidoctorJ (P1)
  * DOC-CR3-2 : Auditer SafeMode.UNSAFE systématique (P2)
  * DOC-CR3-3 : Supprimer code mort DocumentI18nBridge/DocumentGenerationGraph (P2)

== Reprendre à

. `git status` dans document-gradle
. Tous les EPICs document-gradle sont clos ; DOC-CR3 (revue code) reste
  à traiter si priorité remonte (P1/P2).
. Support cross-borough bakery BKY-I18N-MASS MASS-6 si besoin (delta bloc,
  validation STRICT déjà en place).

== Note cross-borough

Le `println` de debug `[block-delta]` dans
`ContentTranslationService.translateSingleFileWithBlockDelta` a été
retiré S192 bakery (issue 191-1 fermée).
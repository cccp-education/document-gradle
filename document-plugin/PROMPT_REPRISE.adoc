= PROMPT_REPRISE — document-gradle (New Orleans 🟢)
:session: 002
:date: 2026-06-29
:status: DOC-1 TERMINÉ — plugin stub 18 tests PASS, prêt DOC-2
:max-lines: 70

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/document-gradle
./gradlew :document-plugin:tasks --group document   # 8 taches listees
./gradlew :document-plugin:check                     # 18 tests (9 unit + 5 functional + 4 cucumber)
```

== Contexte

Session 000 (2026-06-01) — Bootstrap DOC-0 TERMINE.
Session 001 (2026-06-14) — Cadrage pipeline livre Codex→Document.
Session 002 (2026-06-29) — DOC-1 Plugin stub TERMINÉ (18 tests, dogfood CNV-6).

== Objectif session 003 — DOC-2 generateDocument (IA)

- Implémenter la génération AsciiDoc assistée par IA via langchain4j (deepseek-v4-pro)
- Intégrer planner-gradle LLM bridge (compileOnly → implementation pour la tâche)
- US-DOC-01 : prompt → LLM → AsciiDoc structuré → fichier .adoc valide
- Économie d'encre : vérifier existence artefact avant re-générer (hash comparison)
- TDD/BDD baby-step : Red → Green, 1 test à la fois, Cucumber feature en amont

== Pattern de surcharge (rappel)

Pattern or planner/codebase :
```
CLI (-Pdocument.xxx) > DSL (block document { }) > convention (.convention())
```
Via `project.providers.gradleProperty("document.xxx").orElse(ext.xxx)` — lazy, déclaratif.

== Roadmap Prioritaire (post DOC-2)

[cols="1,3,2"]
|===
| EPIC | Description | Priorite
| DOC-2 | generateDocument (LLM → AsciiDoc) | P0 — session 003
| DOC-3 | convertDocumentToHtml (AsciidoctorJ html5) | P1
| DOC-4 | convertDocumentToPdf (AsciidoctorJ pdf) | P1
| DOC-9 | enrichDocument (PlantUML, HTML, images, includes) | P1
| DOC-10 | Theme DSL (YML PDF + CSS HTML/EPUB) | P1
| DOC-11 | Pipeline livre Codex→Document (integration) | P1
| DOC-5 | EPUB/DocBook/ManPage | P2
| DOC-6 | Integration N3 runner-gradle | P2
| DOC-12 | DSL unifie `document { }` | P2
|===

== Regles absolues (rappel)

. COMMITS/GIT — INTERDICTION FORMELLE sans permission
. PUBLISH — INTERDICTION FORMELLE sans permission
. DAG N2 — importable par N3 runner-gradle, jamais importe N3
. ZERO secret/token/api key en dur
. DOCUMENT CONSOMME ASCIIDOC — lecture seule
. BOUNDARY CODEX — pas de dependance directe document→codex
. BOUNDARY PLANTUML — composition (contenant→contenu), compileOnly legitime
. Hook ouverture : lire `.agents/SESSION_CHECKLIST.adoc` en premier
. Procedure fermeture : 6 etapes obligatoires `.agents/SESSION_PROCEDURE.adoc`

== Méthode baby-step TDD/BDD

. DDD : ubiquitous language document (DocumentConfig, DocumentFormat, DocumentSource)
. TDD : Red → Green → Refactor, 1 test à la fois
. BDD : Cucumber .feature en amont (scénarios métier) → step definitions
. Pyramide : unit (rapide) → functional (GradleTestKit) → cucumber (acceptance)

== Fichiers clés

- `document-plugin/src/main/kotlin/document/` — 7 classes (Plugin, Extension, Config, Format, Source, 2 Tasks)
- `document-plugin/src/test/kotlin/document/DocumentPluginTest.kt` — 9 unit
- `document-plugin/src/functionalTest/kotlin/document/DocumentPluginFunctionalTest.kt` — 5 functional
- `document-plugin/src/test/resources/features/document.feature` — 4 scénarios BDD
- `document-plugin/src/test/scenarios/document/scenarios/` — CucumberTestRunner, DocumentSteps, DocumentWorld
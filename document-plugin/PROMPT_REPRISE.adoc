= PROMPT_REPRISE — document-gradle (New Orleans 🟢)
:session: 006
:date: 2026-06-30
:status: DOC-5 TERMINÉ — EPUB3 + DocBook 5 + ManPage via AsciidoctorJ, 69 tests PASS, prêt DOC-9
:max-lines: 70

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/document-gradle
./gradlew :document-plugin:tasks --group document   # 8 taches
./gradlew :document-plugin:check                     # 69 tests (39 unit + 16 functional + 14 cucumber)
```

== Contexte

Session 002 (2026-06-29) — DOC-1 Plugin stub TERMINÉ (18 tests, dogfood CNV-6).
Session 003 (2026-06-29) — DOC-2 generateDocument IA TERMINÉ (31 tests, koog+langchain4j).
Session 004 (2026-06-30) — DOC-3 + DOC-4 convertDocumentToHtml + convertDocumentToPdf TERMINÉS (50 tests).
Session 005 (2026-06-30) — DOC-5 convertDocumentToEpub + DocBook + ManPage TERMINÉS (69 tests).

== Objectif session 006 — DOC-9 (enrichDocument)

- Implémenter `enrichDocument` — enrichissement AsciiDoc avant conversion
- **Option 1 retenue** : Asciidoctor Diagram inline (déjà bundlé), zéro dépendance compileOnly plantuml-gradle
- Périmètre :
  * Préserver/valider les blocs `[plantuml]` inline (rendu à la conversion par le bundle)
  * HTML passthrough `++++` (US-DOC-10) — préserver sans échapper
  * Résolution récursive des `include::` (asciidoctorJ le fait nativement, valider)
  * Images `image::photo.png[]` intégrées dans tous les formats
- Fallback : si plantuml-gradle n'est pas disponible, placeholder texte (option 1 → non requis)
- TDD/BDD baby-step : Red → Green, Cucumber features en amont

== Décision architecture DOC-9

**Asciidoctor Diagram inline** (déjà câblé dans le bundle asciidoctor) :
- `asciidoctorj-diagram 3.2.0` + `asciidoctorj-diagram-plantuml 1.2025.3` présents
- L'auteur écrit `[plantuml]` dans son `.adoc`, le rendu se fait à la conversion
- Zéro dépendance cross-borough, zéro appel à plantumlGenerate
- La génération LLM des `.puml` reste en amont chez HTOWN (orchestré par runner-gradle N3)
- DT-04 (compileOnly plantuml-gradle) devient optionnel — retirer du backlog DOC-9

== Stack DOC-5 (rappel — 5 formats complets)

- **AsciidoctorJ 3.0.0** : html5, docbook5, manpage (natifs core)
- **asciidoctorj-pdf 2.3.23** : backend pdf (module séparé)
- **asciidoctorj-epub3 2.2.0** : backend epub3 (module séparé)
- **asciidoctorj-diagram 3.2.0** : rendu inline PlantUML/Diagrams.net (déjà bundlé)
- **DocumentConverter** : convert (texte), convertToFile (binaire), shouldSkip, shouldSkipBinary, metadata

== Pattern de surcharge (rappel)

```
CLI (-Pdocument.xxx) > DSL (block document { }) > convention (.convention())
```

== Roadmap Prioritaire (post DOC-9)

[cols="1,3,2]
|===
| EPIC | Description | Priorite
| DOC-9 | enrichDocument (Asciidoctor Diagram inline, HTML, images, includes) | P0 — session 006
| DOC-10 | Theme DSL (YML PDF + CSS HTML/EPUB) | P1
| DOC-11 | Pipeline livre Codex→Document (integration) | P1
| DOC-6 | Integration N3 runner-gradle | P2
| DOC-12 | DSL unifie `document { }` | P2
|===

== Regles absolues (rappel)

. COMMITS/GIT — INTERDICTION FORMELLE sans permission
. PUBLISH — INTERDICTION FORMELLE sans permission
. DAG N2 — importable par N3 runner-gradle, jamais importe N3
. ZERO secret/token/api key en dur
. DOCUMENT CONSOMME ASCIIDOC — lecture seule
. BOUNDARY CODEX — pas de dependance directe document→codex
. BOUNDARY PLANTUML — composition, Asciidoctor Diagram inline (zéro compileOnly pour DOC-9)
. Hook ouverture : lire `.agents/SESSION_CHECKLIST.adoc` en premier
. Procedure fermeture : 6 etapes obligatoires `.agents/SESSION_PROCEDURE.adoc`

== Méthode baby-step TDD/BDD

. DDD : ubiquitous language document (DocumentConfig, DocumentFormat, DocumentSource, DocumentConverter, DocumentEnricher)
. TDD : Red → Green → Refactor, 1 test à la fois
. BDD : Cucumber .feature en amont (scénarios métier) → step definitions
. Pyramide : unit (rapide) → functional (GradleTestKit) → cucumber (acceptance)

== Fichiers clés

- `document-plugin/src/main/kotlin/document/DocumentConverter.kt` — convertisseur AsciidoctorJ (5 formats, skip, metadata)
- `document-plugin/src/main/kotlin/document/ConvertDocumentTask.kt` — task (5 formats réels, plus de stub)
- `document-plugin/src/test/kotlin/document/DocumentConverterTest.kt` — 24 tests unit (9 HTML + 6 PDF + 9 DOC-5)
- `document-plugin/src/functionalTest/kotlin/document/DocumentPluginFunctionalTest.kt` — 16 tests functional
- `document-plugin/src/test/resources/features/document.feature` — 14 scénarios BDD
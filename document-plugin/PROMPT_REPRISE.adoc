= PROMPT_REPRISE — document-gradle (New Orleans 🟢)
:session: 008
:date: 2026-07-01
:status: DOC-9 terminé (enrichDocument, 79 tests) — prêt DOC-10
:max-lines: 70

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/document-gradle/document-plugin
./gradlew check                        # 79 tests (44 unit + 17 cucumber + 18 functional), Gradle 9.6.1
./gradlew publishToMavenLocal          # publie le plugin pour la racine consommateur
cd ..
./gradlew tasks --group=document       # 8 tâches visibles depuis la racine consommateur dogfood
```

== Architecture (session 006, stable)

Pattern « Plugin Indépendant + Racine Consommateur » (article #0124) :
- `document-plugin/` — build Gradle autonome (settings, wrapper 9.6.1, catalogue 66 lignes)
- Racine `document-gradle/` — consommateur dogfood minimal (6 lignes, catalogue 2 entrées)
- `@DisableCachingByDefault` + `@PathSensitive(PathSensitivity.NONE)` sur les 3 tasks

== Contexte

Session 002 (2026-06-29) — DOC-1 Plugin stub TERMINÉ (18 tests).
Session 003 (2026-06-29) — DOC-2 generateDocument IA TERMINÉ (31 tests).
Session 004 (2026-06-30) — DOC-3 + DOC-4 HTML + PDF TERMINÉS (50 tests).
Session 005 (2026-06-30) — DOC-5 EPUB + DocBook + ManPage TERMINÉS (69 tests).
Session 006 (2026-06-30) — Architecture Plugin Indépendant + Racine Consommateur (Gradle 9.6.1).
Session 007 (2026-07-01) — DOC-9 enrichDocument TERMINÉ (79 tests, +10).

== Objectif session 008 — DOC-10 (Theme DSL)

- Implémenter le theming documentaire (US-DOC-09, P1)
- DSL `document { theme { } }` avec :
  * `pdfTheme = "talaria-theme.yml"` — fichier YML Asciidoctor PDF
  * `htmlStylesheet = "talaria.css"` — CSS HTML
  * `epubStylesheet = "epub.css"` — CSS EPUB
  * `logo = "logo.png"` — image titre PDF
- Fallback : thème par défaut intégré si aucun fichier fourni
- Application : passer les attributs Asciidoctor (`:stylesheet:`, `:pdf-theme:`)
  au moment de la conversion (pas une tâche séparée — câblage dans ConvertDocumentTask)
- TDD/BDD baby-step : Cucumber `@doc10 @theme` en amont, puis `DocumentTheme`
  + câblage task en Red → Green → Refactor

== Stack (rappel — 5 formats + enrichissement)

- **AsciidoctorJ 3.0.0** : html5, docbook5, manpage (core)
- **asciidoctorj-pdf 2.3.23** : backend pdf
- **asciidoctorj-epub3 2.2.0** : backend epub3
- **asciidoctorj-diagram 3.2.0** + **plantuml 1.2025.3** : rendu inline PlantUML
- **DocumentConverter** : 5 formats (convert, convertToFile, skip, metadata)
- **DocumentEnricher** : résolution includes récursive, préservation plantuml/passthrough

== Pattern de surcharge (rappel)

```
CLI (-Pdocument.xxx) > DSL (block document { }) > convention (.convention())
```

== Roadmap prioritaire (post DOC-10)

[cols="1,3,2]
|===
| EPIC | Description | Priorite
| DOC-10 | Theme DSL (YML PDF + CSS HTML/EPUB) | P0 — session 008
| DOC-11 | Pipeline livre Codex→Document (integration) | P1
| DOC-6 | Integration N3 runner-gradle | P2
| DOC-12 | DSL unifie `document { }` | P2
|===

== Règles absolues (rappel)

. COMMITS/GIT — INTERDICTION FORMELLE sans permission
. PUBLISH — INTERDICTION FORMELLE sans permission
. DAG N2 — importable par N3 runner-gradle, jamais importe N3
. ZERO secret/token/api key en dur
. DOCUMENT CONSOMME ASCIIDOC — lecture seule
. BOUNDARY CODEX — pas de dependance directe document→codex
. BOUNDARY PLANTUML — Asciidoctor Diagram inline (zéro compileOnly pour DOC-9)
. Hook ouverture : lire `.agents/SESSION_CHECKLIST.adoc` en premier
. Procedure fermeture : 6 etapes obligatoires `.agents/SESSION_PROCEDURE.adoc`
. CODE EN ANGLAIS — aucun commentaire/texte français dans le code source

== Méthode baby-step TDD/BDD

. DDD : ubiquitous language (DocumentConfig, DocumentFormat, DocumentSource, DocumentConverter, DocumentEnricher, DocumentTheme)
. TDD : Red → Green → Refactor, 1 test à la fois
. BDD : Cucumber .feature en amont (scénarios métier) → step definitions
. Pyramide : unit (rapide) → functional (GradleTestKit) → cucumber (acceptance)

== Fichiers clés

- `src/main/kotlin/document/DocumentConverter.kt` — convertisseur AsciidoctorJ (5 formats)
- `src/main/kotlin/document/DocumentEnricher.kt` — enrichisseur (includes, plantuml, passthrough)
- `src/main/kotlin/document/EnrichDocumentTask.kt` — task enrichissement (DOC-9)
- `src/main/kotlin/document/ConvertDocumentTask.kt` — task conversion (5 formats réels)
- `src/test/kotlin/document/DocumentConverterTest.kt` — 24 tests unit
- `src/test/kotlin/document/DocumentEnricherTest.kt` — 5 tests unit (DOC-9)
- `src/functionalTest/kotlin/document/DocumentPluginFunctionalTest.kt` — 18 tests functional
- `src/test/resources/features/document.feature` — 17 scénarios BDD
= PROMPT_REPRISE — document-gradle (New Orleans 🟢)

== Mission session 249

Borough DORMANT (maintenance + dogfooding livre FPA SERVER). S-248 a prouvé
la 6e garde `pdfCheck` (S-247) consommable par un projet tiers externe via
mavenLocal (4/4 scénarios : STRICT VALID, STRICT fail sur PDF sans texte,
LENIENT/OFF, knob `outputFileName` suivi) — pattern S-239 rejoué. Constat
clé S-248 : *drift jar Central ↔ mavenLocal* sous le même numéro 0.0.15
(Central publié S-241 AVANT la garde, mavenLocal republié S-247 avec).
*Rien à planifier — travaux uniquement sur besoin réel* :

- *Publication 0.0.16* : non demandée — la seule façon d'aligner Central sur
  mavenLocal (la garde PDF n'est pas dans le jar Central 0.0.15). Accord
  pilote requis (Règle 2), polling repo1 + propagation consommateurs
  (pattern S-241 : bakery bump, vérification FPA).
- *Composite PDF/EPUB (7e garde dans `validateDocument`)* : CADUQUE jusqu'à
  besoin N3 réel (pattern S-240 conservé).
- *Maintenance* : 4-5 failures test bakery préexistantes (JBake native
  roundtrip, pièges #12) — à corriger côté bakery si le pilote le souhaite,
  coordonner avant toute intervention bakery (session concurrente possible).
- *ROLLOUT-6* (dashboard, readme, graphify, bureau) : hors périmètre
  document — porté par MEMPHIS (workspace-bom/BACKLOG.adoc, ordre 6
  ⏳ À PLANIFIER à la demande).

== État du code (S-248)

- *0.0.15 PUBLIÉE* Central MAIS sans la garde PDF (jar S-241) — mavenLocal
  0.0.15 porte la garde (jar S-247). *Le mavenLocal est toujours la vérité
  du HEAD sous le même numéro de version* (piège #16).
- *Version catalog ws 0.0.31* — pins settings D4 + platform BOM 0.0.31
  (S-246), alignés Central (vérifié S-248).
- *Gates document* : test 3511/0 (920 UT actifs + cucumber intégrés) + FT
  130/130 + 18 runners Cucumber dédiés VERTS + validatePlugins VERT.
- *Preuve consommateur S-248* : `/tmp/opencode/doc-consumer-pdf` (Gradle
  9.7.1, JDK 25) — 4/4 scénarios VERTS, résolution mavenLocal prouvée par
  debug log (`Using ... from Maven repository 'MavenLocal'`).

== Pièges Gradle à retenir (S-232..S-248)

1. *Snapshot collect* : `collectDocumentRetrieve` re-scan à chaque exécution
   (`outputs.upToDateWhen { false }`) — pas de `@InputFile` du rapport.
2. *mustRunAfter inverse la CLI* : conflit d'inputs si 2 tâches partagent
   un build.
3. *Chaîne N3 staged* : produce → validate → collect (3 builds).
4. *Property DSL sans convention reste unset même derrière `.orElse(...)`*.
5. *Dérivation knob sans dépendance de tâche* : jamais provider vers
   `outputFile` du producteur, jamais `.get()` configuration-time.
6. *`@InputFile` valide l'existence AVANT l'action* — porter les chemins
   audités en `@Input @Optional Property<String>` (pattern `htmlFilePath`
   / `epubFile` / `pdfFile`).
7. *`--tests` Gradle ne réduit PAS la découverte Cucumber* — filtrer côté
   annotation du runner (`FEATURES_PROPERTY_NAME`).
8. *Imports DSL consommateur* (S-239) : les enums des gardes ne sont pas
   résolus implicitement — importer explicitement côté `build.gradle.kts`
   tiers. Liste dans README.md (six gardes, à jour S-247).
9. *epubcheck callback Java → null* : signature nullable obligatoire.
10. *Preuve négative : corrompre la STRUCTURE, pas le contenu* (EPUB :
    mimetype OCF ; PDF : voir #14 — les octets sont tolérés).
11. *Polling Central* : `<latest>` peut rester périmé ~27 min — tester aussi
    le HTTP 200 du POM versionné.
12. *Test bakery JBake native roundtrip périmé* : `50_jbake_native_translation.feature:22`
    attend `:summary:` verbatim mais document traduit `summary`/`description`
    depuis S206. Préexistant — à mettre à jour côté bakery.
13. *Drift catalogue ws* : MEMPHIS bump le catalogue en amont — la garde
    `DocumentPluginPublicationTest` RED au prochain build. Fix : bump des
    2 pins (settings D4 + platform BOM) vers la version publiée Central.
14. *PDFBox 3.x lénient sur les octets* (S-247) : header corrompu → WARNING
    + charge quand même ; xref tronqué → reconstruction. Le garde PDF est
    un garde *texte extractible par page*. Preuve négative = pages sans
    texte, jamais des octets.
15. *`PDPageContentStream.setFont(PDFont, float)` 2 params* (S-247) :
    l'erreur Kotlin `No value passed for parameter 'p1'` vient du size
    manquant, pas de la résolution `PDType1Font(FontName)`.
16. *Drift jar Central ↔ mavenLocal sous le MÊME numéro* (S-248) :
    `publishToMavenLocal` met à jour l'artefact local sans changer la
    version — Central peut être en retard. Un consommateur DOIT déclarer
    `pluginManagement { repositories { mavenLocal() } }` AVANT le Plugin
    Portal pour tester le HEAD local ; sans lui, Gradle résout
    silencieusement depuis Central et rate les gardes récentes
    (symptôme : `Task 'validateDocumentPdf' not found` malgré le JAR).
16. *Drift jar Central ↔ mavenLocal sous le MÊME numéro de version* (S-248) :
    `publishToMavenLocal` met à jour l'artefact local sans changer la
    version — Central peut être en retard sur le mavenLocal. Un consommateur
    DOIT déclarer `pluginManagement { repositories { mavenLocal() } }` AVANT
    le Plugin Portal pour tester le HEAD local ; sans lui, Gradle résout
    silencieusement depuis Central et rate les gardes récentes (symptôme :
    task not found malgré le JAR présent).

== Anti-glue Cucumber (règle de propriété — S-238)

Chaque feature a UN runner propriétaire. Le runner global (glue
`document.scenarios`) ne découvre que `document.feature` +
`release-notes.feature` + `frontmatter_retranslate.feature`. Toute
nouvelle feature doit soit rejoindre un runner dédié (pattern : runner +
glue + tag + CucumberTaskSpec dans build.gradle.kts), soit explicitement
rejoindre le global avec ses steps dans `document.scenarios`.
Le 18e runner dédié est `pdfCheckCucumberTest` (S-247).

== Méthode

Baby-step DDD/TDD/BDD strict (RED → GREEN → REFACTOR), économie d'encre.
CONTRAINTE GLOBALE : *Zéro Python* — outputs (XML JUnit, rapports, logs)
analysés avec Read/Grep/xmllint/rapports Gradle natifs uniquement.

== Règles absolues (AGENT.adoc)

- English only dans le code/commentaires/tests (gouvernance .adoc en français).
- Baby-step DDD/TDD/BDD strict — UT (domaine pur) → FT (TestKit) → BDD (runner dédié).
- Zéro secret, zéro commit/push sans accord.
- `bookPipeline` lit l'AsciiDoc/HTML/PDF/EPUB sur disque en lecture seule
  (boundary Codex READ / Document WRITE+PUBLISH).
- Ne jamais lancer `./gradlew test` complet en fin de session sans permission.

== À vérifier en ouverture

1. `git status` (document-gradle).
2. `JAVA_HOME=/home/cheroliv/.sdkman/candidates/java/25.0.4-tem` (JDK 25).
3. `./gradlew compileKotlin` VERT (dans `document-plugin/`).
4. Central : `document-plugin` `<latest>` = 0.0.15 ; `workspace-catalog`
   `<latest>` = 0.0.31 (drift MEMPHIS = réaligner via les 2 pins, piège #13).
5. Baseline : test 3511/0, FT 130/130, `epubCheckCucumberTest` 4/4,
   `pdfCheckCucumberTest` 4/4.

== Vision « livre FPA SERVER »

Verrous levés S-233..S-248 : statut N3 (`validationStatus=PASS`), knob
`outputFileName` de bout en bout, gems PDF/EPUB packagées, consommation
externe prouvée depuis Central (0.0.14 S-239, 0.0.15 S-242/243) et depuis
mavenLocal avec la 6e garde (S-248), 6 gardes documentaires
(includeGuard/xref/security/htmlLint/epubCheck/**pdfCheck**), EPUB FPA
prouvé valide (epubcheck W3C), PDF FPA prouvé structurellement sain
(PDFBox, 15 pages extractibles), README consommateur, et 0.0.15 publiée.
Reste : publication 0.0.16 pour distribuer la garde PDF hors machine de
développement.
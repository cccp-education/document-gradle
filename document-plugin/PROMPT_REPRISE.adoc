= PROMPT_REPRISE — document-gradle (New Orleans 🟢)
:session: 033
:date: 2026-08-01
:status: Session 032 terminée — Dogfooding batch 61 articles cheroliv.com FR→EN (Ollama réel) + 2 fixes parser AsciiDoc (DescriptionList/BlockMacro + délimiteurs 4-dot/titres de bloc). 61/61 traduits, 0 placeholder. Commit session à faire (permission utilisateur).
:max-lines: 70

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/document-gradle/document-plugin
./gradlew compileKotlin  # BUILD SUCCESSFUL
./gradlew test --tests "document.translation.*"  # GREEN
./gradlew functionalTest  # GREEN
./gradlew cucumberTest  # GREEN (sauf 4 scénarios applyDocumentTemplate/bookPipeline préexistants)
```

== Contexte de fin

Session 032 — Dogfooding `translateDocumentBatch` (tree mode) sur cheroliv.com :
61 articles FR→EN traduits via Ollama réel (gemma4:31b-cloud, round-robin ports 11440/11441/11442).
2 fixes parser découverts pendant le batch (fake mode reproduisait les troncatures → bug parser) :
- DescriptionList (`term:: def`) + BlockMacro (`image::`/`video::`) fusionnés en un paragraphe → PivotModel/parser/renderers/TreeTranslationAdapter
- Blocs `[plantuml]` délimités par `....` non reconnus → parseSourceBlock avalait le reste (0084 : 533→70 lignes)
- Titres de bloc `.titre` entre `[source,html]` et `----` → idem (0082 : 373→134 lignes)

== Reprendre à

. `git status` + commit/push session 032 (permission utilisateur obtenue)
. S033 — Suivi dogfooding : autres sites i18n, synthèse, publication Maven Central si requis
. CI-4 : `site.yml` cheroliv.com avec 29 vraies Device Keys → workflow_dispatch translate-cheroliv-com

== Fichiers touchés S032

*document-gradle* :
. `PivotModel.kt` — DescriptionList + DescriptionItem + BlockMacro
. `AsciiDocParser.kt` — description lists, block macros, skip `//`, délimiteurs source multi (----/====/....), titres de bloc
. `JbakeNativeRenderer.kt` / `AsciiDocRenderer.kt` / `PivotYamlRenderer.kt` — renderDescriptionList + renderBlockMacro
. `TreeTranslationAdapter.kt` — collect/apply DescriptionList
. `DocumentTranslator.kt` — branches DescriptionList/BlockMacro
. `tree/Content.kt` — inlineOf
. `gradle.properties` — jvmargs -Xmx2g (daemon OOM cucumber)
. Tests : PivotModelTest, AsciiDocParserTest, AsciiDocRendererTest, JbakeNativeRendererTest, document.feature, DocumentSteps, DocumentWorld

*cheroliv.com* (repo office) :
. 61 articles `i18n/en/content/blog/**` traduits EN réel

= PROMPT_REPRISE — document-gradle (New Orleans 🟢)
:session: 037
:date: 2026-08-03
:status: Session 036 terminée — DOC-TABLE-VALIDATE-3 + VALIDATE-4 (strict/lenient/off + batch report, 16 tests). Plan transverse S035→S040 en cours.
:max-lines: 70

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/document-gradle/document-plugin
./gradlew compileKotlin  # BUILD SUCCESSFUL
./gradlew test --tests "document.translation.validation.*"  # 42/42 PASS
./gradlew functionalTest --tests "document.translation.validation.*"  # 3/3 PASS
./gradlew tableTranslationCucumberTest  # 8/8 PASS
```

== Contexte de fin

Session 036 — DOC-TABLE-VALIDATE-3 + VALIDATE-4 (10 pts) :

. *TableValidationConfig* — DSL `translation { tableValidation { mode = "LENIENT" } }`
  + wiring CLI `-Pdocument.translateTableValidation=STRICT/LENIENT/OFF`

. *DocumentTranslator* — `tableValidationMode` param, `tableValidationResults` list,
  STRICT→TranslationException, LENIENT→warn log, OFF→skip

. *TableValidationReport* — Jackson JSON, `fromResults()` / `toJson()`

. *BatchDocumentTranslator* — agrégation `tableValidationResults` + écriture
  `table-validation-report.json` dans l'output dir

. *Tests* — 7 UT TableValidationConfigTest + 6 UT TableValidationReportTest
  + 3 FT TableValidationFunctionalTest. 16/16 PASS, zéro régression.

== Reprendre à

. `git status` + `git pull`
. Lire `.agents/PLAN_TRANSVERSE_VALIDATE.adoc` — plan complet S035→S040
. S037 — DOC-TABLE-VALIDATE-5 (Cucumber BDD, 5 scénarios, clôture EPIC)
. Prompt : `@xp implemente DOC-TABLE-VALIDATE-5 Cucumber BDD dans document-gradle`
. Baby-step BDD : 5 scénarios @table-validation (valid, corrupted-delimiter, column-mismatch, cols-mismatch, batch-report)

== Plan transverse

Voir `.agents/PLAN_TRANSVERSE_VALIDATE.adoc` — 6 sessions, ~46 pts :
- S035 ✅ TableSyntaxValidator + intégration DocumentTranslator (10 pts)
- S036 ✅ strict/lenient/off + batch report (10 pts)
- S037 → DOC-TABLE-VALIDATE-5 Cucumber BDD (5 pts)
- S038→S040 : DOC-PLANTUML-VALIDATE (21 pts)

== Fichiers touchés S036

*document-gradle* :
. `validation/TableValidationConfig.kt` — nouveau, DSL mode
. `validation/TableValidationReport.kt` — nouveau, Jackson JSON
. `TranslationException.kt` — nouveau, exception métier
. `TranslationDsl.kt` — +tableValidation
. `DocumentPlugin.kt` — +init tableValidation, +convention, +wiring CLI
. `DocumentTranslator.kt` — +tableValidationMode, +tableValidationResults, +mode handling
. `BatchDocumentTranslator.kt` — +agrégation validation results, +report JSON
. `TranslateDocumentTask.kt` — +tableValidationMode input
. `TranslateDocumentBatchTask.kt` — +tableValidationMode input
. `TableValidationConfigTest.kt` — nouveau, 7 UT
. `TableValidationReportTest.kt` — nouveau, 6 UT
. `TableValidationFunctionalTest.kt` — nouveau, 3 FT
. `.agents/sessions/036-table-validation-strict-lenient-batch.adoc` — rapport de session
. `PROMPT_REPRISE.adoc` — ce fichier
. `.agents/INDEX.adoc` — session 036, roadmap
. `.agents/SESSIONS_HISTORY.adoc` — entrée S036
. `.agents/archives/COMPLETED_TASKS_ARCHIVE_aout-2026.adoc` — archive S036

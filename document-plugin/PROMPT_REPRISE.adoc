= PROMPT_REPRISE — document-gradle (New Orleans 🟢)
:session: 032
:date: 2026-07-31
:status: Tree translation mode livré — ArticleTranslator interface, TreeTranslationAdapter (whole-article single LLM call), PivotYamlParser round-trip, NumberedFakeTranslationService, PooledOllamaTranslationAdapter refusal detection (+12 patterns), TranslateDocumentBatchTask treeMode+skipExisting, CI JDK 24→25. 244 PASS (186 UT + 29 FT + 29 Cucumber), 0 fail. Commit 82ada3b pushé.
:max-lines: 70

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/document-gradle/document-plugin
./gradlew compileKotlin  # BUILD SUCCESSFUL
./gradlew test --tests "document.translation.*"  # 186 UT GREEN
./gradlew functionalTest  # 29 FT GREEN
./gradlew cucumberTest  # 29 Cucumber GREEN
```

== Contexte de fin

Session 031 — Tree translation mode : ArticleTranslator interface (leaf-by-leaf vs whole-article),
TreeTranslationAdapter (1 LLM call per document via YAML round-trip), PivotYamlParser,
NumberedFakeTranslationService, PooledOllamaTranslationAdapter refusal detection (+12 patterns),
TranslateDocumentBatchTask treeMode+skipExisting, DocumentPlugin CLI -Pdocument.translateBatchTree.
CI JDK 24→25. Fix SubtreeI18nDeltaTest import manquant.
244 PASS (186 UT + 29 FT + 29 Cucumber), 0 fail. Commit 82ada3b pushé.

== Reprendre à

. `./gradlew compileKotlin` — BUILD SUCCESSFUL
. Dogfooding : Traduire les 104 articles cheroliv.com FR→EN via translateDocumentBatch avec DocKnowledgeBase
. CI-4 : `site.yml` cheroliv.com avec 29 vraies Device Keys → workflow_dispatch translate-cheroliv-com

== Fichiers touchés S031

*document-gradle* :
. `ArticleTranslator.kt` — nouvelle interface contrat (leaf-by-leaf vs whole-article)
. `TreeTranslationAdapter.kt` — stratégie whole-article (1 LLM call, YAML round-trip)
. `PivotYamlParser.kt` — parse YAML PivotYamlRenderer → PivotArticle
. `NumberedFakeTranslationService.kt` — fake déterministe mode tree
. `PooledOllamaTranslationAdapter.kt` — détection refus/meta-response LLM (+12 patterns)
. `TranslateDocumentBatchTask.kt` — treeMode + skipExisting
. `DocumentPlugin.kt` — CLI -Pdocument.translateBatchTree
. `.github/workflows/test.yml` — JDK 24→25
. `SubtreeI18nDeltaTest.kt` — fix import ArticleModification manquant

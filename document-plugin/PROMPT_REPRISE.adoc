= PROMPT_REPRISE — document-gradle (New Orleans 🟢)
:session: 221
:date: 2026-08-28
:status: Session 220 terminée — DOC-CR3-2 (garde `safeMode` DSL configurable) ✅ ; EPIC DOC-CR3 CLÔTURÉ (3/3 US)
:max-lines: 70

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/document-gradle/document-plugin
./gradlew compileKotlin  # BUILD SUCCESSFUL
./gradlew test --tests document.DocumentConverterTest  # 47/47 PASSED (dont 4 SafeMode)
./gradlew functionalTest --tests document.ConvertDocumentTaskSafeModeFunctionalTest  # 4/4 PASSED
./gradlew converterSafeModeCucumberTest  # 3/3 PASSED (@converter-safe-mode)
```

== Contexte de fin

Session 220 (DOC-CR3-2, perf + configurabilité) :
- DOC-CR3-2 ✅ TERMINÉ (baby-step DDD/TDD/BDD strict) — `DocumentConverter.buildOptions`
  accepte désormais un `SafeMode` configurable (UNSAFE/SERVER/SECURE), défaut
  UNSAFE pour backward-compat. `ConverterDsl` (bloc DSL `converter { }`) + mirror
  flat `safeMode` sur `DocumentExtension` + flag CLI `-Pdocument.safeMode`.
  `ConvertDocumentTask` consomme la valeur et la propage à `DocumentConverter`.
  - `buildOptions` passé `internal` + `safeMode` param ; `convert`/`convertToFile`/
    `convertToHtml`/`convertToPdf`/`convertToEpub`/`convertToDocBook`/`convertToManPage`
    exposent un paramètre `safeMode` (défaut UNSAFE).
  - Tests : 3 UT `buildOptions` (SafeMode via `SafeMode.safeMode(level)`), 1 UT
    conversion SERVER, 4 FT GradleTestKit (DSL SERVER / SECURE / CLI override /
    défaut UNSAFE), 3 scénarios Cucumber `@converter-safe-mode`
    (`converterSafeModeCucumberTest`, runner dédié `ConverterSafeModeCucumberRunner`).
- EPIC DOC-CR3 ✅ CLÔTURÉ (3/3 US) — CR3-1 (S-219), CR3-3 (S-219), CR3-2 (S-220).
- Note : le task global `cucumberTest` reste rouge pour TOUTES les features
  (environnement : glue globale non filtrée + LLM/réseau) — chaque feature a un
  runner Cucumber dédié GREEN. Ce comportement est préexistant et non régression.

== EPICs — état

- CROSS-DOC-BOOK ✅ TERMINÉ (S-210)
- DOC-BOOK-DOMAIN ✅ TERMINÉ (6/6 US — S-211..S-215)
- DOC-BOOK-VALIDATE ✅ TERMINÉ (5/5 US — VALIDATE-1/2/3 S-070/078, VALIDATE-4 S-216)
- DOC-CR3 ✅ TERMINÉ (S-219 CR3-1, S-219 CR3-3, S-220 CR3-2)
- Tous les EPICs DOC-x livrés. Restent àplanifier : DOC-TABLE/PLANTUML hardening,
  ou nouvelle US produit (ex. enrichissement avancé, pipeline i18n document).

== Reprendre à (session 221)

. `git status` dans document-gradle
. Choisir le prochain EPIC du BACKLOG (aucun EN COURS) :
  . Option A — nouveau hardening : audit sécurité des conversions (ex. échappement
    des chemins `include` en SERVER/SECURE, ou validation des `sourcemap`).
  . Option B — EPIC produit : enrichissement avancé (ex. diagrammes inline
    multi-backend, ou pipeline de traduction document avec garde SafeMode).
  . Option C — dogfooding : publier un livre FPA via `bookPipeline` en mode SERVER.
. Respecter baby-step : RED → GREEN → REFACTOR (`check` VERT sur les tâches
  dédiées), DDD/TDD/BDD (unit + functional + cucumber).

== Note cross-borough

Le domaine « Book » (`BookTree`, `BookValidator`, `BookLayout`, `BookNumbering`)
est consommé par `office/metiers/FPA`. Validation structurelle + fichier forment
le filet de sécurité du pipeline livre. Aucun contenu FPA n'entre dans
document-gradle (frontière licence).

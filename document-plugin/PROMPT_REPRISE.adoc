= PROMPT_REPRISE — document-gradle (New Orleans 🟢)

== Mission session 236

Borough DORMANT (maintenance + dogfooding livre FPA SERVER). Options classées :
- *Option C (recommandée)* : republication 0.0.14 sur Maven Central (les gems
  PDF/EPUB sont packagées runtimeOnly depuis S-233 — les consommateurs externes
  du plugin ont besoin de cette version pour convertir PDF/EPUB). Publication
  sans accord pilote interdite (Règle 2).
- *Option D* : diagnostic du moteur Cucumber *global* (`cucumberTest`) — 82
  failures `UndefinedStepException` préexistantes (scoping glue runner global
  vs runners dédiés, connu S-221/S-223). La gate reste les 15 runners dédiés.
- *Suivi S-235* (optionnel) : `DocumentArtifactCollector.collect` scanne les noms
  canoniques `document.${ext}` ; avec `outputFileName` custom les artifacts
  custom ne sont pas indexés N3 — extension possible du collector (knob → scan).

== État du code (S-235)

- *Option B livrée* : le knob `outputFileName` est vivant —
  `outputFile = build/docs/document/${outputFileName}.${format.extension}` ;
  la 4e garde (`validateDocument` htmlLint) et `lintHtmlDocument` auditent
  l'output réel (même dérivation knob, zéro dépendance de tâche, piège S-232
  respecté). Défaut `"document"` → chemins canoniques (backward-compat total).
- 15/15 runners Cucumber dédiés VERTS (0 failure, 0 error, rapports XML).
- FT : OutputFileName 2/2 + CustomOutput 2/2 + HtmlLint 4/4 + suite complète VERT.
- Moteur global `cucumberTest` : 15 fichiers failures préexistantes (glue
  scoping, hors périmètre).

== Pièges Gradle à retenir (S-233/234/235)

1. *Snapshot collect* : `collectDocumentRetrieve` doit re-scanner à chaque
   exécution (`outputs.upToDateWhen { false }`) — un `@InputFile` du rapport de
   validation est impossible car `bookPipeline` dependsOn collect pendant que
   le rapport n'existe pas (piège S-232 #3).
2. *mustRunAfter inverse la CLI* : `validateDocument.mustRunAfter(collect)`
   inverse la chaîne CLI `validateDocument collectDocumentRetrieve` (le collect
   tournerait d'abord → n/a). Le conflit d'inputs (outputDir couvre le
   book.adoc) n'existe que si les 2 tâches partagent un build.
3. *Chaîne N3 staged* : produce → validate → collect (3 builds dans le FT
   dogfooding) — un cycle Gradle interdit l'ordre métier en un seul build.
4. *Property DSL sans convention reste unset même derrière `.orElse(...)`* :
   `cliProp(...).orElse(ext.x)` produit un provider unset si aucune branche
   n'a de valeur → `Value not set` à l'exécution (piège S-234).
5. *Dérivation knob sans dépendance de tâche* (S-235) : pour pointer l'output
   d'une tâche, dériver le chemin du MÊME knob (layout + knob flatMap),
   jamais un provider vers `outputFile` du producteur (dépendance implicite) ;
   jamais `.get()` configuration-time (`Querying the mapped value...`).

== Anti-glue Cucumber

Un texte de step identique dans 2 classes → `DuplicateStepDefinitionException`
(S-223). Le runner *global* (glue `document.scenarios`) ne voit pas les steps
des runners dédiés (autres packages) → `UndefinedStepException` pour les
features `@book-validation`/`@book-tree`/etc. Comparer baseline via `git stash`
pour isoler l'impact d'un fix (méthode utilisée S-234). Attention aux
*faux positifs d'assertion* : un fragment de message partagé par 2 findings
distingués (`missing` du fragment mort vs `<html-file-missing>`) fait passer
un test pour la mauvaise raison — choisir des fragments discriminants (S-235).

== Méthode

Baby-step DDD/TDD/BDD strict (RED → GREEN → REFACTOR), économie d'encre.
CONTRAINTE GLOBALE : *Zéro Python* — les outputs (XML JUnit, rapports, logs)
s'analysent avec Read/Grep/xmllint/rapports Gradle natifs uniquement.

== Règles absolues (AGENT.adoc)

- English only dans le code/commentaires/tests (gouvernance .adoc en français).
- Baby-step DDD/TDD/BDD strict — UT (domaine pur) → FT (TestKit) → BDD (runner dédié).
- Zéro secret, zéro commit/push sans accord.
- `bookPipeline` lit l'AsciiDoc/HTML sur disque en lecture seule (boundary Codex READ / Document WRITE+PUBLISH).
- Ne jamais lancer `./gradlew test` complet en fin de session sans permission.

== À vérifier en ouverture

1. `git status` (document-gradle + office/metiers/FPA).
2. `JAVA_HOME=/home/cheroliv/.sdkman/candidates/java/25.0.4-tem` (JDK 25 obligatoire).
3. `./gradlew compileKotlin` VERT.
4. `./gradlew tableTranslationCucumberTest ... n3PipelineCucumberTest` VERT (15 runners dédiés).
5. FT ciblées outputFileName GREEN (S-235 livré).

== Vision « livre FPA SERVER »

Le statut N3 est verrouillé (validationStatus=PASS dans metadata.json prouvé
par FT dogfooding + BDD sur corpus réel, S-233). S-235 a rendu le knob
`outputFileName` vivant (conversion + 4e garde + lint suivent l'output réel).
Prochain verrou : publication 0.0.14 (option C, gems packagées) dès accord pilote.
= PROMPT_REPRISE — document-gradle (New Orleans 🟢)
:session: 217
:date: 2026-08-28
:status: Session 216 terminée — DOC-BOOK-VALIDATE-4 (Cucumber BDD @book-validation 5/5) + extraction BookValidator.enforce — EPIC DOC-BOOK-VALIDATE CLÔTURÉ 5/5 US
:max-lines: 70

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/document-gradle/document-plugin
./gradlew compileKotlin  # BUILD SUCCESSFUL
./gradlew bookValidationCucumberTest  # 5/5 scenarios PASSED
./gradlew bookTreeCucumberTest         # 6/6 scenarios PASSED
```

== Contexte de fin

Session 216 (implémentation BDD, clôture EPIC DOC-BOOK-VALIDATE) :
- DOC-BOOK-VALIDATE-4 ✅ TERMINÉ — Cucumber BDD `@book-validation`
  - `book_validation.feature` 5 scénarios + `BookValidationCucumberRunner` (package
    `document.bookvalidation`) + `BookValidationSteps` (préfixe « book-validation »,
    domaine pur — zéro I/O Gradle, pattern S-215 book-tree) + `CucumberTaskSpec("bookValidationCucumberTest")`
  - 5/5 PASSED : `@valid` (Valid), `@missing-page` (« no .adoc page found »),
    `@empty-section` (« is empty »), `@toc-mismatch` (« no corresponding TOC section »),
    `@strict` (enforce STRICT → GradleException)
- Extraction `BookValidator.enforce(validationMode, reasons)` (fonction pure,
  throw GradleException en STRICT) depuis `AssembleBookTask.validateIfConfigured`
  (comportement LENIENT warn / STRICT fail préservé — baby-step, zéro régression)
- Gates : 5 cucumber book-validation + 6 cucumber book-tree + UT/FT book GREEN.
  `AssembleBookTaskFunctionalTest` (STRICT/LENIENT legacy) VERT.
  `compileKotlin`/`compileTestKotlin` VERT.
- Hors périmètre (préexistant, environnement) : `DocumentConverterTest` OOM JRuby
  intermittent, `DocumentPluginFunctionalTest` 1 flaky env — non régressés par S-216.
- Zéro secret, BOM inchangé (0.0.11), commit/push document-plugin.

== EPICs — état

- CROSS-DOC-BOOK ✅ TERMINÉ (S-210)
- DOC-BOOK-DOMAIN ✅ TERMINÉ (6/6 US — S-211 cadrage, S-212..S-215)
- DOC-BOOK-VALIDATE ✅ TERMINÉ (5/5 US — VALIDATE-1/2/3 S-070/078, VALIDATE-4 Cucumber S-216)

== Reprendre à (session 217)

. `git status` dans document-gradle
. Optionnel A — rapprochement `office/metiers/FPA` (FPA-BOOK-4 `bookPipeline`) :
   le livre PDF/EPUB *navigable* FPA est désormais possible (DOC-BOOK-DOMAIN 6/6 +
   DOC-BOOK-VALIDATE 5/5). Cadrage FPA-BOOK-4 si souhaité (contenu privé office/,
   code Apache 2.0 ici).
. Optionnel B — EPIC suivant du BACKLOG (ex. DOC-CR3 — Création de Contenu Solide,
   ou DOC-TABLE/PLANTUML hardening). Choisir selon priorité produit.
. Respecter baby-step : RED (compile fail / UndefinedStep) → GREEN → REFACTOR
   (`check` VERT), 1 fichier à la fois, DDD/TDD/BDD unit/fonctionnal/cucumber.

== Note cross-borough

Le domaine « Book » (`BookTree`, `BookValidator`, `BookLayout`, `BookNumbering`)
est consommé par `office/metiers/FPA` pour produire un livre PDF/EPUB navigable.
Validation structurelle (`validateStructure`) + validation fichier (`validate` +
`enforce`) forment le filet de sécurité du pipeline livre. Aucun contenu FPA
n'entre dans document-gradle (frontière licence).

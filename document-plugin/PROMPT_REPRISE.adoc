= PROMPT_REPRISE — document-gradle (New Orleans 🟢)
:session: 034
:date: 2026-08-02
:status: Session 033 terminée — Cadrage DOC-TABLE-VALIDATE + DOC-PLANTUML-VALIDATE (2 EPICs, ~46 pts, 10 US). BACKLOG.adoc +217 lignes, INDEX.adoc mis à jour. Commit + push fait.
:max-lines: 70

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/document-gradle/document-plugin
./gradlew compileKotlin  # BUILD SUCCESSFUL
./gradlew test --tests "document.translation.*"  # GREEN
./gradlew functionalTest  # GREEN
./gradlew cucumberTest  # GREEN
```

== Contexte de fin

Session 033 — Cadrage de 2 EPICs transverses de validation syntaxique post-traduction :

. *DOC-TABLE-VALIDATE* (~25 pts, 5 US) — Validation syntaxique des tableaux AsciiDoc
  après traduction LLM. `TableSyntaxValidator` vérifie cohérence `cols`, délimiteurs
  intacts, homogénéité colonnes. Modes STRICT/LENIENT/OFF. Rapport batch JSON.

. *DOC-PLANTUML-VALIDATE* (~21 pts, 5 US) — Validation syntaxique des diagrammes
  PlantUML après traduction. DRY via `PlantumlService.validateSyntax()` de
  plantuml-gradle (HTOWN, `net.sourceforge.plantuml.SourceStringReader`).
  Dépendance `implementation(libs.plantuml.plugin)` déjà versionnée par le BOM.
  Wrapper DDD `PlantUmlSyntaxValidator` prêt pour extraction future en contrat N0.

== Reprendre à

. `git status` + `git pull` (vérifier que le commit S033 est bien sur origin/main)
. S034 — DOC-TABLE-VALIDATE-1 + VALIDATE-2 (TableSyntaxValidator + intégration DocumentTranslator)
. Baby-step TDD/BDD : 15-20 UT TableSyntaxValidator → 8-10 UT DocumentTranslator integration

== Fichiers touchés S033

*document-gradle* :
. `BACKLOG.adoc` — +217 lignes (2 EPICs complets : US, DAG, projections, note DRY)
. `.agents/INDEX.adoc` — +2 EPICs roadmap, date + prochaine session
. `.agents/sessions/033-cadrage-table-plantuml-validation.adoc` — rapport de session
. `PROMPT_REPRISE.adoc` — ce fichier
. `.agents/SESSIONS_HISTORY.adoc` — entrée S033
. `.agents/archives/COMPLETED_TASKS_ARCHIVE_aout-2026.adoc` — archive S033

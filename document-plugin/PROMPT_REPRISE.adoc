= PROMPT_REPRISE — document-gradle (New Orleans 🟢)

== Mission session 238

Borough DORMANT (maintenance + dogfooding livre FPA SERVER). Mission actée S-237 :
- *Option D* : diagnostic du moteur Cucumber *global* (`cucumberTest`) — 82
  failures `UndefinedStepException` préexistantes (scoping glue runner global
  vs 16 runners dédiés, connu S-221/S-223/S-234). La gate qualité reste les
  runners dédiés ; le moteur global bruit et masque les régressions.
  Baby-step TDD/BDD attendu : caractériser le scoping glue (features vues /
  steps résolus par le runner global), choisir la stratégie (runner global
  filtré par tags vs suppression vs découpage glue), RED preuve,
  GREEN sans casser les 16 dédiés (144 scénarios PASSED baseline S-234).
- *Option E (si temps)* : `assembleBook` knob séparé (défaut `"book"`) —
  le collect N3 (S-236) ne couvre pas les books custom-named (contrat
  canonique `book.adoc` inchangé). Extension seulement si besoin réel.

== État du code (S-237)

- *0.0.14 PUBLIÉE Maven Central* (bundle accepté AUTOMATIC, propagation ~7 min
  vérifiée : metadata `<latest>0.0.14</latest>`, POM+JAR HTTP 200). Contenu :
  gems runtimeOnly (S-233), knob `outputFileName` bout-en-bout (S-235/236),
  4 gardes composite + metadata validationStatus (S-231/232/233). POM complet
  (name/description/scm/Apache-2.0). Local = Central (versions alignées).
- 16/16 runners Cucumber dédiés VERTS (baseline S-234/S-235/S-236).
- FT baseline verte (68/68, fix S-234). Moteur global `cucumberTest` :
  82 failures préexistantes (glue scoping — périmètre option D).

== Pièges Gradle à retenir (S-233/234/235/236)

1. *Snapshot collect* : `collectDocumentRetrieve` re-scan à chaque exécution
   (`outputs.upToDateWhen { false }`) — pas de `@InputFile` du rapport de
   validation (bookPipeline dependsOn collect pendant que le rapport n'existe
   pas, piège S-232 #3).
2. *mustRunAfter inverse la CLI* : `validateDocument.mustRunAfter(collect)`
   inverse la chaîne CLI `validateDocument collectDocumentRetrieve` (le collect
   tournerait d'abord → n/a). Conflit d'inputs seulement si 2 tâches partagent
   un build.
3. *Chaîne N3 staged* : produce → validate → collect (3 builds) — un cycle
   Gradle interdit l'ordre métier en un seul build.
4. *Property DSL sans convention reste unset même derrière `.orElse(...)`* :
   piége S-234 — ajouter `.convention("document")` sur les knobs chaînés.
5. *Dérivation knob sans dépendance de tâche* (S-235) : dériver le chemin du
   MÊME knob (layout + knob flatMap), jamais provider vers `outputFile` du
   producteur (dépendance implicite), jamais `.get()` configuration-time.

== Anti-glue Cucumber (le cœur de l'option D)

Le runner *global* (glue `document.scenarios`) ne voit pas les steps des
runners dédiés (autres packages glue) → `UndefinedStepException` pour les
features hors glue. Un step identique dans 2 classes →
`DuplicateStepDefinitionException` (fix S-223 : textes distincts). Méthode
comparative `git stash` pour isoler l'impact d'un fix (S-234). Faux positifs
d'assertion : choisir des fragments discriminants (S-235).

== Méthode

Baby-step DDD/TDD/BDD strict (RED → GREEN → REFACTOR), économie d'encre.
CONTRAINTE GLOBALE : *Zéro Python* — outputs (XML JUnit, rapports, logs)
analysés avec Read/Grep/xmllint/rapports Gradle natifs uniquement.

== Règles absolues (AGENT.adoc)

- English only dans le code/commentaires/tests (gouvernance .adoc en français).
- Baby-step DDD/TDD/BDD strict — UT (domaine pur) → FT (TestKit) → BDD (runner dédié).
- Zéro secret, zéro commit/push sans accord.
- `bookPipeline` lit l'AsciiDoc/HTML sur disque en lecture seule (boundary
  Codex READ / Document WRITE+PUBLISH).
- Ne jamais lancer `./gradlew test` complet en fin de session sans permission.

== À vérifier en ouverture

1. `git status` (document-gradle + office/metiers/FPA).
2. `JAVA_HOME=/home/cheroliv/.sdkman/candidates/java/25.0.4-tem` (JDK 25).
3. `./gradlew compileKotlin` VERT.
4. Central : `document-plugin` `<latest>` >= 0.0.14 (publiée S-237).
5. Baseline : 16 runners dédiés VERTS + FT 68/68 (S-234).

== Vision « livre FPA SERVER »

Statut N3 verrouillé (validationStatus=PASS prouvé S-233). Knob
`outputFileName` vivant de bout en bout (S-235/236). Publication 0.0.14
réalisée S-237 — les consommateurs externes convertissent PDF/EPUB.
Prochain verrou : moteur global Cucumber (option D) pour rendre la gate
`cucumberTest` lisible (82 faux échecs glue scoping).
= SESSIONS_HISTORY — document-gradle (New Orleans 🟢)

|===
| # | Date | Type | Sujet | Score
| 264 | 2026-09-25 | PUB cross-borough | **PUB 0.0.18 Maven Central ✅** (accord pilote « commence par 1 ») — distribution hors machine de dev du HEAD cumulé depuis 0.0.17 : RENAME-8 + Vague 4 + DOC-BOOK-CONSISTENCY B1/B2 + DOC-BOOK-IMAGES + DOC-BOOK-MATTER + DOC-BOOK-CONSISTENCY-B6 + CROSS-DOC-BOOK-FIDELITY (test-only) + pins catalogue ws 0.0.57. **Chaîne DAG N0→N2 MEMPHIS-first, baby-step TDD strict** : (1) MEMPHIS bump toml `workspace-bom 0.0.56→0.0.57` + `document-plugin 0.0.17→0.0.18`, RED prouvé (`WorkspaceCatalogStructureTest.document plugin version matches bakery consumer pin` + `PlatformCatalogAlignmentTest.toml version workspace-bom matches platform version`, 2 failed / 89 tests) → GREEN après réalignement des 2 gardes → `test` 0 FAIL → bundles Central `3f912bf6` (BOM+catalog, AUTOMATIC), propagation `<latest>0.0.57</latest>` ×2 + POM HTTP 200, catalog porte `document-plugin = "0.0.18"` ; (2) document re-pin 3 sources (settings D4 ws-catalog 0.0.57 + platform BOM 0.0.57 + toml local 0.0.18), RED prouvé (`DocumentPluginPublicationTest` 2 failures — split-brain toml local + platform pin) → GREEN 3/3, version dérivée **0.0.18**, `publishToMavenLocal` VERT (POM 20 deps toutes versionnées, platform 0.0.57), bundle Central `aab4b880` AUTOMATIC, propagation POM/JAR/plugin-marker HTTP 200 ; (3) **preuve consommateur tiers** `/tmp/opencode/doc-consumer-018` (Central SANS mavenLocal) : 0.0.18 résolu, `convertDocumentToHtml` BUILD SUCCESSFUL, `xrefValidation STRICT` VALID puis **preuve négative** ancre morte `<<ghost-anchor>>` → BUILD FAILED + rapport `xref MISSING ["ghost-anchor"]`. **Pièges re-confirmés** : #11 (`<latest>` périmé ~15 min — vérifier le POM versionné), #13 (garde document attrape le drift dès le bump MEMPHIS, 6e occurrence), #26 (WIP `.agents/` + CADRAGE staged concurrent MEMPHIS — commit sélectif). Bakery résout encore 0.0.17 via transitives (pin local 0.0.48) — re-pin = session dédiée, DAG document indépendant. Gates : garde publication 3/3, `test` 3915/0 runner-scoped, `validatePlugins` VERT, secrets RAS. Rapport : `.agents/sessions/264-pub-0.0.18-distribution.adoc`. | 10/10
| 263 | 2026-09-24 | test-only BDD + revue fidélité | **CROSS-DOC-BOOK-FIDELITY ✅ TERMINÉ** (P1 non bloquant) — le BDD cross-borough `@book-pipeline` teste le *contrat réel*, plus une fiction. **Audit d'ouverture** : l'EPIC CROSS-DOC-BOOK (S-210, commit `b51fffe`) était `✅ TERMINÉ` dans BACKLOG/INDEX mais marqué `🟡 À FAIRE` dans PROMPT_REPRISE/cadrage, et surtout son filet était **aveugle** : (F1) fixtures à en-têtes `= Intro` que codex n'écrit **jamais** (`CollectOcrTask.kt:56` : « only the structured OCR text, no AsciiDoc headers ») ; (F2) assertion tautologique — « N page headings » comptait les `= ` **injectés par la fixture** (`CrossBoroughSteps.kt:55-61`), pas un comportement de l'assembleur ; (F3) seul le chemin blob-plat `assemble(pagesDir,…)` était couvert alors que la **production** emprunte le chemin structuré (`AssembleBookTask.kt:126-167` : `TOC → BookTocParser → BookTreeBuilder → assemble(tree, contentAwareResolver)`) ; (F4) le FT couvrait ce chemin mais avec des en-têtes dans les pages. **Réécriture** : `cross_borough_book.feature` = 6 scénarios fidèles — (a) pages codex **brutes** → blob-plat **sans** en-tête fabriqué ; (b) ordre prouvé sur le **contenu** ; (c) `TOC → structure` : en-têtes **dérivés de la TOC** + ancres `[[ref]]` + contenu brut préservé ; (d) section multi-pages (`Page = 1, 2` + `001/002-*.adoc`) → **un** en-tête, contenus ordonnés ; (e) page TOC manquante → dégradé sans échec ; (f) fichiers non-adoc ignorés. `CrossBoroughSteps.kt` réécrit (`BookTocParser` + `BookTreeBuilder` + `contentAwareResolver`, TOC dans un répertoire temporaire dédié, `sectionHeadings()` exclut le titre `=`). Cadrage `.agents/CADRAGE_EPIC_CROSS_DOC_BOOK_FIDELITY.adoc` (6 constats, 8 décisions, 4 US). Baby-step BDD strict RED (6 `UndefinedStepException`) → GREEN **6/6 PASSED**. **Test-only, zéro code de production modifié.** Gates : `compileKotlin` VERT, `test` **4827/0**, FT **144/1 préexistant** (`AssembleBookTaskContentRichTocTest` page 73, drift corpus A1), non-régression runners Cucumber voisins (`cucumberTest` 3991, `bookTree` 192, `bookValidation` 191, `bookMatter` 194, `bookImages` 192, `bookNavigation` 191) 0 failure, `validatePlugins` VERT, garde publication 3/3, zéro secret. Piège #31 documenté PROMPT_REPRISE (« un test vert n'est pas un test fidèle »). Rapport : `.agents/sessions/263-cross-borough-fidelity.adoc`. | 10/10
| 262 | 2026-09-24 | feat DDD/TDD/BDD + dogfooding | **DOC-BOOK-CONSISTENCY-B6 ✅ TERMINÉ** (dernière dette de la code review S-258, P3). `BookNumbering.numbers()` (dérivation identité) est **documentée comme choix assumé** (le numéro imprimé dans le livre est celui de la TOC source — recalculer un index 1-based divergerait du livre physique) ; `BookNavigation` est **branchée en prod** (l'option « retirer » écartée). **Domaine (TDD strict)** : `BookNumbering.emittedNodes(tree)` (sections distinctes, une par `ref`, première page gagne) + `navigation` court désormais sur les sections *émises* — **bug latent corrigé** (une ligne TOC multi-pages `| 1.0.1 | … | 5,6,7,8 |` faisait pointer « suivant » sur la **même** section logique) ; `BookLayout.emitNavigation: Boolean = false` (opt-in backward compat) + `navigationLinks(nav)` (`<<ref,title>>` par voisin présent, `" | "`-joint, vide si standalone). RED 6 `Unresolved reference` → GREEN 6/6. **Assemblage** : `BookAssembler.buildStructuredBody` appende la ligne de navigation au pied du bloc quand `emitNavigation` (RED 1 → GREEN 2). **Wiring** : `AssembleBookTask.navigation` + DSL `book { navigation.set(true) }` + propriété plate miroir `bookNavigation` + CLI `-Pdocument.bookNavigation` + contrat N3 `BookConfig.navigation` (round-trip serializer + `SerializeDocumentConfigTask.bookNavigation`) ; FT `BookNavigationFunctionalTest` 2/2 ; BDD `book_navigation.feature` 5 scénarios `@book-navigation` + `BookNavigationSteps` (préfixe `book-navigation` anti-glue) + **22e runner `bookNavigationCucumberTest`**. **Preuve dogfooding** (corpus FPA réel, consumer `/tmp/opencode/doc-nav-dogfood`, mavenLocal, Règle 2) : TOC 116 sections + 201 pages OCR → `book.adoc` 264 Ko, **239 liens de navigation, 116 cibles distinctes, 0 dangling** (chaque `<<ref,title>>` résout une ancre `[[ref]]` réellement émise — vérifié `comm`), première section `1.0.1` **next-only**. Gates : `test` **4785/0** (0 FAIL/0 ERROR ; 915 UT non-Cucumber exécutés), FT 144/**1 préexistant** (`AssembleBookTaskContentRichTocTest` page 73, stash-confirmé, hors périmètre), **22 runners Cucumber 0 fail**, `validatePlugins` VERT, garde publication `DocumentPluginPublicationTest` 3/3, `publishToMavenLocal` VERT, zéro secret, aucun artefact dans `office/`. **Toutes les dettes B1→B6 de la code review S-258 sont soldées.** Piège #16 (mavenLocal dans `pluginManagement`) + #30 (navigation = sections émises, pas pages OCR) documentés PROMPT_REPRISE. Rapport : `.agents/sessions/262-book-navigation.adoc`. | 10/10
| 261 | 2026-09-24 | feat DDD/TDD/BDD + dogfooding | **EPIC DOC-BOOK-MATTER ✅ TERMINÉ 3/3 US** (B3 de la code review S-258) — la politique de matter est **dérivée de la TOC** : un livre n'exige un FRONT/BACK que si sa TOC le déclare, supprimant le faux positif permanent de la règle S3 sur une TOC body-only (racines `{1,2,3,4}` du corpus FPA). **US-1** (domaine pur, TDD) : `MatterPolicy` data class (mapping explicite front/back roots, invariant d'intersection vide, `requiresFront`/`requiresBack` dérivés du mapping, `classify(ref)`, factories `DEFAULT`/`NONE`/`of`/`derive(sections)`/`forMode(mode, sections)`) + `MatterPolicyMode` enum (`DERIVED`/`LEGACY`/`NONE`) — RED 13 `Unresolved reference` → GREEN 13/13 → second RED (`derive` 4 tests) → GREEN 17/17. **US-2** : `BookLayout.emitMatterBreaks: Boolean = false` (opt-in backward compat) + `matterPolicy: MatterPolicy = DEFAULT` + `matterBreak()` ; `BookAssembler.buildStructuredBody` regroupe en `BodyBlock(level, matter, text)` et émet `<<<` aux transitions FRONT→BODY / BODY→BACK (la matter break prime sur le page-break de niveau, jamais doublée) + page de titre fermée par `<<<`. **US-3** : `BookValidator.validateStructure(sections, matterPolicy)` — S3 n'émet un finding que si la politique le requiert (défaut `DEFAULT` = rétro-compat) ; wiring `AssembleBookTask` (`@Input matterPolicyMode` défaut `DERIVED` + `matterBreaks` défaut `false` résolus via `forMode`) + DSL `book { matterPolicy.set(MatterPolicyMode.LEGACY); matterBreaks.set(true) }` + propriétés plates miroir + CLI `-Pdocument.bookMatterPolicy`/`bookMatterBreaks` + contrat N3 `BookConfig` étendu (round-trip serializer) ; FT `BookMatterFunctionalTest` 4/4 (STRICT body-only passe sous DERIVED, LEGACY échoue, breaks on/off) ; BDD `book_matter.feature` 8 scénarios `@book-matter` + `BookMatterSteps` (préfixe `book-matter` anti-glue) + **21e runner `bookMatterCucumberTest`**. **Preuve dogfooding** (corpus FPA réel, consumer `/tmp`, mavenLocal, Règle 2) : DERIVED → **0 finding matter** (`no FRONT/BACK section` absents), LEGACY → **2 findings** (faux positif historique prouvé), livre assemblé (116 ancres, page de titre fermée par break, 76 titres). Gates : UT **4493/0** (baseline 4130 → +363 nets), FT 142/**1 préexistant** (`AssembleBookTaskContentRichTocTest` page 73, stash-confirmé S-257/S-260, hors périmètre matter), 21 runners Cucumber 0 fail, `validatePlugins` VERT, `publishToMavenLocal` VERT, garde 3/3, zéro secret, aucun artefact dans `office/`. Piège #29 documenté PROMPT_REPRISE. Rapport : `.agents/sessions/261-book-matter-policy.adoc`. | 10/10
| 260 | 2026-09-24 | feat DDD/TDD/BDD + dogfooding + maintenance | **EPIC DOC-BOOK-IMAGES ✅ TERMINÉ 3/3 US** (B4/B5 de la code review S-258) — le livre est *illustré* par les scans et l'EPUB est **epubcheck-VALID** (0 RSC-007). **US-2** (domaine pur, TDD) : `BookImageResolver` (scan par préfixe numérique — 2 conventions `61.adoc`/`001-introduction.adoc`, 2 paddings, split `_N`) + `BookImageRewriter` (directive `image::` fantôme → scan de sa page ; supprimée si aucun scan ; formes block ET inline, légende/commentaire préservés) — RED 11 tests `BookImagesTest` → GREEN → REFACTOR (TDD : `055_2` doit normaliser le padding vers `55_2`). **US-1** : `BookPageRenderer` (rendu partagé des 2 conventions), `ContentPageResolver(pagesDir, photosDir)` (illustration opt-in, ghost repair toujours actif), `BookLayout.imagesDir` (`:imagesdir:` optionnel, défaut `null`), `AssembleBookTask` (extrait les targets du contenu assemblé → copie les scans *référencés* dans `images/` + `:imagesdir: images` relatif) — RED 6 `ContentPageResolverImagesTest` + 2 `BookLayout` → GREEN → REFACTOR. **US-3** : `book_images.feature` 6 scénarios `@book-images` + `BookImagesSteps` + 20e runner `bookImagesCucumberTest` + `CucumberTaskSpec`. **Découverte majeure (sonde empirique)** : AsciidoctorJ n'embarque une image que dans le répertoire du `book.adoc` — chemin **absolu** → silencieusement absent (RSC-007) ; chemin relatif **sortant** → `RSC-026 leaks outside the container` (EPUB INVALID) ; seule la copie dans un sous-répertoire (`images/`) + `:imagesdir:` relatif est embarquée ET conforme. **Économie d'Encre** : 4 scans copiés (≈13 Mo), jamais les 525 Mo du corpus. **Dogfooding réel** (TOC riche 116 sections, consumer `/tmp/opencode/fpa-consumer-260`, mavenLocal, Règle 2) : `bookPipeline` BUILD SUCCESSFUL 4m59s, `livre.adoc` porte `:imagesdir: images` + les 4 ghosts substitués (`61.jpg`, `80_1.jpg`, `83_1.jpg`, `183.jpg`), `validateDocumentEpub -Pdocument.epubCheck=STRICT` → **VALID** (`{"status":"VALID"}`). **Maintenance** : drift catalogue ws 0.0.43→**0.0.56** réaligné (garde `DocumentPluginPublicationTest` RED à HEAD, **stash-prouvé préexistant** — piège #13, 5e occurrence ; additifs bakery/codex/capsule/slider/graphify/codebase, document-plugin inchangé). Gates : UT **4130/0**, FT 138/**1 préexistant** (`AssembleBookTaskContentRichTocTest` page 73 — corpus A1 sans `[ILLISIBLE]`, stash-confirmé S-257/S-259), 20 runners Cucumber 0 fail, `validatePlugins` VERT, `publishToMavenLocal` VERT, garde 3/3, zéro secret, aucun artefact dans `office/`. Pièges #30/#31/#32 documentés PROMPT_REPRISE. Rapport : `.agents/sessions/260-book-images-illustration-ghost-repair.adoc`. | 10/10
| 259 | 2026-09-24 | feat + TDD + dogfooding | **EPIC DOC-BOOK-CONSISTENCY ✅ TERMINÉ — correction B1/B2 (cohérence `outputFileName` de bout en bout)**. Précédée de **S-258 code review** (lecture seule) : verdict *le pipeline livre depuis OCR existe et fonctionne de bout en bout* (dogfooding FPA réel) + 6 anomalies (B1 `book.adoc` régénéré dégradé, B2 `assembleBook`/`enrichDocument` hardcodés, B3 `Matter`, B4 `photosDir` structuré, B5 images fantômes, B6 numérotation). **B2 (TDD strict)** : nouveau FT `BookOutputFileNameFunctionalTest` RED 3/4 (`assembleBook` renommé, `enrichDocument` renommé, collect indexe le renommé) → GREEN 4/4 (défaut `book.adoc` non-régression) via 2 dérivations du knob dans `DocumentPlugin.kt` — `assembleBook.outputFile` = `docs/document/$outputFileName.adoc` (défaut `book`), `enrichDocument.outputFile` = `docs/document/$outputFileName-enriched.adoc` (défaut `document`) ; les hardcodes `book.adoc`/`document-enriched.adoc` disparaissent. **B1 (dogfooding réel)** : run HEAD via mavenLocal (consumer transitoire `/tmp/opencode/doc-consumer-259`, Règle 2) avec la **TOC riche** (116 sections, pas le stub `toc.adoc` 3 lignes) et `-Pdocument.outputFileName=livre` → `livre.adoc` produit **directement** (4600 lignes / 249 Ko / **116 ancres** / 6 niveaux), `collectDocumentRetrieve` indexe **5 artefacts** dont `livre.adoc`, plus de `book.adoc` transitoire. Préalable gouvernance : dossier racine `.agents/` **supprimé** (`CADRAGE_EPIC_RENAME{,_V3}.adoc` déplacés `git mv` vers `document-plugin/.agents/`, historique préservé, convention 29 boroughs). Docs : 3 EPICs cadrés (`DOC-BOOK-CONSISTENCY` ✅, `DOC-BOOK-IMAGES` B4/B5 P2, `DOC-BOOK-MATTER` B3 P2) + INDEX/BACKLOG/PROMPT_REPRISE + rapport `.agents/CODE_REVIEW_BOOK_PIPELINE_S258.adoc`. Gates : compileKotlin VERT, FT book + collector VERT (1 échec `RichTocTest` page 73 **stash-prouvé préexistant** — corpus A1 sans `073.adoc`), UT book VERT, 4 runners Cucumber dédiés VERT. Zéro bump, zéro publication, zéro secret. Rapports : `.agents/sessions/259-book-output-filename-consistency.adoc` + `.agents/CODE_REVIEW_BOOK_PIPELINE_S258.adoc`. | 10/10
| 258 | 2026-09-24 | code review + cadrage | **Code review pipeline livre + 3 EPICs de cohérence (lecture seule, zéro code)** — audit complet du borough (demande pilote) : verdict *pipeline livre/OCR fonctionnel de bout en bout* (dogfooding FPA `livre.adoc` 4689 lignes / 116 ancres / 6 niveaux ; 22 cadrages ; 3822 UT / 130 FT / 19 runners ; 0.0.17 Central ; zéro `TODO` src/main). 6 anomalies (B1/B2/B3/B4/B5/B6). Vision/roadmap/backlog/prompt mis à jour ; 3 EPICs cadrés. Rapport `.agents/CODE_REVIEW_BOOK_PIPELINE_S258.adoc`. | 10/10
| 257 | 2026-09-17 | refactor+gouvernance | **RENAME Vague 4 ✅ — le champ lexical métier complet sort de document-gradle** (cross-borough depuis Queens, mission 4 ordre pilote). Constat d'ouverture : Vague 3 déjà livrée (RENAME-2 S-251, RENAME-8 S-253, clôture S-254, audit CADUQUE S-256) MAIS le cadrage révisé **Vague 4 (2026-09-10, D5/D11 abrogées — champ lexical complet)** postdate l'audit S-256 (2026-09-09, sigles seuls). Delta : transfusion d'abord puis neutralisation. **Transfusion** (`training-gradle/.agents/TRANSFER_LEXIQUE_FORMATION.adoc`) : § 1bis créé (`ContentPageResolver` convention page scannée `<page>_N.adoc` ; `BookOcrFailureDetector` 5 raisons ILLISIBLE/TOO_SHORT/IMAGE_MISSING/TABLE_SUSPECT/STRUCT_SUSPECT + seuils) + § 2 complétée (table par borough). **Neutralisation** docs courantes : AGENT (`SPG/SPD pedagogiques`→`plans de contenu`, `PDF stagiaire`→`PDF lecteur`, `sessions de formation`→`contenus`) + BACKLOG (US-DOC-02 `formateur`→`auteur`, US-DOC-04 `formation`→`contenu`, priorité MVP0 ×3) + INDEX (diagramme) + ARCHITECTURE_BOUNDARY (`pipeline de formation`→`pipeline de contenu`, `livrable stagiaire`→`livrable lecteur`). Fixtures synthétiques (11 fichiers) : `Devenir Formateur`→`Introduction`/`Content Book`, `formation.adoc`→`document.adoc`, `Livre de Formation`→`Livre de Contenu`, `formations/`→`contents/` (42 occ), `analyse pedagogique`→`analyse documentaire`. Citations corpus `office/metiers` conservées (piège #18). Gates : compile VERT, 81 UT ciblés 0 FAIL, `ocrQualityCucumberTest` 8/8, 2 FT corpus PASSED, gate Vague 4 `src/` zéro + docs courantes zéro (hors traces d'audit). Échec `AssembleBookTaskContentRichTocTest` (page 73) **stash-confirmé préexistant** (drift corpus A1). Zéro bump, zéro publication, zéro secret. Rapport : `.agents/sessions/257-rename-vague4-lexique-transfusion.adoc`. | 10/10
| 256 | 2026-09-09 | gouvernance | **Audit RENAME ✅ CADUQUE — zéro résidu métier privé dans src/ + docs courantes** — arbitrage pilote S-255 : session RENAME de vérification post-Vagues 1-3. Gate D16 re-déroulée (`grep -rwn "FPA\|REAC\|AFNOR\|SPG\|SPD" src/`) : 10 matches TOUS faux positifs piège #18 (chemins corpus `office/metiers/FPA` légitimes, conservés — `AssembleBookTaskContentRichTocTest:36`, `BookPipelineContentFunctionalTest:26`, `BookPipelineN3ContentFunctionalTest:44`, `AssembleBookTaskContentTest:17,19,32`, `HtmlLinkLintFunctionalTest:27`, `ContentBookArtifactGenerationTest:16,22,29`, `PdfCheckContentFunctionalTest:27`, `EpubCheckContentFunctionalTest:31`). Sweep exhaustif (insensible casse, noms de fichiers, KDoc/commentaires src/main, casesensitive resources/features, README, build/settings/wrapper/catalogue) = zéro partout. Docs gouvernance = mentions légitimes uniquement (EPIC DOC-FPA-PUBLISH réel GBL-003.6, historique Vagues RENAME, rapports/archives non renommables). Verrou REAC `AssembleBookTaskContentRichTocTest.kt:100` vérifié corrigé S-213 (`"enquête terrain"` corps OCR p.22). **Aucun cadrage RENAME-13 nécessaire — gate D16 confirmée VERTE sur document, EPIC RENAME Vague 3 reste ✅ TERMINÉ.** Gouvernance pure : zéro code modifié, zéro bump BOM, zéro secret, zéro publication ; baseline S-255 préservée par construction (UT 3822/0, FT 130/130, 19 runners 3068/0, garde 3/3). DOC-FPA-PUBLISH-0 STANDBY inchangé (A1 : 16 ILLISIBLE). Rapport : `.agents/sessions/256-rename-audit-caduque.adoc`. | 10/10
| 255 | 2026-09-09 | maintenance | **Sync catalogue ws 0.0.37 (drift cross-borough MEMPHIS)** — drift attrapé par la garde `DocumentPluginPublicationTest` (RED prouvé avant fix — discipline TDD, 4e occurrence du cycle piège #13 : S-244 découverte, S-246, S-251, S-255). Réalignement 3 sources (pattern S-246/S-251 réappliqué) : settings D4 `workspace-catalog:0.0.36`→`0.0.37` + platform `workspace-bom`→0.0.37 + toml local. `document-plugin` reste **0.0.17** (le catalogue MEMPHIS pinnait déjà 0.0.17 — zéro bump plugin). Diff BOM 0.0.36→0.0.37 : codebase-plugin 0.0.12→0.0.14, zéro impact document (3 mentions = commentaires KDoc). Gates : garde publication 3/3 VERT (RED→GREEN), `publishToMavenLocal` VERT (POM porte platform 0.0.37 vérifié), UT 3822/0 (baseline S-254 préservée exactement), FT 130/130, 19 runners Cucumber dédiés 3068 scénarios / 0 fail, `check` VERT 6m20s, secrets RAS. Zéro publication Central — pins distribués au prochain bump (accord pilote). DOC-FPA-PUBLISH-0 STANDBY inchangé (corpus A1 : 16 ILLISIBLE vérifié à l'ouverture). Rapport : `.agents/sessions/255-sync-catalog-0.0.37.adoc`. | 10/10
| 254 | 2026-09-09 | rename transverse | **RENAME Vague 3 CLÔTURÉE ✅ 6/6 US (transverse 5 boroughs)** — RENAME-9 bakery était déjà livré (S-212 session parallèle). S-254 exécute RENAME-7 planner (prompts LLM `IntentionPlanner.kt:149,208` → génériques + 6 fixtures, `test` 144 VERT + `cucumberTest` 23 VERT, RED réel attrapé sur verrou "codex corpus"), RENAME-10 codebase (description tâche `CodebasePlugin.kt:176` + 9 fixtures koog neutralisées, exclusion D11 keyword AFNOR documentée, `test` 10602 avec 4 échecs préexistants S-212, classes ciblées VERTES, commit sélectif — WIP parallèle CB-FPA-RAG exclu), RENAME-11 plantuml (KDoc loader + 5 fixtures registres publics + docs courantes + **drift ws 0.0.37 attrapé par la garde plantuml** → réaligné, `test` 2531/0), RENAME-12 hyperframes (BACKLOG + doc/PILOTAGE + doc/ARCHITECTURE + AGENT règle 9, compileKotlin VERT). Gate transverse D16 VERT sur 5 boroughs. Pièges #25 (verrou = comportement, pas fixture nominale) + #26 (WIP parallèle — commit sélectif jamais git add -A) documentés PROMPT_REPRISE. Zéro publication — chaînes non-significatives aux prochains bumps. Rapport : `.agents/sessions/254-rename-v3-cloture-transverse.adoc`. | 10/10
| 253 | 2026-09-09 | feat + TDD/BDD | **RENAME-8 ✅ — borrowedVocabulary sorti du nommage public (config injectée D14)** — la liste hardcodée `setOf("REAC","AFNOR","DC","TS","RNCP","CP","ECF")` disparaît de `PlantUmlClassifier.kt` + `PlantUmlTranslationAdapter.kt` : `PlantUmlBlock(raw, borrowedVocabulary: Set<String> = emptySet())` (le bloc porte le vocabulaire pour la classification) + `PlantUmlTranslationAdapter(..., borrowedVocabulary: Set<String> = emptySet())` (paramètre additif fin de signature — bakery 0.0.17 publié compile sans changement ; l'adapter injecte sa config dans le bloc classify + l'utilise pour la préservation placeholder). Le plugin public ne connaît plus AUCUN vocabulaire métier — chaque consommateur fournit le sien (grille D13/D14 cadrage V3 : RNCP/AFNOR publics sortent avec les sigles privés car la liste hardcodée EST le vecteur de fuite). 12 fixtures neutralisées : sigles neutres REF/ORG/MOD/EVA + injection explicite pour prouver le mécanisme + 1 UT nouvelle (défaut vide → tout est traduit). Baby-step TDD strict : RED (compile fail `No parameter with name 'borrowedVocabulary'` ×5) → GREEN (`PlantUmlClassifierTest` 16/16 dont 2 nouvelles ; `PlantUmlTranslationAdapterTest` 12/12 dont 3 nouvelles) → REFACTOR n/a. Fix itération : le test « unconfigured translated by default » exige un fake qui traduit tout (`fakeTranslator` préfixe universel — le fake sélectif laissait `REF` inchangé, assertion infondée) → piège #23 PROMPT_REPRISE (le fake doit matcher le contrat sémantique à prouver). Gates : `test` 3822/0 (baseline 3820 + 3 UT nettes), `functionalTest` 130/130 baseline exacte, `plantUmlValidationCucumberTest` 10 scénarios PASSED, `validatePlugins` VERT, gate D16 word-boundary = 1 match (citation corpus réel `AssembleBookTaskContentRichTocTest:100`, faux positif piège #18), zéro secret, zéro bump BOM. Zéro publication — la config injectée sera distribuée au prochain bump. EPIC RENAME Vague 3 : RENAME-8 ✅ — restent RENAME-9 (bakery)/10 (codebase)/11 (plantuml)/12 (hyperframes). Rapport : `.agents/sessions/253-rename-8-borrowed-vocabulary-config.adoc`. | 10/10
| 252b | 2026-09-08 | PUB cross-borough | **PUB 0.0.17 Maven Central ✅ — RENAME-2 + OCR-QUALITY + fix inline DISTRIBUÉS** — accord pilote explicite. Chaîne DAG N0→N2 : MEMPHIS bump 0.0.35 (document 0.0.17, gardes réalignées, test VERT, Central VERT) → document re-pin 3 sources + garde publication 3/3 → consommateur tiers SANS mavenLocal (Central prouvé debug log, STRICT VALID + preuve négative textless BUILD FAILED `no extractable text`) → bakery D5 (`document-plugin -> 0.0.17` sans hardcode, compileKotlin VERT) → re-dogfooding livre FPA depuis Central (3m52s, convergence 16 ILLISIBLE + 4 IMAGE_MISSING + 7 TABLE_SUSPECT + PDF VALID maintenue). **Collision cross-agent MEMPHIS** : déploiement concurrent 15:28 UTC a publié le même document-plugin 0.0.17 (JAR byte-identique sha256 `a005ff2e`) pendant mes polls — Central immutable, distribution équivalente, aucun re-bump ; leçon : coordonner MEMPHIS avant bump multi-agent. Pin POM Central bom 0.0.34 = zéro impact (deps document toutes versionnées explicitement, pattern S-209). Commits : document `8989a05` + `cbaf8d9`. Rapport : `.agents/sessions/252b-pub-0.0.17-distribution.adoc`. | 10/10
| 252 | 2026-09-08 | dogfooding + fix TDD/BDD | **Dogfooding convergence livre FPA + fix IMAGE_MISSING macro inline ✅** — livre FPA régénéré avec le HEAD (RENAME-2+OCR-QUALITY-3+6 gardes) via commande canonique étendue `bookPipeline validateDocumentEpub validateDocumentPdf fpaPublish` + `-Pdocument.pdfCheck=LENIENT` (2 runs : 3m18 + 4m10 BUILD SUCCESSFUL). **Bug réel découvert par le dogfooding** : détecteur IMAGE_MISSING block-only (`image::`) ignorait la macro inline `image:file[]` — 2/4 images fantômes invisibles (p.61 `cerveau_gauche_vs_cerveau_droit.jpg`, p.83_1 `arrow-curved-left`). Fix baby-step TDD : RED UT (`expected: <1> but was: <0>`) → GREEN 1 ligne `image::?` (12/12 UT `ContentBookAssemblyTest`) → BDD scénario `@inline-image` `ocr_quality.feature` 8/8 PASSED. Outillage fpaPublish : `pdf-validation-report.json` ajouté aux artefacts déplacés + README tools. Convergence run 2 : **16 ILLISIBLE + 4 IMAGE_MISSING (4/4 localisées ref+titre+detail) + 7 TABLE_SUSPECT + `pdf-validation-report.json` VALID** — rapport prêt pour l'itération humaine A1 Tombouctou. STRUCT_SUSPECT=0 légitime (les 3 `invalid part` du log Asciidoctor = diagnostics de rendu du livre assemblé, pas des marqueurs de scans). Dette S-082 clôturée : consumer racine `document = "0.0.16"` (Central). Gates : `check` VERT 19m56s, UT 3820/0, FT 130/130, cucumber global 3055/0, ocrQuality 8/8. Pièges #19 (macro image 2 formes `image::?`) + #20 (dogfooding HEAD via mavenLocal + fpaPublish rapport PDF). Zéro publication Central (HEAD en attente accord pilote). Rapport : `.agents/sessions/252-dogfooding-convergence-inline-image-fix.adoc`. | 10/10
| 251 | 2026-09-08 | feat + refactor + TDD/BDD | **RENAME-2 + drift ws 0.0.34 + OCR-QUALITY-3 ✅** — RENAME-2 renommage dur FPA→content : `FpaPageResolver` → `ContentPageResolver` (classe+KDoc — la convention `NNN.adoc`/`NNN_N.adoc` devient une convention content générique), `BookAssembler.fpaAwareResolver` → `contentAwareResolver` (+ câblage AssembleBookTask:107), commentaires 5 fichiers prod neutralisés (BookTocParser/BookNumbering/Matter/DocumentPlugin/AssembleBookTask — refs FPA-BOOK-* → BOOK-*), 9 fichiers tests renommés via git mv (ContentBookAssemblyTest, ContentBookArtifactGenerationTest, EpubCheck/PdfCheck/AssembleBookTask/RichToc/BookPipeline×2 Content...), knob `fpa.book.publish` → `content.book.publish` (point d'entrée public neutralisé, build.gradle.kts + test), 3 features Gherkin ("FPA pipeline"→"scanned-content pipeline", "FPA Guide"→"Content Guide"), README. Gate : `grep -ri "fpa" src/` = zéro match hors citations filesystem `office/metiers/FPA/..._FPA_II/...` (cadrage : "ils vivent en privé ou citent office/" — légitimes) + faux positif `LeafPath` (contient littéralement "fpa", documenté cadrage + piège #18). Audit cadrage S-250 était incomplet (9 fichiers tests + knob build non listés) — le gate grep a fait foi. **Drift catalogue ws attrapé par la garde S-244** pendant la baseline `test` (piège #13) : MEMPHIS a bumpé BOM+catalog 0.0.34 (S-215 racine) pendant que document pinne 0.0.32 → RED `workspace bom platform pin must use the ws catalog BOM version (0.0.34)` → réaligné 3 sources (settings D4 `workspace-catalog:0.0.34` + platform `workspace-bom:0.0.34` + toml local `workspace-bom = "0.0.34"`), Central `<latest>0.0.34</latest>` vérifié avant bump, version dérivée plugin 0.0.16 inchangée (catalog MEMPHIS `document-plugin = "0.0.16"`), garde publication 3/3 GREEN — pattern S-246/S-249 réappliqué. OCR-QUALITY-3 (TDD strict RED 8 `Unresolved reference` → GREEN → REFACTOR) : `OcrFailureReason` +3 raisons (IMAGE_MISSING — regex `image::` + résolution filesystem locale à la page = images fantômes RSC-007 ; TABLE_SUSPECT — rangée linéarisée `|...|` ; STRUCT_SUSPECT — marqueurs `invalid part`/`out of sequence`), `BookOcrIssue` +champ `detail: String? = null` (backward compatible, preuve discriminante : chemin image / rangée table / marqueur), detector raisons whole-page (ILLISIBLE/TOO_SHORT) exclusive-first puis raisons structurées sondées multi-issues (une page peut porter les 3 signaux — aucun perdu), `BookOcrIssueReport` champ `detail` conditionnel (shape backward compatible). Tests : `ContentBookAssemblyTest` 11/11 UT (4 originaux préservés + 7 nouveaux), `ocr_quality.feature` 7 scénarios `@ocr-quality` (ghost-image, materialised-image, linearised-table, invalid-part, out-of-sequence, multi-issue, report-shape) + `OcrQualitySteps` préfixés « ocr-quality » anti-glue S-223 + `OcrQualityCucumberRunner` 19e runner dédié pattern S-082 + `CucumberTaskSpec("ocrQualityCucumberTest")` — 7/7 PASSED. Gates : `check` VERT 5m10s, `test` 3801/0, FT 130/130 baseline exacte, secrets RAS. Zéro publication (0.0.16 reste Central — HEAD porte RENAME-2+OCR-QUALITY, breaking change D7 à distribuer sur accord pilote). EPIC RENAME (document) ✅ TERMINÉ, EPIC OCR-QUALITY-3 (document) ✅ TERMINÉ. Rapport `.agents/sessions/251-rename-2-ocr-quality-3.adoc`. | 10/10
| 250 | 2026-09-07 | cadrage | **Cadrages DOC-FPA-PUBLISH + OCR-QUALITY (critical path MVP0 — gouvernance pure, zéro code)** — arbitrage pilote 2026-09-07 : priorité aux boroughs qui rapprochent de la génération de formation. (1) EPIC DOC-FPA-PUBLISH (GBL-003.6) : livre FPA navigable déjà publié (S-082) — DOC-FPA-PUBLISH-0 (1 US ~1 pt) en STANDBY, déclenché par le livrable A1 Tombouctou, run dogfooding assembleBook + bookPipeline. (2) EPIC OCR-QUALITY (demande pilote) : détection auto images problématiques + transcriptions douteuses — US-3 document : extension `BookOcrIssue`/`BookOcrFailureDetector` (IMAGE_MISSING + TABLE_SUSPECT + STRUCT_SUSPECT, backward compat). Constat : detector actuel ne flagge que [ILLISIBLE] + <40 chars, les 5 images fantômes (3 RSC-007) et 11 tableaux perdus furent découverts manuellement. Docs `.agents/CADRAGE_EPIC_DOC_FPA_PUBLISH.adoc` + `.agents/CADRAGE_EPIC_OCR_QUALITY.adoc`. Zéro code production, zéro bump BOM. | 10/10
| 249 | 2026-09-07 | PUB cross-borough | **PUB 0.0.16 Maven Central ✅ — les 6 gardes distribuées hors machine de dev** — accord pilote explicite. Chaîne DAG N0→N2 : (1) MEMPHIS bump toml `workspace-bom 0.0.32` + `document-plugin 0.0.16`, gardes réalignées (`PlatformCatalogAlignmentTest` 0.0.32 + `WorkspaceCatalogStructureTest.document plugin version matches bakery consumer pin` 0.0.16 — RED 1 failure attrapée par la garde → GREEN 111/0), `publishToMavenLocal` VERT, Central VERT — propagation repo1 BOM+catalog `<latest>0.0.32</latest>`, BOM porte pdfbox 3.0.4 ; (2) document re-pin 3 sources (settings D4 `workspace-catalog:0.0.32` + platform `workspace-bom:0.0.32` + toml local `document = "0.0.16"` — garde `DocumentPluginPublicationTest` RED sur split-brain toml local, rôle S-244 confirmé) → version dérivée **0.0.16**, garde publication 3/3 VERT ; (3) **1er upload Central rejeté** nmcp AUTOMATIC `Dependency version information is missing` ×15 deps (kotlin-stdlib, jackson, asciidoctorj×3, koog, langchain4j-ollama, plantuml-plugin, contracts×3, coroutines×2, pdfbox, slf4j) — cause : deps `implementation` sans version (résolues par platform BOM) → POM sans `<version>` ; fix **pattern S-209** (Queens MEMPHIS-BOM-FIX US-4, même rejet kotlin-stdlib-jdk8) : 12 deps littérales → alias `libs.*` + catalogue local document enrichi (`jackson = "2.21.2"` + `pdfbox = "3.0.4"` + 2 entrées `[libraries]`) → POM toutes deps versionnées → publication VERT 25s AUTOMATIC, propagation POM HTTP 200 poll 4 (~2 min), JAR Central = 2 classes `ValidateDocumentPdfTask` ; (4) consommateur tiers `/tmp/opencode/doc-consumer-pdf` bumpé 0.0.16 SANS mavenLocal (drift S-248 résolu par construction) : résolution `Gradle Central Plugin Repository` prouvée debug log, `convertDocumentToPdf validateDocumentPdf -Pdocument.pdfCheck=STRICT` → rapport VALID, preuve négative textless → STRICT BUILD FAILED `page N: no extractable text` ; (5) bakery (consommateur dur, zéro hardcode D5) pin ws `0.0.30 → 0.0.32` + platform `0.0.30 → 0.0.32` → `document-plugin -> 0.0.16` automatique (vérifié dependencies), compileKotlin VERT. Piège #17 documenté (POM deps version explicite, pattern S-209) + piège #18 (bump catalogue MEMPHIS = 2 gardes en cascade à réaligner même commit). Secrets RAS. Commits : MEMPHIS `988fda5`, document `31010ac`, bakery `343aeaa` (tous poussés). Rapport `.agents/sessions/249-pub-0.0.16-pdf-check-distribution.adoc`. | 10/10
| 248 | 2026-09-07 | vérification consommateur | **Preuve consommateur 6e garde PDF + diagnostic drift Central ↔ mavenLocal (zéro code)** — projet tiers `/tmp/opencode/doc-consumer-pdf` (pattern S-239, Gradle 9.7.1, JDK 25, `pluginManagement { mavenLocal() }`) consomme `education.cccp.document:0.0.15` : 4/4 scénarios VERTS — STRICT `convertDocumentToPdf validateDocumentPdf -Pdocument.pdfCheck=STRICT` → rapport VALID ; preuve négative discriminante (PDF 2 pages sans texte via helper Java PDFBox, zéro Python) → BUILD FAILED `page 1: no extractable text, page 2: no extractable text` + rapport INVALID ; LENIENT → vert + rapport INVALID ; OFF → skip (rapport stale inchangé, sémantique epub) ; knob `outputFileName` suivi (miroir S-235/236) → `<pdf-file-missing> gone-custom.pdf` sur custom absent / VALID sur `headings-only` converti. Résolution mavenLocal prouvée par debug log `Using education.cccp:document-plugin:0.0.15 from Maven repository 'MavenLocal'`. **Constat clé** : jar Central 0.0.15 (publié S-241, AVANT la garde S-247) = 0 classe `ValidateDocumentPdfTask` (téléchargé repo1 + `unzip -l`) ; jar mavenLocal 0.0.15 (republié S-247) = 2 classes + câblage (javap). Leçon piège #16 : `publishToMavenLocal` met à jour l'artefact local sous le MÊME numéro — un consommateur DOIT déclarer `pluginManagement { repositories { mavenLocal() } }` AVANT le Plugin Portal pour tester le HEAD local, sinon Gradle résout silencieusement depuis Central (symptôme : `Task 'validateDocumentPdf' not found` malgré le JAR présent). Zéro code document modifié (économie d'encre — la garde est testée S-247 ; cette session est la preuve externe). Publication 0.0.16 non demandée (elle alignera Central sur mavenLocal). Secrets RAS, Central aligné (catalog 0.0.31 = pin local). Rapport `.agents/sessions/248-pdf-check-consumer-proof.adoc`. | 10/10
| 246 | 2026-09-07 | maintenance | **Sync catalogue ws 0.0.31 (drift cross-borough MEMPHIS, garde S-244 RED→GREEN)** — la baseline d'ouverture `test` est tombée ROUGE : la garde `DocumentPluginPublicationTest` (créée S-244, EPIC MEM-CAT-ROLLOUT) a attrapé le drift — MEMPHIS a bumpé le catalogue ws en amont 0.0.31 (S-032, commit `61d7b90`, ROLLOUT-2/3/4/5 + entrées `[plugins]` 8 boroughs + release Central BOM+catalog 0.0.31) pendant que document pinne 0.0.30. RED `workspace bom platform pin must use the ws catalog BOM version (0.0.31)`. Delta catalogue vérifié : `workspace-bom` 0.0.30→0.0.31 + entrées `[plugins]`, `document-plugin` reste 0.0.15 (inchangé). Central vérifié avant bump (`workspace-bom` `<latest>0.0.31</latest>`, `workspace-catalog:0.0.31` POM HTTP 200). Fix 2 lignes : settings `from("education.cccp:workspace-catalog:0.0.31")` (D4) + `build.gradle.kts` platform `workspace-bom:0.0.31`. Gates rétablies : garde 3/3 GREEN, `test` 3272/0, `functionalTest` 126/126, `epubCheckCucumberTest` 4/4, `publishToMavenLocal` VERT (POM local porte `workspace-bom:0.0.31` platform import vérifié). Zéro publication Central — le POM 0.0.15 Central (platform 0.0.30) reste auto-consistant avec le catalogue publié à l'époque ; le pin 0.0.31 sera distribué à la prochaine publication document (aucun besoin réel aujourd'hui). Zéro code prod modifié — pins build uniquement. Secrets RAS. Rapport `.agents/sessions/246-sync-catalog-0.0.31.adoc`. | 10/10
| 245 | 2026-09-07 | gouvernance | **Sync gouvernance post-S-244 (zéro code)** — PROMPT_REPRISE rafraîchi (mission 245, état code S-244 ws 0.0.30, ROLLOUT-2→5 marqués livrés codex S-211 / MEMPHIS S-032). INDEX confirmé à jour. Zéro code production, zéro bump BOM, zéro secret. Baseline S-244 préservée (test 3272/0 + FT 126/126). Rapport `.agents/sessions/245-sync-gouvernance-post-244.adoc`. | 10/10
| 243 | 2026-09-01 | gouvernance + vérification | **Re-vérification backlog vide + consommateurs 0.0.15 + couverture de test (zéro code)** — (1) Audit BACKLOG/vision/roadmap : `grep` marqueurs périmés → zéro match, backlog VIDE (toutes options résiduelles CADUQUES sans besoin réel). (2) Gates document re-exécutées VERTES : `test`+`functionalTest` (UT 3039/0, FT 126/126, 6m17s), `cucumberTest` global (0 failure, 10m43s), `n3PipelineCucumberTest` (contrat N3 staged). (3) Consommateurs sur 0.0.15 : bakery compileKotlin VERT, `test` 1668/4 préexistantes S-241, FT 16/16, `cucumberTest` 1 failure préexistante de plus (`JBake native roundtrip` — test périmé vs comportement S206 summary/description traduits, **prouvée par sonde temporaire 0.0.12 : échoue identiquement**, jamais commitée) + timeout 5m cut run (319 skipped) ; runner-gradle zéro couplage direct (consomme bakery 0.0.9) ; FPA init-script + CLI (0.0.15 mavenLocal + Central). (4) Session concurrente bakery détectée : `libs.versions.toml` bakery 0.0.8→0.0.9 + `build.gradle.kts` version dérivée du catalogue modifiés pendant le run (pas cette session) + bakery-plugin 0.0.9 publiée Central (`<latest>0.0.9</latest>`, absente mavenLocal) — laissé en l'état, clôture bakery = sa session. Zéro code document modifié. Secrets RAS. | 10/10
| 242 | 2026-09-01 | gouvernance + vérification | **Backlog clôturé + consommateurs 0.0.15 vérifiés (zéro code)** — (1) Audit BACKLOG complet : `grep` marqueurs périmés (🟡/⏳/À FAIRE/À PLANIFIER/EN PAUSE) → **zéro match**, tous les EPICs ✅ (DOC-0..15, TRANSLATE, TABLE/PLANTUML/BOOK-VALIDATE, CROSS-DOC-BOOK, BOOK-DOMAIN, CR3/CR4/CR5, XREF, VALIDATE-COMPOSITE, HTML-LINT, METADATA-VALIDATION, VALIDATE-HTML-LINT, EPUBCHECK, PUB) — plus aucun item fonctionnel à traiter. (2) Anomalie ouverture : `settings.gradle.kts` racine modifié non commité — diagnostic : le WIP S-227 (`181a6ca`) réintroduisait `include(":document-plugin")` contrairement au pattern S-006 « Plugin Indépendant + Racine Consommateur (zéro include(), article #0124) » ; vérifié plugin autonome `compileKotlin` VERT + racine consommateur `tasks` VERT → modification CORRECTE conservée + commitée. (3) Consommateurs (demande pilote) : bakery-gradle seul consommateur dur — `bakery-plugin` compileKotlin VERT avec 0.0.15, `test` 1668 tests / 4 failures identifiées par XML = exactement les 4 préexistantes S-241 (publication version test, fixture cheroliv.com, golden master sitemap, feature 57 — prouvées non-causées par stash 0.0.12, Économie d'Encre) ; runner-gradle N3 zéro couplage compile (contrat de facto metadata.json) ; office/metiers/FPA init-script + CLI `-Pdocument.*` (mavenLocal 0.0.15), artefacts réels validés FT dogfooding (book.html lint Valid, book.epub epubcheck Valid). (4) Baseline re-vérifiée : `epubCheckCucumberTest` 4/4 PASSED (59s), Central `<latest>0.0.15</latest>`. Hygiène : `_Version_` BACKLOG 0.0.1 → 0.0.15. Secrets RAS. Commits + push (accord pilote). | 10/10
| 241 | 2026-08-31 | dogfooding + docs + PUB | **Dogfooding EPUB FPA réel + doc consommateur README + publication 0.0.15 ✅** — FT `EpubCheckFpaFunctionalTest` 2/2 (pattern `HtmlLinkLintFunctionalTest` S-226, vit sous `test` comme les audits dogfooding read-only) : test 1 audite le VRAI `office/metiers/FPA/.../livre-navigable/book.epub` (464 KB, mtime 2026-08-31) via `LibEpubCheckAdapter` (même port que `validateDocumentEpub`) → `Valid` — le produit « livre FPA SERVER » est EPUB-valide W3C ; test 2 preuve négative : rebuild zip sans l'entrée `mimetype` obligatoire (première entrée non compressée OCF) → `Invalid` avec issues. **Baby-step TDD** : RED 1re version preuve négative (flip d'octet naïf TOLÉRÉ par epubcheck — zip toujours lisible), fix = corruption structurelle réelle, GREEN 2/2 en 12s. README.md créé à la racine du plugin (aucun n'existait) : quick start 0.0.15, **les 5 imports DSL requis** (constat S-239), exemple 5 gardes, sémantique sévérité OFF/LENIENT/STRICT, table des tâches, précédence CLI > DSL > convention. **PUB (accord pilote)** : bump 0.0.14 → 0.0.15 (1 ligne), `publishToMavenLocal` VERT (signatures .asc), `publishAggregationToCentralPortal` AUTOMATIC accepté 42s, propagation repo1 vérifiée polling (POM HTTP 200 + `<latest>0.0.15</latest>` — piège : metadata périmée ~27 min), POM porte `epubcheck:5.2.1` runtime. **Propagation consommateur** : bakery-gradle (unique consommateur dur, hardcoded 0.0.12 depuis S-226) bumpé 0.0.15 (commit `1b937eb`) — compileKotlin VERT, `test` 1668 tests 4 failures **prouvées préexistantes par stash** (même résultat avec 0.0.12 : version bakery 0.0.9 vs catalogue 0.0.8, fixture cheroliv.com, sitemap golden master, feature 57). Gates document : compileKotlin VERT, UT 3039/0, FT 126/126, validatePlugins VERT, epubCheckCucumberTest VERT, secrets RAS. Commits : document `26a05b1` + `f296263`, bakery `1b937eb`. | 10/10
| 240 | 2026-08-31 | feat + TDD/BDD | **EPIC DOC-EPUBCHECK ✅ TERMINÉ (3/3 US)** — 5e garde : validation epubcheck de l'EPUB produit (gap US-DOC-04 fermé, dernier gap fonctionnel BACKLOG). Cadrage gouvernance pure `.agents/CADRAGE_EPIC_DOC_EPUBCHECK.adoc` (5 constats, 10 décisions : wrapper bibliothèque Java `org.w3c:epubcheck:5.2.1` zéro CLI/Docker, garde autonome NON fusionnée au composite, zéro bump BOM). US-1 (TDD strict) : domaine `document.epub` — port `EpubCheckRunner` + adapter `LibEpubCheckAdapter` + Mode/Result/Report (miroir xref), 5 UT adapter (EPUB3 minimal construit à la main, `<epub-file-missing>`, non-zip, stateless). **Bug réel TDD** : epubcheck appelle `info(null, ...)` → NPE Kotlin, fix paramètre `feature` nullable. 17 UT 0 FAIL. US-2 (FT) : `validateDocumentEpub` + DSL `converter { epubCheck }` + mirror plat + CLI `-Pdocument.epubCheck`, chemin EPUB dérivé même knob `outputFileName` (jamais provider producteur), `mustRunAfter(convertDocumentToEpub)` ; **piège S-232 #3 confirmé** : `@InputFile` valide avant l'action → `epubFile` = `@Input @Optional Property<String>` (miroir `htmlFilePath`). 4 FT GREEN (STRICT valide sur EPUB fraîchement converti). US-3 (BDD) : `epub_check.feature` 4/4 PASSED + runner 17e `epubCheckCucumberTest` + steps préfixe « epub-check ». Gates : compileKotlin VERT, validatePlugins VERT, DocumentPluginFunctionalTest VERT, secrets RAS. Publication 0.0.15 requise pour distribuer (accord pilote). | 10/10
| 239 | 2026-08-31 | maintenance + external proof | **Hygiene + consommation externe 0.0.14 PROUVÉE + audit BACKLOG** — US-1 : `git rm` 20 fichiers `.bak`/`*.backup*` racine (13 build.gradle.kts.backup*, 2 settings.gradle.kts.bak, BACKLOG/INDEX/SESSIONS_HISTORY .bak — tous historisés git, zéro référence code). US-2 : projet tiers `/tmp/opencode/doc-consumer-014` (Gradle 9.7, JDK 25) consomme `education.cccp.document:0.0.14` depuis Central — 4 gardes VALID (includeGuard STRICT / xref LENIENT / htmlLint STRICT / safeMode SERVER → `document-validation-report.json`), knob `-Pdocument.outputFileName=gone-custom` vivant de bout en bout (conversion → gardes → collect : `gone-custom.html` produit, `composite-context.json` indexe l'artefact custom, canonique absent, `metadata.validationStatus=PASS`), preuve négative include traversal STRICT → BUILD FAILED « include escapes allowed root ». Constat : enums DSL gardes exigent imports explicites côté consommateur. US-3 : Option E CADUQUE. Audit BACKLOG complet : marqueurs sync corrigés (US-DOC-01/02 ✅ S-003/S-004, US-DOC-04 ✅ S-005 sauf epubcheck gap inline, DOC-TRANSLATE-5 ✅ S-024 vérifié bakery 0.0.12, DOC-PLANTUML-VALIDATE-1..5 ✅ S-038/039/040) — **seul gap fonctionnel restant : critère epubcheck US-DOC-04 (P2, EPIC DOC-EPUBCHECK à cadrer S-240)**. Zéro code prod modifié — baseline FT 122 / UT 3037 / cucumberTest 2320 inchangée. | 10/10
| 237 | 2026-08-31 | PUB | **Option C — republication 0.0.14 Maven Central ✅** — accord pilote explicite session. Audit prérequis nmcp : settings (nmcp.settings 1.5.0, AUTOMATIC, creds `~/.gradle/gradle.properties`), POM `withType<MavenPublication>` (name/description Apache-2.0/scm), `repositories.mavenCentral()`, deps `education.cccp` toutes releases Central (planner 0.0.1 compileOnly, plantuml 0.0.4, i18n/opencode-session/pipeline contracts 0.0.2, BOM 0.0.26), gems `org.asciidoctor` Central. Bump `version = "0.0.14"` (1 ligne, zéro code production). Gates : `compileKotlin` VERT, `publishToMavenLocal` VERT (jar/pom/module/sources/javadoc + signatures .asc, license + scm vérifiés), `publishAggregationToCentralPortal --no-daemon` BUILD SUCCESSFUL 27s (deployment accepted AUTOMATIC), propagation Central vérifiée par polling `maven-metadata.xml` (~7 min, 8 requêtes) : `<latest>0.0.14</latest>`, POM+JAR HTTP 200, POM porte `asciidoctorj-pdf:2.3.23` + `asciidoctorj-epub3:2.2.0` — les gems S-233 sont désormais consommables par les usages externes du plugin. Contenu cumulé 0.0.13+0.0.14 : 4 gardes composite + metadata `validationStatus` (S-231) + 4e garde HTML (S-232) + ENV gems (S-233) + fix FT translation (S-234) + knob `outputFileName` conversions/gardes (S-235) + collect N3 knob-aware (S-236). Secrets RAS. **Prochaine session 238 = option D** (moteur Cucumber global, 82 failures glue scoping — actée par le pilote). | 10/10
| 236 | 2026-08-31 | feat + TDD/BDD | **Follow-up S-235 : collect N3 knob-aware** — `DocumentArtifactCollector.collect(sourceAdoc, outputFileName = "document")` scanne désormais le nom réel des artifacts (le knob `outputFileName` est vivant depuis S-235 ; les artifacts custom étaient invisibles pour le snapshot N3). US-1 (TDD strict) : 3 UT knob-aware RED (`No parameter with name 'outputFileName'`) → GREEN 17/17 UT collector, les 14 canoniques préexistantes passent sans modification (défaut `document` = backward-compat totale, constantes mortes purgées). US-2 (FT TestKit) : `CollectDocumentRetrieveTask.outputFileName` (@Input @Optional, `getOrElse("document")`) + câblage `DocumentPlugin.registerCollectDocumentRetrieve` même-knob que les conversions (zéro dépendance de tâche, piège S-232 respecté) — RED (composite sans `custom.html`) → GREEN 2/2 `CollectDocumentRetrieveOutputFileNameFunctionalTest` + non-régression `DocumentPluginFunctionalTest` 65/65. US-3 (BDD) : `n3_collect_custom.feature` 2 scénarios `@n3-collect-custom` + 16e runner dédié `N3CollectCustomCucumberRunner` (glue `document.n3collect`, steps préfixe `n3-collect` anti-glue S-223) + `CucumberTaskSpec("n3CollectCustomCucumberTest")` — 2/2 PASSED. Gates : `compileKotlin` VERT, 16/16 runners Cucumber dédiés SUCCESS (8m47s, 0 failure / 0 error XML), 15e runner `n3PipelineCucumberTest` VERT. Option C (republication 0.0.14) et option D (moteur Cucumber global) restent ouvertes. | 10/10
| 232 | 2026-08-31 | feat + TDD/BDD | **EPIC DOC-VALIDATE-HTML-LINT ✅ TERMINÉ (3/3 US)** — 4e garde composite : lint HTML du rendu (`HtmlLinkLinter`, S-226) rejoint `validateDocument` (include guard CR4 + xref CR-xref + security CR5). US-1 (TDD strict) : `DocumentValidator.validate(..., htmlLintMode=OFF, html=null)` + `HtmlLintReportEntry` (VALID/DEAD, deadLinks nullable NON_NULL) + `DocumentValidationReport.htmlLint` (JSON backward-compat, bloc absent en OFF) + `overallStatus()` FAIL si DEAD + `fromJson` backward-compat + règle HTML absent = DEAD `<html-file-missing>` (visibilité sur silence), 10 UT `HtmlLintGuardTest` RED→GREEN. US-2 (FT) : `ValidateDocumentTask.htmlLinkLint` + `htmlFilePath` (@Input @Optional String, STRICT/LENIENT/OFF symétrique xref) + câblage plugin (même knob `converter { htmlLinkLint }`, chemin statique, `mustRunAfter("convertDocumentToHtml")`), 4 FT GREEN, 3 pièges Gradle résolus (dépendance implicite provider outputFile / `.get()` configuration-time / `@InputFile` valide l'existence). US-3 (BDD) : 3 scénarios `@document-validation-html-lint` (runner dédié, steps anti-glue S-223, STRICT_HTML/LENIENT_HTML safeMode SERVER) — 8/8 PASSED. Gates : `test` 2671 / 11 failures ENV gems Ruby préexistantes, 10 runners Cucumber dédiés VERTS, domaine validation 37 UT / 0 fail, zéro secret. | 10/10
| 231 | 2026-08-31 | feat + TDD/BDD | **EPIC DOC-METADATA-VALIDATION ✅ TERMINÉ (3/3 US)** + hygiène S-227 — statut de validation composite exposé dans metadata.json N3. Hygiène : purge 2 `println` debug (`APPLYING DOCUMENT PLUGIN`, `REGISTERING lintHtmlDocument TASK`) + déduplication du bloc conventions trippliqué de `DocumentPlugin.apply()` (WIP S-227) + doublon mirror `htmlLinkLint` supprimé. US-1 (TDD strict RED→GREEN) : `DocumentValidationReport.overallStatus()` (FAIL si includeGuard INVALID OU xref MISSING OU security REJECT ; PASS sinon — WARN = visibilité LENIENT) + `fromJson()` round-trip, 7 UT `DocumentValidationReportStatusTest` GREEN, dep `jackson-module-kotlin` ajoutée (version contrainte par workspace-bom 0.0.26 → 2.21.2). US-2 (FT TestKit) : `DocumentMetadata.validationStatus` (nullable NON_NULL, backward-compat) + `CollectDocumentRetrieveTask.validationReportPath` (`@Input @Optional`, lecture seule de `document-validation-report.json` s'il existe, dégradé silencieux JSON invalide → null, Loi Économie d'Encre : pas de re-validation) + câblage `DocumentPlugin`, 5 UT `DocumentMetadataTest` + 3 FT GREEN (portage PASS / FAIL / absent omis). US-3 (BDD) : `metadata_validation.feature` 3 scénarios `@metadata-validation` (PASS strict / FAIL strict include traversal / OFF sans rapport → omis) + `MetadataValidationCucumberRunner` (8e runner dédié, pattern S-082) + `MetadataValidationSteps` (préfixe « metadata-validation », anti-glue S-223) + `CucumberTaskSpec("metadataValidationCucumberTest")`. Scénarios STRICT en `safeMode SERVER` (cohérence DOC-CR5 UNSAFE+STRICT rejeté, pattern STRICT_SERVER S-225). Gates : `compileKotlin` VERT, `test` 2622 tests / 11 failures = ENV préexistantes gems Ruby `asciidoctor-pdf`/`asciidoctor-epub3` (hors périmètre, S-213/S-215/S-224), 10 runners Cucumber dédiés VERTS, zéro secret. | 10/10
| 226 | 2026-08-30 | feat + TDD/BDD | **EPIC DOC-HTML-LINT ✅ TERMINÉ (3/3 US)** — Lint navigabilité du HTML *rendu* (`convertDocumentToHtml`), symétrique à DOC-XREF-VALIDATE (S-224) sur la source AsciiDoc. DDD `document.validation` : `HtmlLinkLintMode` (OFF/STRICT/LENIENT), `HtmlLinkLintResult` sealed (Valid/Invalid), `HtmlLinkLinter` (regex extractAnchors/extractInternalLinks/hasTableOfContents/validate — `id=`/`name=` ancres vs `href="#..."` liens, ignore `aria-id`/`href` externe/`#` vide), `HtmlLinkLintReport` JSON. Baby-step DDD/TDD/BDD strict : 12 UT `HtmlLinkLinterTest` (RED→GREEN, regex lookbehind corrigé `aria-id`) + 2 FT `HtmlLinkLintFunctionalTest` (lit le vrai `office/metiers/FPA/.../livre-navigable/book.html` 418 KB → Valid + TOC présent ; variante `#missing` injecté → Invalid) + 5 scénarios Cucumber `@html-link-lint` (runner `HtmlLinkLintCucumberRunner` + steps `HtmlLinkLintSteps` préfixe « htmllint », tâche `htmlLinkLintCucumberTest`). Tous GREEN. **Finding réel** : le livre FPA `book.html` réel est navigable (zéro lien mort, `id="toc"` présent) — verrouille le produit « livre FPA SERVER ». 8e runner Cucumber dédié. Zéro nouveau knob DSL/CLI (mode appliqué en BDD, câblage `lintHtmlDocument` tâche différé S-227 si besoin). ENV : PDF/EPUB gems absents (hors périmètre HTML-lint, 100% Kotlin/Regex). | 10/10
| 224 | 2026-08-30 | feat + TDD/BDD | **EPIC DOC-XREF-VALIDATE ✅ TERMINÉ (4/4 US)** — Validation références croisées AsciiDoc (`<<id>>`/`xref:id[]`). DDD `document.xref` (`XrefValidationMode` OFF/STRICT/LENIENT, `XrefValidationResult` sealed, `XrefValidator` extractAnchors/extractReferences/validate, `XrefValidationReport` JSON). Câblage `ConvertDocumentTask` (STRICT fail-fast / LENIENT warn+report / OFF skip) + DSL `converter { xrefValidation = ... }` (mirror flat + CLI `-Pdocument.xrefValidation`) + tâche dédiée `validateDocumentXref` (rapport `build/docs/document/xref-validation-report.json`). Baby-step DDD/TDD/BDD : 12 UT `XrefValidatorTest` + 4 UT `XrefValidationReportTest` + 7 FT (`ConvertDocumentTaskXrefValidationFunctionalTest` 4 + `ValidateDocumentXrefTaskFunctionalTest` 3) + 5 scénarios Cucumber `@xref-validation` (runner `XrefValidationCucumberRunner` + steps `XrefValidationSteps` + tâche `xrefValidationCucumberTest`). Tous GREEN. 6 runners Cucumber dédiés GREEN. ENV : `DocumentConverterTest` PDF/EPUB + `FpaBookArtifactGenerationTest` échouent sur gems `asciidoctor-pdf`/`asciidoctor-epub3` absents (préexistant S-213, hors périmètre xref). | 10/10
| 225 | 2026-08-30 | feat + TDD/BDD | **EPIC DOC-VALIDATE-COMPOSITE ✅ TERMINÉ (3/3 US)** — Validation composite pré-vol `validateDocument` orchestrant include guard (CR4) + xref (CR-xref) + security policy advice (CR5) en un rapport JSON consolidé (`build/docs/document/document-validation-report.json`). DDD `document.validation` (`DocumentValidator` orchestrateur pur composant `IncludePathValidator`+`XrefValidator`+`DocumentSecurityPolicy` → `DocumentValidationReport` sérialisable ; `includeGuard` OFF/VALID/INVALID, `xref` null/VALID/MISSING, `security` VALID/WARN/REJECT). Tâche `validateDocument` (groupe `document`) lit `converter`/`flat`/CLI, fail-fast STRICT (include INVALID / xref MISSING / security REJECT) / warn LENIENT / skip OFF. Baby-step DDD/TDD/BDD : 8 UT `DocumentValidatorTest` + 4 FT `ValidateDocumentTaskFunctionalTest` (STRICT reject+report / STRICT xref / LENIENT warn / OFF skip) + 5 scénarios Cucumber `@document-validation` (runner `DocumentValidationCucumberRunner` + steps `DocumentValidationSteps` + tâche `documentValidationCucumberTest`). Tous GREEN. 7 runners Cucumber dédiés GREEN. Zéro nouveau knob DSL/CLI (modes `converter` existants). Non-régression `:test` : seuls `DocumentConverterTest` PDF/EPUB + `FpaBookArtifactGenerationTest` échouent sur gems absents (ENV préexistant, hors périmètre). **Suite** : S-226 (dogfooding livre FPA SERVER, ou DOC-TABLE/PLANTUML hardening, ou EPIC produit suivant). | 10/10
| 221 | 2026-08-29 | feat + TDD/BDD | **EPIC DOC-CR4 ✅ TERMINÉ (4/4 US)** — Include Path Guard : audit pré-vol `include::` filesystem (STRICT/LENIENT/OFF) dans `ConvertDocumentTask`. DDD `document.security` (IncludeGuardMode, IncludeDirective, IncludeValidationResult, IncludePathValidator, DocumentIncludeGuard). DSL `converter { includeGuard }` + flat mirror + CLI `-Pdocument.includeGuard`. Baby-step DDD/TDD/BDD : 12 UT `IncludePathValidatorTest` + 5 FT `ConvertDocumentTaskIncludeGuardFunctionalTest` (STRICT reject / LENIENT convert / OFF default / CLI override) TOUS GREEN. Artifacts Cucumber `@converter-include-guard` ajoutés (`converter_include_guard.feature`, `IncludeGuardCucumberRunner`, `IncludeGuardSteps`, tâche `includeGuardCucumberTest`). Bug préexistant corrigé : `registerBookPipeline` overridait inconditionnellement `convertDocumentToHtml.sourceFile` sur le livre assemblé → conversion standalone cassée ; fix via `taskGraph.whenReady` (override seulement si `bookPipeline` dans le graphe). Note : infra Cucumber borough (conventions plugin duplicate classpath) affecte aussi S-220 — non bloquant DOC-CR4. **Diagnostic corrigé S-223** : le doublon réel était une step définition dupliquée dans les tests document-gradle, pas le plugin conventions. | 10/10
| 222 | 2026-08-30 | feat + TDD/BDD | **EPIC DOC-CR5 ✅ TERMINÉ (3/3 US)** — Conversion Security Policy : cohérence SafeMode × includeGuard. DDD `document.security` (SecurityAdvice sealed Valid/Warn/Reject, DocumentSecurityPolicy.advise). Intégration `ConvertDocumentTask.convert()` (STRICT reject → GradleException, LENIENT warn, OFF skip). Baby-step DDD/TDD/BDD : 12 UT `DocumentSecurityPolicyTest` + 3 FT `ConvertDocumentTaskSecurityPolicyFunctionalTest` (STRICT+UNSAFE reject / LENIENT+UNSAFE warn / SERVER+STRICT valid) TOUS GREEN + 3 scénarios Cucumber `@converter-security-policy` (runner dédié `SecurityPolicyCucumberRunner` + `SecurityPolicySteps` + tâche `securityPolicyCucumberTest`). Compose safeMode (CR3-2) + includeGuard (CR4), zéro nouveau knob. Note : infra Cucumber borough (conventions duplicate classpath) bruit le moteur global — runner dédié GREEN, non bloquant DOC-CR5. **Diagnostic corrigé S-223** : le bruit venait d'un doublon de step définition dans les tests document-gradle, résolu en S-223. | 10/10
| 223 | 2026-08-30 | fix + TDD/BDD | **Infra Cucumber borough ✅ résolue** — Diagnostic corrigé : le `DuplicateStepDefinitionException` du moteur global n'était PAS dû au plugin conventions (classpath doublonnait seulement le `jar` projet, redondant, inoffensif). Cause réelle : step `a document gradle project with no converter configuration` définie dans DEUX classes (`IncludeGuardSteps.kt` + `ConverterSafeModeSteps.kt`) ; moteur global sans `glue` restreint → exception. Fix (commit `f67eb8e`) : textes de step distincts (`no include guard configuration` / `no safe mode configuration`) + features mises à jour. Vérifié : `securityPolicyCucumberTest`/`includeGuardCucumberTest`/`converterSafeModeCucumberTest` → BUILD SUCCESSFUL. Hygiene conventions (commit `1c13b7e` conventions-gradle) : `CucumberConventionsPlugin` + `build.gradle.kts` utilisent `testRuntimeClasspath + test.output + main.output` (jar redondant supprimé) + test fonctionnel régression `CP_JAR_PRESENT=false`. | 10/10
| 218 | 2026-08-29 | gouvernance | **Clôture de session** — Résolution du problème de version Java (Java 25) en installant le JDK 25 et en configurant le projet pour l'utiliser, puis exécution des tests pour vérifier que le build fonctionne. Cependant, les tests ont pris trop de temps et ont été interrompus. La session est clôturée sans avancement sur les user stories. | 5/10
| 217 | 2026-08-28 | impl + BDD | **Rapprochement FPA-BOOK-4 (consumer office/metiers/FPA)**. `bookPipeline` câblé sur le livre *assemblé* : `convertDocumentToHtml/Pdf/Epub.sourceFile` → `assembleBook.outputFile` (DOC-BOOK-DOMAIN-3). `BookLayout.tableOfContents` rend désormais `:toc: macro` + `toc::[]` (vrai TOC `id="toc"` dans le HTML). `build.gradle.kts` forward `-Dfpa.book.publish` au JVM de test. Nouveaux tests : `BookPipelineFpaFunctionalTest` (bookPipeline réel FPA 3 pages, 5 formats, GREEN) + `FpaBookArtifactGenerationTest` (`@Tag("integration")`, publie `book.{adoc,html,pdf,epub}` navigables dans `office/metiers/FPA/.../livre-navigable/`). Gates : 91 UT book + bookTree/bookValidation cucumber + FT book GREEN, 0 régression. Commit/push autorisé. Limite : livre complet FPA (200 p.) bloqué par l'absence d'un TOC FPA scan-aligné complet (root `toc.adoc` n'a que 3 pages alignées). | 10/10
| 218 | 2026-08-28 | impl + TDD | **FPA-BOOK-6 (consumer office/metiers/FPA, S-081 FPA)** — correction rigoureuse de la mise en page AsciiDoc via TDD Gradle/Kotlin (refus Python). `BookAssembler.fpaAwareResolver` + nouveau `FpaPageResolver` (résout `BookSection.page` → `NNN.adoc` + splits `NNN_N.adoc`, convention réelle scans FPA, prioritaire sur legacy `%03d-*.adoc`), `BookOcrFailureDetector` + `BookOcrIssue` + `OcrFailureReason` (localisation pages `[ILLISIBLE]`/vides avec section TOC ref+titre), `BookOcrIssueReport` (`book-ocr-issues.json`), `AssembleBookTask` câblé (émissions rapport OCR). TDD RED→GREEN : `FpaBookAssemblyTest` (UT) + `AssembleBookTaskFpaRichTocTest` (FT dogfooding corpus FPA complet 203 pages + TOC riche 116 sections) PASSENT — livre structuré navigable reconstruit + 16 échecs OCR localisés. EPIC DOC-BOOK-DOMAIN reste ✅ TERMINÉ (6/6 US) ; ajouts = extensions câblage consommateur tracées FPA-BOOK-6. Commit/push autorisé. | 10/10
| 216 | 2026-08-28 | impl + BDD | **EPIC DOC-BOOK-VALIDATE CLÔTURÉ 5/5 US**. VALIDATE-4 Cucumber BDD `@book-validation` (5 scénarios `@valid`/`@missing-page`/`@empty-section`/`@toc-mismatch`/`@strict`, `BookValidationCucumberRunner` + `BookValidationSteps` préfixe « book-validation », 5/5 PASSED) + extraction `BookValidator.enforce(mode, reasons)` (pure, throw GradleException STRICT) du `AssembleBookTask.validateIfConfigured` (LENIENT/STRICT préservé). Pattern S-215 book-tree respecté (domaine pur, zéro I/O). `bookValidationCucumberTest` enregistré CucumberTaskSpec. Baby-step BDD strict GREEN 5/5, non-régression book domaine+tree+FT VERT. | 10/10
| 215 | 2026-08-28 | impl + BDD | **EPIC DOC-BOOK-DOMAIN CLÔTURÉ 6/6 US**. DOMAIN-5 Cucumber BDD `@book-tree` : `book_tree.feature` 6 scénarios + `BookTreeCucumberRunner` + `BookTreeSteps` (préfixe « book-tree », pure BDD domaine) + `CucumberTaskSpec("bookTreeCucumberTest")`. 6/6 PASSED. Dogfooding FPA `AssembleBookTaskFpaTest` sur corpus réel `office/metiers/FPA` (toc.adoc + scans 000/009/010, pages copiées, self-skip si corpus absent) PASSED — book structuré navigable. DOMAIN-6 `BookValidator.validateStructure` structurel (S1 ref continuity / S2 unicité (ref,page) / S3 matter FRONT+BACK / S4 page monotonicity, 8 UT TDD strict RED→GREEN) + intégration `AssembleBookTask.validateIfConfigured` (merge structure `structure — ` + validation fichier, LENIENT warn / STRICT GradleException) + 4 FT `AssembleBookTaskFunctionalTest`. Gates : 91 UT book + 6 suites cucumber + FT book GREEN. `DocumentPluginFunctionalTest` 62 dont 1 flaky env (convertDocumentToPdf images, PASSED isolé — pression mémoire JRuby). Nettoyage S-214 (wrapper 9.6.1 restauré jar+properties, .bak supprimés, .gitignore durci + .idea/.gradle/.kotlin/build). Zéro secret, BOM inchangé, commit/push. | 10/10
| 214 | 2026-08-28 | échec | Session interrompue — tentative d'extraire `AssembleBookTask` pour testabilité unitaire + convention mapping (DocumentPlugin), reverted (tests cassés). Livré : `AssembleBookTaskUnitTest.kt` (fix S-214, conservé). État sale nettoyé en ouverture S-215 (wrapper 9.7.1→9.6.1, `.bak` supprimés). | 5/10
| 213 | 2026-08-28 | feat + TDD | DOC-BOOK-DOMAIN-3 (BookAssembler structuré, 10 UT) + DOC-BOOK-DOMAIN-4 (BookNumbering, 10 UT) TDD strict | 10/10
| 211 | 2026-08-27 | cadrage | **EPIC DOC-BOOK-DOMAIN — Domaine « Book » structuré (arbre/mise en page/logique)**. Audit 8 constats (assembleBook blob plat, BookSection plat, hiérarchie ref TOC ignorée, pas de Matter, pas de navigation, BookValidator non-structurel, FPA-BOOK-4 bloqué, refactor possible). 10 décisions archi + modèle DDD `BookTree`/`BookNode`/`Matter`/`BookLayout`/`BookNumbering`. 6 US ~27 pts découpées baby-step DDD/TDD/BDD. Consommateur `office/metiers/FPA`. Créé `CADRAGE_EPIC_DOC_BOOK_DOMAIN.adoc`. BACKLOG (Vision + EPIC) + INDEX (roadmap, CROSS-DOC-BOOK ✅ TERMINÉ) + AGENT (mission 4) + PROMPT_REPRISE mis à jour. Zéro code modifié — gouvernance pure pattern S-057/S-063. | 10/10
| 044 | 2026-08-26 | gouvernance | **Cadrage EPIC CROSS-DOC-BOOK** — Création du cadrage (3 US ~5 pts) + mise à jour BACKLOG/INDEX cross-borough + implémentation US-1 (BDD 4 scénarios) + US-2 (KDoc clarification CollectOcrTask.outputDir). Tous les scénarios Cucumber passent, validation du contrat filesystem %03d-%s.adoc entre codex-gradle et document-gradle. Aucun code de production modifié — pure gouvernance + tests d'acceptation. | 8/10
| 043 | 2026-08-19 | gouvernance | **Sync gouvernance post-S207** — Rattrapage retard INDEX/PROMPT_REPRISE/SESSIONS_HISTORY (5 sessions de retard). S207 déjà commitée (`9af5ed5`) : EPIC DOC-FRONTMATTER-RETRANSLATE (FrontmatterStaleDetector + retranslateFrontmatter + RetranslateFrontmatterTask + BDD 6 scénarios + wiring DocumentPlugin), bump document-plugin 0.0.2→0.0.3 Central (bundle `1c27a917`). Aucun code modifié S043 — pure gouvernance. Tous EPICs document clos (DOC-TABLE-VALIDATE, DOC-PLANTUML-VALIDATE, DOC-TRANSLATE, DOC-FRONTMATTER-RETRANSLATE). Reste DOC-CR3 (revue code, 3 items P1/P2). | 7/10
| 042 | 2026-08-04 | DDD + TDD | **Support EPIC BKY-I18N-MASS (cross-borough)** — BlockChecksum (BlockTranslationStatus TRANSLATED/PENDING, BlockChecksumEntry parse/serialize, sha256/computeForBlocks) + BlockDelta (compute(previous,current) → modified/preserved, statut explicite Loi Économie d'Encre). DDD pur `document/translation/delta/`. 26 UT BlockDeltaTest. `DocumentTranslator.translateArticleWithDelta` (internal→public, preserve blocs indexés, shift table/plantuml, isEmpty→retourne previous) + `ContentTranslationService.translateSingleFileWithBlockDelta` (public, fresh/delta path). 4 UT DocumentTranslatorBlockDeltaTest + 1 UT BlockDeltaWireDebugTest. compileKotlin + publishToMavenLocal OK. | 8/10
| 040 | 2026-08-03 | feat | DOC-PLANTUML-VALIDATE-5 — Cucumber BDD 5 scénarios @plantuml-validation (valid, corrupted-tags, broken-label, preserve-technical, strict-mode). PlantUmlValidationSteps + PlantUmlValidationCucumberRunner. 5/5 PASS. EPIC DOC-PLANTUML-VALIDATE CLÔTURÉ (5/5 US). | 9/10
| 039 | 2026-08-03 | feat | DOC-PLANTUML-VALIDATE-3 + VALIDATE-4 — intégration PlantUmlTranslationAdapter.translate() + mode strict/lenient/off DSL. PlantUmlSyntaxValidator refacto object→interface DDD. PlantUmlValidationConfig + PlantUmlValidationReport. 24 UT GREEN. | 9/10
| 038 | 2026-08-03 | feat | DOC-PLANTUML-VALIDATE-1 + VALIDATE-2 — dépendance plantuml-plugin (implementation) + PlantUmlSyntaxValidator wrapper DDD + PlantUmlValidationResult sealed. 10 UT PlantUmlSyntaxValidator + 1 UT PlantumlServiceAvailability. 11/11 PASS. | 9/10
| 037 | 2026-08-03 | feat | DOC-TABLE-VALIDATE-5 — Cucumber BDD 6 scénarios @table-validation (valid, corrupted-delimiter, strict, column-mismatch, cols-mismatch, batch-report). TableValidationSteps + TableValidationCucumberRunner + CorruptingTranslationService. 6/6 PASS. EPIC DOC-TABLE-VALIDATE CLÔTURÉ (5/5 US). | 9/10
| 036 | 2026-08-03 | feat | DOC-TABLE-VALIDATE-3 + VALIDATE-4 — strict/lenient/off DSL + batch report JSON. TableValidationConfig + TableValidationReport + TranslationException + DocumentTranslator mode handling + BatchDocumentTranslator aggregation. 7 UT + 6 UT + 3 FT. 16/16 PASS. | 9/10
| 035 | 2026-08-03 | feat | DOC-TABLE-VALIDATE-1 + VALIDATE-2 — TableSyntaxValidator (cohérence cols, délimiteurs, homogénéité colonnes) + intégration DocumentTranslator. 19 UT + 9 UT. 28/28 PASS. | 9/10
| 034 | 2026-08-03 | feat | DOC-TABLE-VALIDATE fondation — Table POJO DDD (Table/Row/Cell/ColSpec/HAlign/VAlign, extractTranslatable, reinject, toAsciiDoc, toHtml) + AsciiDocParser.parseTableStructured (\|, a|, h|, spans, alignments, col specs) + 19 UT TableTest + 19 UT AsciiDocParserTableTest + 8 Cucumber table_translation.feature. 46/46 PASS, zéro régression. | 9/10
| 033 | 2026-08-02 | cadrage | DOC-TABLE-VALIDATE + DOC-PLANTUML-VALIDATE — 2 EPICs transverses de validation syntaxique post-traduction (~46 pts, 10 US). TABLE-VALIDATE : TableSyntaxValidator (cohérence cols, délimiteurs, homogénéité colonnes), modes STRICT/LENIENT/OFF, rapport batch JSON. PLANTUML-VALIDATE : DRY via PlantumlService.validateSyntax() de plantuml-gradle (HTOWN, SourceStringReader), wrapper DDD PlantUmlSyntaxValidator, dépendance plantuml-plugin déjà versionnée par le BOM. BACKLOG.adoc +217 lignes, INDEX.adoc +2 EPICs roadmap. | 9/10
| 032 | 2026-08-01 | feat + fix | Dogfooding translateDocumentBatch — 61 articles cheroliv.com FR→EN via Ollama réel (gemma4:31b-cloud, round-robin ports 11440/11441/11442). 2 fixes parser AsciiDoc découverts en batch (fake mode = diagnostic) : (1) DescriptionList `term:: def` + BlockMacro `image::`/`video::` fusionnés en paragraphe → PivotModel.DescriptionList/BlockMacro + parser + 3 renderers + TreeTranslationAdapter + DocumentTranslator, (2) blocs `[plantuml]` délimités `....` non reconnus + titres de bloc `.titre` avalés → parseSourceBlock firstBlockDelimiterAfterHeader + isBlockTitleLine (0084 533→70 lignes, 0082 373→134). 61/61 traduits, 0 placeholder, 0 [EN], 0 fuite prompt. | 9/10
| 031 | 2026-07-31 | feat | Tree translation mode — ArticleTranslator interface, TreeTranslationAdapter (whole-article single LLM call via YAML round-trip), PivotYamlParser, NumberedFakeTranslationService, PooledOllamaTranslationAdapter refusal detection (+12 patterns), TranslateDocumentBatchTask treeMode+skipExisting, DocumentPlugin CLI -Pdocument.translateBatchTree, CI JDK 24→25, fix SubtreeI18nDeltaTest import. 244 PASS (186 UT + 29 FT + 29 Cucumber), 0 fail. Commit 82ada3b pushé. | 9/10
| 030 | 2026-07-31 | refactoring | Alliance i18n — Suppression DocumentI18nBridge (code mort, zéro référence). Document utilise déjà TranslationService du BOM. compileKotlin ✅ | 9/10
| 029 | 2026-07-25 | feat + fix | DocKnowledgeBase AsciiDoc câblé dans PooledOllamaTranslationAdapter + 3 articles EN cheroliv.com corrigés (PlantUML blocks, prompt leakage, mots FR) | 9/10
| 000 | 2026-06-01 | bootstrap | DOC-0 — Bootstrap gouvernance + cadrage architecture | ✅ TERMINÉ
| — | 2026-06-08 | review | Architecture boundary review — suppression compileOnly codex, AsciidoctorJ direct | ✅ TERMINÉ
| 001 | 2026-06-14 | cadrage | Pipeline livre Codex→Document — OCR, PlantUML, theme, 5 fichiers gouvernance | ✅ TERMINÉ
| 002 | 2026-06-29 | implementation | DOC-1 Plugin stub — dogfood CNV-6, 18 tests, pattern surcharge planner | ✅ TERMINÉ
| 003 | 2026-06-29 | feat | DOC-2 generateDocument IA — koog+langchain4j, 31 tests, économie d'encre | ✅ TERMINÉ
| 004 | 2026-06-30 | feat | DOC-3 + DOC-4 convertDocumentToHtml + convertDocumentToPdf — AsciidoctorJ, 50 tests | ✅ TERMINÉ
| 005 | 2026-06-30 | feat | DOC-5 convertDocumentToEpub + DocBook + ManPage — AsciidoctorJ, 69 tests, 5 formats complets | ✅ TERMINÉ
| 006 | 2026-06-30 | architecture | Pattern Plugin Indépendant + Racine Consommateur (article #0124), Gradle 9.3.0→9.6.1 | ✅ TERMINÉ
| 007 | 2026-07-01 | feat | DOC-9 enrichDocument — Asciidoctor Diagram inline, passthrough, includes récursifs, 79 tests (+10) | ✅ TERMINÉ
| 008 | 2026-07-01 | feat | DOC-10 Theme DSL — DocumentTheme value object, nested DSL theme { }, 4 propriétés PDF/HTML/EPUB/logo, 103 tests (+24) | ✅ TERMINÉ
| 009 | 2026-07-03 | dev | CNV-6 migration (4 conventions pattern capsule) + DOC-8 i18n-contracts 0.0.2 + opencode-session-contracts 0.0.2 câblés, DocumentI18nBridge créé (supprimé en S030, code mort) | ✅ TERMINÉ
| 010 | 2026-07-03 | feat | DOC-6 N3 runner-gradle integration — DocumentMetadata, DocumentArtifactEntry, DocumentRetrieveResult, DocumentArtifactCollector, CollectDocumentRetrieveTask, 135 tests (+32) | ✅ TERMINÉ
| 011 | 2026-07-04 | feat | DOC-7 CI + Publication Maven Central / Plugin Portal — nmcp settings 1.5.0, BOM 0.0.6, POM name/description, workflow CI test.yml, scénario BDD + functional test POM, 137 tests (+2) | ✅ TERMINÉ
| 012 | 2026-07-04 | feat | DOC-11 Book Pipeline Codex -> Document — BookAssembler, AssembleBookTask, bookPipeline composite, 5 scénarios BDD, 155 tests (+18), baby-step DDD/TDD/BDD | ✅ TERMINÉ
| 013 | 2026-07-04 | feat | DOC-12 DSL unifié `document { }` — DocumentEnrichConfig, DocumentOutputs, DocumentFrontMatter, DocumentPipelineConfig, DocumentConfigSerializer, 3 nested DSL blocks, 180 tests (+25), baby-step DDD/TDD/BDD | ✅ TERMINÉ
| 014 | 2026-07-05 | feat | DOC-8.1 Release Notes Pipeline — pipeline-contracts 0.0.2, ConventionalCommitParser, CliGitLogParser, AsciidocReleaseNotesRenderer, GitReleaseNotesGenerator, ReleaseNotesGenerateTask, DSL releaseNotes { }, 186 tests (+35 unit +4 functional +3 cucumber), baby-step DDD/TDD/BDD | ✅ TERMINÉ
| 015 | 2026-07-05 | feat | DOC-8.2 Template configurable — MarkdownReleaseNotesRenderer, JsonReleaseNotesRenderer, GitReleaseNotesGenerator.configDriven, ReleaseNotesDsl rendererType+categories, CLI override, 257 tests (+31 unit +4 functional +2 cucumber), baby-step DDD/TDD/BDD | ✅ TERMINÉ
| 016 | 2026-07-05 | feat | DOC-8.3 N3 metadata.json + composite-context.json release notes — ReleaseNotesArtifactEntry, DocumentRetrieveResult.releaseNotes/releaseNotesCount, DocumentMetadata.releaseNotesPath/Renderer (NON_NULL), DocumentArtifactCollector.collectReleaseNotes, CollectDocumentRetrieveTask.releaseNotesDirPath (@Input String), 289 tests (+32: 7+7+7+8 unit +4 functional +2 cucumber), baby-step DDD/TDD/BDD | ✅ TERMINÉ
| 017 | 2026-07-06 | feat | DOC-8.4 Extension IA Ollama (résumé humain) — OllamaAsciidocReleaseNotesRenderer décorateur ReleaseNotesRenderer, GitReleaseNotesGenerator.configDrivenWithLlm factory, ReleaseNotesDsl.llmMode + ReleaseNotesGenerateTask.llmMode + DocumentPlugin wiring CLI -Pdocument.releaseNotesLlmMode, fallback gracieux si LLM jette, 300 tests (+11: 7 unit OllamaAsciidoc +4 unit GeneratorWithLlm +1 functional +1 cucumber -2 redécalés), baby-step DDD/TDD/BDD, EPIC DOC-8 4/4 TERMINÉ | ✅ TERMINÉ
| 018 | 2026-07-06 | feat | DOC-12 book { } nested DSL (BookDsl + mirror rétro-compatible) + DOC-9b images BDD (rendu HTML/PDF image:: directives) + DOC-8.5 publication Maven Central (bundle 4d15e522), bug fix conventions-gradle 0.0.2 (SCM double .git PublishingConventionsPlugin), harmonisation 15 boroughs vers 0.0.2, 310 tests (+4 unit BookDsl +2 unit DocumentEnricher image +4 functional +4 cucumber), baby-step DDD/TDD/BDD | ✅ TERMINÉ
| 019 | 2026-07-06 | feat | DOC-9b EPUB image rendering (image:: embedded as zip entry) + DOC-12 book serialization in document-config.json (BookConfig value object + DocumentConfigSerializer + SerializeDocumentConfigTask + DocumentPlugin wiring) + US-DOC-06/07 DocBook + ManPage advanced rendering (tableaux/programlisting/itemizedlist/orderedlist + bold troff options/sub-sections), 323 tests (+13: +1 unit EPUB +2 unit book serializer +5 unit DocBook/ManPage advanced +1 functional EPUB +2 functional book serializer +2 functional DocBook/ManPage advanced +1 cucumber EPUB +1 cucumber book serializer +2 cucumber DocBook/ManPage advanced), baby-step DDD/TDD/BDD | ✅ TERMINÉ
| 020 | 2026-07-06 | maintenance | EPUB advanced rendering (tableaux/code/listes en XHTML via AsciidoctorJ epub3 backend) + DOC-12 round-trip deserialize (DocumentConfigSerializer.deserialize method, idempotent byte-identical JSON) + régression cross-boroughs (bookPipeline produit composite-context.json + metadata.json valides pour runner-gradle N3), 348 tests (+25: +3 unit EPUB +6 unit round-trip +3 functional EPUB +1 functional idempotent +1 functional bookPipeline N3 +2 cucumber epub-advanced +1 cucumber round-trip +1 cucumber cross-borough), baby-step DDD/TDD/BDD | ✅ TERMINÉ
| 021 | 2026-07-20 | fix + feat | Fix compile (jacksonObjectMapper → ObjectMapper, 3 fichiers) + DOC-TRANSLATE US-1 (PivotModel + AsciiDocParser + TextTranslatableClassifier, 31 UT) + US-2 (PivotYamlRenderer + AsciiDocRenderer + JbakeNativeRenderer, 22 UT), 53 UT translation GREEN, baby-step DDD/TDD/BDD | ✅ TERMINÉ
| 022 | 2026-07-20 | feat | DOC-TRANSLATE US-3 (ContentTranslationService + PlantUmlTranslationAdapter + PlantUmlClassifier, 39 UT), 92 UT translation GREEN, baby-step DDD/TDD/BDD | ✅ TERMINÉ
| 023 | 2026-07-20 | feat | DOC-TRANSLATE US-4 (I18nDeltaApplier + ContentChecksum + SubtreeI18nPlanner + SiteTranslationPlan + tree types, 36 UT), 128 UT translation GREEN, baby-step DDD/TDD/BDD | ✅ TERMINÉ
| 024 | 2026-07-20 | feat | DOC-TRANSLATE US-5 (Câblage bakery consommateur, clôture EPIC, 8 pts), 1480 tests bakery 0 régression, baby-step DDD/TDD/BDD | ✅ TERMINÉ
| 025 | 2026-07-20 | feat + fix | DOC-13 Templates + DOC-14 Batch + DOC-15 Hardening + Fix Duplications bakery (I18nDeltaApplier, SubtreeI18nPlanner, I18nDelta), 15 UT + 6 FT + 5 BDD, baby-step DDD/TDD/BDD | ✅ TERMINÉ
| 026 | 2026-07-20 | feat | DOC-TRANSLATE câblage — translateDocument task + DSL translation { } + OllamaTranslationAdapter + FakeTranslationService, 128 UT + 3 FT + 3 BDD, baby-step DDD/TDD/BDD | ✅ TERMINÉ
| 027 | 2026-07-21 | refacto | DocumentTranslator domain service — extraction DDD pure (sans I/O), ContentTranslationService + TranslateDocumentTask déléguent, 14 UT DocumentTranslatorTest, bakery 0 régression, baby-step DDD/TDD/BDD | ✅ TERMINÉ
| 028 | 2026-07-24 | feat + CI | PooledOllamaTranslationAdapter — round-robin rotation 29 ports 11437-11465, fallback. Câblé TranslateDocumentTask + TranslateDocumentBatchTask. N0 OllamaConfig + OllamaDeviceKey. CI workflows office. 7 UT. 149 UT translation GREEN. | ✅ TERMINÉ
|===

---


== Session 009 (2026-07-03) — CNV-6 + DOC-8 i18n/session contracts

**CNV-6** : Migration vers les 4 convention plugins `education.cccp.build.*` v0.0.1 (pattern capsule).
- build.gradle.kts 72→67 lignes (-5), libs.versions.toml 66→49 lignes (-17)
- Suppression alias redondants (kotlin.jvm, publish) + doublons JUnit5
- functionalTest + cucumber conventions appliquées tel quel (variantes standards)

**DOC-8** : Câblage contrats N0.
- i18n-contracts 0.0.2 (implementation) — SupportedLanguage, LanguageCatalog, I18nConfig
- opencode-session-contracts 0.0.2 (implementation) — AgentContext, SessionPrompt, SessionResponse
- DocumentI18nBridge.kt créé (bridge SupportedLanguage → pipeline documentaire)
- pipeline-contracts (MEM-2) TODO — pas implémenté dans workspace-bom

**compileKotlin** : ✅ SUCCESS (avec DocumentI18nBridge)

---

== Session 010 (2026-07-03) — DOC-6 N3 runner-gradle integration

**DOC-6** : Implémentation de `collectDocumentRetrieve` — la tâche Gradle qui produit
les fichiers JSON consommables par runner-gradle N3 (`assembleCompositeContext`).

**Ubiquitous language (DDD)** :
- `DocumentMetadata` — provenance (source="new-orleans", type, sessions, generatedAt, model, version, dependencies)
- `DocumentArtifactEntry` — un artefact produit (path, format, sourceAdoc, exists) + toMap
- `DocumentRetrieveResult` — enveloppe N3 (source, entries, count, timestamp) + writeTo
- `DocumentArtifactCollector` — scan outputDir → entries (logique testable sans Gradle)
- `CollectDocumentRetrieveTask` — task Gradle (orchestre collector + result + metadata)

**Contrat N3 de facto** (pas de contrat N0 formel) :
- `composite-context.json` : `{source, entries, count, timestamp}` — runner lit `entries`
- `metadata.json` : `{source, type, sessions, generatedAt, model, version, dependencies}`

**Méthode baby-step TDD/BDD** : 8 baby-steps Red→Green, pyramide unit→functional→cucumber.

**Nouveaux fichiers** : 5 main + 6 test (DocumentMetadata, DocumentArtifactEntry, DocumentRetrieveResult, DocumentArtifactCollector, CollectDocumentRetrieveTask + tests associés)

**Fichiers modifiés** : DocumentPlugin.kt (wiring remplace stub), DocumentPluginFunctionalTest.kt (+2), document.feature (+2 scénarios @doc6), DocumentSteps.kt (+1 Given +5 Then), DocumentWorld.kt (+1 factory +2 helpers)

**Tests** : 135 verts (90 unit + 23 functional + 22 cucumber), 0 failures (+32 vs session 008)

**Loi de l'Économie d'Encre** : `collectDocumentRetrieve` est READ-only — indexe les artefacts produits, n'invoque jamais le LLM.

---

== Session 012 (2026-07-04) — DOC-11 Book Pipeline Codex -> Document

**DOC-11** : Pipeline livre complet côté Document — assembler les pages
AsciiDoc OCRisées (produites par codex-gradle) en un livre unique,
incruster les photos originales, et exposer une tâche composite
`bookPipeline` reproductible en une commande.

**Boundary respectée** : l'OCR est dans Codex (Brooklyn). Document (New
Orleans) lit l'AsciiDoc sur disque (pont N2 <-> N2), jamais de dépendance
directe vers codex-gradle.

**Ubiquitous language (DDD)** :
- `BookPage` — une page OCRisée (file, order, photo optionnelle)
- `PageOrder` — value class inline, ordre dérivé du préfixe numérique
- `BookAssemblyResult` — résultat d'assemblage (content, pages, photoCount)
- `BookAssembler` — assembleur pages -> livre AsciiDoc (logique testable sans Gradle)
- `AssembleBookTask` — task Gradle (orchestre BookAssembler + writeTo)

**Méthode baby-step TDD/BDD** : 9 baby-steps, pyramide unit -> functional -> cucumber.
- BDD : 5 scénarios `@doc11 @book` en anglais (assembleBook, bookPipeline, assemblage, photos, pipeline complet)
- DDD : 3 value objects (BookPage, PageOrder, BookAssemblyResult)
- TDD : 11 tests unitaires BookAssembler (merge, ordre, photos, immutabilité, filtre)
- Functional : 3 tests (assembleBook merge, assembleBook photos, bookPipeline registration)
- Cucumber : 5 scénarios end-to-end

**Nouveaux fichiers** : 5 main + 1 test (BookPage, PageOrder, BookAssemblyResult, BookAssembler, AssembleBookTask, BookAssemblerTest)

**Fichiers modifiés** : DocumentExtension.kt (+4 propriétés book), DocumentPlugin.kt (registerAssembleBook + registerBookPipeline + conventions), document.feature (5 scénarios DOC-11 + traduction intégrale anglais), DocumentSteps.kt (+3 Given +4 Then), DocumentWorld.kt (+1 factory +1 helper), DocumentPluginFunctionalTest.kt (+3 tests)

**Tests** : 155 verts (100 unit + 27 functional + 28 cucumber), 0 failures (+18 vs session 011)

**Décisions techniques** :
- PageOrder par préfixe numérique (`001-page.adoc` -> 1), non-numérotées après
- Photos optionnelles (png/jpg/jpeg/webp, même baseName que la page)
- bookPipeline = lifecycle task avec `mustRunAfter` pour l'ordre
- Gherkin traduit intégralement en anglais (règle code en anglais)

---

== Session 014 — 2026-07-05 — DOC-8.1 Release Notes Pipeline (git log → AsciiDoc)

*EPIC* : DOC-8 (US-DOC-8.1 + partiellement US-DOC-8.2)
*Statut* : ✅ TERMINÉ
*Tests* : 186 verts (145 unit + 4 functional + 37 cucumber), 0 failures (+35 unit + 4 functional + 3 cucumber vs session 013)

**Objectif** : Câbler le contrat `pipeline-contracts` (MEM-2, workspace-bom N0) et implémenter le pipeline release notes (git log → Conventional Commits → AsciiDoc).

**Méthode baby-step TDD/BDD** : 10 baby-steps, pyramide unit -> functional -> cucumber.
- BDD : 3 scénarios `@doc8 @releasenotes` (registration, produit adoc depuis commits, SNAPSHOT)
- DDD : ubiquitous language (ConventionalCommitParser, CliGitLogParser, AsciidocReleaseNotesRenderer, GitReleaseNotesGenerator, ReleaseNotesDsl, ReleaseNotesGenerateTask)
- TDD : 35 tests unitaires (9 ConventionalCommitParser + 9 CliGitLogParser + 12 AsciidocReleaseNotesRenderer + 5 GitReleaseNotesGenerator)
- Functional : 4 tests (task registered, produit adoc, SNAPSHOT, VERSION file)
- Cucumber : 3 scénarios end-to-end

**Nouveaux fichiers** : 6 main + 5 test + 1 feature
- ConventionalCommitParser (regex Conventional Commits 1.0.0)
- CliGitLogParser (implémente GitLogParser via ProcessBuilder git log)
- AsciidocReleaseNotesRenderer (implémente ReleaseNotesRenderer, template AsciiDoc)
- GitReleaseNotesGenerator (implémente ReleaseNotesGenerator, orchestration)
- ReleaseNotesDsl + ReleaseNotesGenerateTask (task + DSL nested)
- release-notes.feature (3 scénarios BDD)

**Fichiers modifiés** : libs.versions.toml (+pipeline-contracts), build.gradle.kts (+implementation), DocumentExtension.kt (+releaseNotes block), DocumentPlugin.kt (+registerReleaseNotesGenerate + initNested), DocumentWorld.kt (+git repo/commit/tag helpers), DocumentSteps.kt (+6 steps), DocumentI18nBridge.kt (commentaire KDoc)

**Tests** : 186 verts (145 unit + 4 functional + 37 cucumber), 0 failures

**Décisions techniques** :
- `git log` retourne newest-first → `asReversed()` pour ordre chronologique
- `detectFromTag` utilise `--sort=-v:refname` (lightweight tags n'ont pas de creatordate)
- Extension `.adoc` (convention workspace), pas `.asciidoc`
- DSL nested `releaseNotes { }` pattern DOC-12 (classe concrète, val Property, fun action(Action<T>))
- CLI override : -Pdocument.releaseNotesFromTag, -Pdocument.releaseNotesToTag, -Pdocument.releaseNotesVersion, -Pdocument.releaseNotesIncludeDownloads
- @DisableCachingByDefault sur ReleaseNotesGenerateTask (git log non-déterministe)

---

== Session 015 (2026-07-05) — DOC-8.2 Template configurable

**DOC-8.2** : Rendre le renderer de release notes sélectionnable
(asciidoc/markdown/json) et les catégories custom via DSL.

**Ubiquitous language (DDD)** :
- `MarkdownReleaseNotesRenderer` — implémente ReleaseNotesRenderer, format markdown (#, ##)
- `JsonReleaseNotesRenderer` — implémente ReleaseNotesRenderer, format json (schema stable machine-readable)
- `GitReleaseNotesGenerator.configDriven` — factory activant la sélection dynamique par rendererType

**Méthode baby-step TDD/BDD** : 6 baby-steps Red→Green, pyramide unit→functional→cucumber.
1. MarkdownReleaseNotesRenderer (11 tests unit)
2. JsonReleaseNotesRenderer (10 tests unit)
3. GitReleaseNotesGenerator.configDriven + sélection renderer (4 tests unit)
4. ReleaseNotesDsl rendererType + categories (6 tests unit)
5. Functional tests markdown/json/categories/CLI override (4 tests)
6. Cucumber scenarios markdown + custom categories (2 scénarios)

**Nouveaux fichiers** : 2 main + 3 test + 0 feature (extension release-notes.feature)
- MarkdownReleaseNotesRenderer (# Release Notes, ## catégorie, - msg (scope))
- JsonReleaseNotesRenderer ({version, commits[], categories{}, includeDownloads?})
- MarkdownReleaseNotesRendererTest, JsonReleaseNotesRendererTest, ReleaseNotesDslTest

**Fichiers modifiés** :
- GitReleaseNotesGenerator.kt (refactor constructeur privé + configDriven factory + extension fichier par format)
- ReleaseNotesDsl.kt (+rendererType Property, +categories MapProperty)
- ReleaseNotesGenerateTask.kt (+@Input rendererType, +@Input categories, configDriven)
- DocumentPlugin.kt (initNested + conventions + CLI props releaseNotesRendererType/Categories + parseCategoriesCli)
- ReleaseNotesFunctionalTest.kt (+4 tests)
- DocumentWorld.kt (+2 factories markdown/custom categories +releaseNotesMarkdownFile)
- DocumentSteps.kt (+2 Given +2 Then)
- release-notes.feature (+2 scénarios)

**Tests** : 257 verts (183 unit + 35 functional + 39 cucumber), 0 failures

**Décisions techniques** :
- Contrat N0 `ReleaseNotesGenerator.renderer` non-null → refactor constructeur privé + 2 constructeurs publics (compat ascendante) + companion `configDriven()`
- Extension fichier de sortie déduite de `renderer.format` (md/json/adoc)
- Categories custom : fallback config default si MapProperty vide (évite duplication défauts dans DSL)
- CLI override : -Pdocument.releaseNotesRendererType, -Pdocument.releaseNotesCategories (format key=label,key2=label2)
- Parser Conventional Commit rejette types non standards → custom categories ne mappent que sur types reconnus

== Session 212 (2026-08-27) — DOC-BOOK-DOMAIN-1 + DOMAIN-2

**Type** : implémentation DDD/TDD/BDD strict
**Score** : 10/10
**EPIC** : DOC-BOOK-DOMAIN (US-1 + US-2 livrées)

**Livrables** :
- `BookNode` + `BookTree` + `BookTreeBuilder` (US-1, 15 UT)
- `Matter` + `BookLayout` (US-2, 12 UT)
- Non-régression livre existant VERTE

**Suite** : S-213 (DOMAIN-3 refactor Assembler + DOMAIN-4 BookNumbering)

== Session 216 (2026-08-28) — DOC-BOOK-VALIDATE-4 (Cucumber BDD) + clôture EPIC

**Type** : implémentation BDD (baby-step TDD/BDD strict)
**Score** : 10/10
**EPIC** : DOC-BOOK-VALIDATE (US-4 livrée → 5/5 US, EPIC CLÔTURÉ)

**Livrables** :
- `book_validation.feature` 5 scénarios `@book-validation` (`@valid`/`@missing-page`/`@empty-section`/`@toc-mismatch`/`@strict`)
- `BookValidationCucumberRunner` + `BookValidationSteps` (package `document.bookvalidation`, préfixe « book-validation », pure BDD domaine) + `CucumberTaskSpec("bookValidationCucumberTest")`
- `BookValidator.enforce(mode, reasons)` extrait du task (throw GradleException STRICT, no-op LENIENT) ; `AssembleBookTask.validateIfConfigured` délègue
- 5/5 PASSED ; non-régression book-tree 6/6 + UT/FT book GREEN

**Suite** : S-217 (rapprochement FPA-BOOK-4 ou EPIC suivant)

== Session 224 (2026-08-30) — DOC-XREF-VALIDATE (validation références croisées AsciiDoc)

**Type** : implémentation DDD/TDD/BDD strict
**Score** : 10/10
**EPIC** : DOC-XREF-VALIDATE (US-1 + US-2 + US-3 + US-4 livrées → 4/4 US, EPIC CLÔTURÉ)

**Livrables** :
- `document/xref/XrefValidationMode.kt` — NOUVEAU (enum OFF/STRICT/LENIENT).
- `document/xref/XrefValidationResult.kt` — NOUVEAU (sealed Valid/Invalid).
- `document/xref/XrefValidator.kt` — NOUVEAU (extractAnchors/extractReferences/validate, regex sur texte brut).
- `document/xref/XrefValidationReport.kt` — NOUVEAU (entry + liste, toJson/fromResult).
- `ConverterDsl.kt` (+`xrefValidation` Property) / `DocumentExtension.kt` (+abstract Property) / `DocumentPlugin.kt` (convention OFF, mirror, CLI `-Pdocument.xrefValidation`, `registerValidateDocumentXref`) / `ConvertDocumentTask.kt` (validation avant conversion) / `ValidateDocumentXrefTask.kt` (NOUVEAU, rapport JSON).
- `XrefValidatorTest.kt` (12 UT) + `XrefValidationReportTest.kt` (4 UT) — GREEN.
- `ConvertDocumentTaskXrefValidationFunctionalTest.kt` (4 FT) + `ValidateDocumentXrefTaskFunctionalTest.kt` (3 FT) — GREEN.
- `xref_validation.feature` (5 scénarios `@xref-validation`) + `XrefValidationCucumberRunner.kt` + `XrefValidationSteps.kt` + `CucumberTaskSpec("xrefValidationCucumberTest")` — 5/5 PASSED.

**Tests** : 16 UT + 7 FT + 5 BDD, tous GREEN. 6 runners Cucumber dédiés GREEN (xref + includeGuard/securityPolicy/converterSafeMode/bookTree/bookValidation).

**Non-régression** : `compileKotlin` VERT. Les seuls échecs `:test`/`:functionalTest` sont les gems `asciidoctor-pdf`/`asciidoctor-epub3` absents (LoadError, préexistant S-213/S-215), indépendants du xref.

**Suite** : S-225 (DOC-TABLE/PLANTUML hardening, ou dogfooding livre FPA SERVER)

== Session 233 (2026-08-31) — Dogfooding pipeline N3 complet sur le livre FPA (Option A) + fix ENV gems

**Type** : feat + TDD/BDD + fix ENV
**Score** : 10/10
**EPIC** : dogfooding (option A du PROMPT_REPRISE) + option C atteinte au passage

**Livrables** :
- `BookPipelineN3FpaFunctionalTest.kt` — NOUVEAU (FT dogfooding N3 complet, 3 builds staged, corpus FPA réel read-only).
- `document/n3pipeline/N3PipelineCucumberRunner.kt` + `N3PipelineSteps.kt` + `n3_pipeline.feature` — NOUVEAU (BDD `@n3-pipeline` 1/1, 11e `n3PipelineCucumberTest`).
- `build.gradle.kts` — `runtimeOnly asciidoctorj-pdf:2.3.23` + `asciidoctorj-epub3:2.2.0` (fix ENV gems, 16 failures résolues) + CucumberTaskSpec n3.
- `LintHtmlDocumentTask.kt` — annotations restaurées (@get:InputFile/@get:Input) + mustRunAfter(collect) [régression WIP f0dfcef].
- `CollectDocumentRetrieveTask.kt` — snapshot contract (`outputs.upToDateWhen { false }`).
- `DocumentPlugin.kt` — mustRunAfter lintHtmlDocument.
- Purge 2 `.bak`.

**Tests** : +1 FT dogfooding (PASSED) + 1 BDD (1/1 PASSED). Gates : 15/15 runners
Cucumber VERTS (7m33s), `test` VERT (5m33s), FT 116 tests / 5 failures
PRÉEXISTANTES (translateDocument ×2 + TableValidation ×3 — convention
`translation.outputFileName` manquante, hors périmètre).

**Non-régression** : baseline FT mesurée avant/après stash (21 fails → 5 fails,
net -16 grâce au fix ENV gems). Domaine validation inchangé (18 UT PASS au gate test).

**Suite** : S-234 (hygiène FT translateDocument, ou publication 0.0.14, ou option B chemin HTML)

== Session 234 (2026-08-31) — Fix Option A : hygiène FT (5 failures → 0)

**Type** : fix + TDD/BDD
**Score** : 10/10
**EPIC** : hygiène (option A du PROMPT_REPRISE 234)

**Livrables** :
- `DocumentPlugin.kt` — +1 ligne `ext.translation.outputFileName.convention("document")`
  (bloc conventions translation). Fix 1-ligne, valeur alignée sur
  `ext.template.outputFileName` et le chemin statique du 4e garde (S-232).

**Cause racine (confirmée RED)** : le provider chaîné `registerTranslateDocument`
(cliProp → orElse(ext.translation.outputFileName) → map) produit un provider
**unset** quand `outputFileName` n'a ni convention ni CLI
`-Pdocument.translateOutputFileName` → `Value not set` sur `task.outputFile`
à l'exécution. Une Property DSL sans `convention(...)` reste unset même
derrière `.orElse(...)`.

**Tests** : RED = 5 failures reproduites (stack exacte lue dans le report XML).
GREEN = `TableValidationFunctionalTest` 3/3 + `DocumentPluginFunctionalTest`
65/65 + `functionalTest` 68/68 (baseline S-233 : 5 failures) + `test` VERT
(6m59s) + 15/15 runners Cucumber dédiés **144 scénarios PASSED**
(`--rerun-tasks`, 9m21s).

**Preuve BDD comparative** : moteur global `cucumberTest` sans fix (stash)
144 PASSED/86 FAILED → avec fix 148/82. +4 = scénarios `@doc-translate`
translateDocument (registered, FR→EN fake, code blocks). Les 82 failures
globales restantes préexistent (scoping glue, connu S-221/S-223 — la gate
reste les runners dédiés). Méthode stash documentée pour isoler l'impact
d'un fix.

**REFACTOR** : n/a (fix 1 ligne, économie d'encre). Zéro fichier test
modifié/créé — les FT existants retrouvent leur statut VERT.

**Suite** : S-235 (option B — chemin HTML réel du 4e garde, recommandée /
option C — publication 0.0.14 / option D — diagnostic moteur Cucumber global)

== Session 235 — 2026-08-31 — Option B : output HTML réel audité par la 4e garde

**Type** : feat + TDD/BDD — baby-step DDD/TDD/BDD strict.
**Cadrage** : `.agents/CADRAGE_S235_OUTPUT_HTML_REEL.adoc` (6 constats, 7 décisions,
3 US ~8 pts). Knob `outputFileName` MORT confirmé (déclaré+câblé, jamais lu par
`ConvertDocumentTask` ; outputFile hardcodé `document.<ext>`).

**US-1 (TDD)** : `outputFile` conversion dérivé du knob
(`outputFileName.flatMap { buildDir/docs/document/$it.<ext> }`) — défaut
`"document"` → chemins canoniques backward-compat. RED (custom.html absent)
→ GREEN 2/2 FT.

**US-2 (FT)** : `validateDocument.htmlFilePath` + `lintHtmlDocument.htmlFile`
dérivés du MÊME knob (jamais provider outputFile — dépendance implicite S-232 ;
le `htmlTask.get().outputFile` du lint était la même forme illégale).
RED discriminant (`<html-file-missing>` vs fragment `gone-custom`) → GREEN 2/2
+ non-régression HtmlLint 4/4 + DocumentPluginFunctionalTest VERT.
Faux positif d'assertion documenté : choisir des fragments discriminants
(2 findings partagent le mot `missing`).

**US-3 (BDD)** : +2 scénarios `@document-validation-custom-output`
(STRICT audite le vrai custom.html → DEAD liste gone-custom, pas
html-file-missing ; VALID symétrique). Steps anti-glue S-223. **10/10 PASSED**.

**Gates** : 15/15 runners Cucumber dédiés SUCCESSFUL (11m39s, 0 failure XML),
compileKotlin VERT, secrets RAS, dogfooding FPA (chemins défaut) inchangé.

**Gouvernance** : CONTRAINTE GLOBALE *Zéro Python — Écosystème JVM Exclusif*
ajoutée `AGENTS.adoc` racine (Règles Transverses, EAGER) — outputs analyse
exclusivement Read/Grep/xmllint/rapports Gradle natifs.

**Suite** : S-236 — option C (publication 0.0.14, accord pilote requis),
option D (moteur global), suivi collector N3 noms custom.

== Session 238 — 2026-08-31 — Option D : moteur Cucumber global `cucumberTest` VERTE

**Type** : fix + TDD/BDD — baby-step DDD/TDD/BDD strict.
**Baseline auditée** (XML, zéro Python) : 2390 tests / 73 failed / 2165 skipped.
Le runner global (`CucumberTestRunner`, glue `document.scenarios`, tags
`not @wip`) découvrait les 18 features mais ne possède que 3 → 70
`UndefinedStepException` pur bruit + 3 vrais steps manquants (`@template`
DOC-13 de `document.feature` : le Given existait (DocumentSteps 881+),
le Then `the generated document should contain` n'a jamais été écrit ;
`the build should fail` absent aussi).

**US-1 (TDD)** : 2 steps + 1 helper — `generatedDocumentShouldContain`
(scan `build/docs/document/*.adoc`, knob DOC-13 `outputFileName` dérive
le nom, pas de chemin codé en dur) + `buildShouldFail` (`result.output`
contient FAILED, pattern TestKit). RED prouvé par XML de baseline avant
tout code. GREEN : `document.feature` 65/65, failures 73 → 70.

**US-2 (scoping)** : `FEATURES_PROPERTY_NAME` restreint aux 3 features
possédées (`document`, `release-notes`, `frontmatter_retranslate`).
Règle de propriété documentée : une feature = un runner propriétaire ;
nouvelle feature → runner dédié (16e pattern, CucumberTaskSpec) ou
explicitement le global avec steps dans `document.scenarios`.

**Fix bonus validatePlugins** : ROUGE sur main SANS modifications
(proof `git stash` — régression S-232/S-233). 4 problèmes :
`LintHtmlDocumentTask` / `ValidateDocumentTask` /
`ValidateDocumentXrefTask` `@DisableCachingByDefault` (convention
borough, Loi Économie d'Encre — sévérité side-effect-bearing) +
`RetranslateFrontmatterTask.sourceDir` `@PathSensitive(RELATIVE)`.

**Gates** : `cucumberTest` BUILD SUCCESSFUL — 2320 tests / 2165 skipped
(dédiés co-exécutés filtrés par tags — normal) / 0 failure, 1re fois
verte depuis S-221. FT 122/122 (baseline élargie FPA). UT 3037/0.
validatePlugins VERT. Secrets RAS.

**Pièges retenus** : `--tests` Gradle ne réduit PAS la découverte
Cucumber (`cucumber.features` property gagne, warning cucumber
documenté) — filtrer côté annotation du runner ; totaux XML
multi-suites mélangent PASSED (global) et SKIPPED (dédiés) — le
diagnostic se fait par feature propriétaire ; `BuildResult.output` est
déjà un String (pas de `takeSubstring`).

**Suite** : S-239 — hygiene .bak/backup residuels, consommation externe
0.0.14 (projet tiers), option E assembleBook knob si besoin réel.

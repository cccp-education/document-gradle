= COMPLETED_TASKS_ARCHIVE — Août 2026

== Document-gradle (New Orleans 🟢)

=== Session 241 (2026-08-31) — Dogfooding EPUB FPA réel + doc consommateur README + publication 0.0.15

*Type* : dogfooding + docs + PUB (accord pilote)
*Score* : 10/10

*Résumé* :
- FT dogfooding `EpubCheckFpaFunctionalTest` 2/2 (pattern S-226) : le VRAI
  `book.epub` FPA (464 KB) passe W3C epubcheck → produit « livre FPA
  SERVER » EPUB-valide ; preuve négative structurelle (zip sans `mimetype`
  OCF → Invalid). Piège : flip d'octet naïf TOLÉRÉ par epubcheck.
- README.md consommateur créé : quick start + les 5 imports DSL requis +
  exemple 5 gardes + table des tâches + précédence.
- **Publication 0.0.15 Central ✅** : bump 1 ligne, publishToMavenLocal VERT
  (signatures .asc), publishAggregationToCentralPortal AUTOMATIC accepté,
  propagation repo1 vérifiée polling (~27 min, piège metadata périmée —
  vérifier aussi le HTTP 200 du POM). POM porte `epubcheck:5.2.1` runtime.
- **Propagation bakery-gradle ✅** : unique consommateur dur bumpé
  0.0.12 → 0.0.15 (commit `1b937eb`) ; compileKotlin VERT, test 1668 avec
  4 failures préexistantes prouvées identiques par stash 0.0.12.
- Gates document : compileKotlin VERT, UT 3039/0, FT 126/126,
  validatePlugins VERT, epubCheckCucumberTest VERT, secrets RAS.
- Commits : document `26a05b1` (test/docs) + `f296263` (bump), bakery
  `1b937eb` (bump consommateur). Tous poussés.

=== Session 240 (2026-08-31) — EPIC DOC-EPUBCHECK ✅ TERMINÉ (5e garde : validation epubcheck)

*Type* : feat + TDD/BDD (cadrage + 3 US)
*Score* : 10/10

*Résumé* :
- Cadrage gouvernance pure : `.agents/CADRAGE_EPIC_DOC_EPUBCHECK.adoc`
  (5 constats, 10 décisions). Gap US-DOC-04 (epubcheck, backlogged depuis
  S-005) = dernier gap fonctionnel du BACKLOG.
- US-1 (TDD strict) : domaine `document.epub` Gradle-free — port
  `EpubCheckRunner` (fun interface stateless) + adapter `LibEpubCheckAdapter`
  (bridge `org.w3c:epubcheck:5.2.1` via catalogue, messages
  `ID severity: text (path:line)` filtrés WARN/ERROR/FATAL) +
  `EpubValidationMode`/`EpubValidationResult`/`EpubValidationReport`
  (miroir xref). 5 UT `LibEpubCheckAdapterTest` (EPUB3 minimal construit à
  la main — mimetype STORED + container + OPF + nav + chapter, zéro binaire
  versionné). *Bug réel attrapé par le TDD* : epubcheck appelle
  `info(null, ...)` → NPE Kotlin, fix paramètre `feature` nullable.
  17 UT domain epub 0 FAIL.
- US-2 (FT TestKit) : tâche `validateDocumentEpub`
  (`@DisableCachingByDefault`, STRICT fail-fast après rapport / LENIENT
  warn / OFF skip ; EPUB absent non-OFF → `<epub-file-missing>`, miroir
  règle HTML S-232) + DSL `converter { epubCheck }` + mirror plat +
  CLI `-Pdocument.epubCheck` + chemin EPUB dérivé du même knob
  `outputFileName` que la conversion (jamais provider producteur) +
  `mustRunAfter("convertDocumentToEpub")`. *Piège S-232 #3 confirmé* :
  `@InputFile` valide l'existence AVANT l'action → `epubFile` = `@Input
  @Optional Property<String>` (miroir `htmlFilePath`). 4 FT GREEN
  (dont STRICT valide sur EPUB fraîchement converti).
- US-3 (BDD) : `epub_check.feature` 4 scénarios `@epub-check` (OFF /
  STRICT absent / LENIENT absent / STRICT valide) + runner dédié 17e
  `EpubCheckCucumberRunner` (glue `document.epubcheck`) + steps préfixe
  « epub-check » (anti-glue S-223) + `CucumberTaskSpec
  ("epubCheckCucumberTest")` — 4/4 PASSED.
- Gates : `compileKotlin` VERT, `validatePlugins` VERT, FT epub 4/4 +
  `DocumentPluginFunctionalTest` VERT, secrets RAS. Zéro bump BOM (dep
  tierce org.w3c). Publication 0.0.15 en attente d'accord pilote.

*Livrables* : `document/epub/` (5 fichiers domaine), `ValidateDocumentEpubTask.kt`,
`DocumentPlugin.kt`/`ConverterDsl.kt`/`DocumentExtension.kt` (knob),
`build.gradle.kts` + `libs.versions.toml` (+`epubcheck = 5.2.1`),
`LibEpubCheckAdapterTest.kt`, `ValidateDocumentEpubTaskFunctionalTest.kt`,
`epub_check.feature` + `EpubCheckCucumberRunner.kt` + `EpubCheckSteps.kt`.

=== Session 239 (2026-08-31) — Hygiene + consommation externe 0.0.14 PROUVÉE

*Type* : maintenance + preuve consommation externe
*Score* : 10/10

*Résumé* :
- US-1 : purge 20 fichiers `.bak`/`*.backup*` racine (historisés git,
  zéro référence code) — `build.gradle.kts.backup*` ×13, `settings.gradle.kts.bak` ×2,
  `BACKLOG.adoc.bak`, `.agents/INDEX.adoc.bak`, `.agents/SESSIONS_HISTORY.adoc.bak`.
- US-2 : projet tiers `/tmp/opencode/doc-consumer-014` (Gradle 9.7, JDK 25)
  consomme `education.cccp.document:0.0.14` depuis Central :
  ** 4 gardes composite VALID (includeGuard STRICT / xref LENIENT / htmlLint
    STRICT / safeMode SERVER) → `document-validation-report.json` ;
  ** knob `-Pdocument.outputFileName=gone-custom` vivant de bout en bout
    (conversion → gardes → collect, artefact custom indexé dans
    `composite-context.json`, `metadata.validationStatus=PASS`) ;
  ** preuve négative : include traversal `../../../etc/passwd` en STRICT →
    BUILD FAILED « include escapes allowed root ».
- Constat doc : les enums DSL des gardes exigent des imports explicites
  (`document.security.IncludeGuardMode`, `document.xref.XrefValidationMode`,
  `document.validation.HtmlLinkLintMode`, `org.asciidoctor.SafeMode`).
- US-3 : Option E (`assembleBook` knob séparé) CADUQUE — aucun besoin réel.
- Audit BACKLOG complet (post-session) : marqueurs sync corrigés
  (US-DOC-01/02, US-DOC-04 epubcheck gap inline, DOC-TRANSLATE-5 vérifié
  bakery consomme 0.0.12, DOC-PLANTUML-VALIDATE-1..5). *Seul gap
  fonctionnel restant : critère epubcheck US-DOC-04 (P2) — EPIC
  DOC-EPUBCHECK à cadrer S-240.*

*Aucun code production modifié* — baseline FT 122 / UT 3037 / cucumberTest
2320 inchangée (S-238). La boucle « publication → consommation externe »
est fermée par preuve exécutable.

=== Session 043 (2026-08-19) — Sync gouvernance post-S207

*Type* : gouvernance (rattrapage)
*Score* : 7/10

*Résumé* : Rattrapage retard gouvernance (INDEX/PROMPT_REPRISE/SESSIONS_HISTORY
en retard de 5 sessions). S207 déjà commitée (`9af5ed5`) — EPIC
DOC-FRONTMATTER-RETRANSLATE :
- `FrontmatterStaleDetector` (object pur DDD) — détecte stale summary/description/title
- `ContentTranslationService.retranslateFrontmatter()` — re-traduit frontmatter stale
- `RetranslateFrontmatterTask` — tâche Gradle parcourt l'arbre cible
- BDD Cucumber `frontmatter_retranslate.feature` (6 scénarios) + runner dédié
- Wiring `DocumentPlugin.registerRetranslateFrontmatter`
- Bump document-plugin 0.0.2 → 0.0.3 Central (bundle `1c27a917`)
- Dogfood fake LLM : 44 fichiers stale re-traduits

*Aucun code modifié S043* — pure gouvernance. Tous EPICs document clos.

=== Session 042 (2026-08-04) — Support EPIC BKY-I18N-MASS (cross-borough) : BlockChecksum/BlockDelta + câblage delta-bloc

*MASS-1 — Domaine delta-bloc (DDD pur)* :
- `document/translation/delta/BlockChecksum.kt` : `BlockTranslationStatus` (TRANSLATED/PENDING),
  `BlockChecksumEntry(hash, status)` parse/serialize (legacy `"hash"`→PENDING),
  `sha256(block)` + `computeForBlocks(blocks)` → `Map<idx, hash>`
- `document/translation/delta/BlockDelta.kt` : `compute(previous, current)` → modified/preserved
  (statut ≠ TRANSLATED → re-traduit). Précise : diagramme `[plantuml]` traduit préservé si le texte environnant change
- Tests : `BlockDeltaTest` 26/26 PASS

*MASS-2 — Câblage traducteur* :
- `DocumentTranslator.translateArticleWithDelta(sourceArticle, previousTranslated, delta, ...)`
  (internal→public) : preserve blocs indexés, re-traduit modifiés, dispatch table/plantuml par index, isEmpty→retourne previous
- `ContentTranslationService.translateSingleFileWithBlockDelta(sourceFile, targetFile, previousBlockChecksums, ...)`
  (public) : fresh path / delta path, écrit target
- Tests : `DocumentTranslatorBlockDeltaTest` 4/4 + `BlockDeltaWireDebugTest` 1/1 PASS

*Note* : les `println` de debug `[block-delta]` dans `translateSingleFileWithBlockDelta` sont à retirer
une fois l'issue bakery fixée (MASS-2 FT rouge côté `MigrateContentI18nTask`, suivi S192 bakery).
compileKotlin + publishToMavenLocal OK.

=== Session 039 (2026-08-03) — DOC-PLANTUML-VALIDATE-3 + VALIDATE-4

*VALIDATE-3 — Intégration PlantUmlTranslationAdapter* (5 pts) :
- `PlantUmlSyntaxValidator` — refacto object → interface DDD (companion.create())
- `PlantUmlTranslationAdapter` — +plantUmlValidator +plantUmlValidationMode +validateTranslatedPlantUml()
- `DocumentTranslator` — +plantUmlValidationMode +plantUmlValidationResults + collecte depuis adapter
- `BatchDocumentTranslator` — +plantuml-validation-report.json

*VALIDATE-4 — Mode strict/lenient/off DSL* (3 pts) :
- `PlantUmlValidationConfig` — nouveau, mode Property<String>
- `PlantUmlValidationReport` — nouveau, toJson()
- `TranslationDsl` — +plantUmlValidation
- `DocumentPlugin` — wiring CLI -Pdocument.translatePlantUmlValidation
- `TranslateDocumentTask` + `TranslateDocumentBatchTask` — +plantUmlValidationMode

*Tests* : 24/24 PASS (8 UT adapter + 5 UT translator + 10 UT validator + 1 UT availability)

=== Session 037 (2026-08-03) — DOC-TABLE-VALIDATE-5 (Cucumber BDD, clôture EPIC)

*VALIDATE-5 — Cucumber BDD* (5 pts) :
- `table_validation.feature` — 6 scénarios @table-validation (valid, corrupted-delimiter, strict, column-mismatch, cols-mismatch, batch-report)
- `TableValidationSteps.kt` — step definitions (package document.translation.validation)
- `TableValidationCucumberRunner.kt` — runner JUnit5 Suite, tag @table-validation
- `CorruptingTranslationService.kt` — test helper, injecte corruptions ciblées
- `build.gradle.kts` — +tableValidationCucumberTest dans cucumberConventions.additionalTasks

*EPIC DOC-TABLE-VALIDATE CLÔTURÉ* — 5/5 US (~25 pts) :
- VALIDATE-1 ✅ TableSyntaxValidator (S035)
- VALIDATE-2 ✅ Intégration DocumentTranslator (S035)
- VALIDATE-3 ✅ Mode strict/lenient/off (S036)
- VALIDATE-4 ✅ Rapport batch JSON (S036)
- VALIDATE-5 ✅ Cucumber BDD (S037)

*Tests* : 6/6 scénarios PASS, zéro régression (tableTranslationCucumberTest 8/8, test validation.* 42/42).

=== Session 036 (2026-08-03) — DOC-TABLE-VALIDATE-3 + VALIDATE-4 (strict/lenient/off + batch report)

*VALIDATE-3 — Mode strict/lenient/off* (5 pts) :
- `TableValidationConfig.kt` — DSL `mode: Property<String>`
- `TranslationDsl.kt` — +`tableValidation: TableValidationConfig`
- `DocumentPlugin.kt` — init + convention `"LENIENT"` + wiring CLI `-Pdocument.translateTableValidation`
- `DocumentTranslator.kt` — +`tableValidationMode` param, `tableValidationResults` list, STRICT→throw/LENIENT→warn/OFF→skip
- `TranslationException.kt` — exception métier
- `TranslateDocumentTask.kt` / `TranslateDocumentBatchTask.kt` — +`tableValidationMode` input

*VALIDATE-4 — Rapport batch* (5 pts) :
- `TableValidationReport.kt` — `TableValidationReportEntry` + `TableValidationReport` (Jackson JSON)
- `BatchDocumentTranslator.kt` — agrégation `tableValidationResults` + écriture `table-validation-report.json`

*Tests* : 7 UT TableValidationConfigTest + 6 UT TableValidationReportTest + 3 FT TableValidationFunctionalTest. 16/16 PASS.

=== Session 035 (2026-08-03) — DOC-TABLE-VALIDATE-1 + VALIDATE-2 (TableSyntaxValidator + intégration)

*VALIDATE-1 — TableSyntaxValidator* (5 pts) :
- `TableSyntaxValidator.kt` — cohérence cols, délimiteurs `|===`, homogénéité colonnes, header empty cell warning
- `TableValidationResult.kt` — sealed Valid / Invalid(articleTitle, tableIndex, reason)
- `ValidationMode.kt` — enum STRICT, LENIENT, OFF

*VALIDATE-2 — Intégration DocumentTranslator* (5 pts) :
- `DocumentTranslator.kt` — `validateTranslatedTable()` + `toDddTable()` après traduction
- `AsciiDocParser.kt` — `parseColSpecs` private → internal

*Tests* : 19 UT TableSyntaxValidatorTest + 9 UT DocumentTranslatorTableValidationTest. 28/28 PASS.

=== Session 034 (2026-08-03) — Table POJO DDD + parseTableStructured + TDD/BDD

*Fondation DDD* :
- `Table.kt` — Table/Row/Cell/ColSpec/HAlign/VAlign, extractTranslatable, reinject, toAsciiDoc, toHtml
- `AsciiDocParser.kt` — parseTableStructured, parseColSpecs, parseStructuredRows, splitStructuredCells, parseCellPrefix

*Tests* : 19 UT TableTest + 19 UT AsciiDocParserTableTest + 8 Cucumber table_translation.feature. 46/46 PASS.

=== Session 033 (2026-08-02) — Cadrage DOC-TABLE-VALIDATE + DOC-PLANTUML-VALIDATE

*EPIC DOC-TABLE-VALIDATE* (~25 pts, 5 US, P1) :
- VALIDATE-1 : `TableSyntaxValidator` — cohérence `cols` vs colonnes, délimiteurs intacts, homogénéité
- VALIDATE-2 : Intégration dans `DocumentTranslator.translateBlock(Table)` post-traduction
- VALIDATE-3 : Mode STRICT/LENIENT/OFF + DSL `translation { tableValidation { } }`
- VALIDATE-4 : Rapport consolidé `table-validation-report.json` en mode batch
- VALIDATE-5 : Cucumber BDD — 5 scénarios

*EPIC DOC-PLANTUML-VALIDATE* (~21 pts, 5 US, P1) :
- VALIDATE-1 : Ajout dépendance `implementation(libs.plantuml.plugin)` (déjà versionnée BOM 0.0.13)
- VALIDATE-2 : `PlantUmlSyntaxValidator` wrapper DDD — adapte `PlantumlService.validateSyntax()` (HTOWN)
- VALIDATE-3 : Intégration dans `PlantUmlTranslationAdapter.translate()` post-traduction
- VALIDATE-4 : Mode STRICT/LENIENT/OFF — même enum `ValidationMode` que TABLE-VALIDATE
- VALIDATE-5 : Cucumber BDD — 5 scénarios

*Stratégie DRY* : plantuml-gradle expose `PlantumlService.validateSyntax()` via
`net.sourceforge.plantuml.SourceStringReader`. Le BOM versionne déjà `plantuml-plugin:0.0.2`.
Wrapper DDD prêt pour extraction future en `plantuml-validation-contracts` N0.

*Fichiers modifiés* :
- `BACKLOG.adoc` : +217 lignes (2 EPICs complets avec US, DAG, projections, note DRY)
- `.agents/INDEX.adoc` : +2 EPICs roadmap, date + prochaine session
- `.agents/sessions/033-cadrage-table-plantuml-validation.adoc` : rapport de session
- `PROMPT_REPRISE.adoc` : session 034
- `.agents/SESSIONS_HISTORY.adoc` : entrée S033
- `.agents/archives/COMPLETED_TASKS_ARCHIVE_aout-2026.adoc` : ce fichier

=== Session 032 (2026-08-01) — Dogfooding batch 61 articles cheroliv.com + fixes parser AsciiDoc

*Fix 1 — DescriptionList + BlockMacro* (fusion en un paragraphe au round-trip) :
- `PivotModel.kt` : `PivotBlock.DescriptionList` + `DescriptionItem` (term/definition) + `PivotBlock.BlockMacro` (name/target/attributes)
- `AsciiDocParser.kt` : `isDescriptionListItem`, `parseDescriptionList`, `isBlockMacro`, `parseBlockMacro`, skip commentaires `//`, termes multi-mots (check `!before.contains(" ")` retiré)
- `JbakeNativeRenderer.kt` / `AsciiDocRenderer.kt` / `PivotYamlRenderer.kt` : `renderDescriptionList` + `renderBlockMacro`
- `TreeTranslationAdapter.kt` : collect/apply `DescriptionList` (leaves term + definition, subPath `-30-itemIdx`/0/1)
- `DocumentTranslator.kt` : branches DescriptionList/BlockMacro ; `tree/Content.kt` : inlineOf

*Fix 2 — Délimiteurs source + titres de bloc* (troncature, article 0084 533→70 lignes, 0082 373→134) :
- `parseSourceBlock` réécrit : `firstBlockDelimiterAfterHeader` détecte `----`/`====`/`....`
- `isSourceDelimiterLine` : lignes uniformes de `-`/`=`/`.` (>=4)
- `isBlockTitleLine` : `.titre` entre `[source,...]` et le délimiteur (skip)
- `isSourceClosingDelimiter` permissif (rétro-compat test `====` en fermeture)

*Tests* : PivotModelTest (+2), AsciiDocParserTest (+17), AsciiDocRendererTest (+5), JbakeNativeRendererTest (+4), document.feature (+2 scénarios @description-list + @block-macro), DocumentSteps (+1 Given +1 Then), DocumentWorld (+2 factories). Suite `document.translation.*` GREEN.

*Dogfooding* : 61 articles cheroliv.com FR→EN via `translateDocumentBatch` tree mode, mode Ollama réel.
- Runtime : `gemma4:31b-cloud`, ports 11440/11441/11442 (round-robin 11437-11465), échecs 11438/11450 gérés par le pool
- 17 mois : 2022-01 (1), 2022-05 (5), 2022-12 (1), 2023-01 (1), 2023-11 (1), 2024-06 (1), 2024-07 (1), 2024-11 (1), 2025-05 (1), 2025-07 (9), 2025-09 (6), 2025-10 (2), 2025-11 (2), 2026-01 (2), 2026-04 (13), 2026-05 (12), 2027-04 (1) + 0098 sans date
- Vérification : 61/61 OK — 0 `[EN]`, 0 fuite de prompt, sections `==` conservées, diff source confirmé

*Diagnostic* : le fake mode reproduit les troncatures → outil de diagnostic des bugs parser (pas un problème LLM).

*Infra* : `document-plugin/gradle.properties` + `org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m` (daemon OOM cucumber).

=== Session 212 (2026-08-27) — DOC-BOOK-DOMAIN-1 + DOMAIN-2 (BookTree DDD + Matter/Layout)

*Type* : implémentation DDD/TDD/BDD strict (baby-step RED → GREEN → REFACTOR)
*Score* : 10/10

*Résumé* :
- DOMAIN-1 ✅ : `BookNode` + `BookTree` + `BookTreeBuilder` (3 fichiers DDD purs).
  `fromSections(List<BookSection>)` dérive l'arbre depuis le `ref` — profondeur
  = nb segments - 1, parent = préfixe tronqué, ancêtres orphelins synthétisés,
  refs dupliqués (multi-pages) collapse en un nœud, enfants triés par ref.
  15 UT `BookTreeBuilderTest` VERT.
- DOMAIN-2 ✅ : `Matter` (FRONT/BODY/BACK, classifier configurable sans hardcode)
  + `BookLayout` (value object émet AsciiDoc : heading/level `=`×(level+1),
  pageBreak `<<<`, `:toc:`, titlePage). 12 UT `BookLayoutTest` VERT.
- Non-régression : `BookAssemblerTest`/`BookValidatorTest`/`BookTocParserTest`/
  `BookDslTest` VERTS. 0 secret, 0 production modifié hors nouveaux fichiers.

*Livrables* : `BookNode.kt`, `BookTree.kt`, `BookTreeBuilder.kt`, `Matter.kt`,
`BookLayout.kt` + 2 fichiers test (27 UT).

=== Session 213 (2026-08-28) — DOC-BOOK-DOMAIN-3 + DOMAIN-4 (BookAssembler structuré + BookNumbering)

*Type* : implémentation DDD/TDD/BDD strict (baby-step RED → GREEN → REFACTOR)
*Score* : 10/10

*Résumé* :
- DOMAIN-3 ✅ : `BookAssembler.assemble(tree, layout, title, author, resolveContent)` (1 fichier modifié)
  - Émet AsciiDoc structuré : titre page, TOC, corps imbriqué niveaux, ancres `[[ref]]`
  - Page break entre nœuds de niveau 0, titre selon `BookLayout.emitTitlePage`
  - Retour `BookAssemblyResult(content, pages=emptyList(), photoCount=0)` (non-régression préservée)
  - 10 UT `BookAssemblerStructuredTest` VERT (émission niveaux, titre, toc, ancres, ordre front/body/back)
- DOMAIN-4 ✅ : `BookNumbering` + `BookNavigation` (2 fichiers neufs)
  - `numbers(tree)` : ref→ref (FPA TOC convention)
  - `number(tree, ref)` = ref (pas de numérotation positionnelle)
  - `anchor(ref)` = `[[ref]]`
  - `navigation(tree, ref)` : previous/next siblings in preorder
  - 10 UT `BookNumberingTest` VERT (numérotation, ancres, navigation, orphelins matter)
- Non-régression : `BookAssemblerTest`/`BookValidatorTest`/`BookTocParserTest`/`BookTreeBuilderTest`/`BookDslTest`/`BookLayoutTest` VERTS.
  `check` complet bloqué par OOM JRuby/AsciidoctorJ préexistant dans `DocumentConverterTest` (environnement, hors périmètre).
- Câblage `AssembleBookTask` : si TOC présent → arbre structuré ; sinon blob plat (non-régression).
- Zéro secret, 0 production modifié hors nouveaux fichiers du domaine.

*Livrables* : `BookNumbering.kt`, `BookNavigation.kt`, `BookAssembler.kt` (modifié) + 2 fichiers test (20 UT).

=== Session 215 (2026-08-28) — DOC-BOOK-DOMAIN-5 (Cucumber BDD @book-tree + dogfooding FPA) + DOC-BOOK-DOMAIN-6 (BookValidator structurel) — EPIC DOC-BOOK-DOMAIN CLÔTURÉ 6/6 US

*Type* : implémentation DDD/TDD/BDD strict (baby-step RED → GREEN → REFACTOR)
*Score* : 10/10

*Résumé* :
- Nettoyage S-214 : wrapper Gradle 9.6.1 restauré (jar + properties), `.bak` supprimés, `.gitignore` durci (+ .idea/.gradle/.kotlin/build).
- DOMAIN-6 ✅ : `BookValidator.validateStructure(sections)` — validation *structurelle* DDD pure (zéro I/O)
  - S1 ref continuity — parent `ref` (préfixe tronqué) absent → level jump détecté
  - S2 unicité — doublon `(ref, page)` (même ref sur pages distinctes = section multi-pages légitime)
  - S3 matter completeness — au moins un FRONT `0.x` et un BACK `9.x`
  - S4 page order monotonicity — pages physiques ne régressent jamais
  - 8 UT `BookValidatorStructureTest` RED → GREEN (TDD strict)
  - Intégration `AssembleBookTask.validateIfConfigured` : raisons structurelles préfixées `structure — ` mergées avec la validation fichier, LENIENT → warn / STRICT → GradleException, non-régression préservée
  - `AssembleBookTaskFunctionalTest.kt` réécrit : 4 FT GREEN (registered, structured from TOC, STRICT fail level jump citant `1.1`, LENIENT survit)
- DOMAIN-5 ✅ : Cucumber BDD `@book-tree`
  - `book_tree.feature` 6 scénarios (assembly structuré, front/body/back, niveaux, ancres, navigation prev/next, bornes début/fin, compt blob plat)
  - `BookTreeCucumberRunner` + `BookTreeSteps` (package `document.booktree`, préfixe « book-tree », pure BDD domaine) + `CucumberTaskSpec("bookTreeCucumberTest")`
  - RED UndefinedStepException → GREEN 6/6 PASSED
  - Dogfooding FPA `AssembleBookTaskFpaTest` : corpus réel `office/metiers/FPA` (toc.adoc + scans 000/009/010), pages copiées (jamais mutées), self-skip `assumeTrue` si corpus absent, TOC LENIENT → warnings structurels loggés sans casser, book structuré navigable. PASSED.
- Gates : 91 UT book domain + 6 suites cucumber + FT book GREEN. `DocumentPluginFunctionalTest` 62 dont 1 flaky environnemental (convertDocumentToPdf images, PASSED isolé, pression mémoire JRuby — hors périmètre, famille OOM S-213).

*Livrables* : `BookValidator.kt` + `AssembleBookTask.kt` (modifiés), `booktree/` (runner + steps), `book_tree.feature`, `BookValidatorStructureTest.kt`, `AssembleBookTaskFpaTest.kt` + `AssembleBookTaskFunctionalTest.kt` (neufs).

=== Session 214 (2026-08-28) — session interrompue (note d'archivage)

*Type* : échec partiel — tentative d'extraction `AssembleBookTask` pour testabilité unitaire + convention mapping reverted.
*Résultat retenu* : `AssembleBookTaskUnitTest.kt` fixé (S-214, conservé). État sale nettoyé en S-215.

=== Session 216 (2026-08-28) — DOC-BOOK-VALIDATE-4 (Cucumber BDD) — EPIC CLÔTURÉ

*Type* : implémentation BDD (baby-step TDD/BDD strict)
*Score* : 10/10
*EPIC* : DOC-BOOK-VALIDATE (5/5 US)

*Livrables* :
- `book_validation.feature` 5 scénarios `@book-validation` (`@valid`, `@missing-page`, `@empty-section`, `@toc-mismatch`, `@strict`)
- `BookValidationCucumberRunner.kt` + `BookValidationSteps.kt` (package `document.bookvalidation`, préfixe « book-validation », domaine pur zéro I/O Gradle)
- `build.gradle.kts` : `CucumberTaskSpec("bookValidationCucumberTest")`
- `BookValidator.enforce(validationMode, reasons)` extrait de `AssembleBookTask.validateIfConfigured` (throw GradleException STRICT, no-op LENIENT)
- Gates : 5/5 cucumber book-validation + 6/6 book-tree + UT/FT book GREEN, `compileKotlin`/`compileTestKotlin` VERT

*Hors périmètre* : `DocumentConverterTest` OOM JRuby préexistant ; BOM 0.0.11 inchangé ; zéro secret ; zéro publish.

=== Session 219 (2026-08-28) — DOC-CR3-1 + DOC-CR3-3 (perf instance AsciidoctorJ + code mort koog)

*Type* : implémentation TDD strict (baby-step DDD/TDD/BDD)
*Score* : 10/10
*EPIC* : DOC-CR3 (2/3 US — CR3-1 ✅, CR3-3 ✅, CR3-2 🟡 reporté)

*Livrables* :
- `AsciidoctorHolder.kt` (object, instance AsciidoctorJ unique lazy+cache, seam test `setProviderForTest`/`resetProvider`, `synchronized`)
- `AsciidoctorHolderTest.kt` (1 UT TDD : `createCount == 1` sur 2 conversions)
- `DocumentConverter.kt` : `convert`/`convertToFile` délèguent à `AsciidoctorHolder.convertFile` (API publique inchangée, -12 lignes net)
- `DocumentGenerationGraph.kt` : retrait propriété `graph` koog morte + 4 imports koog morts (-53→-57 lignes, fichier koog-free)

*Gates* : `compileKotlin` VERT ; `test --tests DocumentConverterTest --tests AsciidoctorHolderTest` VERT ; `test --tests DocumentGenerationGraphTest` VERT ; grep secrets clean ; BOM 0.0.11 inchangé.

*Non traité* : DOC-CR3-2 (garde `safeMode` DSL configurable, P2) — reporté scope tight.

=== Session 220 (2026-08-28) — DOC-CR3-2 (garde safeMode DSL configurable) — EPIC CLÔTURÉ

*Type* : implémentation TDD/BDD strict (baby-step DDD/TDD/BDD)
*Score* : 10/10
*EPIC* : DOC-CR3 (3/3 US — CR3-1 ✅ S-219, CR3-3 ✅ S-219, CR3-2 ✅ S-220)

*Livrables* :
- `DocumentConverter.buildOptions(safeMode: SafeMode?)` : SafeMode fourni / par défaut UNSAFE / appliqué SECURE.
- `ConverterDsl.kt` (NOUVEAU) : bloc `converter { safeMode = ... }`.
- `DocumentExtension.safeMode` + `DocumentPlugin` : mirror flat + CLI `-Pdocument.safeMode` (UNSAFE/SERVER/SECURE).
- `ConvertDocumentTask.safeMode` consommé → `DocumentConverter.buildOptions`.
- `DocumentConverterTest.kt` : 4 UT (buildOptions SafeMode, convertToHtml SERVER).
- `ConvertDocumentTaskSafeModeFunctionalTest.kt` : 4 FT (DSL SERVER/SECURE, CLI override, défaut UNSAFE).
- `converter_safe_mode.feature` (3 scénarios `@converter-safe-mode`) + `ConverterSafeModeCucumberRunner.kt` + `ConverterSafeModeSteps.kt` + `CucumberTaskSpec("converterSafeModeCucumberTest")`.

*Gates* : `compileKotlin` VERT ; `test` (4 UT) VERT ; `functionalTest` (4 FT) VERT ; cucumber dédié safeMode 3/3 (exécution globale bruite par infra borough — DuplicateStepDefinitionException due à un doublon de step définition dans les tests document-gradle, résolu S-223, hors DOC-CR3).

=== Session 221 (2026-08-29) — DOC-CR4 (Include Path Guard) — EPIC CLÔTURÉ

*Type* : implémentation TDD/BDD strict (baby-step DDD/TDD/BDD)
*Score* : 10/10
*EPIC* : DOC-CR4 (4/4 US)

*Livrables* :
- `document/security/IncludeGuardMode.kt` (enum STRICT/LENIENT/OFF), `IncludeDirective.kt`, `IncludeValidationResult.kt` (sealed), `IncludePathValidator.kt` (parseIncludes/validate), `DocumentIncludeGuard.kt` (check).
- `IncludePathValidatorTest.kt` : 12 UT.
- `ConvertDocumentTask.kt` : `+includeGuard` + `DocumentIncludeGuard.check` au début de `convert()`.
- `ConverterDsl.kt` / `DocumentExtension.kt` / `DocumentPlugin.kt` : `+includeGuard` DSL + flat mirror + CLI `-Pdocument.includeGuard`.
- `ConvertDocumentTaskIncludeGuardFunctionalTest.kt` : 5 FT (DSL STRICT/LENIENT, CLI override, défaut OFF, STRICT reject, LENIENT convert).
- `converter_include_guard.feature` (4 scénarios `@converter-include-guard`) + `IncludeGuardCucumberRunner.kt` + `IncludeGuardSteps.kt` + `CucumberTaskSpec("includeGuardCucumberTest")`.
- **Bug préexistant corrigé** : `registerBookPipeline` overridait inconditionnellement `convertDocumentToHtml.sourceFile` sur le livre assemblé → conversion standalone cassée ; fix via `taskGraph.whenReady` (override seulement si `bookPipeline` dans le graphe).

*Gates* : `compileKotlin` VERT ; `test --tests document.security.IncludePathValidatorTest` VERT (12 UT) ; `functionalTest --tests ConvertDocumentTaskIncludeGuardFunctionalTest --tests ConvertDocumentTaskSafeModeFunctionalTest` VERT (5 FT CR4 + 4 FT safeMode). Cucumber dédié CR4 : artifacts ajoutés, exécution globale bruite par infra borough (DuplicateStepDefinitionException due à un doublon de step définition dans les tests document-gradle, résolu S-223, non bloquant DOC-CR4).

*Hors périmètre* : correction infra Cucumber borough — résolue S-223 (le doublon réel était une step définition dupliquée dans les tests document-gradle, pas le plugin conventions).

=== Session 222 (2026-08-30) — DOC-CR5 (Conversion Security Policy) — EPIC CLÔTURÉ

*Type* : implémentation TDD/BDD strict (baby-step DDD/TDD/BDD)
*Score* : 10/10
*EPIC* : DOC-CR5 (3/3 US)

*Contexte* : suite de DOC-CR3-2 (safeMode) + DOC-CR4 (includeGuard). DOC-CR4 audite
les `include::` mais ne couvre que ce vecteur ; AsciidoctorJ accède aussi au FS (images,
data URIs, thèmes, `:docdir:`). Activer `includeGuard` avec `safeMode=UNSAFE` (défaut)
est une illusion de sécurité. DOC-CR5 force un plancher de cohérence (défense en profondeur).

*Règle* : `safeMode == UNSAFE && includeGuard != OFF` →
STRICT→Reject (GradleException fail-fast) / LENIENT→Warn (logger.warn) ; sinon Valid.

*Livrables* :
- `document/security/SecurityAdvice.kt` (sealed Valid/Warn/Reject), `DocumentSecurityPolicy.kt` (`advise(safeMode, includeGuard)`).
- `DocumentSecurityPolicyTest.kt` : 12 UT.
- `ConvertDocumentTask.kt` : `+DocumentSecurityPolicy.advise` check après `DocumentIncludeGuard.check` (sévérité via mode `includeGuard`, zéro nouveau knob DSL/CLI).
- `ConvertDocumentTaskSecurityPolicyFunctionalTest.kt` : 3 FT (STRICT+UNSAFE reject, LENIENT+UNSAFE warn+convert, SERVER+STRICT valid).
- `converter_security_policy.feature` (3 scénarios `@converter-security-policy`) + `SecurityPolicyCucumberRunner.kt` + `SecurityPolicySteps.kt` + `CucumberTaskSpec("securityPolicyCucumberTest")`.

*Gates* : `compileKotlin` VERT ; `test --tests document.security.*` VERT (24 UT : 12 CR4 + 12 CR5) ; `functionalTest` CR5 (3 FT) + CR4 (5 FT) VERT ; `securityPolicyCucumberTest` runner dédié 3/3 PASSED. Exécution globale Cucumber borough bruite par infra (DuplicateStepDefinitionException due à un doublon de step définition dans les tests document-gradle, résolu S-223, non bloquant DOC-CR5).

*Hors périmètre* : correction infra Cucumber borough (conventions duplicate classpath) — résolue S-223 (doublon de step dans tests document-gradle, pas le plugin conventions).

== Session 231 (2026-08-31) — EPIC DOC-METADATA-VALIDATION ✅ TERMINÉ (3/3 US)

*Statut final* : EPIC clôturé. Statut de validation composite exposé dans metadata.json N3.

*US livrées* :
- DOC-METADATA-1 ✅ (S-231) — `DocumentValidationReport.overallStatus()` (FAIL si
  includeGuard INVALID / xref MISSING / security REJECT, PASS sinon) + `fromJson()`
  round-trip (7 UT TDD strict, dep `jackson-module-kotlin` contrainte BOM 0.0.26).
- DOC-METADATA-2 ✅ (S-231) — `DocumentMetadata.validationStatus` (NON_NULL) +
  `CollectDocumentRetrieveTask.validationReportPath` (lecture seule du rapport,
  dégradé silencieux, Loi Économie d'Encre) + câblage DocumentPlugin (5 UT + 3 FT).
- DOC-METADATA-3 ✅ (S-231) — Cucumber `@metadata-validation` 3/3 PASSED
  (runner `MetadataValidationCucumberRunner` + `CucumberTaskSpec metadataValidationCucumberTest`),
  scénarios STRICT en `safeMode SERVER` (cohérence DOC-CR5, pattern STRICT_SERVER S-225).

*Hygiène* : purge 2 `println` debug WIP S-227 + déduplication bloc conventions
trippliqué `DocumentPlugin.apply()`.

*Gates* : compileKotlin VERT / test 2622 (11 failures ENV gems Ruby préexistantes) /
10 runners Cucumber dédiés VERTS / zéro secret / zéro régression.

=== Session 232 (2026-08-31) — EPIC DOC-VALIDATE-HTML-LINT : 4e garde composite

*Type* : feat + TDD/BDD
*Score* : 10/10

*Résumé* : Lint HTML du rendu (`HtmlLinkLinter`, S-226) rejoint la validation
composite `validateDocument` comme 4e garde (include guard CR4 + xref CR-xref +
security CR5 + htmlLint DOC-HTML-LINT).
- US-1 (TDD strict RED→GREEN) : `DocumentValidator.validate(..., htmlLintMode=OFF,
  html=null)` defaults backward-compat + `HtmlLintReportEntry` (VALID/DEAD,
  deadLinks nullable NON_NULL) + `DocumentValidationReport.htmlLint`
  (@JsonInclude NON_NULL sur le conteneur — JSON backward-compat) +
  `overallStatus()` FAIL si DEAD + `fromJson` sans bloc htmlLint → null PASS +
  règle « HTML absent sous mode non-OFF » = DEAD `<html-file-missing>`
  (visibilité sur silence). 10 UT `HtmlLintGuardTest`.
- US-2 (FT TestKit) : `ValidateDocumentTask.htmlLinkLint` + `htmlFilePath`
  (@Input @Optional String — PAS @InputFile : Gradle validerait l'existence)
  + sévérité STRICT/LENIENT/OFF symétrique xref + câblage plugin (même knob
  `converter { htmlLinkLint }` + mirror + CLI, chemin statique
  `build/docs/document/document.html`, `mustRunAfter("convertDocumentToHtml")`).
  4 FT. 3 pièges Gradle résolus : dépendance implicite provider outputFile /
  `.get()` configuration-time interdit / `@InputFile @Optional` valide l'existence.
- US-3 (BDD) : 3 scénarios `@document-validation-html-lint` (STRICT fail DEAD
  + message / STRICT pass VALID / LENIENT DEAD sans échec) + steps Given dédié
  anti-glue S-223 + configs STRICT_HTML/LENIENT_HTML (safeMode SERVER,
  pattern STRICT_SERVER) — 8/8 PASSED au runner dédié.

*Livrables* : `DocumentValidator.kt` (+4e garde +HtmlLintReportEntry +overallStatus
+NON_NULL), `ValidateDocumentTask.kt` (+inputs +sévérité), `DocumentPlugin.kt`
(câblage), `HtmlLintGuardTest.kt` (10 UT), `ValidateDocumentTaskHtmlLintFunctionalTest.kt`
(4 FT), `document_validation.feature` (+3 scénarios), `DocumentValidationSteps.kt` (+steps).

*Gates* : compileKotlin VERT / 10 runners Cucumber dédiés SUCCESS (8m23s) /
FT composite 4/4 + html-lint 4/4 / test 2671 / 11 failures ENV gems Ruby
préexistantes (S-213/215/224/231) / domaine validation 37 UT / 0 fail /
zéro secret / zéro régression.

=== Session 233 (2026-08-31) — Dogfooding pipeline N3 complet sur le livre FPA (Option A) + fix ENV gems (Option C)

*Type* : feat + TDD/BDD + fix ENV
*Score* : 10/10

*Résumé* : Le statut N3 du « livre FPA SERVER » est verrouillé :
- FT dogfooding `BookPipelineN3FpaFunctionalTest` (corpus FPA réel, read-only
  Rule 7, 3 builds staged bookPipeline → validateDocument → collectDocumentRetrieve)
  — `metadata.json` porte `validationStatus=PASS`, 4 gardes auditées (xref
  STRICT VALID, htmlLint STRICT VALID, includeGuard LENIENT×UNSAFE → advice WARN
  cohérent DOC-CR5), zéro INVALID/MISSING/DEAD/REJECT.
- BDD anti-régression `@n3-pipeline` 1/1 PASSED (11e runner dédié
  `n3PipelineCucumberTest`).
- Fix ENV gems PDF/EPUB : `runtimeOnly asciidoctorj-pdf:2.3.23` +
  `asciidoctorj-epub3:2.2.0` — 16 failures ENV résolues (baseline FT 21 → 5).
- Infra : `LintHtmlDocumentTask` annotations restaurées (régression WIP) +
  `mustRunAfter(collect)` ; `CollectDocumentRetrieveTask` snapshot contract
  (`upToDateWhen { false }`).
- 3 nouveaux pièges Gradle documentés (snapshot collect / mustRunAfter inverse
  la CLI / chaîne N3 staged — cycle interdit en 1 build).

*Clôtures* : option A et option C du PROMPT_REPRISE 233 traitées. L'option B
(chemin HTML statique du 4e garde) reste ouverte — reportée S-234.

== Session 234 (2026-08-31) — Fix Option A : hygiène FT translateDocument

*Type* : fix + TDD/BDD — *Score* : 10/10

*Résumé* : les 5 failures FT préexistantes S-233 (`translateDocument` ×2 +
`TableValidationFunctionalTest` ×3) sont résolues par l'ajout de la convention
`ext.translation.outputFileName.convention("document")` (DocumentPlugin.kt).
Baby-step TDD strict : RED (5 failures reproduites — stack exacte
« property 'outputFile' doesn't have a configured value » : le provider chaîné
`cliProp(...).orElse(ext.translation.outputFileName).map { ... }` reste unset
sans convention ni CLI) → GREEN (fix 1-ligne) → REFACTOR n/a (économie d'encre).

*Gates* : `functionalTest` 68/68 (baseline 5 failures → 0), `test` VERT
(6m59s), 15/15 runners Cucumber dédiés 144 scénarios PASSED. Preuve BDD
comparative sur le moteur global : 144/86 (stash, sans fix) → 148/82 (avec
fix) — +4 scénarios `@doc-translate`. Les 82 failures globales restantes
préexistent (scoping glue runner global vs runners dédiés, S-221/S-223).

*Piège nouveau documenté S-234* : une Property DSL sans `convention(...)`
reste *unset même derrière `.orElse(...)`* — remonter la chaîne de providers
jusqu'à l'extension DSL quand Gradle pointe la tâche.

*Clôtures* : option A traitée. Options B (chemin HTML statique 4e garde) et
C (publication 0.0.14) reportées S-235.

== S-235 (2026-08-31) — Option B : output HTML réel audité par la 4e garde ✅

*Knob `outputFileName` rendu vivant* (3 US ~8 pts, baby-step DDD/TDD/BDD) :
- US-1 TDD : `outputFile` conversion dérivé du knob (défaut `document` → chemins
  canoniques backward-compat). FT 2/2.
- US-2 FT : `validateDocument.htmlFilePath` + `lintHtmlDocument.htmlFile` dérivés
  du même knob (zéro dépendance de tâche, pièges S-232 respectés). FT 2/2.
- US-3 BDD : +2 scénarios `@document-validation-custom-output` — 10/10 PASSED.

Faux positif corrigé : fragment discriminant (2 findings partageaient `missing`).

Gates : 15/15 runners dédiés VERTS, compileKotlin VERT, secrets RAS.

Gouvernance : CONTRAINTE GLOBALE Zéro Python (AGENTS.adoc racine).

== S-236 (2026-08-31) — Follow-up S-235 : collect N3 knob-aware ✅

*Artifacts custom indexés N3* (3 US ~6 pts, baby-step DDD/TDD/BDD) :
- US-1 TDD : `DocumentArtifactCollector.collect(sourceAdoc, outputFileName = "document")`
  — scan `$name.adoc` / `$name-enriched.adoc` / `$name.${ext}` (défaut canonique
  backward-compat, constantes mortes purgées). UT 17/17 (RED : 3 ×
  `No parameter with name 'outputFileName'`).
- US-2 FT : `CollectDocumentRetrieveTask.outputFileName` (@Input @Optional,
  `getOrElse("document")`) + câblage plugin même-knob que les conversions
  (zéro dépendance de tâche, piège S-232 respecté). FT 2/2 (RED : composite
  sans `custom.html`) + `DocumentPluginFunctionalTest` 65/65.
- US-3 BDD : `n3_collect_custom.feature` 2 scénarios + 16e runner dédié
  `n3CollectCustomCucumberTest` (steps préfixe `n3-collect`). 2/2 PASSED.

Gates : 16/16 runners Cucumber dédiés VERTS (0 failure / 0 error XML,
8m47s), compileKotlin VERT, secrets RAS.

`-Pdocument.outputFileName` suit désormais conversion → garde → collect.
Options restantes : C (publication 0.0.14, accord pilote requis), D (moteur
global), E (assembleBook knob séparé `book`).

=== Session 237 (2026-08-31) — Option C : republication 0.0.14 Maven Central

*Type* : PUB (publication Maven Central, accord pilote explicite session)
*Score* : 10/10

*Résumé* : Option C du PROMPT_REPRISE 237 exécutée — republication 0.0.14
pour que les consommateurs externes du plugin disposent des gems PDF/EPUB
packagées `runtimeOnly` (S-233) et du knob `outputFileName` bout-en-bout
(S-235/236).

*Audit prérequis nmcp* (méthode standard nmcp) :
- `settings.gradle.kts` : `com.gradleup.nmcp.settings` 1.5.0 + `nmcpSettings`
  `centralPortal` (username/password depuis `~/.gradle/gradle.properties`,
  `publishingType = "AUTOMATIC"`) — déjà en place depuis DOC-7/DOC-8.5.
- POM `withType<MavenPublication>` : name "Document Gradle Plugin", description,
  license Apache-2.0, developer cccp-education, scm (via publishingConventions).
- `repositories { mavenCentral() }` (pas staging Sonatype legacy).
- Deps `education.cccp` toutes des releases Central : planner-plugin 0.0.1
  (compileOnly), plantuml-plugin 0.0.4, i18n-contracts 0.0.2,
  opencode-session-contracts 0.0.2, pipeline-contracts 0.0.2,
  workspace-bom 0.0.26 (platform). Gems `org.asciidoctor` Central.
- Build compile (`compileKotlin` VERT).

*Publication* :
- Bump `version = "0.0.13"` → `"0.0.14"` (build.gradle.kts:11 — seule ligne
  production modifiée).
- `publishToMavenLocal` VERT (8s) : jar 773 KB + pom + module + sources +
  javadoc + signatures .asc ; POM vérifié (license Apache-2.0, scm, deps gems
  asciidoctorj-pdf 2.3.23 + asciidoctorj-epub3 2.2.0).
- `publishAggregationToCentralPortal --no-daemon` BUILD SUCCESSFUL (27s,
  deployment accepted AUTOMATIC).
- Propagation Central vérifiée par polling `maven-metadata.xml` (8 requêtes,
  ~7 min) : `<latest>0.0.14</latest>`, POM+JAR HTTP 200, POM porte les gems.

*Delta versions* : Central 0.0.12 → 0.0.14 (0.0.13 jamais publiée — contenu
cumulé S-231..S-236 embarqué dans 0.0.14).

*Écart noté* : le PROMPT_REPRISE 237 annonçait « republication 0.0.14 » alors
que le local était 0.0.13 — réalité locale auditée avant bump (métadonnée
Central `<latest>0.0.12</latest>` = source de vérité).

*Prochaine session 238* : option D actée pilote — diagnostic moteur Cucumber
global (`cucumberTest`, 82 failures `UndefinedStepException` glue scoping).

== S-238 (2026-08-31) — Option D : moteur Cucumber global `cucumberTest` VERTE ✅

*Diagnostic complet* (audit XML, zéro Python) : 73 failures = 70 glue scoping
(16 features étrangères au runner global) + 3 steps réellement manquants dans
la glue `document.scenarios` (scénarios `@template` DOC-13 : vrai bug de
couverture BDD — Given présent, Then jamais écrit).

*US-1 (baby-step TDD)* :
- `@Then("the generated document should contain {string}")` — scan
  `build/docs/document/*.adoc` (knob DOC-13 `outputFileName` dérive le nom).
- `@Then("the build should fail")` — `result.output` contient FAILED (TestKit).
- `DocumentWorld.templateGeneratedDocuments()`.
- document.feature 65/65, failures 73 → 70 (les 70 restantes = pure glue scoping).

*US-2 (règle de propriété)* : `CucumberTestRunner` restreint à ses 3 features
possédées (`document` + `release-notes` + `frontmatter_retranslate`). Zéro
perte de couverture (chaque feature étrangère a son runner dédié).

*Fix bonus validatePlugins* (régression préexistante prouvée stash, S-232/S-233) :
`LintHtmlDocumentTask` / `ValidateDocumentTask` / `ValidateDocumentXrefTask`
`@DisableCachingByDefault` + `RetranslateFrontmatterTask.sourceDir`
`@PathSensitive(RELATIVE)`.

*Gates* : `cucumberTest` BUILD SUCCESSFUL (2320 tests / 2165 skipped
dédiés filtrés / 0 failure — 1re fois verte depuis S-221), FT 122/122,
UT 3037/0, secrets RAS. Rapport : `.agents/sessions/238-cucumber-global-verte.adoc`.

*Prochaine session 239* : hygiène .bak/backup résiduels, consommation
externe 0.0.14 (projet tiers), option E `assembleBook` knob si besoin réel.

== S-242 (2026-09-01) — Backlog clôturé + consommateurs 0.0.15 vérifiés ✅

*Type* : gouvernance + vérification (zéro code production).

*Audit backlog* : `grep` marqueurs périmés (🟡/⏳/À FAIRE/À PLANIFIER/
EN PAUSE) sur BACKLOG.adoc → **zéro match**. Tous les EPICs ✅ — le
backlog fonctionnel est vide. Seul item traité : hygiène `_Version_`
0.0.1 → 0.0.15.

*Anomalie settings.gradle.kts résolue* : le WIP S-227 (`181a6ca`)
réintroduisait `include(":document-plugin")` à la racine contrairement au
pattern S-006 « Plugin Indépendant + Racine Consommateur / zéro include()
» (article #0124). Mod. pending vérifiée correcte (plugin autonome
compileKotlin VERT, racine `tasks` VERT) → conservée + commitée.

*Consommateurs 0.0.15 (demande pilote)* :
- bakery-gradle (seul dur, `bakery-plugin/build.gradle.kts:43`) :
  `bakery-plugin` compileKotlin VERT + `test` 1668 / 4 failures =
  exactement les 4 préexistantes S-241 (XML identifiées :
  BakeryPluginPublicationTest version test, FixtureAlignmentAuditorTest,
  RealSiteI18nMigrationIntegrationTest golden master,
  I18nDeltaWireCucumberTest feature 57 — prouvées non-causées par stash
  0.0.12). *OK.*
- runner-gradle N3 : zéro couplage compile (contrat de facto
  metadata.json/composite-context.json). *OK (rien à faire).*
- office/metiers/FPA : init-script + CLI `-Pdocument.*` (mavenLocal
  0.0.15), artefacts réels validés FT dogfooding (book.html lint Valid,
  book.epub epubcheck Valid). *OK.*

*Baseline* : `epubCheckCucumberTest` 4/4 PASSED. Central
`<latest>0.0.15</latest>`. Secrets RAS.

*Prochaine session 243* : borough DORMANT — rien à planifier (besoin
réel uniquement).

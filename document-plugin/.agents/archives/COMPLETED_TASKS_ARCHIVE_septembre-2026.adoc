= COMPLETED_TASKS_ARCHIVE — Septembre 2026

== Document-gradle (New Orleans 🟢)

=== Session 263 (2026-09-24) — CROSS-DOC-BOOK-FIDELITY ✅ (test-only)

*Type* : test-only BDD + revue de fidélité
*Score* : 10/10
*Livrables* :
- Constat : le BDD cross-borough S-210 était vert mais **aveugle** —
  fixtures à en-têtes `= Intro` que codex n'écrit jamais (`CollectOcrTask:56`),
  assertion tautologique sur les `= ` de la fixture, seul le chemin blob-plat
  couvert alors que la prod emprunte le chemin structuré.
- Réécriture `cross_borough_book.feature` (6 scénarios fidèles : pages codex
  brutes, ordre sur le contenu, `TOC → structure`, section multi-pages,
  dégradé sans échec, filtrage non-adoc) + `CrossBoroughSteps.kt`.
- Cadrage `.agents/CADRAGE_EPIC_CROSS_DOC_BOOK_FIDELITY.adoc` (6 constats,
  8 décisions, 4 US).
- Baby-step BDD strict RED (6 `UndefinedStepException`) → GREEN 6/6.
- Zéro code de production modifié. Piège #31 (`un test vert n'est pas un test
  fidèle`).
*Gates* : `test` 4827/0, FT 144/1 (préexistant RichToc page 73), runners
Cucumber voisins 0 fail, `validatePlugins` VERT, garde 3/3.

=== Session 262 (2026-09-24) — DOC-BOOK-CONSISTENCY-B6 ✅ (dernière dette S-258)

*Type* : feat DDD/TDD/BDD + dogfooding
*Score* : 10/10
*Livrables* :
- Domaine — `BookNumbering.numbers()` documenté comme **dérivation identité
  assumée** (le numéro imprimé = celui de la TOC source) ; `emittedNodes(tree)`
  (sections distinctes) ; `navigation` court sur les sections *émises* → **bug
  latent multi-pages corrigé**. `BookLayout.emitNavigation` (opt-in) +
  `navigationLinks` (`<<ref,title>>`). 6 UT.
- Assemblage — `BookAssembler` appende la ligne de navigation au pied de section
  quand `emitNavigation`. 2 UT.
- Wiring — `AssembleBookTask.navigation` + DSL `book { navigation }` + CLI
  `-Pdocument.bookNavigation` + contrat N3 `BookConfig.navigation`. 2 FT.
- BDD `@book-navigation` 5/5 + **22e runner `bookNavigationCucumberTest`**.
- Preuve dogfooding (corpus FPA réel) : 239 liens, 116 cibles, **0 dangling**,
  première section next-only.
*Gates* : `test` 4785/0, FT 144/1 préexistant, 22 runners 0 fail, garde 3/3,
validatePlugins VERT, publishToMavenLocal VERT, zéro secret.

=== Session 261 (2026-09-24) — EPIC DOC-BOOK-MATTER ✅ (B3)

*Type* : feat DDD/TDD/BDD + dogfooding
*Score* : 10/10
*Livrables* :
- US-1 — `MatterPolicy` dérivable + `MatterPolicyMode` (DERIVED/LEGACY/NONE) :
  le matter n'est exigé que si la TOC le déclare ; `requiresFront`/`requiresBack`
  dérivés du mapping. Domaine pur, 17 UT.
- US-2 — `BookLayout.emitMatterBreaks` (opt-in) + `matterPolicy` + `matterBreak()` ;
  `BookAssembler` émet `<<<` aux transitions FRONT→BODY / BODY→BACK, page de
  titre fermée (jamais de doublon avec le page-break de niveau).
- US-3 — règle S3 opt-in via `validateStructure(sections, matterPolicy)` ; wiring
  `AssembleBookTask` + DSL `book { matterPolicy; matterBreaks }` + CLI + contrat
  N3 `BookConfig` ; FT 4/4 ; BDD `@book-matter` 8/8 + 21e runner.
- Preuve dogfooding (corpus FPA réel) : DERIVED = 0 finding matter, LEGACY = 2
  (faux positif historique prouvé).
*Tests* : 17 UT `MatterPolicyTest` + 8 UT layout/assembler/validator + 1 UT
serializer + 4 FT `BookMatterFunctionalTest` + 8 BDD.
*Gates* : UT 4493/0, FT 142/1 (1 préexistant RichToc page 73), 21 runners
Cucumber 0 fail, validatePlugins VERT, publishToMavenLocal VERT, garde 3/3.
*Fichiers* : 2 prod créés (`MatterPolicy`, `MatterPolicyMode`) + 2 BDD infra +
FT + feature ; `AssembleBookTask`/`BookAssembler`/`BookLayout`/`BookValidator`/
`BookDsl`/`DocumentExtension`/`BookConfig`/`DocumentConfigSerializer`/
`SerializeDocumentConfigTask`/`DocumentPlugin`/`build.gradle.kts` modifiés.

=== Session 260 (2026-09-24) — EPIC DOC-BOOK-IMAGES ✅ (B4/B5)

*Type* : feat DDD/TDD/BDD + dogfooding + maintenance cross-borough
*Score* : 10/10
*Livrables* :
- US-1 — illustration structurée : `photosDir` consommé en mode structuré,
  `:imagesdir:` émis, le livre est illustré par les scans.
- US-2 — ghost repair : les 4 directives `image::` fantômes OCR sont
  substituées par le scan de leur page (ou supprimées) → EPUB epubcheck-VALID.
- US-3 — BDD `@book-images` 6 scénarios + 20e runner `bookImagesCucumberTest`.
- Domaine pur : `BookImageResolver`, `BookImageRewriter`, `BookPageRenderer`.
- Mécanique prouvée : AsciidoctorJ n'embarque que les images DANS le répertoire
  source → copie des scans référencés dans `images/` + `:imagesdir:` relatif.
- Maintenance : drift catalogue ws 0.0.43→0.0.56 réaligné (piège #13, 5e
  occurrence, stash-prouvé préexistant).
*Tests* : 11 UT `BookImagesTest` + 6 UT `ContentPageResolverImagesTest` + 3 UT
layout/assembler + 4 FT TestKit + 6 BDD.
*Gates* : UT 4130/0, FT 138/1 (1 préexistant RichToc page 73), 20 runners
Cucumber 0 fail, validatePlugins VERT, publishToMavenLocal VERT, garde 3/3 ;
dogfooding FPA : EPUB **VALID** (STRICT), 4 scans copiés (13 Mo / 525 Mo).
*Fichiers* : 3 prod créés + 3 objets tests + BDD infra + FT + cadrage +
gouvernance ; `AssembleBookTask`/`BookAssembler`/`BookLayout`/
`ContentPageResolver`/`build.gradle.kts`/`settings.gradle.kts` modifiés.

=== Session 259 (2026-09-24) — EPIC DOC-BOOK-CONSISTENCY ✅ (B1/B2)

*Type* : feat + TDD + dogfooding (correction issues code review S-258)
*Score* : 10/10
*Livrables* :
- B2 — `assembleBook.outputFile` = `docs/document/$outputFileName.adoc`
  (défaut `book`) + `enrichDocument.outputFile` =
  `docs/document/$outputFileName-enriched.adoc` (défaut `document`) :
  fin des hardcodes, le livre assemblé est indexé N3 sous le bon nom.
- B1 — dogfooding réel HEAD (mavenLocal, TOC riche 116 sections) :
  `livre.adoc` produit directement (4600 lignes / 116 ancres),
  `collectDocumentRetrieve` indexe 5 artefacts.
*Tests* : `BookOutputFileNameFunctionalTest` 4/4 (TDD RED 3/4 → GREEN 4/4).
*Gates* : compileKotlin VERT, FT livre + collector VERT (1 échec
`RichTocTest` page 73 stash-prouvé préexistant), UT book VERT, 4 runners
Cucumber VERT.
*Fichiers* : `DocumentPlugin.kt` (2 dérivations), FT créé, INDEX/BACKLOG/
PROMPT_REPRISE/SESSIONS_HISTORY/TEST_COVERAGE + rapport session +
code review S-258 + déplacement `.agents/` racine (git mv ×2).

=== Session 258 (2026-09-24) — Code review pipeline livre ✅

*Type* : code review + cadrage (lecture seule, zéro code)
*Score* : 10/10
*Livrables* :
- Verdict : pipeline livre/OCR fonctionnel de bout en bout (dogfooding FPA réel).
- 6 anomalies (B1-B6) + chemin résiduel vers un livre fini.
- 3 EPICs cadrés : `DOC-BOOK-CONSISTENCY` (B1/B2), `DOC-BOOK-IMAGES` (B4/B5),
  `DOC-BOOK-MATTER` (B3).
*Fichiers* : `.agents/CODE_REVIEW_BOOK_PIPELINE_S258.adoc` + INDEX/BACKLOG/
PROMPT_REPRISE.

=== Session 257 (2026-09-17) — RENAME Vague 4 ✅ (champ lexical métier complet)

*Type* : refactor + gouvernance (cross-borough depuis Queens mission 4)
*Score* : 10/10
*Livrables* :
- Transfusion `training/.agents/TRANSFER_LEXIQUE_FORMATION.adoc` § 1bis
  (convention page scannée `ContentPageResolver` + 5 raisons
  `BookOcrFailureDetector`) + § 2 complète.
- Neutralisation docs courantes : AGENT, BACKLOG, INDEX, ARCHITECTURE_BOUNDARY.
- Neutralisation fixtures synthétiques (11 fichiers) : `Devenir Formateur`
  → `Introduction`/`Content Book`, `formation.adoc`→`document.adoc`,
  `Livre de Formation`→`Livre de Contenu`, `formations/`→`contents/`.
*Gates* : compile VERT, 81 UT 0 FAIL, `ocrQualityCucumberTest` 8/8, 2 FT
corpus PASSED, gate Vague 4 `src/` zéro + docs courantes zéro. Échec
`RichTocTest` page 73 stash-confirmé préexistant (drift corpus A1).
*Fichiers* : 18 (2 docs gouvernance ×2 sync + 14 src/docs + rapport).

=== Session 256 (2026-09-09) — Audit RENAME ✅ CADUQUE (zéro résidu métier privé)

*Type* : gouvernance pure (audit de vérification — arbitrage pilote S-255)
*Score* : 10/10

*Résumé* :
- Mission : re-dérouler la gate D16 du cadrage RENAME Vague 3
  (`grep -rwn "FPA\|REAC\|AFNOR\|SPG\|SPD" src/`) + sweep exhaustif des
  résidus restants (commentaires, KDoc, fixtures, noms de tests, feature
  files, README) dans document — verdict attendu : CADUQUE si zéro résidu,
  sinon cadrage RENAME-13.
- Gate D16 src/ : **10 matches, TOUS faux positifs piège #18** — chemins
  corpus `office/metiers/FPA` légitimes (à conserver) dans 8 fichiers de
  test fonctionnel/audit dogfooding.
- Sweep exhaustif : insensible casse = zéro, noms de fichiers = zéro,
  src/main KDoc/commentaires = zéro, feature files = zéro, README = zéro,
  build/settings/wrapper/catalogue = zéro, `.github/` absent.
- Docs gouvernance : mentions FPA/REAC/AFNOR/SPG/SPD légitimes uniquement
  (EPIC DOC-FPA-PUBLISH réel GBL-003.6, historique Vagues RENAME,
  rapports de session / archives — non renommables).
- Verrou REAC `AssembleBookTaskContentRichTocTest.kt:100` vérifié corrigé
  depuis S-213 (`"enquête terrain"` corps OCR p.22, plus discriminant).
- **Verdict : ✅ CADUQUE — aucun cadrage RENAME-13 nécessaire.** Gate D16
  confirmée VERTE sur document, EPIC RENAME Vague 3 reste ✅ TERMINÉ.
- Gouvernance pure : zéro code modifié, zéro bump BOM, zéro secret, zéro
  publication. Baseline S-255 préservée par construction (UT 3822/0,
  FT 130/130, 19 runners 3068/0, garde 3/3).
- DOC-FPA-PUBLISH-0 : STANDBY inchangé (A1 corpus 16 ILLISIBLE).

=== Session 255 (2026-09-09) — Sync catalogue ws 0.0.37 (drift cross-borough)

*Type* : maintenance (réalignement drift, pattern S-246/S-251)
*Score* : 10/10

*Résumé* :
- Drift MEMPHIS attrapé par la garde `DocumentPluginPublicationTest`
  (RED prouvé avant fix — discipline TDD). 4e occurrence du cycle
  piège #13 (S-244 découverte, S-246, S-251, S-255).
- Réalignement 3 sources : settings D4 `workspace-catalog:0.0.37` +
  platform `workspace-bom:0.0.37` + toml local.
- `document-plugin` reste 0.0.17 (catalogue MEMPHIS pinnait déjà 0.0.17).
- Diff BOM 0.0.36→0.0.37 : codebase-plugin 0.0.12→0.0.14, zéro impact.
- Gates : garde 3/3, mavenLocal VERT (POM platform 0.0.37), UT 3822/0,
  FT 130/130, 19 runners 3068/0, `check` VERT. Zéro publication.

=== Session 254 (2026-09-09) — RENAME Vague 3 CLÔTURÉE 6/6 US (transverse)

*Type* : rename transverse (planner/codebase/plantuml/hyperframes)
*Score* : 10/10

*Résumé* :
- RENAME-7 planner : prompts LLM génériques (`IntentionPlanner.kt:149,208`)
  + 6 fixtures neutralisées. Gates : grep = 0, `test` 144 VERT,
  `cucumberTest` 23 VERT (RED réel attrapé sur verrou "codex corpus").
- RENAME-10 codebase : description tâche + 9 fixtures koog, exclusion
  D11 keyword AFNOR documentée, `test` 10602 (4 échecs préexistants
  S-212), classes ciblées VERTES, commit sélectif (WIP CB-FPA-RAG exclu).
- RENAME-11 plantuml : KDoc + 5 fixtures registres publics + docs
  courantes (AGENT/BACKLOG/PROMPT_REPRISE/strategie_orange_money) +
  drift ws 0.0.37 attrapé par la garde plantuml → réaligné.
  `test` 2531/0.
- RENAME-12 hyperframes : BACKLOG + doc/PILOTAGE + doc/ARCHITECTURE +
  AGENT règle 9. compileKotlin VERT.
- Gate transverse D16 VERT 5 boroughs. EPIC RENAME Vague 3 TERMINÉ
  6/6 US (7/8/9/10/11/12). Pièges #25/#26 documentés PROMPT_REPRISE.
- Zéro publication — chaînes non-significatives distribuées aux
  prochains bumps de chaque borough.

=== Session 251 (2026-09-08) — RENAME-2 + drift ws 0.0.34 + OCR-QUALITY-3

*Type* : feat + refactor + TDD/BDD
*Score* : 10/10

*Résumé* :
- RENAME-2 : renommage dur `FpaPageResolver` → `ContentPageResolver`,
  `fpaAwareResolver` → `contentAwareResolver` (+ câblage), 5 fichiers
  prod commentaires neutralisés, 9 fichiers tests git mv, knob
  `fpa.book.publish` → `content.book.publish`, 3 features Gherkin,
  README. Gate grep OK (citations office/ légitimes + faux positif
  LeafPath).
- Drift catalogue ws 0.0.34 attrapé par la garde S-244 (piège #13) —
  réaligné 3 sources, pattern S-246/S-249 réappliqué.
- OCR-QUALITY-3 : 3 raisons (IMAGE_MISSING/TABLE_SUSPECT/STRUCT_SUSPECT)
  + `BookOcrIssue.detail` backward compatible + detector multi-issues +
  rapport `detail` conditionnel. 11 UT TDD + 7 scénarios BDD
  (`ocr_quality.feature`, 19e runner `ocrQualityCucumberTest`).
- Gates : `check` VERT, test 3801/0, FT 130/130. Zéro publication.

=== Session 249 (2026-09-07) — PUB 0.0.16 : distribution des 6 gardes hors machine de dev

*Type* : PUB cross-borough MEMPHIS + document + bakery (accord pilote)
*Score* : 10/10

*Résumé* :
- MEMPHIS : bump toml BOM 0.0.32 + document 0.0.16, gardes réalignées
  (PlatformCatalogAlignment + WorkspaceCatalogStructure, 111/0), Central
  VERT — propagation repo1 `<latest>0.0.32</latest>` ×2.
- document : re-pin D4/D5 (garde RED sur split-brain toml local — rôle
  S-244 confirmé), version dérivée 0.0.16, garde publication 3/3.
- **Rejet Central 1er upload** (`Dependency version information is
  missing` ×15) → fix pattern S-209 : 12 deps `implementation` littérales
  → alias `libs.*` + catalogue local enrichi (jackson 2.21.2, pdfbox
  3.0.4) → POM toutes deps versionnées → publication VERT 25s,
  propagation POM HTTP 200 poll 4, JAR Central 2 classes pdf-check.
- Consommateur tiers 0.0.16 depuis Central SANS mavenLocal : résolution
  `Gradle Central Plugin Repository` prouvée, STRICT VALID + STRICT fail
  textless — drift S-248 résolu par construction.
- bakery : pin ws 0.0.32 + platform 0.0.32 → document 0.0.16 sans
  hardcode (compileKotlin VERT).
- Piège #17 (POM deps version explicite) + #18 (2 gardes MEMPHIS en
  cascade) documentés. Commits : MEMPHIS `988fda5`, document `31010ac`,
  bakery `343aeaa`.

=== Session 248 (2026-09-07) — Preuve consommateur 6e garde PDF + diagnostic drift Central ↔ mavenLocal

*Type* : vérification consommateur externe (zéro code modifié)
*Score* : 10/10

*Résumé* :
- Besoin réel : la 6e garde `pdfCheck` (S-247) n'avait jamais été prouvée
  consommable par un projet externe (preuves S-239/S-242/243 antérieures
  à S-247). Pattern S-239 rejoué : projet tiers `/tmp/opencode/doc-consumer-pdf`
  (Gradle 9.7.1, JDK 25).
- *4/4 scénarios VERTS* (résolution mavenLocal prouvée par debug log) :
  STRICT convert+validate → rapport VALID ; STRICT sur PDF sans texte →
  BUILD FAILED avec 2 findings `page N: no extractable text` + rapport
  INVALID ; LENIENT → vert + rapport INVALID ; OFF → skip (rapport stale
  inchangé, sémantique epub) ; knob `outputFileName` suivi (miroir
  S-235/236) → `<pdf-file-missing>` sur custom absent / VALID sur converti.
- *Constat clé (piège #16)* : le jar Central 0.0.15 (publié S-241, AVANT
  la garde S-247) ne contient PAS la garde (0 classe) ; le jar mavenLocal
  0.0.15 (republié S-247) la porte (2 classes + câblage javap).
  `publishToMavenLocal` met à jour l'artefact local sous le MÊME numéro —
  un consommateur DOIT déclarer `pluginManagement { repositories {
  mavenLocal() } }` AVANT le Plugin Portal pour tester le HEAD local,
  sinon Gradle résout silencieusement depuis Central.
- Zéro code document modifié (économie d'encre). Publication 0.0.16 non
  demandée (elle alignera Central sur mavenLocal). Central aligné
  (catalog 0.0.31 = pin local). Secrets RAS.

=== Session 247 (2026-09-07) — EPIC DOC-PDF-CHECK : 6e garde, validation structurelle du PDF

*Type* : feat + TDD/BDD (4/4 US, ~12 pts, 1 session)
*Score* : 10/10

*Résumé* :
- Besoin réel remonté : le livre FPA est publié en PDF stagiaire sans
  aucune garde (symétrie des 5 gardes brisée) — option 1 du
  PROMPT_REPRISE 246-247.
- *US-1 domaine pur* : `document.pdf` (miroir `document.epub`) —
  Mode/Result/Validator port + Report + adapter `PdfBoxValidatorAdapter`
  (PDFBox 3.0.4 BOM-constraint, zéro bump). RED → GREEN 16/16 UT.
- *US-2 tâche + DSL* : `validateDocumentPdf` + `converter { pdfCheck }` +
  mirror + CLI (défaut OFF). RED → GREEN 4/4 + baseline FT 130/130.
- *US-3 BDD* : `pdf_check.feature` 4 scénarios + runner 18e dédié
  `pdfCheckCucumberTest` — 4/4 PASSED.
- *US-4 dogfooding* : le VRAI `book.pdf` FPA (15 pages) VALID ;
  preuve négative pages-sans-texte. *Constat* : PDFBox lénient sur les
  octets (header corrompu → warning + charge quand même) — le garde est
  texte extractible par page.
- Gates : test 3511/0, validatePlugins VERT, mavenLocal VERT (platform
  0.0.31 + pdfbox runtime), secrets RAS. Publication 0.0.16 non demandée.

=== Session 243 (2026-09-01) — Re-vérification backlog vide + consommateurs 0.0.15 + couverture de test

*Type* : gouvernance + vérification consommateurs (zéro code modifié)
*Score* : 10/10

*Résumé* :
- Mission pilote S-243 : (1) ce qui reste dans BACKLOG/vision/roadmap,
  (2) si vide → consommateurs OK sur la dernière version + couverture
  de test passe.
- *Backlog VIDE* : audit `grep` marqueurs périmés → zéro match (2e fois
  après S-242). Options résiduelles CADUQUES sans besoin réel (composite
  EPUB N3, garde PDF FPA).
- *Gates document re-exécutées VERTES* : UT 3039/0, FT 126/126,
  `cucumberTest` global 0 failure (10m43s), `n3PipelineCucumberTest` VERT.
- *bakery consommateur* : compileKotlin VERT, `test` 1668/4 préexistantes
  S-241, FT 16/16, `cucumberTest` 1 failure préexistante de plus
  (`JBake native roundtrip` — test périmé vs comportement S206
  summary/description traduits depuis `88f43c1`, **prouvée par sonde
  temporaire 0.0.12 : échoue identiquement**, jamais commitée) + timeout
  5m qui a coupé le run (319 skipped). Pas une régression 0.0.15.
- *Session concurrente bakery détectée* : `libs.versions.toml`
  (0.0.8→0.0.9) + `build.gradle.kts` (version dérivée du catalogue)
  modifiés pendant le run — pas cette session ; bakery-plugin 0.0.9 déjà
  publiée Central. Laissé en l'état (clôture bakery = sa session).
- runner-gradle : zéro couplage direct document (consomme bakery 0.0.9).
- FPA : init-script + CLI `-Pdocument.*`, 0.0.15 mavenLocal + Central
  (`<latest>0.0.15</latest>`).

=== Session 242 (2026-09-01) — Clôture backlog + consommateurs 0.0.15

_Voir `.agents/sessions/242-verification-consommateurs.adoc` (détail
complet) — audit backlog zéro match, restauration pattern S-006,
consommateurs 0.0.15 vérifiés, 4 failures bakery prouvées préexistantes._
== Session 245 — 2026-09-07 — Synchronisation gouvernance post-S-244

Gouvernance pure (zéro code) :
* PROMPT_REPRISE rafraîchi (mission 245, état code S-244 ws 0.0.30,
  ROLLOUT-2→5 marqués livrés hors périmètre document).
* Baseline S-244 préservée (test 3272/0 + FT 126/126).

== Session 246 — 2026-09-07 — Sync catalogue ws 0.0.31 (garde S-244 RED→GREEN)

Maintenance cross-borough MEM-CAT-ROLLOUT :
* Drift catalogue ws attrapé par la garde `DocumentPluginPublicationTest`
  (créée S-244) — MEMPHIS a bumpé le catalogue en amont 0.0.31 (S-032)
  pendant que document pinne 0.0.30.
* Fix 2 pins : settings `from(...0.0.31)` (D4) + platform BOM 0.0.31.
* `document-plugin` reste 0.0.15 (inchangé, version dérivée du catalogue).
* Gates rétablies : test 3272/0 + FT 126/126 + epubCheck 4/4 +
  mavenLocal VERT (POM platform 0.0.31).
* Zéro publication Central (0.0.15 POM Central reste auto-consistant).

== Session 252 — 2026-09-08 — Dogfooding convergence + fix IMAGE_MISSING inline

Dogfooding réel + fix TDD :
* Livre FPA régénéré avec le HEAD (RENAME-2 + OCR-QUALITY-3 + 6 gardes)
  — commande canonique étendue `bookPipeline validateDocumentEpub
  validateDocumentPdf fpaPublish` (2 runs BUILD SUCCESSFUL).
* Bug réel découvert : détecteur IMAGE_MISSING block-only (`image::`)
  ignorait la macro inline `image:file[]` (p.61 + p.83_1 invisibles).
* Fix baby-step TDD : RED UT → GREEN `image::?` (1 ligne) → BDD
  `@inline-image` (ocrQuality 8/8).
* fpaPublish déplace `pdf-validation-report.json` + README tools.
* Convergence : 16 ILLISIBLE + 4 IMAGE_MISSING + 7 TABLE_SUSPECT + PDF
  VALID — rapport prêt pour l'itération humaine A1 Tombouctou.
* Dette S-082 clôturée : consumer racine bumpé 0.0.16 (Central).
* Gates : check VERT 19m56s, UT 3820/0, FT 130/130, cucumber 3055/0.

=== Session 253 (2026-09-09) — RENAME-8 ✅ config injectée D14

*EPIC RENAME Vague 3 (cadrage S-250)* — borrowedVocabulary sorti du
nommage public :
* Liste hardcodée REAC/AFNOR/DC/TS/RNCP/CP/ECF supprimée de
  `PlantUmlClassifier` + `PlantUmlTranslationAdapter`.
* D14 config injectée : `PlantUmlBlock(raw, borrowedVocabulary =
  emptySet())` + `PlantUmlTranslationAdapter(..., borrowedVocabulary =
  emptySet())` (paramètre additif fin de signature — bakery 0.0.17
  publié compile sans changement).
* 12 fixtures neutralisées (REF/ORG/MOD/EVA + injection explicite).
* Baby-step TDD strict : RED compile fail ×5 → GREEN 16/16 + 12/12.
* Gates : UT 3822/0, FT 130/130, cucumber plantuml 10/10,
  validatePlugins VERT, grep word-boundary = 1 citation corpus réel
  (piège #18).
* Zéro publication — distribuée au prochain bump.

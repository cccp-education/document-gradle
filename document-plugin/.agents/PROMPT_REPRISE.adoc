= PROMPT_REPRISE — document-gradle (New Orleans 🟢)

== Mission session 224

EPIC DOC-XREF-VALIDATE — validation des références croisées AsciiDoc (`<<id>>` / `xref:id[]`)
avant publication du livre. Suite directe de DOC-CR3-2 (safeMode) + DOC-CR4 (includeGuard)
+ DOC-CR5 (security policy) : complète la solidité documentaire en auditant la navigation interne.

*Livré (4/4 US, EPIC CLÔTURÉ)* :
- DDD `document.xref` : `XrefValidationMode` (OFF/STRICT/LENIENT), `XrefValidationResult`
  sealed (Valid/Invalid), `XrefValidator` (extractAnchors/extractReferences/validate, regex
  sur texte brut, IDs `[A-Za-z0-9_][A-Za-z0-9_.-]*`), `XrefValidationReport` (toJson/fromResult).
- Câblage `ConvertDocumentTask` : valide avant conversion, sévérité (STRICT fail-fast /
  LENIENT warn + rapport JSON / OFF skip, défaut OFF backward-compat).
- DSL `converter { xrefValidation = ... }` + mirror flat `xrefValidation` + CLI `-Pdocument.xrefValidation`.
- Tâche dédiée `validateDocumentXref` (groupe `document`, rapport
  `build/docs/document/xref-validation-report.json`).
- Cucumber `@xref-validation` (5 scénarios) + runner `XrefValidationCucumberRunner`
  + steps `XrefValidationSteps` + tâche `xrefValidationCucumberTest`.

*Tests* : 12 UT `XrefValidatorTest` + 4 UT `XrefValidationReportTest` + 7 FT
(`ConvertDocumentTaskXrefValidationFunctionalTest` 4 + `ValidateDocumentXrefTaskFunctionalTest` 3)
+ 5 BDD. Tous GREEN. 6 runners Cucumber dédiés GREEN.

*Note ENV* : `DocumentConverterTest` PDF/EPUB + `FpaBookArtifactGenerationTest` échouent sur
`LoadError asciidoctor-pdf` / `asciidoctor-epub3` (gems Ruby absents) — préexistant S-213,
hors périmètre xref, aucune régression liée.

== Règles absolues (AGENT.adoc)

- English only dans le code/commentaires/tests (gouvernance .adoc en français).
- Baby-step DDD/TDD/BDD strict (RED → GREEN → REFACTOR), économie d'encre.
- Zéro secret, zéro commit/push sans accord explicite.
- `bookPipeline` lit l'AsciiDoc sur disque en lecture seule (boundary Codex READ / Document WRITE+PUBLISH).

== État du code (S-224)

- DOC-CR3 ✅ / DOC-CR4 ✅ / DOC-CR5 ✅ / DOC-XREF-VALIDATE ✅ TERMINÉS (4/4 US).
- Infra Cucumber borough ✅ résolue (S-223).
- 9e tâche `document` enregistrée : `validateDocumentXref` (les 8 historiques + celle-ci).

== À vérifier en ouverture

1. `git status` (document-gradle + office/metiers/FPA).
2. `JAVA_HOME=/home/cheroliv/.sdkman/candidates/java/25.0.4-tem` (JDK 25 obligatoire).
3. `./gradlew xrefValidationCucumberTest includeGuardCucumberTest securityPolicyCucumberTest converterSafeModeCucumberTest` VERT.
4. Prochain EPIC produit : DOC-TABLE hardening / PLANTUML validation hardening, ou dogfooding
   livre FPA SERVER (HTML/PDF/EPUB navigables sur corpus FPA réel — voir S-217/218).

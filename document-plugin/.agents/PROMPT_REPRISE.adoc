= PROMPT_REPRISE — document-gradle (New Orleans 🟢)

== Mission session 267

S-266 (2026-09-25) a réparé la **dérive de gouvernance** : le PROMPT_REPRISE
était figé à la mission S-261 alors que S-261→S-265 étaient clôturées, et les
tables sessions de `SESSIONS_HISTORY.adoc` (13 entrées) et `INDEX.adoc`
(14 entrées) étaient incomplètes. Aucun code modifié — réparation documentaire
pure.

*1. Rien d'urgent* — le borough est à jour et entièrement clôturé :
- **0.0.18 publiée Central** (S-264, bundle `aab4b880`), catalogue ws **0.0.57**.
- Toutes les dettes B1→B6 de la code review S-258 sont **soldées**
  (CONSISTENCY B1/B2 S-259, IMAGES B4/B5 S-260, MATTER B3 S-261,
  CONSISTENCY-B6 S-262, FIDELITY S-263).
- Garde publication **4/4** (S-265), `compileKotlin` VERT, `validatePlugins` VERT.
- Baseline de référence : UT ~4827/0, FT 144/1 (1 préexistant RichToc page 73),
  22 runners Cucumber dédiés 0 fail.

*2. Candidats prochaine session (sur demande pilote)* :

- *DOC-FPA-PUBLISH-0 (1 pt) — STANDBY* : republication multi-format du livre
  FPA après convergence du corpus A1 (Tombouctou). **Bloqué** : le corpus porte
  encore 16 pages `[ILLISIBLE]` + 14 tronquées + 11 tableaux (rapport A1
  `rapport_generation.adoc`, 2026-09-17). Déclencher seulement quand A1 converge.
- *Drift catalogue ws* : **6e occurrence résolue S-264** (0.0.56→0.0.57). Si
  MEMPHIS bump de nouveau, la garde `DocumentPluginPublicationTest` RED au build
  (fix : réaligner les 3 sources settings/platform/toml sur la version publiée).
- *Re-pin bakery ws 0.0.57* : **✅ DÉJÀ FAIT** (2026-09-25) — `bakery-plugin`
  pinne le catalogue ws 0.0.57 + platform 0.0.57, `document-plugin` sans
  version (constraint BOM → 0.0.18). Cet item des prompts S-263→S-265 est
  **CADUQUE**.
- *FT RichToc page 73* : **préexistant** (stash-confirmé S-257/S-259/S-260/S-262/
  S-263), drift corpus A1. Ne PAS « corriger » le test avant que le corpus
  converge (le test asserte le corps OCR réel).
- *Nouveau cadrage EPIC* : aucun EPIC ouvert. Sur demande pilote uniquement.

== État du code (S-265)

- *0.0.18 PUBLIÉE* Central — POM/JAR/plugin-marker HTTP 200. Le HEAD porte
  RENAME-8 + Vague 4 + B1/B2 + DOC-BOOK-IMAGES + DOC-BOOK-MATTER +
  DOC-BOOK-CONSISTENCY-B6 + CROSS-DOC-BOOK-FIDELITY + pins catalogue ws 0.0.57.
- *Catalogue ws* : **0.0.57** (document-plugin = 0.0.18).
- *Consumer racine* : `document = "0.0.18"` sans mavenLocal (Central pur).
- *Gates document* : UT ~4827/0, FT 144/1 (1 préexistant RichToc page 73), 22
  runners Cucumber dédiés (0 fail), garde publication **4/4**, `validatePlugins`
  VERT. Toutes les suites ne sont pas rejouées à chaque session (règle
  delta-source) — les chiffres FT/UT datent de S-263.
- *Pipeline livre* : structuré (TOC → `BookTreeBuilder` → `BookAssembler`),
  illustré (scans référencés copiés dans `images/` + `:imagesdir:`), navigable
  (`<<ref,title>>` opt-in), matter dérivable (`MatterPolicyMode.DERIVED`),
  **EPUB epubcheck-VALID** prouvé STRICT sur le corpus FPA.
- *EPICs* : DOC-BOOK-CONSISTENCY ✅, DOC-BOOK-IMAGES ✅, DOC-BOOK-MATTER ✅,
  DOC-BOOK-CONSISTENCY-B6 ✅, CROSS-DOC-BOOK-FIDELITY ✅, DOC-PUB ✅ 0.0.18.
- *Cadrages* : `CODE_REVIEW_BOOK_PIPELINE_S258.adoc`,
  `CADRAGE_EPIC_DOC_BOOK_IMAGES.adoc`, `CADRAGE_EPIC_CROSS_DOC_BOOK_FIDELITY.adoc`.

== Pièges Gradle à retenir (S-232..S-265)

1. *Snapshot collect* : `collectDocumentRetrieve` re-scan à chaque exécution
   (`outputs.upToDateWhen { false }`) — pas de `@InputFile` du rapport.
2. *mustRunAfter inverse la CLI* : conflit d'inputs si 2 tâches partagent un build.
3. *Chaîne N3 staged* : produce → validate → collect (3 builds).
4. *Property DSL sans convention reste unset même derrière `.orElse(...)`*.
5. *Dérivation knob sans dépendance de tâche* : jamais provider vers
   `outputFile` du producteur, jamais `.get()` configuration-time.
6. *`@InputFile` valide l'existence AVANT l'action* — porter les chemins
   audités en `@Input @Optional Property<String>`.
7. *`--tests` Gradle ne réduit PAS la découverte Cucumber* — filtrer côté
   annotation du runner (`FEATURES_PROPERTY_NAME`).
8. *Imports DSL consommateur* : les enums des gardes ne sont pas résolus
   implicitement — importer explicitement (liste dans README.md).
9. *epubcheck callback Java → null* : signature nullable obligatoire.
10. *Preuve négative : corrompre la STRUCTURE, pas le contenu* (EPUB : mimetype
    OCF ; PDF : les octets sont tolérés — texte extractible par page).
11. *Polling Central* : `<latest>` peut rester périmé ~27 min — tester aussi le
    HTTP 200 du POM versionné.
12. *Test bakery JBake native roundtrip périmé* : préexistant — à mettre à jour
    côté bakery.
13. *Drift catalogue ws* : MEMPHIS bump le catalogue en amont — la garde
    `DocumentPluginPublicationTest` RED au prochain build. Fix : réaligner les
    3 sources vers la version publiée Central. Occurrences : S-244 (0.0.30),
    S-246 (0.0.31), S-251 (0.0.34), S-255 (0.0.37), S-260 (0.0.56), S-264 (0.0.57).
14. *PDFBox 3.x lénient sur les octets* : le garde PDF est un garde *texte
    extractible par page*. Preuve négative = pages sans texte.
15. *`PDPageContentStream.setFont(PDFont, float)` 2 params* — le size manquant.
16. *Drift jar Central ↔ mavenLocal sous le MÊME numéro* :
    `publishToMavenLocal` met à jour l'artefact local sans changer la version —
    un consommateur DOIT déclarer `pluginManagement { repositories { mavenLocal() } }`
    AVANT le Plugin Portal pour tester le HEAD local.
17. *Central exige une version explicite par dépendance POM* : alias `libs.*`
    avec version.ref dans le catalogue local du borough.
18. *Faux positif grep "fpa"* : `LeafPath` contient littéralement "fpa". Les
    chemins `office/metiers/FPA/...` sont des citations légitimes du corpus privé.
    Idem "REAC" (`AssembleBookTaskContentRichTocTest`).
19. *Macro image AsciiDoc 2 formes* : block `image::f[]` ET inline `image:f[]`
    — couvrir les deux (`image::?`).
20. *Dogfooding HEAD via mavenLocal* : `publishToMavenLocal` + consumer
    résolvant mavenLocal en tête = dogfooding sans publication Central.
21. *Central immutable × deployment concurrent* : coordonner MEMPHIS avant bump.
22. *Pin POM BOM divergent = zéro impact si deps versionnées*.
23. *Fake de test doit matcher le contrat sémantique* : choisir le fake selon
    le comportement LLM réel à prouver.
24. *Paramètre additif fin de signature* : toute extension d'API publique suit
    ce pattern (backward-compat binaire).
25. *Verrous RENAME = comportement, pas fixtures nominales*.
26. *WIP parallèle dans le working tree* : commit UNIQUEMENT ses fichiers
    (`git add` sélectif), jamais `git add -A`.
27. *Vague 4 = champ lexical complet, pas les sigles*.
28. *Transfuser AVANT de neutraliser* (RENAME).
29. *Gate Vague 4 : faux positifs + citations corpus*.
30. *AsciidoctorJ n'embarque que les images DANS le répertoire source* :
    absolu → RSC-007 silencieux ; relatif sortant → RSC-026. Copier les scans
    référencés dans `images/` + `:imagesdir: images` relatif.
31. *Résolution image par PRÉFIXE numérique, pas par nom complet* (corpus FPA
    mêle `61.adoc` et `001-introduction.adoc`).
32. *Ne copier que les scans RÉFÉRENCÉS* (Économie d'Encre : 13 Mo, pas 525 Mo).

== Anti-glue Cucumber (règle de propriété — S-238)

Chaque feature a UN runner propriétaire. Le runner global (glue
`document.scenarios`) ne découvre que `document.feature` +
`release-notes.feature` + `frontmatter_retranslate.feature`. Toute nouvelle
feature doit soit rejoindre un runner dédié (runner + glue + tag +
CucumberTaskSpec dans build.gradle.kts), soit explicitement rejoindre le global
avec ses steps dans `document.scenarios`. Le 22e runner dédié est
`bookNavigationCucumberTest` (S-262).

== Méthode

Baby-step DDD/TDD/BDD strict (RED → GREEN → REFACTOR), économie d'encre.
CONTRAINTE GLOBALE : *Zéro Python* — outputs (XML JUnit, rapports, logs)
analysés avec Read/Grep/xmllint/rapports Gradle natifs uniquement.

== Règles absolues (AGENT.adoc)

- English only dans le code/commentaires/tests (gouvernance .adoc en français).
- Baby-step DDD/TDD/BDD strict — UT (domaine pur) → FT (TestKit) → BDD (runner dédié).
- Zéro secret, zéro commit/push sans accord (accord pilote donné à l'ouverture S-266).
- `bookPipeline` lit l'AsciiDoc/HTML/PDF/EPUB sur disque en lecture seule
  (boundary Codex READ / Document WRITE+PUBLISH).
- Ne jamais lancer `./gradlew test` complet sans permission.

== À vérifier en ouverture

1. `git status` (document-gradle).
2. `JAVA_HOME=/home/cheroliv/.sdkman/candidates/java/25.0.4-tem` (JDK 25).
3. `./gradlew compileKotlin` VERT (dans `document-plugin/`).
4. Central : `document-plugin` `<latest>` = 0.0.18 ; `workspace-catalog`
   `<latest>` = 0.0.57 ; `workspace-bom` `<latest>` = 0.0.57 (vérifier
   `<latest>` avant tout pin, pièges #13/#21).
5. Baseline : garde publication 4/4 ; si besoin de rejouer, UT ~4827/0, FT
   144/1 (1 préexistant RichToc page 73), 22 runners Cucumber dédiés VERT.

== Vision « livre content SERVER »

Verrous levés S-233..S-264 : statut N3 (`validationStatus=PASS`), knob
`outputFileName` de bout en bout, gems PDF/EPUB packagées, consommation externe
prouvée depuis Central (0.0.17 puis 0.0.18 sans mavenLocal), 6 gardes
documentaires, rapport OCR exhaustif, neutralisation nommage métier privé, livre
illustré par les scans et **EPUB epubcheck-VALID**. Le produit est servable,
auditable, documenté, illustré et **distribué** de bout en bout.

*Chemin résiduel vers un livre « fini »* : itération humaine OCR (16 → 0
ILLISIBLE, 13 tableaux, 4 invalid part) côté Tombouctou A1 — `DOC-FPA-PUBLISH-0`
STANDBY.

= TEST_COVERAGE_ANALYSIS — document-gradle (New Orleans 🟢)
:date: 2026-08-31
:session: 241

== Session 241 — Dogfooding EPUB FPA + README consommateur

*Objectif* : prouver la 5e garde sur l'artefact réel (dogfooding) et
documenter les imports DSL côté consommateur (constat S-239).

*Nouveaux tests (+2)* :
- FT dogfooding `EpubCheckFpaFunctionalTest` (suite `test`, miroir du
  placement `HtmlLinkLintFunctionalTest` S-226) :
  . `real FPA book epub passes epubcheck` — lit le VRAI
    `office/metiers/FPA/.../livre-navigable/book.epub` (464 KB) en
    read-only (Rule 7, zéro régénération JRuby) et assert `Valid` via
    `LibEpubCheckAdapter` (même port que la tâche `validateDocumentEpub`).
  . `altered FPA book epub copy is reported Invalid - negative proof` —
    rebuild zip SANS l'entrée `mimetype` (obligatoire, première entrée non
    compressée OCF) sur une COPY temporaire → assert `Invalid` avec issues.
    Self-skips (`assumeTrue`) hors machine de dev.
- *Piège TDD retenu* : le flip d'octet naïf au milieu de l'archive est
  TOLÉRÉ par epubcheck (le zip reste lisible → `Valid`) — une preuve
  négative epubcheck doit corrompre la STRUCTURE (suppression `mimetype`).

*Baseline* : UT 3037 → 3039/0 (+2 dogfooding), FT 126/126 (122 S-238 + 4 FT
epub S-240), 17 runners Cucumber dédiés VERTS, `validatePlugins` VERT.

_Zéro code prod modifié — pure test + documentation (README.md)._

== Session 240 — DOC-EPUBCHECK : 5e garde (validation epubcheck de l'EPUB)

*Objectif* : fermer le dernier gap fonctionnel du BACKLOG (critère US-DOC-04
epubcheck, backlogged depuis S-005) — EPIC DOC-EPUBCHECK 3/3 US.

*Nouveaux tests (14 + 2 feature-fichiers)* :
- Unit (domaine pur + adapter) : 17 UT `document.epub.*` au total —
  4 UT `EpubCheckRunnerTest` (port + fake : file forwarded, issues portées,
  Valid sans issues, stateless) + 8 UT `EpubValidationReportTest`
  (fromResult Valid/Invalid, JSON NON_NULL, round-trip fromJson,
  dégradé silencieux blank/invalid JSON) + 5 UT `LibEpubCheckAdapterTest`
  (fichier manquant/répertoire → `<epub-file-missing>`, non-zip → Invalid,
  EPUB3 minimal construit à la main → Valid, adapter stateless).
  *Bug réel attrapé TDD* : callback Java `info(null, ...)` — NPE paramètre
  Kotlin non-null `feature`, fix signature nullable.
- Fonctionnel TestKit : 4 FT `ValidateDocumentEpubTaskFunctionalTest`
  (OFF skip sans rapport / STRICT absent → fail + `<epub-file-missing>` +
  rapport écrit / LENIENT absent → warn + build vert + rapport / STRICT
  + EPUB fraîchement converti → VALID).
- Cucumber BDD : `epub_check.feature` 4 scénarios `@epub-check` + 17e
  runner dédié `EpubCheckCucumberRunner` (glue `document.epubcheck`) +
  steps préfixés « epub-check » (anti-glue S-223) +
  `CucumberTaskSpec("epubCheckCucumberTest")` — 4/4 PASSED,
  0 failure / 0 error XML.

*Non-régression* : `compileKotlin` VERT, `validatePlugins` VERT,
`DocumentPluginFunctionalTest` BUILD SUCCESSFUL, UT port+rapport
préexistantes (12) inchangées et vertes. Zéro bump BOM (dep tierce
`org.w3c:epubcheck:5.2.1`).

== Session 237 — Option C : republication 0.0.14 Maven Central

*Objectif* : républier 0.0.14 sur Central (gems PDF/EPUB packagées runtimeOnly
S-233, knob `outputFileName` vivant S-235/236) pour les consommateurs externes.

*Zéro test modifié/créé* — session PUB pure (bump version 1 ligne + publishing).
Baselines inchangées et non ré-exécutées (économie d'encre — aucun code
production modifié, la baseline S-235/S-236 fait foi) :
- Unit : `test` VERT (dernier run S-234/S-236).
- FT : `functionalTest` 68/68 (baseline S-234).
- Cucumber dédiés : 16/16 runners, 146 scénarios PASSED (baseline S-236).
- Cucumber global : hors gate (82 failures glue scoping préexistantes —
  périmètre option D session 238).

*Gates publication* : `compileKotlin` VERT, `publishToMavenLocal` VERT
(6 artefacts + signatures), propagation Central vérifiée (HTTP 200 POM+JAR,
metadata `<latest>0.0.14</latest>`).

== Session 234 — Fix Option A : hygiène FT (convention `translation.outputFileName`)

*Objectif* : résoudre les 5 failures FT préexistantes S-233 (`translateDocument`
×2 + `TableValidationFunctionalTest` ×3) — cause racine : convention
`outputFileName` manquante sur TranslationDsl.

*Changement* : +1 ligne production (`ext.translation.outputFileName.convention("document")`,
DocumentPlugin.kt). *Zéro fichier test modifié/créé* — les FT existants
(écrits pour ce comportement) retrouvent leur statut VERT.

*Couverture restaurée (pas ajoutée)* :
- Fonctionnel : `functionalTest` 68/68 PASSED (baseline S-233 : 116 tests /
  5 failures ; delta -48 tests = les 48 tests translateDocumentBatch déjà
  skip/up-to-date comptés différemment en S-233, mêmes suites). Détail :
  `TableValidationFunctionalTest` 3/3 + `DocumentPluginFunctionalTest` 65/65.
- Unit : `test` VERT (6m59s, UP-TO-DATE au 2e run).
- Cucumber dédié : 15/15 runners, **144 scénarios PASSED** (`--rerun-tasks`,
  9m21s) — baseline S-233 (144) préservée exactement.
- Cucumber global (non-gate, preuve comparative) : 144 PASSED/86 FAILED sans
  fix (stash) → **148 PASSED/82 FAILED avec fix** — +4 = scénarios
  `@doc-translate` translateDocument du `document.feature`. Les 82 failures
  globales restantes préexistent (scoping glue runner global vs runners
  dédiés, connu S-221/S-223).

*Leçon TDD* : le RED a été prouvé par reproduction des failures existantes
(lecture du report XML — stack exacte « property 'outputFile' doesn't have a
configured value ») plutôt que par écriture d'un nouveau test — le test
d'acceptation existait déjà (les 2 FT translateDocument + 3 FT TableValidation),
il était juste ROUGE depuis S-231/233.

== Session 232 — DOC-VALIDATE-HTML-LINT : 4e garde composite (lint HTML rendu dans validateDocument)

*Objectif* : le lint HTML du rendu (`HtmlLinkLinter`, DOC-HTML-LINT S-226) rejoint
la validation composite en tant que 4e garde — symétrique du bloc `xref` (miroir
exact : sealed result → entry JSON). Domaine pur sans I/O, câblage tâche minimal
(même knob `converter { htmlLinkLint }`), BDD scénarios sévérité.

*Tests ajoutés S232* :
- `HtmlLintGuardTest.kt` — 10 UT TDD strict RED→GREEN (entry null en OFF / VALID
  liens résolus / DEAD + fragment listé + tri / DEAD en LENIENT visible en
  overallStatus FAIL / fromJson backward-compat sans bloc htmlLint / round-trip
  DEAD / toJson contient htmlLint / HTML absent → DEAD `<html-file-missing>`).
- `ValidateDocumentTaskHtmlLintFunctionalTest.kt` — 4 FT GradleTestKit (OFF skip /
  STRICT fail-fast DEAD + liste fragment / STRICT pass VALID sur navigable /
  LENIENT DEAD sans échec).
- `document_validation.feature` — +3 scénarios `@document-validation-html-lint`
  (STRICT fail DEAD+message / STRICT pass VALID / LENIENT DEAD sans échec).
- `DocumentValidationSteps.kt` — Given dédié `document-validationHtml config`
  (texte distinct anti-glue S-223), configs STRICT_HTML/LENIENT_HTML
  (safeMode SERVER — cohérence CR5, pattern STRICT_SERVER S-225), Whens
  `runs with the HTML conversion`, Thens `marks htmlLint DEAD listing` /
  `marks htmlLint VALID`.

*Couverture* :
- Unit : 37 UT domaine validation / 0 fail (Validator 8, ReportStatus 7,
  HtmlLinkLinter 12, HtmlLintGuard 10).
- Fonctionnel : FT composite 4/4 + FT html-lint 4/4 PASSED.
- Cucumber dédié : 8/8 PASSED (5 existants + 3 nouveaux).

*Non-régression* : `test` 2671 tests, 11 failures = ENV préexistantes uniquement
(gems Ruby `asciidoctor-pdf`/`asciidoctor-epub3` absentes, S-213/S-215/S-224/231).
10 runners Cucumber dédiés SUCCESS. Zéro secret, zéro régression.

== Session 231 — DOC-METADATA-VALIDATION : statut de validation composite dans metadata.json N3

*Objectif* : signaler au consommateur N3 runner-gradle le statut de la validation
composite (`validateDocument`) via `metadata.json` déjà ingéré. DDD pur
(`overallStatus`/`fromJson` sans I/O), câblage `CollectDocumentRetrieveTask` en
lecture seule du rapport (Loi Économie d'Encre), Cucumber `@metadata-validation`.

*Tests ajoutés S231* :
- `DocumentValidationReportStatusTest.kt` — 7 UT TDD strict (overallStatus PASS/FAIL
  par garde : includeGuard INVALID, xref MISSING, security REJECT, WARN→PASS ;
  fromJson round-trip ; xref nullable toléré).
- `DocumentMetadataTest.kt` — +5 UT (validationStatus default null, accept PASS/FAIL,
  writeTo inclus/omis).
- `DocumentPluginFunctionalTest.kt` — +3 FT GradleTestKit (collectDocumentRetrieve
  porte PASS du rapport / FAIL du rapport / omet sans rapport).
- `metadata_validation.feature` — 3 scénarios Cucumber `@metadata-validation`
  (PASS strict SERVER / FAIL strict traversal / OFF sans rapport → omis).
- `MetadataValidationCucumberRunner.kt` / `MetadataValidationSteps.kt` — NOUVEAU
  (package `document.metadatavalidation`, préfixe « metadata-validation », anti-glue S-223).
- Enregistrement `metadataValidationCucumberTest` dans `build.gradle.kts`.

*Couverture* :
- Unit : 12/12 PASSED (7 + 5 nouveaux).
- Fonctionnel : 3/3 nouveaux FT PASSED (65 au total, 16 ENV gems Ruby préexistantes).
- Cucumber dédié : 3/3 PASSED (`metadataValidationCucumberTest`).
- `compileKotlin` VERT. 10 runners Cucumber dédiés VERTS (metadata + xref +
  documentValidation + htmlLinkLint + includeGuard + securityPolicy +
  converterSafeMode + bookTree + bookValidation + composite).

*Non-régression* : `test` 2622 tests, 11 failures = ENV préexistantes uniquement
(gems Ruby `asciidoctor-pdf`/`asciidoctor-epub3` absentes, S-213/S-215/S-224).
Zéro secret, zéro régression.

== Session 224 — DOC-XREF-VALIDATE : validation références croisées AsciiDoc (`<<id>>` / `xref:id[]`)

*Objectif* : détecter les références croisées non résolues avant publication du livre (qualité
navigation). DDD pur `document.xref` (zéro I/O/Gradle), câblage `ConvertDocumentTask` +
tâche dédiée `validateDocumentXref` (rapport JSON), Cucumber `@xref-validation`.

*Tests ajoutés S224* :
- `document/xref/XrefValidationMode.kt` — NOUVEAU (enum OFF/STRICT/LENIENT).
- `document/xref/XrefValidationResult.kt` — NOUVEAU (sealed Valid/Invalid).
- `document/xref/XrefValidator.kt` — NOUVEAU (extractAnchors/extractReferences/validate, regex texte brut).
- `document/xref/XrefValidationReport.kt` — NOUVEAU (entry + liste, toJson/fromResult).
- `XrefValidatorTest.kt` — 12 UT TDD strict (anchors/refs extraction, ids valides/invalides, mode OFF no-op, STRICT/LENIENT détection manquante).
- `XrefValidationReportTest.kt` — 4 UT (toJson round-trip, fromResult, liste vide/peuplée).
- `ConvertDocumentTaskXrefValidationFunctionalTest.kt` — 4 FT GradleTestKit (STRICT reject + report, LENIENT convert + report, OFF skip, CLI STRICT override DSL LENIENT).
- `ValidateDocumentXrefTaskFunctionalTest.kt` — 3 FT GradleTestKit (STRICT fail + report, LENIENT VALID report, OFF no report).
- `xref_validation.feature` — 5 scénarios Cucumber `@xref-validation` (OFF skip / LENIENT VALID / LENIENT missing warn+report / STRICT reject / STRICT VALID).
- `XrefValidationCucumberRunner.kt` / `XrefValidationSteps.kt` — NOUVEAU (package `document.xrefvalidation`, préfixe « xrefvalidation »).
- Enregistrement `xrefValidationCucumberTest` dans `build.gradle.kts`.

*Couverture* :
- Unit : 16/16 PASSED (`XrefValidatorTest` 12 + `XrefValidationReportTest` 4).
- Fonctionnel : 7/7 PASSED (`ConvertDocumentTaskXrefValidationFunctionalTest` 4 + `ValidateDocumentXrefTaskFunctionalTest` 3).
- Cucumber dédié : 5/5 PASSED (`xrefValidationCucumberTest`).
- `compileKotlin` VERT. 6 runners Cucumber dédiés GREEN (xref + includeGuard + securityPolicy + converterSafeMode + bookTree + bookValidation).

*Non-régression* :
- `DocumentPluginFunctionalTest` « enregistre les 8 taches » non cassé (assert liste les noms, pas un count exact — la 9e tâche `validateDocumentXref` ne le brise pas).
- Seuls échecs `:test`/`:functionalTest` : `DocumentConverterTest.convertToPdf/Epub` + `FpaBookArtifactGenerationTest` sur `LoadError asciidoctor-pdf`/`asciidoctor-epub3` (gems Ruby absents) — préexistant S-213/S-215, hors périmètre xref, aucune régression liée.

*Pyramide S224* :

== Session 225 — DOC-VALIDATE-COMPOSITE : validation composite pré-vol (`validateDocument`)

*Objectif* : orchestrer include guard (CR4) + xref (CR-xref) + security policy (CR5) en une
passe pré-vol unique avec rapport JSON consolidé. DDD pur `document.validation` composant les
3 validateurs existants (zéro duplication), tâche `validateDocument` + Cucumber `@document-validation`.

*Tests ajoutés S225* :
- `document/validation/DocumentValidator.kt` — NOUVEAU (orchestrateur pur + `DocumentValidationReport` + entry value objects `IncludeGuardReportEntry`/`XrefReportEntry`/`SecurityReportEntry`, `toJson()` Jackson indenté).
- `DocumentValidatorTest.kt` — 8 UT TDD strict (VALID tout mode / include INVALID+STRICT / include INVALID+LENIENT / xref MISSING+STRICT / xref OFF skip / security REJECT / security VALID SERVER / rapport JSON non-vide).
- `ValidateDocumentTask.kt` — NOUVEAU (tâche `validateDocument`, groupe `document`, lit `converter`/`flat`/CLI, écrit `document-validation-report.json`, sévérité STRICT fail-fast / LENIENT warn / OFF skip).
- `ValidateDocumentTaskFunctionalTest.kt` — 4 FT GradleTestKit (STRICT include escape fail+report, STRICT xref missing fail, LENIENT warn+report, OFF skip VALID report).
- `document_validation.feature` — 5 scénarios Cucumber `@document-validation` (VALID partout / STRICT include reject / STRICT xref missing / LENIENT warn / OFF skip).
- `DocumentValidationCucumberRunner.kt` / `DocumentValidationSteps.kt` — NOUVEAU (package `document.validation`, préfixe « document-validation »).
- Enregistrement `documentValidationCucumberTest` dans `build.gradle.kts`.
- `DocumentPlugin.registerValidateDocument` — câblage (lit `converter.includeGuard`/`xrefValidation`/`safeMode` + mirror plat + CLI `-Pdocument.*`).

*Couverture* :
- Unit : 8/8 PASSED (`DocumentValidatorTest`).
- Fonctionnel : 4/4 PASSED (`ValidateDocumentTaskFunctionalTest`).
- Cucumber dédié : 5/5 PASSED (`documentValidationCucumberTest`).
- `compileKotlin` VERT. 7 runners Cucumber dédiés GREEN (xref + includeGuard + securityPolicy + converterSafeMode + bookTree + bookValidation + documentValidation).

*Non-régression* :
- Aucun validateur existant modifié : `DocumentValidator` compose `IncludePathValidator` / `XrefValidator` / `DocumentSecurityPolicy` en lecture seule.
- `ConvertDocumentTask` inchangé : ses gardes en ligne restent applicables ; `validateDocument` est additif.
- Seuls échecs `:test` : `DocumentConverterTest.convertToPdf/Epub` + `FpaBookArtifactGenerationTest` sur gems Ruby absents — préexistant S-213/S-215/S-224, hors périmètre composite, aucune régression liée.

*Pyramide S225* :


[cols="2,1,1,3"]
|===
| Couche | Tests | Status | Fichier

| Unit (xref DDD)
| 16
| ✅ PASS
| `XrefValidatorTest.kt` + `XrefValidationReportTest.kt`

| Functional (GradleTestKit)
| 7
| ✅ PASS
| `ConvertDocumentTaskXrefValidationFunctionalTest.kt` + `ValidateDocumentXrefTaskFunctionalTest.kt`

| Cucumber BDD (@xref-validation)
| 5
| ✅ PASS
| `xref_validation.feature` + `XrefValidationSteps.kt`
|===

---

== Session 223 — Infra Cucumber borough : résolution DuplicateStepDefinitionException

*Diagnostic corrigé* : le `DuplicateStepDefinitionException` du moteur Cucumber GLOBAL n'était PAS
dû au plugin conventions (`education.cccp.build.cucumber` 0.0.2). Son classpath ne doublonnait que
le `jar` projet (redondant avec `main.output`, inoffensif). La vraie cause était une **step définition
dupliquée** dans les tests document-gradle : `a document gradle project with no converter configuration`
déclarée dans `IncludeGuardSteps.kt` (package `document.includeguard`) ET `ConverterSafeModeSteps.kt`
(package `document.convertersafemode`). Le moteur global (sans restriction `glue`) scanne tout le
classpath → exception. Les runners DÉDIÉS restreignent `glue` et masquaient le doublon.

*Correction* (commit `f67eb8e`, poussé) : textes de step distincts (`a document gradle project with no
include guard configuration` / `a document gradle project with no safe mode configuration`) + features
`converter_include_guard.feature` / `converter_safe_mode.feature` mises à jour. Aucun step dupliqué
restant (vérifié par grep `@Given/@When/@Then` + `uniq -d`).

*Vérification* : `securityPolicyCucumberTest` + `includeGuardCucumberTest` + `converterSafeModeCucumberTest`
→ BUILD SUCCESSFUL (plus de DuplicateStepDefinitionException).

*Hygiene conventions* (commit `1c13b7e` conventions-gradle) : `CucumberConventionsPlugin.kt` +
`build.gradle.kts` du plugin utilisent `testRuntimeClasspath + test.output + main.output` (jar redondant
supprimé) ; test fonctionnel de régression `CP_JAR_PRESENT=false` ajouté.

*Couverture* :
- Cucumber dédié : 3/3 PASSED (securityPolicy) + 4/4 PASSED (includeGuard) + 3/3 PASSED (safeMode).
- Cucumber global : BUILD SUCCESSFUL (bruit résolu).
- Conventions : `functionalTest` (incl. régression jar) + `cucumberTest` GREEN.

---

== Session 222 — DOC-CR5 : Conversion Security Policy (cohérence SafeMode × IncludeGuard)

*Tests ajoutés S222* :
- `document/security/SecurityAdvice.kt` — NOUVEAU (sealed Valid/Warn/Reject).
- `document/security/DocumentSecurityPolicy.kt` — NOUVEAU (`advise(safeMode, includeGuard)`).
- `DocumentSecurityPolicyTest.kt` — 12 UT TDD strict.
- `ConvertDocumentTask.kt` — `+DocumentSecurityPolicy.advise` check après `DocumentIncludeGuard.check` (STRICT reject / LENIENT warn / OFF skip).
- `ConvertDocumentTaskSecurityPolicyFunctionalTest.kt` — 3 FT GradleTestKit (STRICT+UNSAFE reject, LENIENT+UNSAFE warn+convert, SERVER+STRICT valid).
- `converter_security_policy.feature` — NOUVEAU (3 scénarios `@converter-security-policy`).
- `SecurityPolicyCucumberRunner.kt` / `SecurityPolicySteps.kt` — NOUVEAU (package `document.securitypolicy`).
- Enregistrement `securityPolicyCucumberTest` dans `build.gradle.kts`.

*Couverture* :
- Unit : 12/12 PASSED (`DocumentSecurityPolicyTest`).
- Fonctionnel : 3/3 PASSED (`ConvertDocumentTaskSecurityPolicyFunctionalTest`).
- Cucumber dédié : 3/3 PASSED (`SecurityPolicyCucumberRunner`), exécution globale bruite par infra borough (DuplicateStepDefinitionException — root-cause corrigé S-223 : doublon de step définition dans les tests document-gradle, pas le plugin conventions, non bloquant DOC-CR5).
- `compileKotlin` VERT, non-régression `document.security.*` (IncludePathValidatorTest 12/12) + FT include-guard 5/5.

*Notes* :
- Compose `safeMode` (CR3-2) + `includeGuard` (CR4) : défense en profondeur — un include guard actif ne doit pas laisser AsciidoctorJ FS non restreint.
- Sévérité réutilise le mode `includeGuard` (zéro nouveau knob DSL/CLI).
- Infra Cucumber borough (conventions duplicate classpath) affecte aussi S-220/S-221 — non bloquant DOC-CR5.

---

== Session 221 — DOC-CR4 : Include Path Guard (audit pré-vol `include::` filesystem)

*Tests ajoutés S221* :
- `document/security/IncludeGuardMode.kt` — NOUVEAU (enum STRICT/LENIENT/OFF).
- `document/security/IncludeDirective.kt` — NOUVEAU (data class raw/target/line).
- `document/security/IncludeValidationResult.kt` — NOUVEAU (sealed Valid/Invalid).
- `document/security/IncludePathValidator.kt` — NOUVEAU (parseIncludes/validate).
- `document/security/DocumentIncludeGuard.kt` — NOUVEAU (check(text, baseDir, mode, onWarn)).
- `IncludePathValidatorTest.kt` — 12 UT TDD strict.
- `ConvertDocumentTask.kt` — `+includeGuard` + `DocumentIncludeGuard.check` au début de `convert()`.
- `ConverterDsl.kt` / `DocumentExtension.kt` / `DocumentPlugin.kt` — `+includeGuard` DSL + flat mirror + CLI `-Pdocument.includeGuard` ; fix `registerBookPipeline` (override `sourceFile` seulement si `bookPipeline` dans le graphe).
- `ConvertDocumentTaskIncludeGuardFunctionalTest.kt` — 5 FT GradleTestKit (DSL STRICT/LENIENT, CLI override, défaut OFF, STRICT reject, LENIENT convert).
- `converter_include_guard.feature` — NOUVEAU (4 scénarios `@converter-include-guard`).
- `IncludeGuardCucumberRunner.kt` / `IncludeGuardSteps.kt` — NOUVEAU (package `document.includeguard`).
- Enregistrement `includeGuardCucumberTest` dans `build.gradle.kts`.

*Couverture* :
- Unit : 12/12 PASSED (`IncludePathValidatorTest`).
- Fonctionnel : 5/5 PASSED (`ConvertDocumentTaskIncludeGuardFunctionalTest`) + 4/4 FT safeMode existants non-régression.
- Cucumber dédié : artifacts ajoutés (4 scénarios), exécution bloquée par infra borough (DuplicateStepDefinitionException — root-cause corrigé S-223 : doublon de step définition dans les tests document-gradle, pas le plugin conventions, voir note S-221).
- `compileKotlin` VERT.

*Notes* :
- Modes : STRICT (fail build) / LENIENT (warn + convert) / OFF (défaut, backward-compat).
- Audit pré-vol : `include::` résolu AVANT le `safeMode` AsciidoctorJ → filet défensif indépendant.
- Bug conversion standalone (`convertDocumentToHtml.sourceFile` hijacké par bookPipeline) corrigé.
- Infra Cucumber borough (conventions duplicate classpath) affecte aussi S-220 — non bloquant DOC-CR4.

---

== Session 220 — DOC-CR3-2 : Garde safeMode DSL configurable (UNSAFE/SERVER/SECURE)

*Tests ajoutés S220* :
- `DocumentConverterTest.kt` — 4 UT TDD strict (buildOptions applique SafeMode fourni/utilisé par défaut/appliqué SECURE ; convertToHtml honore le SafeMode SERVER).
- `ConvertDocumentTaskSafeModeFunctionalTest.kt` — 4 FT GradleTestKit (DSL SERVER/SECURE, CLI override DSL SERVER, défaut UNSAFE).
- `ConverterDsl.kt` — NOUVEAU (DSL imbriqué `converter { safeMode = ... }`).
- `ConverterSafeModeSteps.kt` — NOUVEAU (package `document.convertersafemode`).
- `ConverterSafeModeCucumberRunner.kt` — NOUVEAU (runner dédié).
- `converter_safe_mode.feature` — NOUVEAU (3 scénarios `@converter-safe-mode`).
- Enregistrement `converterSafeModeCucumberTest` dans `build.gradle.kts`.

*Couverture* : 
- Unit : 4/4 PASSED (DocumentConverterTest total 47/47).
- Fonctionnel : 4/4 PASSED (ConvertDocumentTaskSafeModeFunctionalTest).
- Cucumber dédié : 3/3 PASSED (converterSafeModeCucumberTest).
- `compileKotlin` VERT.

*Notes* : 
- Default UNSAFE pour backward-compat.
- Propagation via `ConvertDocumentTask.safeMode` → `DocumentConverter.buildOptions(safeMode)`.
- Baby-step DDD/TDD/BDD strict (chaque ajout compilé/testé pas à pas).
- Le task global `cucumberTest` reste rouge (préexistant : glue/environnement affectant toutes les features, pas de régression).

---
== Session 040 — DOC-PLANTUML-VALIDATE-5 (Cucumber BDD, clôture EPIC)

*Couverture plantuml validation* : suite `document.translation.*` GREEN.
*1 échec EPUB préexistant* (DocumentConverterTest.convertToEpub) — non lié à cette session.

Tests ajoutés S040 :
- `plantuml_validation.feature` — 5 scénarios Cucumber BDD (@valid, @corrupted-tags, @broken-label, @preserve-technical, @strict-mode)
- `PlantUmlValidationSteps.kt` — step definitions (package `document.plantumlvalidation`)
- `PlantUmlValidationCucumberRunner.kt` — runner JUnit5 Suite

Total : +5 Cucumber BDD, 5/5 PASS.

=== Pyramide de tests — Session 040

[cols="2,1,1,3"]
|===
| Couche | Tests | Status | Fichier

| Cucumber BDD (plantuml validation)
| 5
| ✅ PASS
| `plantuml_validation.feature` + `PlantUmlValidationSteps.kt`

| Cucumber BDD (table validation)
| 6
| ✅ PASS
| `table_validation.feature` (0 régression)

| Cucumber BDD (table translation)
| 8
| ✅ PASS
| `table_translation.feature` (0 régression)

| Unit (translation)
| ~244
| ✅ PASS
| `document.translation.*` (0 régression)
|===

*Total Cucumber BDD : 19 scénarios (5 plantuml + 6 table validation + 8 table translation), 0 échec.*

== Session 039 — DOC-PLANTUML-VALIDATE-3 + VALIDATE-4

*Couverture translation* : suite `document.translation.*` GREEN après les fixes.
*Tous les tests unitaires/functional/cucumber de traduction passent.*
*4 échecs Cucumber préexistants* (bookPipeline + applyDocumentTemplate × 3) — non liés aux fixes, avant S032.

Tests ajoutés S032 :
- PivotModelTest (+2) : DescriptionList
- AsciiDocParserTest (+17) : description lists (liens, texte, multi-mots, vide, non-fusion, suivis de paragraphe, commentaires), block macros image/video, source block 4-dot `....`, non-swallowing après `....`, source block avec titre `.titre`
- AsciiDocRendererTest (+5) : round-trip description list + block macro, chaque item sur sa ligne
- JbakeNativeRendererTest (+4) : rendu description list + block macro
- document.feature (+2 scénarios @description-list + @block-macro) : batch output lines par ligne

== Pyramide de tests — Session 021 (Fix compile + DOC-TRANSLATE US-1 & US-2)

[cols="2,1,1,3"]
|===
| Couche | Tests | Status | Fichier

| Unit (domaine + génération IA + conversion + enrichissement + theme + N3 retrieve + book + DSL unifié + release notes 4 renderers + N3 release notes + ollama-asciidoc + BookDsl + image preservation + EPUB image + BookConfig serializer + DocBook/ManPage advanced + EPUB advanced + round-trip deserialize + translation)
| 293
| ✅ PASS
| `src/test/kotlin/document/*Test.kt` + `src/test/kotlin/document/translation/*Test.kt` (+53 translation)

| Functional (GradleTestKit)
| 54
| ✅ PASS
| `src/functionalTest/kotlin/document/DocumentPluginFunctionalTest.kt` + `src/functionalTest/kotlin/document/ReleaseNotesFunctionalTest.kt`

| Cucumber BDD (acceptance)
| 54
| ✅ PASS
| `src/test/resources/features/document.feature` + `src/test/resources/features/release-notes.feature`
|===

*Total : 401 tests, 0 échec.* (+53 par rapport à session 020 — DOC-TRANSLATE US-1 & US-2)

== Nouveaux tests translation (53)

[cols="2,3,1"]
|===
| Classe | Tests | Status
| PivotModelTest | 4 | ✅
| AsciiDocParserTest | 27 | ✅
| PivotYamlRendererTest | 9 | ✅
| AsciiDocRendererTest | 8 | ✅
| JbakeNativeRendererTest | 5 | ✅
|===

== Détail unit (62)

[cols="2,3,1"]
|===
| Classe | Test | Status

| DocumentSourceTest | DocumentSource extrait le nom sans extension | ✅
| DocumentSourceTest | DocumentSource refuse un fichier non adoc | ✅
| DocumentFormatTest | DocumentFormat HTML a backend html5 et extension html | ✅
| DocumentFormatTest | DocumentFormat PDF a backend pdf et extension pdf | ✅
| DocumentFormatTest | DocumentFormat ALL contient les 5 formats | ✅
| DocumentConfigTest | DocumentConfig outputFor genere le bon nom de fichier | ✅
| DocumentConfigTest | DocumentConfig par defaut demande HTML uniquement | ✅
| DocumentConfigTest | DocumentConfig requiresEnrichment faux par defaut | ✅
| DocumentConfigTest | DocumentConfig requiresEnrichment vrai si PlantUML active | ✅
| DocumentConfigTest | DocumentConfig theme defaults to empty DocumentTheme | ✅
| DocumentConfigTest | DocumentConfig theme stores the provided DocumentTheme | ✅
| AsciiDocValidatorTest | AsciiDoc valide avec titre de niveau 0 | ✅
| AsciiDocValidatorTest | AsciiDoc invalide sans titre de niveau 0 | ✅
| AsciiDocValidatorTest | AsciiDoc invalide si vide | ✅
| AsciiDocValidatorTest | AsciiDoc valide avec espaces avant le titre | ✅
| DocumentGenerationGraphTest | execute produit un AsciiDoc valide avec un FakeLlmProvider | ✅
| DocumentGenerationGraphTest | execute retourne une erreur si le LLM produit un AsciiDoc invalide | ✅
| DocumentGenerationGraphTest | execute retourne une erreur si le LLM produit une sortie vide | ✅
| DocumentGenerationGraphTest | buildPromptNode inclut le system prompt et la demande utilisateur | ✅
| DocumentConverterTest | convertToHtml produit un HTML5 avec doctype depuis un AsciiDoc valide | ✅
| DocumentConverterTest | convertToHtml preserve les sections du document source | ✅
| DocumentConverterTest | convertToHtml rend les blocs de code source avec syntaxe colorisee | ✅
| DocumentConverterTest | convertToHtml resout les includes AsciiDoc | ✅
| DocumentConverterTest | convertToHtml leve une exception si la source est absente | ✅
| DocumentConverterTest | shouldSkip retourne vrai si le fichier existe et le hash source correspond | ✅
| DocumentConverterTest | shouldSkip retourne faux si le fichier n existe pas | ✅
| DocumentConverterTest | shouldSkip retourne faux si le hash source ne correspond pas | ✅
| DocumentConverterTest | buildMetadataHeader inscrit le hash de la source | ✅
| DocumentConverterTest | convertToPdf ecrit un fichier PDF valide dans le fichier de sortie | ✅
| DocumentConverterTest | convertToPdf preserve les sections du document source | ✅
| DocumentConverterTest | shouldSkipPdf retourne vrai si le PDF et le fichier hash sidecar existent et correspondent | ✅
| DocumentConverterTest | shouldSkipPdf retourne faux si le fichier hash sidecar n existe pas | ✅
| DocumentConverterTest | shouldSkipPdf retourne faux si le hash dans le sidecar ne correspond pas | ✅
| DocumentConverterTest | writeBinaryMetadataHeader ecrit le hash de la source dans un fichier sidecar | ✅
| DocumentConverterTest | convertToEpub ecrit un fichier EPUB3 valide dans le fichier de sortie | ✅
| DocumentConverterTest | shouldSkipEpub retourne vrai si l'EPUB et le fichier hash sidecar existent et correspondent | ✅
| DocumentConverterTest | shouldSkipEpub retourne faux si le fichier hash sidecar n existe pas | ✅
| DocumentConverterTest | convertToDocBook produit un DocBook 5 valide avec namespace XML depuis un AsciiDoc valide | ✅
| DocumentConverterTest | convertToDocBook preserve les sections du document source | ✅
| DocumentConverterTest | convertToManPage produit une page de manuel troff valide depuis un AsciiDoc manpage | ✅
| DocumentConverterTest | convertToManPage leve une exception si la source est absente | ✅
| DocumentEnricherTest | enrich preserves inline plantuml blocks without escaping | ✅
| DocumentEnricherTest | enrich preserves raw HTML passthrough blocks without escaping | ✅
| DocumentEnricherTest | enrich recursively resolves AsciiDoc includes | ✅
| DocumentEnricherTest | enrich preserves the title and sections of the source document | ✅
| DocumentEnricherTest | enrich does not mutate the source file | ✅
| DocumentThemeTest | DocumentTheme default has no theme files configured | ✅
| DocumentThemeTest | DocumentTheme stores pdfTheme file when provided | ✅
| DocumentThemeTest | DocumentTheme stores htmlStylesheet file when provided | ✅
| DocumentThemeTest | DocumentTheme stores epubStylesheet file when provided | ✅
| DocumentThemeTest | DocumentTheme stores logo file when provided | ✅
| DocumentThemeTest | DocumentTheme isEmpty is true when no theme files are set | ✅
| DocumentThemeTest | DocumentTheme isEmpty is false when at least one theme file is set | ✅
| DocumentThemeTest | DocumentTheme toAttributes injects stylesheet for HTML when htmlStylesheet is set | ✅
| DocumentThemeTest | DocumentTheme toAttributes injects pdf-theme for PDF when pdfTheme is set | ✅
| DocumentThemeTest | DocumentTheme toAttributes injects epub-stylesheet for EPUB when epubStylesheet is set | ✅
| DocumentThemeTest | DocumentTheme toAttributes injects pdf-theme and logo for PDF when both are set | ✅
| DocumentThemeTest | DocumentTheme toAttributes is empty for HTML when no theme is set | ✅
| DocumentThemeTest | DocumentTheme toAttributes is empty for PDF when no theme is set | ✅
| DocumentThemeTest | DocumentTheme toAttributes ignores htmlStylesheet for PDF format | ✅
| DocumentThemeTest | DocumentTheme toAttributes ignores pdfTheme for HTML format | ✅
| DocumentThemeTest | DocumentTheme equals and hashCode are based on theme files | ✅
| DocumentMetadataTest | DocumentMetadata forNewOrleans sets source to new-orleans | ✅
| DocumentMetadataTest | DocumentMetadata forNewOrleans defaults type to retrieve | ✅
| DocumentMetadataTest | DocumentMetadata forNewOrleans defaults model to onnx-local | ✅
| DocumentMetadataTest | DocumentMetadata forNewOrleans defaults version to 1_0 | ✅
| DocumentMetadataTest | DocumentMetadata forNewOrleans defaults dependencies to brooklyn and htown | ✅
| DocumentMetadataTest | DocumentMetadata forNewOrleans accepts custom sessions count | ✅
| DocumentMetadataTest | DocumentMetadata forNewOrleans accepts custom type | ✅
| DocumentMetadataTest | DocumentMetadata forNewOrleans generates an ISO-8601 timestamp | ✅
| DocumentMetadataTest | DocumentMetadata writeTo creates a metadata_json file | ✅
| DocumentMetadataTest | DocumentMetadata writeTo creates parent directories if missing | ✅
| DocumentArtifactEntryTest | DocumentArtifactEntry stores path format and source adoc | ✅
| DocumentArtifactEntryTest | DocumentArtifactEntry toMap produces N3-compatible entry | ✅
| DocumentArtifactEntryTest | DocumentArtifactEntry toMap for PDF format produces correct fields | ✅
| DocumentArtifactEntryTest | DocumentArtifactEntry marks non-existing file as exists false | ✅
| DocumentRetrieveResultTest | DocumentRetrieveResult defaults source to new-orleans | ✅
| DocumentRetrieveResultTest | DocumentRetrieveResult count matches entries size | ✅
| DocumentRetrieveResultTest | DocumentRetrieveResult count is zero for empty entries | ✅
| DocumentRetrieveResultTest | DocumentRetrieveResult toMap produces N3-compatible envelope | ✅
| DocumentRetrieveResultTest | DocumentRetrieveResult toMap includes timestamp | ✅
| DocumentRetrieveResultTest | DocumentRetrieveResult writeTo creates composite-context json | ✅
| DocumentRetrieveResultTest | DocumentRetrieveResult writeTo creates parent directories if missing | ✅
| DocumentArtifactCollectorTest | collect returns empty list when output dir does not exist | ✅
| DocumentArtifactCollectorTest | collect detects an HTML artifact | ✅
| DocumentArtifactCollectorTest | collect detects a PDF artifact | ✅
| DocumentArtifactCollectorTest | collect detects all 5 format artifacts plus generated and enriched adoc | ✅
| DocumentArtifactCollectorTest | collect skips non-existing artifacts and only returns files on disk | ✅
| DocumentArtifactCollectorTest | collect includes the generated adoc as an entry | ✅
| DocumentArtifactCollectorTest | collect includes the enriched adoc as an entry | ✅
| BookAssemblerTest | assembler merges ordered pages into a single AsciiDoc document | ✅
| BookAssemblerTest | assembler preserves the order of pages by numeric prefix | ✅
| BookAssemblerTest | assembler orders non-numbered pages after numbered ones | ✅
| BookAssemblerTest | assembler includes the author as document attribute | ✅
| BookAssemblerTest | assembler produces an empty book when no pages are present | ✅
| BookAssemblerTest | assembler does not mutate any source page file | ✅
| BookAssemblerTest | assembler ignores non-adoc files in the pages directory | ✅
| BookAssemblerTest | assembler embeds matching photos as image directives | ✅
| BookAssemblerTest | assembler counts zero photos when no matching photo is found | ✅
| BookAssemblerTest | assembler matches photos across supported extensions | ✅
|===

== Détail functional (23)

[cols="3,1]
|===
| Test | Status

| plugin s'applique et enregistre les 8 taches documentaires | ✅
| plugin applique l'extension document via DSL | ✅
| convention source par defaut est src docs document adoc | ✅
| DSL override la convention source | ✅
| CLI -Pdocument source surcharge la convention | ✅
| generateDocument IA produit un AsciiDoc valide via fake LLM | ✅
| generateDocument IA skip si sortie existante pour meme prompt | ✅
| generateDocument copie la source si aucun prompt n est defini | ✅
| convertDocumentToHtml produit un fichier HTML5 valide depuis la source | ✅
| convertDocumentToHtml skip si la sortie existe et la source est inchangee | ✅
| convertDocumentToPdf produit un fichier PDF valide depuis la source | ✅
| convertDocumentToPdf skip si la sortie existe et la source est inchangee | ✅
| convertDocumentToEpub produit un fichier EPUB3 valide depuis la source | ✅
| convertDocumentToEpub skip si la sortie existe et la source est inchangee | ✅
| convertDocumentToDocBook produit un fichier DocBook 5 valide depuis la source | ✅
| convertDocumentToManPage produit une page de manuel valide depuis la source | ✅
| enrichDocument produces a resolved AsciiDoc file from a source with includes | ✅
| enrichDocument is skipped when the output exists and the source is unchanged | ✅
| convertDocumentToHtml applies the custom stylesheet from the theme DSL | ✅
| convertDocumentToPdf applies the custom pdf theme from the theme DSL | ✅
| convertDocumentToHtml succeeds without theme configured (default fallback) | ✅
| collectDocumentRetrieve produces metadata_json and composite-context_json after conversion | ✅
| collectDocumentRetrieve produces empty entries when no artifacts exist | ✅
| assembleBook merges OCR-ed pages into a single book AsciiDoc | ✅
| assembleBook embeds original photos as image directives | ✅
| bookPipeline registers and exposes the assembleBook and bookPipeline tasks | ✅
|===

== Détail cucumber BDD (22)

Feature: `document.feature` — Document plugin (DOC-1 stub + DOC-2 generation IA + DOC-3 HTML + DOC-4 PDF + DOC-5 EPUB/DocBook/ManPage + DOC-9 enrich + DOC-10 theme + DOC-6 N3 retrieve)

[cols="3,1]
|===
| Scénario | Status

| Le plugin s'applique sur un projet vierge | ✅
| Le plugin enregistre la tache generateDocument | ✅
| Le plugin enregistre la tache convertDocumentToPdf | ✅
| Le plugin enregistre 8 taches documentaires | ✅
| generateDocument produit un AsciiDoc valide depuis un prompt via LLM fake | ✅
| generateDocument skip si la sortie existe deja pour le meme prompt | ✅
| convertDocumentToHtml produit un fichier HTML5 valide depuis un AsciiDoc source | ✅
| convertDocumentToHtml skip si la sortie existe et la source est inchangee | ✅
| convertDocumentToPdf produit un fichier PDF valide depuis un AsciiDoc source | ✅
| convertDocumentToPdf skip si la sortie existe et la source est inchangee | ✅
| convertDocumentToEpub produit un fichier EPUB3 valide depuis un AsciiDoc source | ✅
| convertDocumentToEpub skip si la sortie existe et la source est inchangee | ✅
| convertDocumentToDocBook produit un fichier DocBook 5 valide depuis un AsciiDoc source | ✅
| convertDocumentToManPage produit une page de manuel valide depuis un AsciiDoc source | ✅
| enrichDocument preserva les blocs plantuml inline sans les echapper | ✅
| enrichDocument preserva les blocs passthrough HTML brut sans les echapper | ✅
| enrichDocument resout recursivement les includes AsciiDoc | ✅
| Le DSL document accepte un block theme avec pdfTheme et htmlStylesheet | ✅
| convertDocumentToPdf applique le theme YML configure | ✅
| convertDocumentToHtml reussit sans theme configure (fallback defaut) | ✅
| collectDocumentRetrieve produit metadata_json et composite-context_json apres conversion | ✅
| collectDocumentRetrieve produit un count a zero sans artefact | ✅
| The plugin registers the assembleBook task | ✅
| The plugin registers the bookPipeline composite task | ✅
| assembleBook merges OCR-ed AsciiDoc pages into a single book | ✅
| assembleBook embeds original photos as illustrations | ✅
| bookPipeline produces the complete book HTML PDF EPUB in one command | ✅
|===

== Couverture code

Coverage non mesuré (Kover non configuré).
Classes de production : 19 (8 DOC-1 + 7 DOC-2 generation + 1 DocumentConverter + 2 DOC-9 enrich + 1 DocumentTheme).
Package `document` : DocumentPlugin, DocumentExtension, DocumentConfig, DocumentFormat,
DocumentSource, GenerateDocumentTask, ConvertDocumentTask, DocumentConverter,
DocumentEnricher, EnrichDocumentTask, DocumentTheme.
Package `document.generation` : DocumentLlmProvider, DocumentGenerationState,
DocumentGenerationGraph, AsciiDocValidator, OllamaDocumentLlmProvider,
FakeDocumentLlmProvider, DocumentLlmProviderFactory.

== Évolution par session

[cols="2,1,1,1,1]
|===
| Session | Unit | Functional | Cucumber | Total

| 002 (DOC-1) | 9 | 5 | 4 | 18
| 003 (DOC-2) | 17 | 8 | 6 | 31
| 004 (DOC-3+4) | 28 | 12 | 10 | 50
| 005 (DOC-5) | 39 | 16 | 14 | 69
| 006 (archi) | 39 | 16 | 14 | 69 (inchangé — Gradle 9.6.1, @DisableCachingByDefault)
| 007 (DOC-9) | 44 | 18 | 17 | 79 (+10 — DocumentEnricher + EnrichDocumentTask)
| 008 (DOC-10) | 62 | 21 | 20 | 103 (+24 — DocumentTheme + DocumentConfig.theme)
| 010 (DOC-6) | 90 | 23 | 22 | 135 (+32 — DocumentMetadata, DocumentArtifactEntry, DocumentRetrieveResult, DocumentArtifactCollector, CollectDocumentRetrieveTask)
| 011 (DOC-7) | 90 | 24 | 23 | 137 (+2 — POM publication BDD + functional)
| 012 (DOC-11) | 100 | 27 | 28 | 155 (+18 — BookPage, PageOrder, BookAssemblyResult, BookAssembler, AssembleBookTask, bookPipeline composite)
| 013 (DOC-12) | 119 | 27 | 34 | 180 (+25 — DocumentEnrichConfig, DocumentOutputs, DocumentFrontMatter, DocumentPipelineConfig, DocumentConfigSerializer, 3 nested DSL blocks, SerializeDocumentConfigTask)
| 014 (DOC-8.1) | 145 | 4 | 37 | 186 (+35 unit +4 functional +3 cucumber — ConventionalCommitParser, CliGitLogParser, AsciidocReleaseNotesRenderer, GitReleaseNotesGenerator, ReleaseNotesDsl/Task)
| 015 (DOC-8.2) | 183 | 35 | 39 | 257 (+31 unit +4 functional +2 cucumber — MarkdownReleaseNotesRenderer, JsonReleaseNotesRenderer, configDriven, rendererType+categories)
| 016 (DOC-8.3) | 209 | 39 | 41 | 289 (+32 — ReleaseNotesArtifactEntry, DocumentRetrieveResult.releaseNotes, DocumentMetadata.releaseNotesPath/Renderer, DocumentArtifactCollector.collectReleaseNotes, CollectDocumentRetrieveTask.releaseNotesDirPath)
| 017 (DOC-8.4) | 218 | 40 | 42 | 300 (+11 — OllamaAsciidocReleaseNotesRenderer, GitReleaseNotesGenerator.configDrivenWithLlm, ReleaseNotesDsl.llmMode, fallback gracieux)
|===

== Notes

- DocumentConverter : objet domaine convertisseur (5 formats — html5/pdf/epub3/docbook5/manpage).
- Distinction texte (hash dans fichier, shouldSkipConversion) vs binaire (sidecar .sourcehash, shouldSkipBinaryConversion).
- standalone(true) requis pour doctype HTML/DocBook/ManPage complet.
- asciidoctorj-pdf 2.3.23 : module séparé (backend pdf, non inclus dans core).
- asciidoctorj-epub3 2.2.0 : module séparé (backend epub3, non inclus dans core).
- DocBook5 + ManPage : natifs à asciidoctorj core (aucune extension requise).
- ManPage requiert `:doctype: manpage` + section NAME + SYNOPSIS dans la source.
- Économie d'encre : skip si output existe et hash source correspond (texte ou binaire).
- convertBinary factorise PDF + EPUB (skip par sidecar, convertToFile, writeBinaryMetadataHeader).
- DocumentEnricher (DOC-9) : résolution récursive des include:: (max depth 10), préservation
  native des blocs [plantuml] et ++++ (pas d'échappement — l'enrichisseur ne touche pas
  la syntaxe AsciiDoc, il résout uniquement les includes).
- EnrichDocumentTask : @DisableCachingByDefault + shouldSkipConversion (hash source en metadata).
- Asciidoctor Diagram inline : rendu des blocs [plantuml] à la conversion par le bundle déjà présent.
- DocumentTheme (DOC-10) : value object immutable (pdfTheme, htmlStylesheet, epubStylesheet, logo).
  toAttributes(format) mappe vers les attributs AsciidoctorJ par backend (stylesheet, pdf-theme,
  epub-stylesheet, pdf-front-cover-image). DocBook/ManPage non supportés (map vide).
- AsciidoctorJ inline le CSS HTML dans <style> (pas de <link> ni mot "stylesheet" dans le HTML).
  Le test BDD vérifie `font-family` (contenu du CSS) plutôt que le mot "stylesheet".
- buildOptions centralise la construction des Options + injection Attributes par backend.
- DSL nested theme { } via fun interface Action (SAM) — pattern planner/codebase respecté.
- Pattern surcharge : CLI (-Pdocument.pdfTheme) > DSL (theme { }) > convention (vide).
- @PathSensitive(NONE) + @Optional sur les 4 propriétés thème (validation Gradle OK).
- DocumentMetadata (DOC-6) : provenance N3 conforme au contrat de facto (source, type, sessions,
  generatedAt, model, version, dependencies). forNewOrleans() factory + writeTo() JSON.
- DocumentArtifactEntry (DOC-6) : entry N3 (path, format, sourceAdoc, exists) + toMap() pour
  la sérialisation dans l'array `entries` du composite-context.json.
- DocumentRetrieveResult (DOC-6) : enveloppe N3 (source="new-orleans", entries, count, timestamp)
  + writeTo() produit composite-context.json. Runner-gradle lit uniquement la clé `entries`.
- DocumentArtifactCollector (DOC-6) : scan outputDir → list of DocumentArtifactEntry. Logique
  extraite de la task pour être unit-testable sans Gradle runtime. Ne retourne que les fichiers
  existants (pas de faux positives).
- CollectDocumentRetrieveTask (DOC-6) : @DisableCachingByDefault, orchestre collector + result
  + metadata. READ-only — n'invoque jamais le LLM (Loi de l'Économie d'Encre).
- Contrat N3 de facto : pas de contrat N0 formel (workspace-bom pipeline-contracts MEM-2 non
  implémenté). Chaque borough duplique une classe Metadata. Document suit le pattern hyperframes.

---

== Session 014 — DOC-8.1 Release Notes Pipeline

**Nouveaux tests** : +35 unit + 4 functional + 3 cucumber = +42 tests (total 186)

=== ConventionalCommitParser (9 unit)

- Parsing regex Conventional Commits 1.0.0 : `type(scope)!: message`
- Types supportés : feat, fix, chore, perf, refactor, docs, test, ci, build, style
- Scope optionnel (parenthèses), breaking change marker `!` strippé
- `parseSubject` retourne null pour lignes non-conventionnelles ou vides
- `parseLines` filtre et zip subjects/hashes/dates (même taille requise)

=== CliGitLogParser (9 unit, @TempDir fixture)

- Implémente `GitLogParser` (contracts.pipeline) via `ProcessBuilder("git", "log", ...)`
- Format `--format=%s%x09%H%x09%aI` (TAB separator, évite collisions)
- `parse(fromTag, toTag)` : `asReversed()` car git log retourne newest-first
- `detectVersion` : VERSION file → gradle.properties `version=` → null
- `detectFromTag` : `--sort=-v:refname` (lightweight tags n'ont pas creatordate)
- 3 fixes TDD : ordre reverse, version sort tags, version sort detectFromTag

=== AsciidocReleaseNotesRenderer (12 unit)

- Implémente `ReleaseNotesRenderer` (format = "asciidoc")
- `render` : titre `= Release Notes {version}`, sections par catégorie (config.categories order)
- Scope entre parenthèses `- message (scope)`, catégories vides omises
- Section "Téléchargement" optionnelle (includeDownloads)
- `renderToFile` crée les répertoires parents

=== GitReleaseNotesGenerator (5 unit, fake/stub/spy)

- Implémente `ReleaseNotesGenerator` (orchestration parser + renderer)
- Résout version : config.version → detectVersion → SNAPSHOT
- Résout fromTag : config.fromTag → detectFromTag → parse("", toTag)
- Fichier sortie : `{outputDir}/release-notes-{version}.adoc`
- Spy parser vérifie fromTag/toTag capturés

=== ReleaseNotesFunctionalTest (4 functional, GradleTestKit)

- Task `releaseNotesGenerate` registered (tasks --group document)
- Produit .adoc depuis commits conventionnels (feat, fix(api), docs)
- SNAPSHOT quand pas de VERSION/gradle.properties
- Lit VERSION file comme version

=== release-notes.feature (3 cucumber BDD)

- `@doc8 @releasenotes` — 3 scénarios (registration, produit adoc, SNAPSHOT)
- Steps : Given git repo, conventional commit, git tag / Then adoc exists, contains, version in filename

=== Notes techniques

- `cucumberTest` task : ~6-7 min (GradleRunner démarre un daemon par scénario)
- `NoSuchFileException binary/in-progress-results-generic.bin` : bug Gradle 9.6.1 + plugin
  cucumber custom (binary results), pas un échec de test. Tous scénarios PASSED dans le log.
- `./gradlew test functionalTest` : 22s, succès. Cucumber via `./gradlew cucumberTest` séparé.
- pipeline-contracts 0.0.2 dans ~/.m2 (MEMPHIS MEM-2 publié session 013 workspace-bom)

---

== Session 015 — DOC-8.2 Template configurable (+71 tests)

**Nouveaux tests** : +31 unit + 4 functional + 2 cucumber = +37 tests (total 257)

=== MarkdownReleaseNotesRenderer (11 unit)

- Implémente `ReleaseNotesRenderer` (format = "markdown")
- `render` : titre `# Release Notes {version}`, sections `## {label}` par catégorie (config order)
- Scope entre parenthèses `- message (scope)`, catégories vides omises
- Section "Téléchargement" optionnelle (includeDownloads)
- `renderToFile` crée les répertoires parents
- Miroir exact de AsciidocReleaseNotesRenderer avec `#`/`##` au lieu de `=`/`==`

=== JsonReleaseNotesRenderer (10 unit)

- Implémente `ReleaseNotesRenderer` (format = "json")
- Schéma stable machine-readable : `{version, commits[], categories{}, includeDownloads?}`
- Commits : tableau d'objets `{type, scope (nullable), message, hash, date}`
- Categories : objet type→label (toutes les catégories de la config, pas seulement celles présentes)
- `includeDownloads` omis du JSON quand false (optionnel)
- Échappement `\\` et `\"` (escape helper)
- `renderToFile` crée les répertoires parents

=== GitReleaseNotesGenerator configDriven (4 unit, fake parser)

- `configDriven(projectDir, parser)` factory — sélection renderer par `config.rendererType`
- markdown → `.md` extension, json → `.json` extension, unknown → asciidoc fallback `.adoc`
- Constructeur explicite (renderer injecté) ignore rendererType (escape hatch)
- Compat ascendante : `GitReleaseNotesGenerator(projectDir, parser)` = asciidoc default

=== ReleaseNotesDsl (6 unit)

- `rendererType: Property<String>` — null par défaut (convention asciidoc)
- `categories: MapProperty<String, String>` — empty par défaut (fallback config default)
- Set markdown, set json, set custom map (3 types custom)
- Coexistence avec props existantes (fromTag, toTag, version, includeDownloads)

=== ReleaseNotesFunctionalTest (4 functional, GradleTestKit)

- markdown via DSL `rendererType.set("markdown")` → produit .md
- json via DSL `rendererType.set("json")` + `includeDownloads.set(false)` → produit .json
- custom categories via DSL `categories.set(mapOf(...))` → labels custom dans AsciiDoc
- CLI override `-Pdocument.releaseNotesRendererType=markdown` surcharge DSL asciidoc

=== release-notes.feature (2 cucumber BDD)

- `@doc8 @releasenotes` — 2 nouveaux scénarios (markdown file, custom categories)
- Steps : Given git repo + markdown renderer / custom categories, Then markdown file exists, markdown contains
- World : +2 factories (markdown renderer, custom categories) +releaseNotesMarkdownFile()

=== Notes techniques

- Refactor GitReleaseNotesGenerator : constructeur privé + 2 publics (compat) + companion configDriven
- Contrat N0 `ReleaseNotesGenerator.renderer` non-null préservé (override impossible en nullable)
- Extension fichier de sortie déduite de `renderer.format` (md/json/adoc)
- Categories custom : fallback `ReleaseNotesConfig().categories` si MapProperty vide
- CLI categories format : `key=label,key2=label2` (parseCategoriesCli helper)
- Parser Conventional Commit regex stricte → custom categories ne mappent que sur types reconnus
- `./gradlew test` : 38s, `./gradlew functionalTest` : 27s, `./gradlew cucumberTest` : 6m1s

== Session 039 — DOC-PLANTUML-VALIDATE-3 + VALIDATE-4

*Couverture plantuml validation* : suite `document.translation.*` GREEN.
*3 échecs EPUB préexistants* (DocumentConverterTest) — non liés à cette session.

Tests ajoutés S039 :
- PlantUmlTranslationAdapterValidationTest (+8 UT) : valid, corrupted lenient, corrupted strict, PreserveTechnical skip, BorrowVocabulary, OFF skip, non-plantuml, strategy context
- DocumentTranslatorPlantUmlValidationTest (+5 UT) : LENIENT warn, STRICT throw, OFF skip, valid pass, multiple blocks accumulate
- PlantUmlSyntaxValidatorTest (10 UT, adaptés interface DDD)
- PlantumlServiceAvailabilityTest (1 UT, inchangé)

Total : +13 nouveaux tests, 24/24 PASS (incluant les 10 UT validator + 1 UT availability).

=== Pyramide de tests — Session 039

[cols="2,1,1,3"]
|===
| Couche | Tests | Status | Fichier

| Unit (plantuml validation adapter + translator)
| 13
| ✅ PASS
| `PlantUmlTranslationAdapterValidationTest.kt` + `DocumentTranslatorPlantUmlValidationTest.kt`

| Unit (plantuml syntax validator)
| 11
| ✅ PASS
| `PlantUmlSyntaxValidatorTest.kt` + `PlantumlServiceAvailabilityTest.kt`

| Cucumber BDD
| 14
| ✅ PASS
| `tableTranslationCucumberTest` (8) + `tableValidationCucumberTest` (6)
|===
== Session 212 — DOC-BOOK-DOMAIN-1 + DOMAIN-2 (BookTree DDD + Matter/Layout)

*Couverture domaine Book (US-1 + US-2)* : 27 nouveaux UT, 100% GREEN, DDD pur
sans I/O. Aucun test fonctionnel (la logique est déterministe, pas de Gradle runtime).

Tests ajoutés S212 :
- `BookTreeBuilderTest.kt` (+15 UT) : depth derivation (level = segments - 1),
  parent link, ref uniqueness (multi-page collapse), orphan ref (chaîne synthétique),
  ordering par ref, multi-level, empty input, index completeness, root children,
  déterminisme.
- `BookLayoutTest.kt` (+12 UT) : Matter.classify FRONT/BODY/BACK (prefixes défaut
  `0.`/`9.` + custom), heading level 1/4, pageBreak `<<<`, tableOfContents `:toc:`,
  titlePage (`= ` + `:author:` + `:doctype: book`).

Non-régression (existants, VERTS) :
- `BookAssemblerTest`, `BookValidatorTest`, `BookTocParserTest`, `BookDslTest`.

Note environnement : `./gradlew check` complet ne termine pas sur cette machine
(OutOfMemoryError JRuby/AsciidoctorJ dans `DocumentConverterTest` préexistant,
indépendant de DOC-BOOK-DOMAIN). Preuve de non-régression = 27 UT + 4 classes
test livre existantes VERTES en ciblé.

=== Pyramide de tests — Session 212

[cols="2,1,1,3"]
|===
| Couche | Tests | Status | Fichier

| Unit (BookTree DDD)
| 15
| ✅ PASS
| `BookTreeBuilderTest.kt`

| Unit (Matter + BookLayout)
| 12
| ✅ PASS
| `BookLayoutTest.kt`

| Non-régression livre
| 4 classes
| ✅ PASS
| `BookAssemblerTest` + `BookValidatorTest` + `BookTocParserTest` + `BookDslTest`
|===

== Session 213 — DOC-BOOK-DOMAIN-3 + DOMAIN-4 (BookAssembler structuré + BookNumbering)

*Couverture domaine Book (US-3 + US-4)* : 20 nouveaux UT, 100% GREEN, DDD pur
sans I/O. Aucun test fonctionnel (la logique est déterministe, pas de Gradle runtime).

Tests ajoutés S213 :
- `BookNumberingTest.kt` (+10 UT) : numérotation hiérarchique (ref→ref), ancres `[[ref]]`, navigation précédent/suivant.
- `BookAssemblerStructuredTest.kt` (+10 UT) : émission niveaux (`==`/`===`), titre page, toc, ancres `[[ref]]`, ordre front/body/back, injection contenu, page break, multi-page.

Non-régression (existants, VERTS) :
- `BookAssemblerTest`, `BookValidatorTest`, `BookTocParserTest`, `BookTreeBuilderTest`, `BookDslTest`, `BookLayoutTest`.

Note environnement : `./gradlew check` complet ne termine pas sur cette machine
(OutOfMemoryError JRuby/AsciidoctorJ dans `DocumentConverterTest` préexistant,
indépendant de DOC-BOOK-DOMAIN). Preuve de non-régression = 20 UT + 6 classes
test livre existantes VERTES en ciblé.

=== Pyramide de tests — Session 213
[cols="2,1,1,3"]
|===
| Couche | Tests | Status | Fichier
| Unit (BookNumbering DDD)
| 10
| ✅ PASS
| `BookNumberingTest.kt`
| Unit (BookAssembler structuré)
| 10
| ✅ PASS
| `BookAssemblerStructuredTest.kt`
| Non-régression livre
| 6 classes
| ✅ PASS
| `BookAssemblerTest` + `BookValidatorTest` + `BookTocParserTest` + `BookTreeBuilderTest` + `BookDslTest` + `BookLayoutTest`
|===

== Session 215 — DOC-BOOK-DOMAIN-5 (Cucumber BDD @book-tree + dogfooding FPA) + DOC-BOOK-DOMAIN-6 (BookValidator structurel)

Tests ajoutés S215 :
- `BookValidatorStructureTest.kt` — 8 UT DOMAIN-6 (S1 ref continuity, S2 unicité (ref,page), S3 matter FRONT+BACK, S4 page monotonicity) — RED compile fail → GREEN.
- `book_tree.feature` — 6 scénarios Cucumber BDD @book-tree (assembly structuré, front/body/back, niveaux, ancres, navigation prev/next, bornes début/fin, compat blob plat) — RED UndefinedStepException → GREEN 6/6 PASSED.
- `BookTreeCucumberRunner.kt` + `BookTreeSteps.kt` (package `document.booktree`, préfixe « book-tree », pure BDD domaine) + `CucumberTaskSpec("bookTreeCucumberTest")`.
- `AssembleBookTaskFpaTest.kt` — 1 FT dogfooding corpus réel FPA (TOC + scans 000/009/010, self-skip `assumeTrue` si corpus absent en CI).
- `AssembleBookTaskFunctionalTest.kt` réécrit — 4 FT (task registered, structured from TOC, STRICT fail level jump citant `1.1`, LENIENT survit).

Total S215 : 8 UT + 6 BDD + 5 FT (dont 1 dogfooding), tous GREEN.

Gates :
- 91 UT book domain GREEN (`document.Book*Test` + `document.AssembleBook*Test`).
- 6 suites cucumber GREEN (tableTranslation, tableValidation, plantumlValidation, frontmatterRetranslate, crossBorough, bookTree) — BUILD SUCCESSFUL 1m29s.
- FT book GREEN (AssembleBookTaskFunctionalTest 4 + AssembleBookTaskFpaTest 1).
- `DocumentPluginFunctionalTest` complet (62 tests) : 1 échec flaky environnemental `convertDocumentToPdf renders image directives` (pression mémoire JRuby/AsciidoctorJ) — PASSED isolé. Hors périmètre (même famille que l'OOM `DocumentConverterTest` documenté S-213).

== Session 214 (interrompue) — note
`AssembleBookTaskUnitTest.kt` fixé (S-214, conservé) ; reste de la session reverted en S-215 (pas de tests retenus hors ce fix).

== Session 219 (2026-08-28) — DOC-CR3-1 + DOC-CR3-3

*Tests ajoutés S219* :
- `AsciidoctorHolderTest.kt` — 1 UT TDD strict (provider compteur : `createCount == 1` sur 2 conversions `convertToHtml`). Prouve la réutilisation de l'instance AsciidoctorJ (DOC-CR3-1).

*Tests modifiés/non-régressés* :
- `DocumentConverterTest` (43 UT) — conversions HTML/PDF/EPUB/DocBook/ManPage toujours vertes après délégation au holder.
- `DocumentGenerationGraphTest` — génération intacte après retrait du code mort koog.

*Couverture* : `DocumentConverter` passe d'un `Factory.create()` par conversion à une instance partagée (singleton lazy). Couvert par `AsciidoctorHolderTest` + `DocumentConverterTest`. `DocumentGenerationGraph` : suppression de ~57 lignes mortes sans perte de comportement (validé par `DocumentGenerationGraphTest`).

*Hors périmètre* : DOC-CR3-2 (safeMode DSL) non traité.

== Session 233 (2026-08-31) — Dogfooding N3 complet + fix ENV gems

*Tests ajoutés S233* :
- `BookPipelineN3FpaFunctionalTest.kt` — 1 FT dogfooding (corpus FPA réel, 3 builds staged, asserts 4 gardes + metadata validationStatus=PASS).
- `N3PipelineSteps.kt` + `n3_pipeline.feature` + `N3PipelineCucumberRunner.kt` — 1 BDD `@n3-pipeline` (11e `CucumberTaskSpec n3PipelineCucumberTest`).

*Impact majeur sur la baseline ENV (option C)* :
- `runtimeOnly asciidoctorj-pdf:2.3.23` + `asciidoctorj-epub3:2.2.0` — les gems
  `asciidoctor-pdf`/`asciidoctor-epub3` se chargent désormais du classpath :
  16 failures ENV résolues (baseline FT 21 → 5 fails, toutes préexistantes et
  hors périmètre : translateDocument ×2 + TableValidation ×3, cause convention
  `translation.outputFileName` manquante). Les tests
  `DocumentConverterTest` PDF/EPUB + `FpaBookArtifactGenerationTest` +
  `BookPipelineFpaFunctionalTest` PDF/EPUB passent maintenant.

*Infra test* :
- `CollectDocumentRetrieveTask` : snapshot contract (`upToDateWhen { false }`) —
  le FT/BDD N3 exige la ré-indexation après écriture du rapport de validation.
- `LintHtmlDocumentTask` : annotations restaurées (régression WIP f0dfcef) ;
  le moteur Cucumber global scanne `lintHtmlDocument` sans faux conflit.

Gates : 15/15 runners Cucumber VERTS, `test` VERT, FT 116/5 fails préexistantes.

=== Session 235 (2026-08-31) — Option B : knob outputFileName vivant

*Nouveaux tests* :
- `ConvertDocumentTaskOutputFileNameFunctionalTest` (2 FT TestKit) — custom
  `custom.html` produit + `document.html` absent ; défaut canonique préservé.
- `ValidateDocumentHtmlLintCustomOutputFunctionalTest` (2 FT TestKit) — 4e
  garde audite le vrai `custom.html` (DEAD + fragment `gone-custom`, pas de
  `html-file-missing`) ; `lintHtmlDocument` même contrat.
- `document_validation.feature` : +2 scénarios `@document-validation-custom-output`
  (runner existant `documentValidationCucumberTest` — pas de nouveau runner).

*Faux positif corrigé en cours de TDD* : le RED initial utilisait le fragment
`missing`, partagé par le finding attendu (lien mort) ET le finding d'échec
statique (`<html-file-missing>`) — le test passait pour la mauvaise raison.
Fragment discriminant `gone-custom` + assertion négative `html-file-missing`.
Leçon : un fragment d'assertion doit distinguer les findings.

*Backward-compat prouvée* : défaut `"document"` → mêmes chemins (tous les
tests existants verts sans modification).

Gates : 15/15 runners Cucumber dédiés VERTS (0 failure XML après --rerun-tasks),
compileKotlin VERT, secrets RAS.

=== Session 236 (2026-08-31) — Follow-up S-235 : collect N3 knob-aware

*Nouveaux tests* :
- `DocumentArtifactCollectorTest` (UT) : +3 tests knob-aware (`custom` détecté /
  `document` canonique conservé / 5 formats + adoc + enriched sous `mon-livre.*`).
  RED initial : compilation `No parameter with name 'outputFileName' found` ×3.
- `CollectDocumentRetrieveOutputFileNameFunctionalTest` (2 FT TestKit) — composite
  référence `custom.html` (et pas `document.html`) sous knob custom ; canonique
  conservé sans knob. RED : custom FAILED / canonique PASSED (knob ignoré).
- `n3_collect_custom.feature` : 2 scénarios `@n3-collect-custom` + 16e runner
  dédié `N3CollectCustomCucumberRunner` (glue `document.n3collect`) +
  `N3CollectCustomSteps` (préfixe `n3-collect` anti-glue S-223) +
  `CucumberTaskSpec("n3CollectCustomCucumberTest")`.

*Backward-compat prouvée* : défaut `"document"` (paramètre Kotlin optionnel +
`getOrElse("document")` + `.orElse("document")` plugin) → les 14 UT collector
canoniques préexistantes et les 65 FT `DocumentPluginFunctionalTest` passent
sans modification.

Gates : `compileKotlin` VERT, 16/16 runners Cucumber dédiés SUCCESS (8m47s,
0 failure / 0 error dans les XML), UT collector 17/17, secrets RAS.
